# 스프린트 계약 — T3.3 체크아웃 (b4 변환 + 차감 기록 + T6.5 미니바 체크리스트)

- 작성: FE/BE / 합의: QA / 날짜: 2026-06-11 (**착수 선점 커밋** — 병렬 세션 규칙 #8)
- 근거: docs/SPEC.md F4 체크아웃 1~4, design/stitch/b4-checkout, ADR-0003(미니바 읽기 전용), docs/contracts/T3.4-cleaning-gate.md(createCheckoutCleaningTask tx 주입), T1.2 admin 변환 패턴
- 선행: T3.4 BE(게이트), T2.5(예약 상세 — 체크아웃 진입점)

## 수정 금지 구역 (병렬 세션 규칙 #2)

- T3.4-FE 세션: `app/(admin)/inspections/**`, `messages/*.json`의 adminInspections.* 키
- T2.6 세션: `app/(admin)/dashboard/**`, `lib/timeline.*`, `components/admin/timeline-matrix.tsx`
- T4.5 세션: `app/(admin)/settlements/**`, `lib/settlement.*`, B7 관련
- T0.7: `reference/**` (테오 수집 중)
- 공유 동결: `tailwind.config.ts`·`package.json`·`tsconfig.json`·`app/globals.css`·`components/admin/**`(사용만)·`lib/cleaning.ts`(소비만 — createCheckoutCleaningTask 호출)
- `messages/*.json` 키 추가만(adminCheckout.*), 완료 즉시 커밋

## 범위 (In)

1. **lib/checkout.ts (BE)** — 체크아웃 단일 소스: `completeCheckout(prisma, input)` 단일 트랜잭션 — ① Booking CHECKED_IN→CHECKED_OUT(status 가드 updateMany) ② CheckOutRecord 생성(photoUrls·damageFound·damageNote·damagePhotoUrls·deductionVnd BigInt·refundedAt·createdBy) ③ 보증금: 파손 없음→REFUNDED / 파손→PARTIAL_DEDUCTED+차감액 필수 ④ **createCheckoutCleaningTask(tx) 호출 — 게이트 닫기 원자 묶음** ⑤ AuditLog. 순수층: `resolveDepositOutcome(damageFound, deductionVnd)` 검증
2. **POST /api/bookings/[id]/checkout** — ADMIN 전용(401/403 분리), zod 검증, serializeBigInt 응답
3. **app/(admin)/bookings/[id]/checkout/page.tsx (FE)** — b4 변환: 기준사진 비교 UI(villa.photos isBaseline vs 촬영 업로드), **미니바 읽기 전용 체크리스트(T6.5 — VillaAmenity MINIBAR 품목·수량 표시, 차감은 수기 금액)**, 파손 기록(사진+차감액 VND), 완료 액션. 업로드는 기존 /api/uploads 파이프라인 재사용
4. i18n: adminCheckout 네임스페이스 ko/vi

## 범위 밖 (Out)

- 체크인 T3.1·동의서 T3.2 (CHECKED_IN 상태 만들기는 본 태스크에서 검증용 시드/QA로만)
- 미니바 수량 자동 차감·재고 추적 (Phase 2 — IDEAS)
- 보증금 환불 실제 송금 (기록만 — SPEC)
- /inspections·dashboard·settlements (타 세션)

## 완료 기준

- [ ] `npm test`·`npm run typecheck`·신규 파일 lint 통과
- [ ] CHECKED_IN 외 상태에서 체크아웃 거부(가드) 증명
- [ ] 파손=true인데 차감액 없음 거부 / 파손=false면 REFUNDED — 테스트 증명
- [ ] 체크아웃 트랜잭션 안에서 CleaningTask 생성+isSellable=false가 원자적(코드 확인)
- [ ] b4 대비 구조·색 일치, 미니바 체크리스트 읽기 전용
- [ ] 차감액 BigInt(float 0건), SUPPLIER에 판매가·마진 무노출(ADMIN 화면)
- [ ] messages 추가만

## 검증 방법

- QA 독립 평가: SPEC F4 대조, 게이트 원자성, 보증금 상태기계, b4 디자인 대조, 테스트 실행. build·prisma generate·dev 서버 금지(병렬 EPERM)
