# 스프린트 계약 — T2.6 대시보드 잔여 (스탯 카드·활동 피드·iCal 충돌 배너·모바일, b1/b1-mobile)

- 작성: FE/BE / 합의: QA / 날짜: 2026-06-11 (**착수 선점 커밋** — 규칙 #8. 과거 T2.6 계약 초안은 미커밋 폐기 확인 — 본 계약이 유일)
- 근거: docs/SPEC.md F7 대시보드, design/stitch/b1-dashboard(+b1-mobile — 2차 회의: <lg 타임라인→"오늘 중심 리스트"), T1.5(타임라인 — /dashboard 기구현), T1.6 `findUnresolvedIcalConflicts`(T2.6용 단일 소스, ADMIN 전제), 2차 회의 결정(환율 표기 USD 기준 폐기 — FX_VND_PER_KRW 기반)
- 선행(커밋 확인): T1.5 타임라인, T1.6 iCal, T3.4 청소, T2.x 예약 전부 완료

## 수정 금지 구역 (병렬 세션 규칙 #2 — 활성: T3.8 마무리 세션)

- T3.8 세션: `app/(supplier)/cleaning/**`, `app/api/uploads/route.ts`, `docs/contracts/T3.8-supplier-cleaning.md`, skills(stitch-conversion·leak-checklist 작업 트리 변경분)
- 공유 동결: `tailwind.config.ts`·`package.json`·`tsconfig.json`·`app/globals.css`·`components/admin/sidebar.tsx`
- **T1.5 산출물 수정 선언(완료·커밋)**: `app/(admin)/dashboard/page.tsx` — 타임라인 둘레에 자기 섹션 추가(comingSoon 제거). `components/admin/timeline-matrix.tsx`·`lib/timeline.*`는 **사용만**
- messages `adminDashboard` 블록 키 추가만

## 범위 (In)

1. **lib/dashboard.ts** — 대시보드 데이터 단일 소스 (전부 ADMIN 화면 전제)
   - 순수층(테스트): `hoursUntil(now, at)`(가예약 "최근 만료 N시간 후" 배지), `feedEntryFor(auditLog)`(entity·action·changes → i18n 키+파라미터+dot 색 매핑 — Booking HOLD 생성/확정/체크인·아웃/만료/취소, CleaningTask 요청/제출/승인/반려, Proposal 생성, Villa 등록·승인 + 일반 폴백), `relativeTimeParts(now, at)`(방금 전/N분 전/N시간 전/날짜)
   - 조회: `loadDashboardStats(db, now)` — VN 기준 오늘: 체크인 예정·완료(checkIn=오늘, CONFIRMED·CHECKED_IN), 체크아웃(checkOut=오늘, CHECKED_IN·CHECKED_OUT), HOLD 진행 수+최근 만료 시각, 청소 승인 대기(PHOTOS_SUBMITTED) + 모바일용 오늘 체크인/아웃 목록(빌라명·고객명·상태)
   - `loadActivityFeed(db, take=8)` — AuditLog 최신순 매핑 (개인정보 비포함 — 고객명은 Booking 조회로 표시하되 여권·금액 미노출)
2. **page.tsx 확장 (b1 그대로)**: 충돌 배너(red — `findUnresolvedIcalConflicts`, 첫 건 표시+건수, "해결하기"→해당 예약 상세 링크), 스탯 카드 4종(아이콘·색 b1 일치, 카드별 목록 화면 링크), 좌 9/우 3 그리드(우측 활동 피드 + "전체 활동 로그 보기"는 추후 /activity 미구현 — 버튼 비표시 편차), 푸터 스탯(환율 1,000,000₫ ≈ X원 — AppSetting FX, 최근 동기화 — ICAL 블록 최신 createdAt)
3. **b1-mobile (<lg)**: 타임라인 hidden → "오늘 중심 리스트"(오늘 체크인/체크아웃 카드 — b1-mobile 구조)
4. i18n adminDashboard 키 ko/vi, 테스트(순수층)

## 디자인 편차 (사전 신고)

- 활동 피드의 청소 사진 썸네일 미표시 — AuditLog에 URL 미기록(개인정보 최소화 정책, T3.1 QA 권고 4)·피드는 텍스트 전용
- "전체 활동 로그 보기" 버튼 미렌더 — /activity 화면 미존재(동작 없는 버튼 금지 원칙). IDEAS 후보
- 푸터 "1 USD = …" 표기 제거 — 2차 회의 결정(USD 기준 표기 결함). FX_VND_PER_KRW 미설정 시 환율 칩 숨김
- 충돌 배너는 미해결 충돌 0건이면 미렌더 (b1은 상시 표시 모크)

## 범위 밖 (Out)

- 활동 로그 전체 화면(IDEAS), 충돌 해결 전용 UI(배너→예약 상세 링크로 갈음), 타임라인 변경(T1.5 그대로)

## 완료 기준

- [ ] `npm test`·typecheck·lint 통과, 순수층 테스트(피드 매핑·시간 계산 경계)
- [ ] 스탯 4종이 실데이터 집계(VN 오늘 기준), HOLD 만료 배지 시간 정확
- [ ] 충돌 배너: 0건 미렌더·N건 표기·ADMIN 외 도달 불가(레이아웃 가드)
- [ ] 피드에 여권·판매가·마진·원가 문자열 부재 (QA grep)
- [ ] b1 대비 구조·색 일치(+신고 편차 4건), <lg 오늘 리스트 전환
- [ ] messages 추가만

## 검증 방법

- QA 독립 평가: b1/b1-mobile 대조, 누수(피드·푸터), 집계 정확성 코드 검토, 테스트 실행. build·prisma generate·dev 서버 금지(병렬 EPERM)
