# 계약: 벤더 발권 현황 명칭 + 카드 아코디언 (대량 대응)

- 상태: 착수 (2026-07-12)
- 브랜치: wt/vendor-issuance-ux
- 배경(테오): ① 티켓 전용 업체에게 "예약 현황"은 부정확 — "발권 현황"이 맞음. ② 카드가 여권
  정보·QR까지 상시 펼침이라 건수 많으면 상하 스크롤 압박 과다.

## 설계
1. **탭 명칭**: ticketOnly(기존 파생 판정)일 때 schedule 탭 라벨="발권 현황"(vi 병행 — 신규 키,
   일반 벤더는 현행 "예약 현황"). 빈 상태 문구도 티켓 전용이면 발권 맥락으로(선택).
2. **아코디언(예약/발권 현황 탭)**: 기본 접힘 요약 행 — 품목 ×수량 · 구분(옵션 라벨) · 이용자
   (첫 명+외 N) · 일정 · 상태 칩(수락/이행완료) · TICKET 발행 카운터(N/M, 미달 amber).
   펼치면 기존 카드 전체(빌라·주소·여권 정보·QR·완료보고 버튼 등 — 기능 유실 0).
   미완료(발행 미달·완료보고 전) 건은 기본 펼침 또는 접힘+강조 배지(모바일 터치 편의 판단 —
   접힘+amber 신호 권장). 발주함 탭은 건수가 적고 즉시 조치 대상이라 현행 유지.
3. 라이트·모바일 우선(벤더 화면), 코치마크 앵커(vendor-tab-*) 불변.

## 범위
1. components/vendor/vendor-board.tsx(ScheduleSection 아코디언·탭 라벨 분기)
2. messages/ko.json·vi.json 키 추가, 테스트(투어 앵커 회귀 등)

## 완료 기준 (QA)
- [ ] 티켓 전용 벤더: 탭 "발권 현황"(vi 포함), 일반 벤더 현행
- [ ] 현황 탭 기본 접힘 요약(미완료 신호 식별 가능), 펼침 시 기존 기능 전부(완료보고·QR·여권정보·다운로드 없음 주의 — 벤더는 열람만)
- [ ] 발주함·정산 탭 현행, 코치마크 회귀 없음, build 통과
