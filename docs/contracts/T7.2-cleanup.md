# 계약: T7.2 정리 — 고아 T7.1 부가서비스 BE 제거 + 마킹 정합

## 배경
T7.2("부가서비스 판매 UI — 예약 상세 서비스 패널")는 **이미 출시됨**. 단, T7.1과 별개의
ADR-0019/0023 카탈로그+원천공급자 발주 시스템이 그 역할을 흡수·대체:
- 예약 상세 `app/(admin)/bookings/[id]/service-orders-panel.tsx`(Stitch b20) — 렌더 중(page.tsx:630)
- 라우트 `/api/bookings/[id]/service-orders`(복수)·`/api/service-orders/[id]`·`/api/service-orders/[id]/dispatch`
- 카탈로그 관리 `/settings/services`·`/api/services/catalog`·`/api/services/catalog/[id]`
- `lib/service-catalog.ts`(가격·옵션), 마진 비공개 showCost(canViewFinance) 게이트

초기 T7.1 단독 BE는 **어떤 UI에도 연결되지 않은 고아**: `lib/service-order.ts`의 판매 입력검증·
마진 + 라우트 `/api/bookings/[id]/services`(단수)·`/api/services/[id]`. → "service order 이중 시스템"
혼란 footgun 제거를 위해 정리.

## 범위 (정리만 — 라이브 카탈로그 시스템 무접촉)
- **삭제**: `app/api/bookings/[id]/services/route.ts`, `app/api/services/[id]/route.ts` (+ 빈 디렉터리)
- **트림** `lib/service-order.ts`: 라이브 `service-orders/[id]` 라우트가 재사용하는 **전이 상태머신만 잔존**
  (`SERVICE_ORDER_STATUSES`·`isServiceOrderStatus`·`TRANSITIONS`·`canTransitionService`·
  `assertServiceTransition`·`InvalidServiceTransitionError`). 제거: `SERVICE_TYPES`·`isServiceType`·
  `ServiceOrderInput`·`validateServiceOrderInput`·`computeServiceMarginKrw`(+미사용 import)
- **트림** `lib/service-order.test.ts`: 전이·상태가드만 검증(삭제된 판매검증·마진 테스트 제거)
- TASKS.md Phase 2 백로그 부가서비스 항목 [~]→[x] 정합

## 수정 금지 구역 (다른 세션/라이브)
- `lib/service-catalog.ts`, `app/api/service-orders/**`, `app/api/services/catalog/**`,
  `service-orders-panel.tsx`, `catalog-manager.tsx`, prisma/schema.prisma, messages/*.json — 전부 무접촉
- 공유 폴더 활성 세션(파트너 포털·vendor·signup) 영역 무접촉

## 안전성 검증(삭제 전 실측)
- `lib/service-order` import는 3곳뿐: 삭제할 2개 라우트 + 라이브 `service-orders/[id]`(전이 2심볼만)
- 삭제 라우트 경로(`/api/bookings/[id]/services` 단수·`/api/services/[id]`)로의 `fetch` 호출 0건
- `validateServiceOrderInput`·`computeServiceMarginKrw`·`SERVICE_TYPES`·`isServiceType` 외부 사용처 0건

## 완료 기준
1. `npm run typecheck` 0 에러 (특히 라이브 `service-orders/[id]` 전이 import 정상)
2. `npx vitest run lib/service-order.test.ts` 통과 + 전체 테스트 회귀 0
3. `next build` 성공 (삭제된 라우트로 인한 빌드 깨짐 0)
4. 라이브 카탈로그/발주 시스템 무변경(diff에 service-catalog·service-orders·catalog 미포함)
