# 스프린트 계약 — T2.2 공개 제안 페이지 /p/[token] (FE/BE, ko)

- 작성: FE / 합의: QA / 날짜: 2026-06-11
- 근거: docs/SPEC.md F3 흐름 2~3·엣지, docs/DESIGN.md C절(+VND 변형 주석), design/stitch/ c1-proposal-page·c1-vnd·c2-proposal-expired·c3-booking-request·c3-vnd (+ NOTES.md), T5.5 변환 처리 목록, T2.3 QA 이관 2건
- 선행: T2.1 lib/proposal.ts(effectiveProposalStatus), T2.3 lib/hold.ts(createHoldFromProposalItem·HoldRejectedError)

## 수정 금지 구역 (병렬 세션 규칙 #2)

- T1.5 세션: `app/(admin)/**`(타임라인), T1.8 세션: `app/api/users/**`
- 공유: `components/**`(관리자 공통 — 공개 페이지 전용 컴포넌트는 `app/p/_components/`에 격리), `messages/*.json`(공개 페이지는 ko 고정 — i18n 키 불사용, DESIGN 규칙상 키 추출은 공급자 화면 한정), `app/globals.css`·`package.json`·`tsconfig.json` 수정 0건
- 기존 lib는 import만 (proposal·hold·serialize·format·date-vn)

## 범위 (In) — 디자인은 Stitch export 그대로 변환 (절대 규칙)

1. **app/p/[token]/page.tsx** — c1 변환 (서버 컴포넌트)
   - effectiveProposalStatus로 분기: ACTIVE→c1 / EXPIRED·REVOKED→c2 만료 상태 / USED→c2 마감 상태. **단일 상태만 렌더** (T5.5)
   - 존재하지 않는 token → 404 (정보 노출 없음)
   - c1: 헤더(Villa PMS)·"{clientName}님을 위한 제안"+만료 배지(잔여 시간 서버 계산)·날짜 요약·빌라 카드(사진 캐러셀, 배지: 침실/수영장/조식, 1박가·총액, 가예약 버튼)·보증금 안내 카드·한국어 푸터
   - **통화 분기 렌더**: saleCurrency=KRW → "1,350,000원" / VND → "25,500,000₫" (쉼표 — c1-vnd, formatThousands 재사용. 1박가는 perNight null이면 비표시)
   - **재고·마진 비공개**: 제안 포함 빌라·날짜·판매가만. 원가·마진·타통화 환산·다른 재고 일절 미노출
2. **app/p/[token]/book/[itemId]/page.tsx** — c3 상태1 변환: 빌라 요약 카드+이름·연락처 폼(react-hook-form+zod)+홀드 안내 배너+신청 버튼. "Step 01/02"→"단계 1/2"(T5.5)
3. **app/api/p/[token]/hold/route.ts** — 비로그인 POST: **itemId가 해당 token의 제안 소속인지 검증**(교차 토큰 차단) → createHoldFromProposalItem 호출. **T2.3 QA 이관 이행**: ① HoldRejectedError detail(검수 게이트 등 reasons) 공개 응답 미노출 — 사유 코드를 "expired"/"closed" 2종으로만 축약 ② MissingRateError → "closed" 처리(500 금지)
4. **app/p/[token]/done/[bookingId]/page.tsx** — c3 상태2 변환: 체크 아이콘+"가예약이 접수되었습니다"+예약번호 칩+무통장 입금 안내 카드+실시간 카운트다운 배지(holdExpiresAt 기준, 클라이언트). Booking 조회는 해당 token 제안 소속 + HOLD/CONFIRMED만, **select로 판매가·날짜만**(supplierCostVnd·fx 미노출)
5. **공개 전용 컴포넌트** `app/p/_components/`: PhotoCarousel(스크롤 스냅+dots), BookingForm, HoldCountdown
6. **운영 설정 (AppSetting 키 — 미설정 시 해당 블록 숨김)**: BANK_NAME·BANK_ACCOUNT_NUMBER·BANK_ACCOUNT_HOLDER(입금 카드), CONTACT_KAKAO_URL·CONTACT_PHONE(c2 문의 버튼) — T1.7 /settings에 입력 UI 추가 잔여(TASKS 메모)

## 디자인 편차 (계약 명시 — QA 판단)

- c3 폼에 **인원수 입력 추가** (디자인엔 없으나 Booking.guestCount 필수 컬럼 — 동일 입력 스타일로 1~16)
- c1 요약 행 "성인 4명"은 데이터 없음(스키마에 인원 없음) → "N박"만 표기. 항목별 날짜가 다르면 요약 행 생략+카드별 날짜 표기
- c3 푸터 영문(Terms of Service 등)·"Deposit Information" 영문 eyebrow → 한국어 통일(T5.5 영어 잔존 규칙). © 표기는 "© 2026 Villa PMS Phu Quoc"
- c1 헤더의 뒤로가기·공유 버튼: 공개 단일 페이지라 뒤로가기는 book/done에서만 의미 — c1 메인은 공유 버튼만(Web Share API, 실패 시 URL 복사)
- 예약번호 칩: `B-{booking.id 끝 4자 대문자}` (디자인 B-2611 형식 대응)
- holdHours: T2.1 스코프 확정대로 24/48h 선택 없음 — 안내 배너 문구는 "최대 48시간"이 아닌 AppSetting 기준 시간 표기
- (QA 1차 추인) c3 완료 화면 버튼: 디자인 2버튼("내 예약 확인"+"메인으로") → 1버튼("제안으로 돌아가기") — 비로그인 사용자에게 "내 예약" 화면·"메인"이 존재하지 않음 (M3)
- (QA 1차 반영) bg-mesh·계좌 복사 버튼·done 뒤로가기·c2 공유 버튼·장식 블롭·done 푸터 링크는 1차 평가 후 디자인 충실도 수정으로 반영 (M1·M2·M4·M5·M6·M7)

## 범위 밖 (Out)

- B2 제안 생성·b12 목록 화면 (T2.1 FE)
- Zalo 실발송(T3.5), Railway cron 등록(OPS)
- 카카오톡 채널 실제 개설(테오 — URL만 AppSetting)

## 완료 기준

- [ ] `npm test`·`npm run typecheck`·신규 파일 lint 통과
- [ ] 상태 4종(ACTIVE/EXPIRED/REVOKED/USED) 분기 — 단일 상태 렌더, 미존재 token 404
- [ ] KRW/VND 변형 렌더가 c1·c1-vnd 표기와 일치 (쉼표·원/₫)
- [ ] 공개 응답·HTML에 supplierCostVnd·margin·fx·타 빌라 재고 문자열 부재 (QA grep)
- [ ] HOLD 거부 사유가 "expired"/"closed"로만 노출 — reasons 원문 미노출
- [ ] hold API가 교차 토큰(다른 제안의 itemId) 거부
- [ ] 디자인 충실도: c1/c2/c3 export 대비 구조·색·간격 일치 (Stitch 우선 규칙)

## 검증 방법

- QA 독립 평가: 디자인 대조(export HTML vs 구현), 권한 누수 4종(특히 비로그인 공개면), 거부 사유 축약 확인, 테스트 실행. build·prisma generate·dev 서버 금지(병렬 EPERM — 렌더 검증은 코드 검토+추후 스테이징)
