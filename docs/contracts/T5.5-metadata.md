# 계약: T5.5-metadata — 공급자 페이지 metadata.title 누락 보강

## 배경
T5.5 "변환 시 처리 목록"의 잔여 점검 결과, 대부분 항목은 선행 태스크에서 완료:
- b3 "지우기 (CLEAR)" 영문 제거 ✓(T3.2), Bước N/5 재번호 ✓(T1.1), globals.css keep-all 전역 ✓(T1.1)
- c1·c2 © 푸터 보일러플레이트 → PublicFooter 컴포넌트로 정리 ✓(T2.2)
- c2 만료/마감 단일 렌더 ✓(T2.2), c3 "단계 1/2" ✓, 사이드바 9메뉴(메시지) ✓(T6.6)

실제 잔여: **공급자(supplier) 서버 컴포넌트 페이지 4건의 `metadata.title` 누락** — 브라우저 탭 제목이 기본값으로 노출.

## 범위 (수정 파일 — 이 세션 전용)
- `app/(supplier)/calendar/page.tsx` — metadata 추가만 (title: "Lịch — Villa PMS")
- `app/(supplier)/cleaning/page.tsx` — (title: "Dọn dẹp — Villa PMS")
- `app/(supplier)/cleaning/[id]/page.tsx` — (title: "Dọn dẹp xong — Villa PMS")
- `app/(supplier)/earnings/page.tsx` — (title: "Thu nhập — Villa PMS")
- `docs/contracts/T5.5-metadata.md`(본 파일), `TASKS.md`(T5.5 행만 갱신)

용어는 messages/vi.json 기존 키(tabs/calendar/cleaning/earnings)와 일치. 정적 metadata(기존 admin·zalo-connect 컨벤션 준용).

## 수정 금지 구역 (다른 세션 점유 — metadata 누락이어도 건드리지 않음)
- `app/(admin)/villas/[id]/page.tsx` (T1.2b 빌라 반려 세션)
- `app/(supplier)/my-villas/[id]/page.tsx`·`[id]/edit/page.tsx`·`new/page.tsx` (T1.10/T1.1 세션)
- `app/page.tsx` (루트 — 항상 redirect라 UI 미렌더, metadata 불요)
- lib/*, messages/*.json, schema.prisma, components/* — 무수정

## 완료 기준
1. 4개 페이지에 `export const metadata: Metadata = { title: ... }` 추가, 빌드 시 탭 제목 노출
2. 각 title은 vi 용어(Lịch·Dọn dẹp·Dọn dẹp xong·Thu nhập) + "— Villa PMS" 접미
3. `npm run typecheck`에서 내 파일 신규 에러 0건(타 세션 WIP 에러는 제외)
4. 서버 컴포넌트 유지(‘use client’ 미도입), 기존 렌더 무변경

## 검증
- typecheck (내 파일 한정 판단)
- 각 파일 export 확인
