# 계약 — T4.2b 시드 전화번호 형식 버그 수정 (로그인 가능화)

2026-06-16 / 담당: BE / 평가: QA(또는 셀프 검증) / 근거: T4.6 인증 검증 발견사항 #1 (docs/LAUNCH.md §7.2), T4.2(prisma/seed.ts·seed-demo.ts)

## 배경
T4.6 시각 검증에서 발견: 로그인 폼이 전화 입력을 숫자만 남기도록 정규화(`+84-90-000-0000`→`84900000000`)하는데, 시드는 `+84-90-...` 형식 그대로 저장 → **시드 계정 로그인 불가**(파일럿 공급자 온보딩 차단). 실제 회원가입은 클라 입력란이 숫자로 저장하므로 정상 — 시드 데이터만의 문제.

## 수정 금지 구역 (병행 세션)
- T4.1: lib/cleaning·hold·proposal (비접촉). /messages 확장 세션: app/(admin)/messages/** (비접촉)
- seed 파일(prisma/seed.ts·seed-demo.ts)은 git status clean — 활성 세션 없음 확인 후 본 세션 수정

## 범위 (In)
- `prisma/seed.ts` — 파일럿 SUPPLIER 전화 `+84-90-000-0000`→숫자 형식(`0900000000`), upsert **update 블록에도 phone 포함**(재시드 시 기존 계정 교정). 시드 ADMIN(현재 phone 없음)에 숫자 전화 부여(로그인 가능화, create+update)
- `prisma/seed-demo.ts` — 데모 공급자/청소자 전화 3건 `+84-90-...`→숫자 형식
- 비번·이름·id·role 등 그 외 무변경. 멱등 upsert 유지

## 범위 밖 (Out)
- 로그인 폼/정규화 로직 변경(현 동작이 정상) · 프로덕션 테스트 데이터 삭제(별도 — 오픈 전 LAUNCH §1)

## 완료 기준
- [ ] seed 전화 전부 숫자 형식(로그인 폼 산출값과 일치)
- [ ] upsert update 블록에 phone 포함 → 재시드 시 기존 계정도 교정
- [ ] `npx tsx prisma/seed.ts` 재실행 멱등 성공 + 패치 계정 로그인 가능 확인
- [ ] typecheck 통과, seed 외 파일 무수정

## 검증 방법
재시드 후 DB 전화값 확인 + (가능 시) 로그인 1회. typecheck.
