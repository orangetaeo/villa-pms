# 역할별 통합 수정 계획 (ROLE-AUDIT + MOBILE-RESPONSIVE 병합, 2026-06-27)

> [ROLE-AUDIT-2026-06-27.md](ROLE-AUDIT-2026-06-27.md)(기능·권한·정합성·i18n)와 [MOBILE-RESPONSIVE-AUDIT-2026-06-27.md](MOBILE-RESPONSIVE-AUDIT-2026-06-27.md)(반응형)를 **역할별로 병합**한 수정 정본. 이 문서 순서대로 수정 진행. 두 원본은 근거·스크린샷 보관용으로 유지.
>
> 표기: `[기능]`=ROLE-AUDIT, `[반응형]`=MOBILE. 심각도 H>M>L.

---

## 공통(Cross-cutting) — 한 번 수정으로 여러 역할 해결

| ID | 항목 | 영향 역할 | 수정 |
|---|---|---|---|
| **C1** `[기능]` H1 | 라이트 포털 pagination i18n raw키(`pagination.summary/perPage`, ko+vi) | 공급자·벤더·파트너 | 라이트 포털 레이아웃 클라 네임스페이스에 `pagination` 추가 + 회귀 테스트 |
| **C2** `[기능]` M2 | 빌라명 한국어 전용(병기 누락) | 공급자(/earnings)·벤더(발주) | 비운영자 경로에 `formatVillaName` 일관 적용 |
| **C3** `[반응형]` | 운영자 넓은 표가 페이지 오버플로 | OWNER·STAFF·김태진(공용 레이아웃) | 표를 `overflow-x-auto` 래퍼/카드 전환, 필터 칩줄 `flex-wrap`. 공용 `ScrollableTable` 권장 |

---

## 1. 빌라 공급자 (SUPPLIER)
- **C1** pagination i18n (공통)
- **C2** /earnings 빌라명 병기 (공통)
- `[기능]` **M3** 수익 추이·가동률 추이 차트 빈 화면 — 데이터 기간 매핑/렌더 수정
- `[반응형]` **M4** 상단 계정 아이콘이 인사말("Xin chào")·빌라 선택칩 가림 — 헤더 패딩/레이아웃
- `[기능]` **L1** 빌라명 쓰레기 데이터(`,ㅏㅏㅏ`) — 빌라명 입력 검증/정규화
- ✅ 유지: 마진 비노출(Giá gốc만), 판매정보 Chỉ xem 잠금

## 2. 원천 공급자 (VENDOR)
- **C1** pagination i18n (공통)
- **C2** 발주함 빌라명 병기 (공통)
- `[기능/반응형]` **M5** 발주함 무페이징(79건 16,500px) — PR#85 라이트 적용 여부 확인 후 누락분 PaginationBar 적용 (C1과 함께)
- ✅ 유지: 마진 비노출(지급액만)

## 3. 파트너 (PARTNER)
- **C1** pagination i18n (공통)
- `[기능]` **H2** 미수 잔액 불일치 — 파트너 자기화면(183,876,468₫) vs 운영자(173,516,468₫). 두 화면 집계 기준(상태·대손·무효 포함) 일원화
- ✅ 유지: 마진 비노출(채권만)

## 4. admin 실무 직원 (STAFF)
- **C3** 운영자 표 반응형(아래 OWNER와 동일 — 공용 레이아웃)
- `[기능]` **L3** 정식 STAFF 계정 운영(현재 유일 계정이 삭제·비활성였음)
- ✅ 유지: 권한·금액 경계 **모범적**(시스템메뉴 숨김·재무는통계만·/revenue 차단·통계 개요탭 숨김·예약 금액 숨김) — 변경 금지

## 5. 오너 (OWNER, 테오·김태진 공용)
- **C3** 운영자 표 반응형 오버플로:
  - `[반응형]` **/revenue** 거래 목록 표(+68px) → `overflow-x-auto` or 카드
  - `[반응형]` **/bookings/[id]** 부가옵션 주문 표(+19px, 컬럼 잘림) → 동일
  - `[반응형]` **/statistics** 기간 필터 칩줄(+26px) → `flex-wrap`
  - `[반응형]` **/users** (+12px 경미) → 칩/표 점검
- `[기능]` 점검용 변경 원복: 임시 비번(김태진 통지), STAFF 상태, prisma/audit-*.ts 삭제
- ✅ 유지: 공유 재고 정상·오너간 메시지 격리

## 6. 청소 직원 (CLEANER)
- `[기능]` **H3** 공급자 전체 탭바 노출 + `ERR_TOO_MANY_REDIRECTS` 무한 리다이렉트 → **청소 전용 최소 네비**(청소+안내+프로필), 라우트 가드 정상 차단
- ✅ 유지: /cleaning 반응형 정상

## 7. 게스트 (GUEST)
- `[기능]` **M1** 옵션 페이지 선택 옵션가 ≠ 하단 합계(과일 200k선택/50k표시·마사지 200k/450k) — 선택 단가 즉시 반영
- `[기능]` **M6** 만료 링크 "제안이 만료되었습니다" 오문구 → 게스트 체크인 전용 만료 문구
- ✅ 유지: 셀프체크인 4단계·5개 언어·반응형·누수 없음

---

## 실행 순서 (권장)

1. **C1** 라이트 포털 pagination i18n — 3역할 동시 해결, 가장 광범위
2. **H3** 청소 직원 네비/리다이렉트 — 기능 차단급
3. **C3** 운영자 표 반응형(/revenue·/bookings/[id]·/statistics·/users) — 공용 래퍼
4. **M1·M6** 게스트 옵션 가격·만료 문구
5. **H2** 파트너 미수 불일치
6. **C2·M3·M4·M5** 빌라명 병기·공급자 차트·헤더 겹침·벤더 페이징
7. **L1·L3** 빌라명 검증·STAFF 계정 / 점검용 변경 원복

> 작업 격리: CLAUDE.md 규칙대로 worktree에서 수정·커밋. 각 수정 후 typecheck/build + 390px 오버플로 0 + i18n 회귀 테스트.
