# PROGRESS.md — Villa PMS

| 날짜 | 스프린트 | 완료 내용 | 비고 |
|---|---|---|---|
| 2026-07-21 | **웹챗 백로그 잔여 2건 정리 (worktree-webchat-backlog-cleanup, T-webchat-backlog-cleanup)** | PR #347 후속 백로그. **(1) phoneTailMatch·tokenPrefixOf lib 통합**: booking-candidates·inbox 두 라우트 중복 → `lib/webchat-candidate-match.ts` 단일 원천(순수 이동·임계값 8자tail/6자prefix 불변, booking-candidates 인라인 prefix 판정=tokenPrefixOf 치환 동치)·단위 테스트 21. **(2) 수동 URL 번역 보존**: lib/gemini.ts BASE/RETRY_PROMPT_NOTE에 URL 보존 지시+`urlsPreserved`(원문 URL 0개=true라 URL 없는 메시지 회귀 0)·translateText 재시도 트리거·선택 로직에 numbers와 동급 통합(공용 함수라 웹챗·Zalo·카탈로그 영향 검증)·테스트 9. **(3)** PR #347 테스트 부채 수정: zalo-counterparty-backfill의 allowedShareKinds CUSTOMER=GUEST_LINK 포함 반영 | QA 독립 검증 **PASS(결함 0)** — 치환 동치성·URL 없는 메시지 동작 불변·공용 함수 회귀 0·수정금지구역 diff 0·대상 vitest 통과·tsc·build 완주. ★남은 전체 실패 2건(youtube/shorts=PR #341·webchat/messages=PR #324 미게이트)은 별개 기존 부채(이번 무관). 계약: docs/contracts/T-webchat-backlog-cleanup.md |
| 2026-07-21 | **웹챗 링크 백로그 3건 — 카드형·인박스 배지·Zalo 게스트링크 (worktree-webchat-cards-zalo-links, T-webchat-cards-inbox-zalo-links)** | PR #340·#344 후속 백로그 3건. ★조사에서 **Zalo엔 이미 공유 카드 시스템**(PROPOSAL/VILLA/SETTLEMENT) 존재 확인 → (C)는 없는 게스트 링크만 추가. **(A) 카드형**: WebChatMessage.kind/payload additive(라이브 적용)·send-link 4종이 kind+payload={url}만 기록(금액·bookingId·proposalId 원문 0)·components/webchat-link-card.tsx(isSafeCardUrl=https?·동일오리진만, //host·javascript:·data: 차단)·위젯+운영자 스레드 카드 렌더(kind null=Linkify 텍스트 폴백)·토큰 재사용 로직 lib/guest-link-token.ts 추출(send-link 무회귀). **(B) 인박스 배지**: inbox API에 bookingLink linked/candidate/none 파생·**배치 2쿼리**(토큰 prefix OR 1+연락처 윈도우 1, N+1 0)·candidate=토큰 유입 or 연락처 매칭. **(C) Zalo 게스트링크**: share 라우트 GUEST_LINK 타입(checkin/options/receipt)·**게이트 CUSTOMER 1:1만(SUPPLIER·여행사·UNKNOWN·그룹 403 서버 이중)**·ensureGuestLinkToken 재사용·receipt 체크아웃 가드·GuestLinkShareModal+guest-link-bookings 검색(금액 0·전화 뒷4자리)·기존 Zalo 3종 무회귀 | QA 독립 검증 **PASS(블로킹 0)** — 완료기준 11+누수 11종·하위호환 4곳·XSS·방문자 폴링 bookingId 부재·CUSTOMER/그룹 게이트·기존 공유 3종 diff=분기추가만·adminWebchat·adminMessages ko/vi 대칭·수정금지구역 diff 0. tsc0·build 통과(lint=worktree 중첩 환경 제약, 규칙 위반 0). 계약: docs/contracts/T-webchat-cards-inbox-zalo-links.md |
| 2026-07-16 | **사업 계약서 전자서명·번역·프린트·포지션별 페이지 (worktree-contracts-admin-docs, T-business-contract-esign)** | 테오: 번역본+프린트+프로그램 내 서명+포지션별 열람+별표 값 입력. ① **서명용 정본 5파일**(docs/business/contracts/signing/, LOC) — 내부 전략 메모(`협상 여지`·`변호사 확인`) 전량 제거+`{{token}}` 플레이스홀더, 빌라·벤더=ko/vi 1:1(끼엔장성 인민법원·베트남어 우선 확정), 파트너=ko. **내부 초안(00~03)과 2벌 체계 — 상대방에겐 정본만**(누수 차단) ② **BusinessContract 모델**(라이브 raw SQL·enum 2종) — termsJson(별표 값: 취소율·정산주기·지급방법, zod `.strict()`로 마진 필드 거부)·서명 시 contentHash(SHA-256) 봉인·서명 이미지=savePassportFile("sig-") 비공개 ③ API 6종 — admin 4(생성=role→type 자동·PATCH DRAFT만·send·void)+상대방 2(mine=SENT/SIGNED 자기 것만·sign=원자 updateMany 레이스 가드·멱등 409·`{{` 주입 400) ④ 화면 4종 — admin /contracts(FINANCE_PATHS 등재·별표 폼·발송/무효화), supplier /contract(vi)·vendor /vendor/contract(vi)·partner /partner/contract(ko)=열람→신분증·주소 입력→캔버스 서명→SIGNED, A4 프린트(@media print 흰 종이)·서명 블록 `<!-- signature-area -->` 앵커 ⑤ NS businessContract(32)·adminContracts(71) ko/vi 대칭 ⑥ 가입 시 서명 안 받음(테오 합의) — admin 승인 후 계약 발송 플로우 | QA 독립 검증 **PASS(P0/P1 0)** — 정본 내부 메모 grep 0·termsJson strict·타인 404·교차 role 차단·DRAFT 미노출·sig 서빙 업로더 매칭·봉인(PATCH 409)·미치환 토큰 0·i18n 103키 대칭. P2 3건 중 미들웨어 /contract unauth 게이트=병합 전 반영, 잔여 2건(sign 레이스 orphan 파일·주석 nit)=백로그. 테스트 신규 37+전체 3239(기지 webchat 2건 외 회귀 0)·tsc·build 통과. ★실계정 E2E는 실제 상대 가입 후 별도. 계약: docs/contracts/T-business-contract-esign.md |
| 2026-07-16 | **웹챗 제안(/p) 링크 생성·발송 (worktree-webchat-proposal-link, T-webchat-proposal-link-send)** | PR #340(예약 고객 링크) 후속 — **예약 전 문의** 고객에게 채팅 안에서 제안 링크 발송. ① send-link에 `kind:"proposal"`+proposalId additive 분기(**not_linked 검사 앞** — 예약 미연결 세션 발송 가능, 기존 3종 로직 무변경)·effectiveProposalStatus ACTIVE만(만료·취소·사용=400 proposal_not_active)·URL=`/p/<token>`+방문자 locale이 공개 5언어면 `?lang=` 부착(ko 생략)·AuditLog proposalId ② `GET proposal-candidates`: ACTIVE+미만료 최신 10건, **금액 select 원천 배제**(STAFF 사용 가능) ③ 템플릿 proposal kind 5언어(Gemini 미경유, LOC 감수) ④ 모달(webchat-proposal-modal.tsx): A=기존 제안 선택→확인→발송 / B=간이 생성(DateField 날짜→기존 `GET /api/proposals/candidates` 빌라 1~3개→24/48h→clientName 기본=세션 연락처)→기존 `POST /api/proposals`(channel=DIRECT)→**성공 시 send-link 자동 체이닝**(생성 실패=발송 미실행) ⑤ 권한 이원화: 발송=isOperator / **생성=canSetPrice(OWNER/MANAGER — 서버 정본, RSC prop 게이트+403 대칭, STAFF는 B 섹션 안내만)** ⑥ 스키마 무변경 | QA 독립 검증 **PASS(결함 0)** — 기존 3종 무회귀·판매가 API=B 게이트 뒤에서만 호출·방문자 폴링 diff 0·수정금지구역 diff 0·adminWebchat.proposal ko/vi 44/44. tsc(신규 0)·lint·build 통과. 계약: docs/contracts/T-webchat-proposal-link-send.md |
| 2026-07-16 | **부가서비스 책임 제한 고지+동의 게이트 (service-order-liability-consent)** | ServiceOrder.liabilityConsentJson(라이브 raw SQL·백필 금지) · lib/service-liability.ts 단일 원천(VERSION 2026-07-16.v1·5언어 ko/en/vi/ru/zh·미지원 로케일 en 폴백, LOC 감수 완료) · /api/g·/api/p service-orders 400 CONSENT_REQUIRED 게이트+서버 스냅샷{agreedAt,version,locale,source}(admin 대리 생성=미적용) · 게스트/파트너 신청 CTA 위 작은 고지(text-[11px])+필수 체크박스(체크 전 disabled·다중 POST 전 행 스냅샷) · admin 주문 상세 동의 1줄 · 감사로그 liabilityConsentVersion | QA 독립 검증 **PASS(결함 0)** — 신규 테스트 9건·전체 3211 통과(기존 실패 2건=별개 마케팅 라우트 화이트리스트 문제)·tsc·build 0·누수 0·수정금지구역 무접촉. ★새 소비자 주문 경로 추가 시 같은 게이트 필수·문구 수정 시 VERSION 올림·법률 문구=VN 변호사 검토 백로그(계약서 3종과 함께). 계약: docs/contracts/service-order-liability-consent.md |
| 2026-07-16 | **marketing-s2 — 직접 촬영 자동 편집·유튜브 성과·privacy·Zalo 푸시·아이콘 (wt/marketing-s2)** | ① 쇼츠 편집 스튜디오: R2 presign 직업로드(SigV4 자체 구현·키 형식 봉인 CLIP_KEY_RE)→ffmpeg 자동 편집(9:16 정규화 crop/blur·워터마크·인트로/아웃트로·자막 satori·BGM)→편집 잡 상태머신(PENDING→PROCESSING 원자 락·retry·고아 회수 cron)→승인 큐 합류, /marketing/youtube/create 3스텝 마법사 ② 유튜브 성과: videos.list statistics(추가 scope 불요)→YT_MEDIA 스냅샷(igMediaId=ytVideoId)+캐시·summary API·성과 섹션 ③ /privacy 3언어(ko/en/vi, 실데이터 흐름 정합·여권 tạm trú 고지·문의 biz.villago@gmail.com)+푸터 연결+스플래시 제외 ④ MARKETING_ALERT enum+notifyMarketing(인앱+Zalo 그룹 병행, kind 10종, cron 6곳 치환) ⑤ 정보바 SVG 아이콘 3종 통합 | QA 독립 검증 **PASS(P0/P1 0)** — build·tsc·lint·회귀 71건·스모크 2종 실측(편집 19.6s 1080×1920·아이콘 렌더)·presign 키 봉인·시크릿 무노출·누수 0. ★enum SQL=코드 배포 전 선적용 완료(TDA 라이브 적용). 잔여=cron-youtube-edit-jobs 등록(OPS)·실촬영 클립 E2E·클립 원본 정리 정책(백로그)·/privacy 법무 검토. 계약: docs/contracts/marketing-s2.md |
| 2026-07-16 | **웹챗 세션↔예약 연결 + 원클릭 게스트 링크 발송 (worktree-webchat-guest-link, T-webchat-guest-link-share)** | 테오 기획 승인: 웹챗에서 고객 식별 후 체크인·부가서비스 URL을 화면 전환 없이 전달. ① 스키마 additive 3컬럼: WebChatSession.bookingId(+index+FK SET NULL)·bookingLinkedAt·bookingLinkedBy(라이브 raw SQL 적용+migrations-manual 보존) ② API 3종(전건 isOperator 첫줄+writeAuditLog): booking-link POST/DELETE(연결/해제, 응답=금액 배제 BookingSummary)·booking-candidates(자동 후보 3단 — ★sourcePage `g:<토큰8자>` startsWith 매칭=최상 신뢰 "링크 유입"·연락처 꼬리 8자리 매칭(윈도우+take 200)·수동 검색 q= limit 10)·send-link(kind 3종, **토큰 사용가능 시 재사용/불가 시만 신규 — 기존 guest-token POST는 무조건 재발급이라 미호출**, 미연결 400·체크아웃 전 receipt 400·CLOSED/BLOCKED 409) ③ 템플릿=lib/webchat-link-templates.ts 5언어(ko/vi/en/zh/ru, 밖=en 폴백) **Gemini 미경유**(translatedText 직접 기록, 번역 비용·실패·URL 훼손 0), LOC vi 접객 감수(Quý khách·khai báo hộ chiếu) ④ 운영자 UI: 스레드 헤더 연결 모달(자동 후보 배지+검색 디바운스+**오발송 확인 다이얼로그** — 게스트명·전화 뒷4자리 대조 경고)·예약 배지(클릭=/bookings 새 탭)·입력창 위 빠른 링크 3버튼 ⑤ components/linkify.tsx 공용 XSS 안전 autolink(위젯+운영자 스레드, https?만·dangerouslySetInnerHTML 미사용) ⑥ 운영자 세션 GET에만 booking 요약 additive(방문자 폴링 라우트 diff 0) | QA 독립 검증 **PASS(P0/P1 0)** — 완료 기준 10/10·금액 필드 select 전수 0·방문자 표면에 예약 필드 유입 경로 0·토큰 재사용 시 DB write 0 확인·adminWebchat ko/vi 83/83. P2 1건(연락처 매칭 take 부재)=take 200 반영. tsc(신규 0)·lint·build 통과. 기획: docs/plans/webchat-guest-link-share.md·계약: docs/contracts/T-webchat-guest-link-share.md. 백로그: 제안 링크(/p) 발송·인박스 후보 배지·Zalo 이식 |
| 2026-07-16 | **유튜브 쇼츠 S1 — 빌라 숏츠·자동 업로드 (youtube-shorts-s1, wt/instagram-p1)** | YoutubeShort+YtShortStatus/YtSourceType(라이브 raw SQL, ★IgPostStatus 재사용 기각=타입명 박제·플랫폼 독립 진화) · OAuth 연동(/api/youtube/oauth start·callback — state 일회성 소진·timingSafeEqual·refresh token 암호화·토큰 무노출) · lib/youtube 6모듈(auth 캐시 리프레시·meta=Gemini 제목≤100자·설명·태그+금칙어 재사용·upload=resumable+킬스위치(미설정=정지)+일 상한·draft=12:00/19:30 KST 슬롯) · instagram-draft cron 확장(YT_SHORTS_PER_DAY 기본 0=**응답 바이트 동일 무변경**, 실패 격리) · cron youtube-publish(PUBLISHING 락·skipped=QUEUED 복구) · admin /marketing/youtube(세로 영상 미리보기·스튜디오 딥링크·OAuth 연결 버튼, NS adminYoutube ko/vi 107키 패리티) · docs/marketing/youtube-setup.md(테오 가이드) | QA 독립 검증 **PASS(P0/P1/P2 0)** — build 130p·tsc·lint 0·OAuth 재생공격 차단·누수 grep 0·AuditLog 전 경로. ★실조사 정정: 쿼터 2025-12 인하(업로드≈100유닛→일 100건 여유)·OAuth 앱 검증 불요 vs API 감사(공개 잠금 해제) 필요 구분. 잔여=테오 P0(채널·GCP·OAuth·★감사 신청 최우선 — youtube-setup.md)·youtube-publish cron 등록(연결 후)·S2(직접 촬영 자동 편집·Analytics). 계약: docs/contracts/youtube-shorts-s1.md |
| 2026-07-16 | **인스타그램 P2 — DM 인박스·릴스·인사이트 (instagram-marketing-p2, wt/instagram-p1)** | InstagramMessage·InstagramInsightSnapshot(라이브 raw SQL, igMessageId 멱등·ACCOUNT 센티널 "" upsert 키) · 웹훅 /api/webhooks/instagram(verify token·HMAC 서명 선검증·P2002 멱등·항상 200) · DM 발신+스레드 1회 카카오 자동응답(IG_DM_AUTOREPLY_PAUSED 킬스위치) · admin DM 인박스(24h 창 서버 409·카카오 원클릭·미읽음 뱃지) · 릴스 ffmpeg-static 슬라이드쇼(1080×1920 H.264+AAC, 음원 무번들=저작권 0, IG_REELS_PER_WEEK 기본 0=끔·실패 시 캐러셀 폴백) · 인사이트 수집 cron(30일 매일+구포스트 주1회 자기페이싱)+summary API+성과 섹션(스파크라인·상위 포스트·도달 뱃지) | QA 독립 검증 **PASS(P0/P1 0)** — build·tsc·lint 0·누수 0·서명 선순위·멱등·릴스 스위치 0=기존 동일 확인. ★실고객 DM=App Review(사업자 인증) 관문(개발 모드=테스터 25명 한정, 폴링 우회 불가 — docs/marketing/instagram-webhook-setup.md)·/privacy 페이지 부재=Live 전환 선행물. 잔여=IG_WEBHOOK_VERIFY_TOKEN·IG_APP_SECRET 값 세팅(웹훅 가동 시)·insights cron 등록. 계약: docs/contracts/instagram-marketing-p2.md |
| 2026-07-16 | **소비자 직행 채팅 /chat + 인스타 유도 라인 (worktree-webchat, T-webchat-chat-landing)** | 테오: 소비자가 로그인창 없이 바로 채팅 연결. ① /chat 공개 라우트(PR #334) — 위젯 컴포넌트 standalone 재사용(전체화면·닫기=뒤로가기), ?src= 화이트리스트(ig/kakao/qr/direct, 그 외=chat — 임의값 DB 차단), 스플래시 게이트 /chat 제외, 인박스 src 라벨 ko/vi ② 인스타 biz.villago 유도 세팅(테오 수동): 자주 묻는 질문 4종+프로필 링크=villa-go.net/chat?src=ig, 2FA(인증 앱) 활성 ③ 소개 게시물 재료: CTA 카드 3종(villa-go-ig-intro-1~3.jpg, 기존 satori ctaTemplate 재사용)+COPY 캡션 2안(금칙어 검수) | 약식 사이클. 프로덕션 모바일(360×800) 실측: 즉시 채팅 UI·스플래시 없음·standalone 닫기 숨김. ★인스타 실고객 DM 직접 연동=App Review 후(DM 인박스는 기구현)·구 마벨모아 비즈니스=죽은 이메일 인증 잠김(App Review 시 새 포트폴리오 권장). 계약: docs/contracts/T-webchat-chat-landing.md |
| 2026-07-16 | **웹챗 확장 — 운영자 3등급 개방 + 부착면 3곳 (worktree-webchat, T-webchat-expand)** | 테오 지시 2건. ① admin 웹챗을 OWNER/MANAGER/STAFF 3등급 개방: 운영자 4라우트 isSystemAdmin→isOperator·세션 스코프=조직 공유(ownerAdminId 필터 제거, Zalo 개인 스코프와 다름 — 컬럼은 알림용 유지)·SSE publish=활성 운영자 전원 fan-out(listActiveOperatorIds 60s 캐시)·미읽음 뱃지 조직 집계·/messages 게이트는 원래 isOperator라 무변경·sentBy=실응답자 기록 확인 ② 위젯 부착: /g 4화면(g:토큰8자, offset=96 — 하단 sticky CTA 회피)·/p 4화면(p:토큰8자)·auth 8화면(auth) — components/webchat-mount.tsx 명시 마운트(admin 구조적 미표출)·로더 __VG_WEBCHAT_PAGE 전역 폴백+오프셋 지원·intro 3종 ?v=20260716b 버전업·인박스 sourcePage 뱃지(parseSourcePage, ko/vi 6키) | QA 독립 검증 **PASS(P0/P1 0)** — 임시 MANAGER/STAFF/SUPPLIER 실계정 로그인 실측(403 AUTHZ_DENY 4건·조직 공유 확증·sentBy 정확)·Playwright 렌더(login/g/p FAB·admin 미표출·전역 폴백)·테스트 데이터 잔존 0. tsc·build 통과. P2 관찰 1건: 미존재 토큰 404에는 위젯 미마운트(Next 기본 — 수용). 계약: docs/contracts/T-webchat-expand.md |
| 2026-07-16 | **가예약 취소·환불 규정 전자 동의 기록 (worktree-contracts-admin-docs, T-proposal-policy-consent)** | 계약서 프레임워크 §7(직판 B2C=입금 전 동의 필요) 후속. ① 스키마 additive: `Booking.policyConsentJson JSONB`(라이브 raw SQL 적용+migrations-manual 보존) — 동의 시각+**당시 정책 스냅샷**(fullDays/partialDays/partialPct, 정책 변경돼도 동의 당시 조건 증빙)+locale ② `/api/p/[token]/hold`: CANCELLATION_POLICY enabled=true면 `policyConsent:true` 필수(400 CONSENT_REQUIRED), 스냅샷=**서버가 AppSetting에서 산출**(body는 boolean+locale만 — 정책 값 주입 경로 0), enabled=false=미요구·미저장(하위호환) ③ /p book 폼: 정책 요약+동의 체크박스(미체크=버튼 disabled, 5언어 public-i18n) ④ admin 예약 상세 동의 시각·당시 정책 1줄(있을 때만, ko/vi) | QA 독립 검증 **PASS(P0/P1/P2 0)** — 주입 불가·누수 0(스냅샷=%·일수만)·수정금지구역 diff 0·구 탭 400=새로고침 자가치유(완화 불요 판정). 신규 테스트 포함 42/42·tsc0·build 통과. 전체 스위트 실패 2건=웹챗 라우트 가드 미등재(origin/main 기존 부채, cat-file로 확정 — 별건 백로그). 계약: docs/contracts/T-proposal-policy-consent.md |
| 2026-07-16 | **사업 계약서 3종 초안 + ADMIN 문서 뷰어 (worktree-contracts-admin-docs)** | 테오: 빌라·랜드사·부가서비스 공급/지급 계약 체계 필요 + ADMIN 열람. ① `docs/business/contracts/` 4종 — 00 프레임워크(돈 흐름 "선취-후취" 고정: 랜드사 선금30%+D-14 완납→빌라 체크인 전일 지급→벤더 이행 완료 후 일/주/월 정산·유리 조항 7종·법률 플래그 6건·서명 운영 절차)+계약서 초안 3종(01 빌라: 지급 전 취소=무정산 기본+back-to-back 취소 상한 / 02 랜드사: 미납 자동취소·여신은 별도 부속 합의만, ADR-0022 정합 / 03 벤더: 완료 건만 정산·티켓 특약·노쇼 대체조달 구상). 원가·단가는 PMS 등록값(스냅샷) 정본 참조=시즌 변경 재서명 불요. ② `/documents` ADMIN 뷰어 — RSC+`?doc=` 화이트리스트 registry(path traversal 봉쇄)·react-markdown+remark-gfm(rehype-raw 미사용=XSS 차단)·마진 전략 포함 문서라 **canViewFinance(OWNER/MANAGER) 게이트+FINANCE_PATHS 미들웨어+사이드바 cap 3중**(STAFF 차단)·adminDocuments NS ko/vi | QA 독립 검증 **PASS(P0/P1 0)** — 권한 3중 가드·traversal·XSS·i18n 패리티 12/12·회귀(additive만) 전수. P2 1건(FINANCE_PATHS 누락) 병합 전 반영. tsc0·build 통과(ƒ /documents). ★서명 전 차단=계약 주체(법인/사업자) 미정+VN 변호사 검토(위약벌 8% §301·준거법·원천징수)+LOC 베트남어 번역. 약식 사이클(문서+뷰어 페이지) |
| 2026-07-16 | **홈페이지 다국어 웹 채팅 MVP (worktree-webchat, T-webchat-mvp)** | 비로그인 방문자↔운영자 실시간 채팅+Gemini 자동번역(vi/ko/en/zh/ru). ① 스키마: WebChatSession/WebChatMessage 신규 2테이블(ADR-0045, 금액 컬럼 구조적 배제, 라이브 DB additive SQL 2건 적용) ② API: 세션 원자생성(HMAC 서명 httpOnly 쿠키·Turnstile siteverify 미설정 시 스킵)·발신(eager ko 번역 캐시)·폴링(INBOUND ko번역 미노출)·운영자 인박스/상세/답장(번역 실패 시 ko 원문+translationFailed 발송)/차단. 가드: WEBCHAT_PAUSED 킬스위치·일일 번역캡(DB 원자 카운터)·스로틀 세션15/IP40/분(HMAC ipHash)·2000자·ko/무문자 번역 스킵 ③ 알림: WEBCHAT_NEW_MESSAGE(A-15) 그룹 라우팅+세션당 10분 인메모리 디바운스 ④ 위젯: /webchat/widget iframe+public/webchat-loader.js(FAB, PC 380×560/모바일 풀스크린)+intro 3종 임베드(?v= 캐시버스팅)+스플래시 게이트 /webchat 제외+CSP report-only에 challenges.cloudflare.com 선반영 ⑤ 인박스: /messages 소스 탭(Zalo\|웹 채팅, 자체 커서)·원문+ko 병기·SSE source 필드 분기 ⑥ LOC 5언어 감수 | TDA·FE·INTEG 3자 회의+QA·OPS 2차 감사 선행(docs/plans/webchat-multilingual.md). QA 독립 검증 **PASS(P0/P1 0)** — 라이브 실측 12항목·누수 4종 0·Zalo 탭 회귀 0·테스트 데이터 잔여 0. tsc0·build 통과. **잔여(OPS·테오)**: Turnstile 키 발급(미설정 시 스킵 동작)·CF WAF 폴링 완화 규칙·배포 후 Playwright 실측. 계약: docs/contracts/T-webchat-mvp.md |
| 2026-07-16 | **인스타그램 자동 포스팅 P1 (instagram-marketing-p1, wt/instagram-p1)** | 한국인 대상 인스타 마케팅 구현: InstagramPost 모델+enum 2종(라이브 raw SQL, PUBLISHING=발행 락, villa SetNull=이력 보존) · lib/instagram 8모듈(sharp+satori 렌더 4종 템플릿 1080×1350·Graph API 캐러셀 발행·빌라 로테이션 초안·Gemini 캡션+금칙어 58종 가드+해시태그 계층 로테이션) · cron 2종(draft 일 3건 PENDING_APPROVAL/publish KST 슬롯) · admin `/marketing/instagram`(승인 큐·캡션 편집·설정 패널·NS adminInstagram ko/vi) · lib/secret-crypto(AES-256-GCM 범용 추출) · docs/marketing 4종(기획 v1.1·copy-guide·hashtags·테오 계정 가이드=사업자 서류 불요 경로) · design/stitch/instagram-templates(satori-safe 4종+SPEC) · .claude/agents/copy.md 신설 | QA 독립 검증 **PASS(P0/P1 0)** — build·tsc·lint 0, 누수 전수 0(VILLA_SELECT 공개 필드 봉인·토큰 last4만·AuditLog 100%), 금칙어 가드 실측. P2 #1(priceValue 계약 주석) 반영. ★잔여: 테오 P0(계정 생성·토큰 → instagram-account-setup.md)+2주 워밍업 후 OPS cron 등록, Zalo 푸시 알림은 인앱만(원하면 TDA enum 추가), DM 인박스=Phase 2. 계약: docs/contracts/instagram-marketing-p1.md |
| 2026-07-16 | **회원가입 비밀번호 2회 입력 (worktree-signup-password-confirm)** | 테오: 회원가입 시 비밀번호 두 번 입력하게 변경. 가입 폼 4종 전부(공급자 SignupForm·청소원 CleanerSignupForm — signupAction 공유 / 파트너 PartnerSignupForm·벤더 VendorSignupForm — fetch)에 "비밀번호 확인" 필드 additive: 기존 showPassword 토글 공유(확인 필드 새 버튼 없음)·autoComplete new-password. 검증=① 서버 액션 폼: 양쪽 input onChange에서 setCustomValidity 재계산(네이티브 제출 차단) + signupAction에 zod 파싱 전 불일치 400 `passwordMismatch`(JS 우회 대비, 구식 클라 passwordConfirm 미전송=fail-closed) ② fetch 폼: onSubmit 비교 후 setError — **API body에 passwordConfirm 미포함**(partner/vendor-signup route 무수정=스키마 하위호환). i18n=4개 NS(auth.signup/cleanerSignup/partnerSignup/vendorSignup)에 passwordConfirm·passwordConfirmPlaceholder·errors.passwordMismatch ko/vi 동시 추가(순수 키 추가만) | UX-VN 구현→QA 독립 검증 **PASS(결함 0·7항목)** — 4폼 전수·null 케이스 fail-closed·i18n 12/12·수정금지구역 diff 0·누수 0. tsc0·build 통과. ★T4.4 파일럿에 "확인 필드 이해도" 항목 추가 후보. 계약: docs/contracts/T-signup-password-confirm.md |
| 2026-07-15 | **스플래시 후속 3건 — 길이 조정 2회 + 모바일 키보드 가림 수정 (PR #320·#321, worktree-splash-intro)** | 테오 지시 순차 반영. ① #320: 총 1.6s→2.6s(완성 로고 홀드 +1s — 등장 템포 불변, 페이드만 이동). ② #321-1: **모바일에서 인트로 재생 중 터치 키보드가 인트로를 가림** — 코드에 autoFocus 없음에도 브라우저/자동완성이 로그인 입력창에 포커스 주는 경로 → splash-intro.tsx에서 재생 활성 동안만 focusin 가드(오버레이 밖 포커스=ref 기억+즉시 blur, 늦은 발화 흡수), 종료/스킵 시 focus({preventScroll}) 복원(isConnected 가드). 미표시 경로는 리스너 미등록=부작용 0. ③ #321-2: 총 2.6s→**2.3s**(홀드 -0.3s — 최종: 등장 1.3s+홀드 0.7s+페이드 0.3s, JS 종료 2300ms·하드상한 2900ms). 계약서 수치 동기 갱신 | 약식 사이클(타이밍 상수·단일 컴포넌트 가드 — PR #314 선례). tsc·build 통과, Playwright 실측 ~2306ms·억제/복원 경로 확인. 프로덕션(cef54f13) 실측: 재생 2371ms·재생 중 activeElement=body |
| 2026-07-15 | **villa-go.net 최초 진입 스플래시 인트로 — 핀 드롭 (worktree-splash-intro)** | 테오: "화면 최초 인트로, 이왕이면 3D, 회사 장점, 1~2초". DESIGN·FE 회의 → 연출 3안 중 **핀 드롭** 테오 확정: 3D 원근 낙하→착지 바운스+링 파동→오렌지 지붕점 팝→"Villa GO"+태그라인(ko 찾던 그 빌라, 여기 있어요 / vi Villa bạn tìm, có ở đây), 총 1.6s·하드상한 2.2s. ① 마운트=루트 layout.tsx만: `<head>` 동기 게이트 스크립트(페인트 전 sessionStorage·reduced-motion·`/p/`·`/g/` 판정→`html[data-splash]`)+NextIntlClientProvider **밖** 정적 오버레이(조건부 렌더 0=하이드레이션 미스매치 0, 오버레이 아래에서 착지 화면 로딩 병행) — 리다이렉트 허브 page.tsx 불변. ② 3D=CSS transform/opacity 키프레임만(신규 의존성 0), 로고=인라인 SVG 레이어 재구성(흰 핀/티얼 집/오렌지점 — 굴뚝 오추가는 원본 PNG 대조로 후속 제거). ③ 세션당 1회(sessionStorage `vg-splash`, **완료/스킵 시점 기록**=/logout 중간 홉 미소진), 탭·키·visibilitychange 즉시 스킵, aria-hidden·포커스트랩 없음. 외부 손님 링크(/p·/g)=미표시. ★CSP enforce 전환 시 게이트 스크립트 nonce 필요(T-sec-csp-enforce 범위에 포함할 것) | QA 독립 검증 **PASS(계약 §4 8/8)** — 프로덕션 빌드 실측: 노출 1616ms·같은 세션 재표시 없음·클릭 즉시 종료·/p·/g data-splash null·reduced-motion 스킵·콘솔/하이드레이션 에러 0·360px 가로 스크롤 0·ko/vi 태그라인·순수 additive(삭제 라인 0)·누수 0. 미검증 2건 비차단(로그인 재진입=실계정 부재, lint=worktree 중첩 환경 제약 — tsc·build 통과). 계약: docs/contracts/T-splash-intro.md |
| 2026-07-15 | **모집용 사업 소개 페이지 3종 + 로그인 진입 링크 (T-supplier-intro-page, wt/intro-page)** | 테오 영업용(가입 전 설득) 정적 소개 문서 3종을 `public/`에 상시 서빙 + `/login` 하단 소개 링크 블록. ① `public/intro.html` — 빌라 관리인(공급자)용, vi 기본+KO 토글: 사업 구조 3단 흐름(등록→Villa GO가 국제 관광객에 판매→전액 수령)·혜택 6카드·지금 등록 이유·4단계·FAQ·CTA(villa-go.net/signup). ② `public/intro-vendor.html` — 부가서비스 업체(마사지·입장권·과일·BBQ·차량 등)용, vi+KO: Zalo 주문 수신·시간 제안·QR 티켓·지역 우선 배정·건별 대사. ③ `public/intro-partner.html` — 여행사·랜드사 B2B용, **6개 언어**(ko/en/vi/ru/zh/hi, JS 사전 토글): 비공개 재고·제안서 링크·24~48h 홀드·검수 게이트·현지 운영 위임·여신 전환. ④ LoginForm에 헤딩+3링크 블록(`auth.login.introHeading/introVilla/introVendor/introPartner` ko·vi, 구 introLink 대체). **누수 원칙 준수**: 판매가·마진·재고 수치 0, 허위 통계 0(정성 표현만). 사용자 지시로 고객 타겟 "한국 손님"→"국제 관광객(한·러·중·인도·영어권)" 전면 교체. CTA 실링크(Zalo OA·카카오톡 채널 URL)는 TODO 주석 — 확정 시 교체 | QA 독립 검증 **PASS(결함 0)** — 실브라우저(포트 3025): 비로그인 3종 200(리다이렉트 0)·6개 언어 토글 69키 empty 0·ru/zh/hi 글리프 렌더·/login ko/vi 3링크·360px scrollWidth 345·누수 grep 0. tsc0·lint·build 통과. 계약: docs/contracts/T-supplier-intro-page.md |
| 2026-07-15 | **로그인 잠금 구분 안내 + 잠금 창 1분 단축 (worktree-login-ratelimit-message)** | 실사고(신규 가입자 0702635421): 가입 전 로그인 5회 실패로 전화번호 rate limit 잠금 → 비번 초기화 후에도 일반 "아이디/비번 오류"와 동일 표시라 "초기화해도 안 됨" 혼란. ① auth.ts authorize 잠금 분기에서 `RateLimitedError extends CredentialsSignin`(code="rate_limited") throw — 서버 액션 catch에서 `error.code` 직접 검사(beta.31 실측: AuthError는 래핑 없이 원본 re-throw). 로그인 화면 "시도가 너무 많습니다. 1분 후 다시 시도해 주세요" 구분 표시(tooManyAttempts). ② **가입 자동 로그인 경로**(signupAction, 이번 사고의 실제 경로): rate_limited면 serverError로 뭉개지 않고 "가입은 완료되었습니다. 1분 후 로그인해 주세요"(signupDoneLoginLater — 계정은 생성됨). vendor/partner 가입=자동 로그인 없음 전수 확인. ③ **잠금 창 10분→1분**(테오 지시): LOGIN_PHONE_LIMIT 5회/1분·LOGIN_IP_LIMIT 20회/1분(안내 문구가 항상 참이 되도록 동일 창). ④ i18n ko/vi 3쌍(login.tooManyAttempts·signup/cleanerSignup.signupDoneLoginLater). 보안: 잠금 판정=DB 조회 전·번호 존재 무관이라 계정 존재 유추 불가(QA 실측 — 미존재 번호도 동일 메시지), 에러는 액션에서 잡혀 값 반환(스택 직렬화 0)·URL code 노출 0 | QA 독립 검증 **PASS(결함 0)** — 실브라우저(포트 3457, Playwright): 1~5회 invalidCredentials·6회째 잠금 문구 ko/vi 실렌더, passkey·mustChangePassword·성공 경로 회귀 0. tsc0·lint·테스트 26·build 통과. 계약: docs/contracts/T-login-ratelimit-message.md |
| 2026-07-15 | **실DB 자동 백업 세팅 (worktree-db-backup-automation)** | 테오: 실DB 초기화·실데이터 입력 개시 — 자동 백업 전무(07-09 수동 스냅샷 1회뿐, 당일 pg_dump 재시도=0바이트 실패·PG18 vs 17 불일치)라 일일 자동 백업 구축. ① `lib/db-snapshot.ts` — export-full-snapshot의 dmmf 전 모델 JSON 스냅샷 로직 공용화(BigInt=`"123n"` 직렬화)+보존 경계 순수 함수 `selectKeysToPrune`. ② `/api/cron/db-backup`(매일 20:00 UTC=VN 03:00) — verifyCronAuth 게이트→스냅샷→gzip→**백업 전용 프라이빗 R2 버킷** 업로드(`daily/` 14개+매월 1일 `monthly/` 12개 보존, 초과분 자동 삭제, 같은 날 재실행=덮어쓰기 멱등). 버킷=`BACKUP_BUCKET_NAME` 필수(미설정 500 — **공개 이미지 버킷 폴백 금지**: 백업엔 여권·원가·마진 전부 포함), 자격증명=`BACKUP_*` ?? `STORAGE_*` 폴백(기존 R2 토큰이 버킷 스코프라 계정 수준 CreateBucket 403 실측). ③ 실패 경보=인앱 직접 적재+SecurityEvent `BACKUP_FAIL`(ZALO_OPERATOR_NOTIFY_PAUSED 비경유 축)·최종 신호=Railway Cron Runs 빨강. ④ `scripts/restore-from-snapshot.ts` — 드라이런 기본·기존 행 존재 시 중단·TRUNCATE/DROP 자동 실행 없음·per-table DISABLE→ENABLE TRIGGER try/finally·**dmmf BigInt 필드 한정 역변환**(lib/snapshot-restore.ts — String "123n" 오변환 차단, QA P2-1 반영). ⑤ 실패 응답 internal_error 일반화+sanitizeReason(연결 문자열 마스킹, QA P2-3). 런북 docs/ops/db-backup.md(복원 절차·부분 삽입 경고)+cron-registration.md `cron-db-backup` 행 | QA 독립 검증 **PASS(P0/P1 0)**→P2 4건 후속 수정→재검증 **PASS**. tsc0·vitest 9/9(prune 경계+BigInt 왕복)·build 통과. 잔여 테오 수동 2건=Cloudflare 프라이빗 버킷 `villa-pms-db-backups` 생성+토큰 권한(런북 §버킷 준비). 계약: docs/contracts/T-db-backup-automation.md |
| 2026-07-15 | **게스트 체크인 G2 비품 수량 표시 (worktree-guest-amenity-qty)** | 테오: "소비자 체크인 화면에서 비품의 수량은 확인을 할 수가 없어". Triage: 공급자 마법사(T-amenity-quantity-custom)로 `VillaAmenity.quantity` 저장·운영자 체크인 시트 인쇄까지 됐으나, 게스트 로더(guest-checkin-load) select에 quantity가 없고 G2 화면은 라벨 칩만 렌더 — 미니바만 수량 표시. 수정: ① 로더 select에 quantity 추가(타입 동기) ② `GuestAmenityGroup.items`=`{label,qty}[]`로 변경(소비처 전수 grep 2곳뿐) ③ G2 칩에 **qty≥2일 때만 `×N` 접미**(기본 1="있음"이라 ×1은 노이즈, ×N=언어 중립이라 5언어 i18n 키 0) ④ 미니바·/p(비품 미노출)는 범위 외 | QA 독립 검증 **PASS(결함 0)** — select 화이트리스트 유지(unitPrice 등 신규 노출 0)·타입 소비처 전수·admin amenityGroups는 별개 로컬 타입 확인. tsc0·lint·build 통과. 계약: docs/contracts/T-guest-amenity-quantity.md |
| 2026-07-14 | **요금 캘린더 디버깅 리뷰 후속 수정 9건 (worktree-rate-calendar-fixes)** | 테오 지시 "업무프로세스 확인 + 코드 디버깅": 8관점 리뷰(파인더 25후보→검증 확정 10건, 반증 2건) 후 수정. **P2 4건**: ① parsePct 로케일 콤마("1,5"가 +15%로 오파싱)→콤마 정규화+소수점 중복 거부 ② 공급자 cost 라우트 max(60) vs admin 무제한 생성 충돌(레이어 60개 초과 시 공급자 원가 저장 영구 400)→`MAX_RATE_PERIOD_ROWS=200` 공유 상수+layers/batch LAYER_LIMIT ③ batch pct 하한 부재(-150→unhandled 500)→`gt(-100).max(500)` ④ #314 회귀: STAFF 프리미엄 요일 편집 상실(showFinance 게이트 안으로 이동)→`!showFinance` 전용 에디터 복원. **P3 5건**: COPY_YEAR 윤년 경계(exclusive end 직접 시프트→마지막 밤 시프트+1일 `shiftEndDateYears`, 밤수 보존)·승자/프리미엄 규칙 단일 원천화(periodBeats·premiumReasonFor·hasAnyPremiumValue export→클라 손복사 제거, ★리터럴 WEEKDAY→WEEKDAY_RULE 소비처 3파일 짝 갱신)·구간화 계산 $transaction 밖 분리+range ≤1100박 캡·CalendarGrid useMemo/React.memo(hover 42셀 재계산 제거)·고아 rate-period-editor.tsx 삭제. 전체교체 PATCH 검증을 lib/rate-period-input 공용 fragment로 통합(특성화 테스트 4건 선행). 반증 2건: cuid v1 단조성으로 tie-break ④ 안전·base 없는 빌라는 생성 플로우상 불가 | **프로세스 완전 준수 사이클**: 계약서→QA 사전 합의(조건 C1~C4 — C1이 계약 초안의 윤년 기준 자기모순을 착수 전 적발)→BE/FE 병렬→QA 독립 검증 **PASS(전 항목)**. tsc0·vitest **213/3188**·build 통과. 누수 0(STAFF payload·/info 응답·export 함수 금액 미반환 확인). 계약: docs/contracts/rate-calendar-fixes.md |
| 2026-07-14 | **프리미엄 요일 편집 캘린더 통합 (PR #314, worktree-rate-cal-premium-merge)** | 테오(스크린샷): 빌라 상세의 "프리미엄 요일" 별도 카드가 요금 캘린더와 중복 + 레이아웃 깨짐 → 카드 제거, 캘린더 범례 "● 프리미엄일" 클릭 시 인라인 요일 칩 편집(토글 즉시 캘린더 ● 마커 실시간 반영, 기존 PATCH /info 재사용, premium-days-editor.tsx 삭제) | 소규모 후속으로 약식 진행(계약·QA 에이전트 생략) — 사후 디버깅 리뷰에서 STAFF 편집 상실 회귀(P2) 발견돼 위 수정 ④로 복원. ★교훈: UI 통합 시 구 컴포넌트의 노출 게이트(전 운영자 vs finance)까지 대조할 것. 프로덕션 실확인 완료 | 테오: 기간별요금제 폼 리스트가 복잡 — 달력 기반 + 기간 중복·중첩 허용으로 대폭 개선(인터랙티브 목업 4차 반복 승인, design/stitch/rate-calendar/interaction-spec.html=정본). ① **겹침 허용 레이어 모델**: 밤별 승자 규칙 `짧은기간→시즌등급→늦은시작→최신`(lib/pricing.ts periodBeats), 프리미엄 폴백·ADR-0031 계층은 승자 결정 후 기존 그대로. 겹침 409 제거(half-open 유지). 라이브 데이터 겹침 0이라 무마이그레이션·견적 회귀 불변. ② **스키마 additive 1컬럼**: `VillaRatePeriod.batchId`(+index, 라이브 적용·migrations-manual 2026-07-14) — 일괄 작업 그룹 취소. ③ **레이어 단위 CRUD + 배치 API**: layers POST/PATCH/DELETE(base 보호)·batch POST(ADJUST/SET/COPY_YEAR — 서버 승자 구간화, 1,000₫/100원 반올림, 2/29 보정)·batch DELETE(그룹 취소). 전체교체 PATCH는 하위호환 유지. 구간화=lib/rate-layers.ts 순수 함수(FE 미리보기 공유). 전 변경 writeAuditLog. ④ **운영자 캘린더**(components/rate-calendar/ 9파일, B21 다크 디자인): 시즌색 셀+승자가+●/★+겹침 밴드(lane-packing)·승자 스택 패널·도구 4종(기간추가 DateField 양방향/날짜선택 바구니/일괄조정/연도복사 음력경고)·레이어 편집 시트·batchId 묶음 취소·연도 필터·원가+마진→판매가 제안 재사용. rateCalendar NS ko/vi 60키+화이트리스트. ⑤ **공급자 원가 캘린더**(A10 라이트·vi·390px): 도구 2종만, cost 라우트 전체교체 유지+겹침만 허용(누수면 0 결정), supplierRatePeriods.cal 31키, 구 rate-period-cost-editor 삭제. ⑥ Stitch 타임아웃 → 승인 목업 기반 정적 HTML 직접 작성(fallback, Playwright 렌더 검증) | QA 독립 검증: 계약 6항목 **전부 PASS**·권한/마진 누수 **0건**(공급자 select=자기 금액 4필드만)·P1 1건(누수 가드 substring 위양성)→가드 무약화 해소(DTO 필드 costVnd→supplierCostVnd 개명). tsc0·vitest **213파일/3168**·build 통과. ★남은 것: 테오 실사용 확인 + T4.4 파일럿에 A10 포함. 계약: docs/contracts/rate-calendar-ux.md |
| 2026-07-14 | **게스트 신청 내역 티켓 문의 안내 위치 이동 (PR #312, wt/orders-ticket-contact-bottom)** | 테오(스크린샷): "티켓 관련 문의는 Villa Go로" 안내 블록(카카오톡·전화 버튼)을 신청 합계 위 → **페이지 맨 아래 정산 안내(settleNote) 밑**으로. 순서=[신청 합계]→[정산 안내]→[티켓 문의]. JSX 블록 이동만(내용·클래스 무변경), guest-orders.tsx 단일 파일 | lint·tsc·build 통과 |
| 2026-07-14 | **게스트 신청 내역 품목 그룹 카드 접고 펴기 (PR #310+#311, wt/orders-group-collapse)** | 테오: 신청 내역 "요청한 옵션"의 콤보 티켓(빈원더스+빈사파리 2일권 등) 카드가 구분 라인·QR 그리드로 길어짐 → 그룹 헤더 탭으로 본문 접기/펴기. `expandedGroups: Set<g.key>` 클라 로컬 state, **기본 전부 접힘**(#311 테오 후속 지시로 반전 — 미해결 시간제안은 상단 전역 배너가 항상 노출하므로 무방, 새 그룹도 자동 접힘), 헤더=button화(aria-expanded)+chevron 회전, 접힘 시 라인 블록 미렌더. 전 품목 그룹 일괄 적용, guest-orders.tsx 단일 파일·i18n 무변경 | FE 구현+QA 독립 검증 PASS(누수 0 — API·로더 무변경). lint·tsc·build 통과 |
| 2026-07-14 | **부가서비스 공급자 변경 허용 확장 (worktree-vendor-change-selector)** | 테오: "필요에 따라 공급업체를 변경해야 되는 상황이 있다 — 패널 벤더 표시를 셀렉터로". 기존 셀렉터(PR #242)가 서버·클라 쌍둥이 가드로 REQUESTED+미발주/거절에만 열려 있던 것을 확장. ① 새 허용 규칙(서버 정본+클라 미러 대칭): status∈{REQUESTED,CONFIRMED} · 미정산 · 미이행완료 · (TICKET) 미발권 — 위반=409 VENDOR_LOCKED+reason(STATUS_CLOSED/SETTLED/COMPLETED/TICKET_ISSUED). ② 안전핀: 살아있는 발주(PENDING_VENDOR·ACCEPTED) 교체 시 **구 업체 발주 취소 통보**(★변경 전 스냅샷 vendorId로 직접 조회 — 변경 후 재조회는 새 업체 오발송, 무료 티켓 제외 게이트 재사용) + 클라 confirm 1회(취소=select 원복). ③ TICKET→직접 제공(null) 전환 400 TICKET_VENDOR_REQUIRED(PR #304 정합) + TICKET 주문엔 "없음" option 미렌더. ④ QA 지적 수정: 교체 대상 벤더 검증에 active 추가(APPROVED+비활성 교체 갭 — canSellItem 정합), 테스트 mock TS2556. 자동 재발주 체이닝·updateMany 동시성 가드(새 규칙과 동일 집합) 유지. ⑤ **실운영 회귀 수정(dd638a3)**: canDispatch가 REQUESTED 전용이라 CONFIRMED 변경→자동 재발주가 409 → 발주 리셋 상태로 벤더 발주함에서 실종(demo-svc-so-2 실사례, 수동 복구) → canDispatch {REQUESTED,CONFIRMED} 확장(vendorStatus 중복 발주 방지 조건 불변)+변경→재발주 체인 통합 테스트 2건. ★교훈: 상태 가드 확장 시 후속 체인(dispatch 등)의 상태 전제 전수 확인. ⑥ **타입 매칭 필터(75553c3, 테오 추가)**: 셀렉터=주문 type 취급 업체만 — ServiceVendor에 타입 컬럼 없어 파생(lib/vendor-service-types.ts 단일 원천: 활성 카탈로그 type ∪ 지역커버리지 ∪ 빌라지정 합집합, 신호 0=미분류 미표시)·서버 대칭 400 VENDOR_TYPE_MISMATCH(기존 findUnique select 병합=추가 쿼리 0)·현 배정 불일치 업체=disabled option 값 유지 | QA 독립 검증 1차 조건부 FAIL(P1 typecheck·P2 active 갭) → BE 재작업 → 재검증 **PASS** → 회귀 수정 재검증 **PASS** → 타입 필터 검증 **PASS(결함 0)**. 테스트 전체 **3128**·tsc0·build 통과. 계약: docs/contracts/service-order-vendor-change-expansion.md |
| 2026-07-14 | **공급자 화면 가격 라벨 "공급자 원가" 통일 (worktree-supplier-cost-label)** | 테오: 공급자가 입력하는 금액 라벨("1박 원가"·"받을 금액")이 소비자 판매가와 혼동됨 → 공급자 대면 문구를 **"공급자 원가"로 통일**(i18n 값만, 키·코드 무변경). ko: 마법사 7단계 title/subtitle("소비자 판매가가 아닙니다" 명시)·빌라 상세 price/manageCost/manageRatePeriods·supplierRatePeriods(title/intro/baseHint/cost/오류 2종/premiumCost)·costSeasons(title/enterPriceHint)·튜토리얼 rates. vi: 마법사 title "Giá gốc của bạn (mỗi đêm)"+subtitle "không phải giá bán cho khách"·manageCost "Giá gốc & mùa"(나머지 vi는 기존부터 "giá gốc"으로 명확). 3단 가격 구조(공급자 원가→NET→소비자가)는 기구현 확인 — 스키마·API 무변경 | JSON 파싱 OK·ko/vi 키 패리티 4185 일치. 문구만이라 tsc/vitest 무관 |
| 2026-07-14 | **TICKET 벤더 미배정 판매 차단 (worktree-ticket-vendor-required)** | 테오 부가서비스 철저 검사 중 발견: 7/13 퀸테센스 콤보 TICKET 2건이 카탈로그 공급자 미지정 상태로 게스트 판매 → vendorId=NONE 스냅샷 → 발주·발권 경로 진입 불가(발권 불가능한 주문). 테오 확정 "벤더가 없는 상태로 판매가 이루어지는 것 자체가 문제". ① `lib/ticket-vendor-guard.ts` 단일 원천 — `canSellItem`(순수: 비TICKET=true, TICKET=해석벤더 존재+APPROVED+active)·`loadCanSellItem`(조회 래퍼, 승인·활성만 select). ② **판매 3경로 전수 가드**(게스트 /g·운영자 /bookings·파트너 /p POST): create 전 400 `TICKET_VENDOR_REQUIRED`, 무료 variant 포함 품목 단위 차단(isFreeTicket 분기보다 앞 — 우회 불가). ③ 노출 차단: loadGuestCheckin·loadPartnerAddon 목록 필터(판정 전용 select, payload에 벤더 내부 필드 미노출) + admin 주문 폼 `ticketSellable` disabled+안내(adminServiceOrders.ticketNotSellable ko/vi). ④ 비TICKET "미지정=직접 제공" 모드 불변(테오 가정 승인 대기 없이 진행). 운영 조치 병행: 기존 costVnd=0 티켓 4건 backfill-ticket-cost --apply 소급(감사로그), 퀸테센스 기존 주문 2건=운영자 수동 벤더 지정+발주 안내 | QA 독립 검증 **PASS(P0/P1/P2 0건)** — 3경로 대칭·4번째 생성 경로 없음(create 전수 grep)·무료 우회 차단·누수 0(명시 필드 열거)·픽스처 갱신 의도 보존. 신규 테스트 28·전체 **3091**·tsc0·build 통과. 잔여 백로그: ①주문 생성 시 카탈로그 원가 스냅샷(현재 costVnd=0 설계 — backfill 반복 필요) ②벤더 미배정 기존 TICKET 주문 경고 뱃지 ③관리자 QR 가시성(허브 미노출·패널 접힘). 계약: docs/contracts/ticket-vendor-required-sale-block.md |
| 2026-07-14 | **운영자 알림 일시정지 스위치 (worktree-operator-notify-pause-switch)** | 테오 지시: 관리자 알림 잠시 정지. AppSetting `ZALO_OPERATOR_NOTIFY_PAUSED`("1"|"true", trim·대소문자 무관) → enqueueOperatorNotification 최상단 게이트에서 그룹·개별 DM 폴백 모두 미적재 0 반환(드롭 — 소급 발송 없음). **fail-open**(키 부재·빈 값·기타 값·조회 throw=정상 발송 — 침묵 사고 방지). 범위=운영자 업무 알림만: SECURITY_ALERT·ZALO_LISTENER_DOWN은 직접 적재 경로라 무영향(의도), 공급자·벤더·게스트 알림 무관. NOTIFICATIONS.md 동시 갱신 | QA 독립 검증 **PASS(결함 0)** — fail-open 전 경로·tx 주입 경로 게이트 통과·보안/장애 경보 비경유 grep 재확인·목 개편 의미 보존. 신규 테스트 4종·전체 3063 통과·tsc 0·build 통과. 계약: docs/contracts/operator-notify-pause-switch.md |
| 2026-07-13 | **게스트 여권 업로드 즉시 자동 OCR 명단 (worktree-guest-passport-auto-ocr-roster, ADR-0043)** | 테오 지시: 여권 필수화(PR #300) 후속 — 업로드 즉시 OCR로 티켓 이용자 명단 생성(운영자 확정 전에도 인원별 선택 가능). ① GuestCheckinToken.passportOcrJson JSONB additive(라이브 raw SQL 적용+migrations-manual 보존) ② 업로드 라우트: 사진 push 후 ocrPassport 동기 실행 → **원자 jsonb append**(COALESCE||, 파라미터 바인딩 — 동시 업로드 lost update 방지), 실패 관용(키 미설정·타임아웃에도 201, OCR 내용 로그 0) ③ lib/checkin-roster.ts 공유 헬퍼: 확정본(CheckInRecord) 1명↑ 우선 → 잠정본 폴백(전부-null 제거+ticketGuestKey 중복 제거) — **소비처 4곳(게스트 옵션·게스트/운영자 주문 API·운영자 예약 상세) 단일 원천 = UI/서버 검증 대칭**(MISMATCH 오탐 봉쇄) ④ 확정본=정본 불변(completeCheckIn 무변경, 셀프 서명 토큰 보관 선례 패턴) | QA 독립 검증 **PASS(결함 0)** — 누수 전수(여권번호·국적 등 원천 미유입, 명단 통로=guestsFromPassportOcr 화이트리스트 단일), 대칭성 4곳 동일 헬퍼, 신규 테스트 9/9·전체 3059 통과·tsc 0·build 통과. RSC dev flight의 passportOcrJson:null 관측=React dev 디버그 메타(프로덕션 미노출, 결함 아님). 계약: docs/contracts/guest-passport-auto-ocr-roster.md |
| 2026-07-13 | **셀프 체크인 여권 사진 필수화 (worktree-guest-passport-photo-required)** | 테오 지시: 여권 사진을 필수 입력으로(스킵 → OCR 명단 부재 → 티켓 인원별 모드 불가 연쇄 차단). G4 여권 단계 — ① 전원(guestCount) 업로드 완료 시에만 "체크인 완료" 활성(passportComplete 게이트, 진행 문구 passport.progress n/N 5언어) ② "나중에 하기" 스킵 버튼 제거(skip 키는 미사용 잔존) ③ 재방문 반영: 로더가 guestCheckinToken.passportPhotoUrls **장수만**(passportUploadedCount number — URL 미전달=증빙 비공개) additive 전달 → 앞 슬롯 초기 done·게이트 반영 ④ 서버·schema 무변경(체크인 완료=클라 단계 개념, 업로드 API 불변) | QA 독립 검증 **PASS(결함 0)** — 실렌더(라이브 토큰 guestCount 11, ko/en): 스킵 부재·disabled·"여권 사진 0/11" 확인, 초기 done 클램프 min(count, slotCount) 부모·자식 동일, error/unprocessable≠done, 누수 0. tsc 0·build 통과. 계약: docs/contracts/guest-passport-photo-required.md |
| 2026-07-13 | **미체크인 티켓 구분별 수량 선택 (worktree-ticket-variant-qty-precheckin)** | 테오 실기기 테스트: 게스트 부가옵션 나이트사파리에서 성인2·어린이1·시니어1 혼합 선택 불가. Triage: 체크인 명단 모드(이름 칩)는 혼합 구매 이미 가능하나, **미체크인 폴백이 구분 1택 라디오+공용 스테퍼 1개**라 UI상 혼합 불가 + 규칙 구분(어린이·시니어·무료)은 서버 TICKET_GUESTS_REQUIRED(P2-B 봉인)로 어차피 거부되는 오해 유발 UI. 회의 결정: 서버 가드 불변, UI 정직화 — ① 미체크인 TICKET+variants 카드를 **구분별 수량 스테퍼**로 교체(variantQtys, quantity=합 동기), 규칙 구분은 lock+셀프 체크인 유도(체크인 링크, 제출 전 안내) ② 제출=수량>0 구분마다 1주문(variantKey+quantity, ticketGuests 미첨부 — 무규칙 구분만이라 서버 통과) ③ 구분별 소계·카드 합계·하단 합계 정합 ④ TICKET 구분 섹션 라벨 timeLabel("시간 선택") 오용 → ticketVariantSectionLabel("구분 선택") 신설+ticketVariantCheckinHint, 5언어(ko/en/ru/zh/vi) | QA 독립 검증 **PASS** — 실렌더(미체크인·체크인 토큰 각 1, 390×844) 금액 950,000×2=1,900,000 정합·person 모드 회귀 0·서버 파일 diff 0·누수 0. tsc 0·build 통과. ★어린이·시니어 혼합은 설계대로 셀프 체크인 후 인원별 선택으로 처리. 계약: docs/contracts/ticket-variant-qty-precheckin.md |
| 2026-07-13 | **ADMIN 전용 빌라명(name+nameVi) 편집 (worktree-villa-name-admin-edit)** | 테오: 활성 빌라의 한국어명 변경 경로가 없음(공급자 info 라우트=name/complex 의도적 미수신·공급자 PUT=REJECTED 전용·ADMIN PATCH=상태 전이 전용·화면=nameVi 편집기뿐). **회의 결정: 공급자에게 이름 변경권 미부여**(name=운영자 판매 식별자, 실시간 조인이라 공급자 개명 시 제안 링크·예약 화면이 운영자 모르게 변경 — 공급자 니즈=Zalo 요청→ADMIN 반영). ① API `name-vi` 라우트 additive 확장: save에 옵셔널 name(trim min1 max100, 공백만=400 VALIDATION_NAME_REQUIRED — 판매 식별자라 null 불가), suggest에 옵셔널 name(초안 기준 Gemini 음역 재제안), 멱등 유지·변경 필드만 트랜잭션 update+AuditLog old/new. name 미전송=기존 nameVi-only 동작 100% 하위호환(유일 호출부=에디터). ② name-vi-editor → name+nameVi 통합 편집기: 한국어명 필드·초안 name 기준 제안·이름 변경 시 amber "병기명 재확인" 안내·formatVillaName 초안 미리보기·저장 시 router.refresh(). ③ i18n 기존 NS `adminVillas.detail.nameVi` 키 추가만(ko/vi 동시, 화이트리스트 변경 0) | QA 독립 검증 **PASS(결함 0)** — SUPPLIER 403 유지·수정 금지 구역(info 라우트·공급자 화면·schema) diff 0·i18n 키 16종 양쪽 대조·멱등/가드 경로 전수 추적. tsc0·build 통과. 계약: docs/contracts/villa-name-admin-edit.md |
| 2026-07-13 | **프리미엄일(요일·공휴일) 2단 요금 — ADR-0042 (worktree-premium-day-pricing)** | 테오: 성수기+주말은 웃돈을 받는 빌라가 있어 별도 금액 필요 — "겹침 기간+금액 큰 쪽 승리"는 가격 다축(원가/Net/소비자가×VND/KRW)이라 승자 정의 불가로 기각, **기간 행 내 프리미엄 컬럼** 방식 확정. ① 스키마 additive 라이브 적용(migrations-manual 2026-07-13): `Villa.premiumDays Int[]`(getUTCDay, 기본 금·토)·`HolidayDate`(전역 공휴일, date unique — 한국·베트남 공용, 전야 자동계산 없음)·`VillaRatePeriod` premium* 6컬럼(원가·Net VND/KRW·소비자가 VND/KRW·공급자정가, 전부 nullable=**컬럼 단위 평일 폴백**). 프리미엄 전용 마진 컬럼 미도입(같은 행 마진으로 UI/서버 파생 — margin*은 원래 엔진 미소비). ② 엔진(lib/pricing.ts): 박별 `프리미엄 = (요일∈premiumDays ∨ 공휴일)`(HOLIDAY 우선) 판정 후 `premiumX ?? X` 유효행에 ADR-0031 계층 그대로 적용, `NightQuote.premium` 사유. quoteStayForVilla가 premiumDays·교차 공휴일 자동 로드(견적·저장·영수증 단일 엔진 유지). ③ API: `/api/admin/holidays` CRUD(isSystemAdmin·중복 409·연도 필터)·ADMIN 요율 저장 premium 5필드·공급자 cost 라우트는 **원가+자기판매정가 2필드만**(운영자 Net은 같은 행 마진으로 서버 파생, 응답 미포함)·premiumDays=info 라우트(가격 아님). 전부 zod+writeAuditLog. ④ FE: 요율 편집기 프리미엄 토글(5칸)·`/settings/holidays` 관리 화면·요일 칩 에디터·견적 카드 박별 주말/공휴일 뱃지. ⑤ UX-VN: 공급자 요율 화면 amber 토글(2칸만)+요일 칩(48px 터치·"공휴일은 회사가 자동 적용" 안내). ⑥ **QA P2 → 수정(1e8d153)**: premiumDays 기본값 [5,6]이 기존 69빌라에 백필돼 미설정 빌라도 주말 뱃지 소급 노출 → `hasAnyPremiumValue` 게이트(뱃지=실제 웃돈/프리미엄 원가 적용 박에만) | QA 독립 검증 **PASS**(P0/P1 0·누수 전수 0 — SUPPLIER·/p·/g에 premium Net/소비자가 부재, cost 라우트 응답 셰이프 확인·API 가드 라이브 401) + P2 재검증 PASS(금액 no-op 증명·69빌라 표시 불변). tsc0·vitest **3039**·build 통과. 계약: docs/contracts/premium-day-pricing.md |
| 2026-07-13 | **공급자 정보수정에 셀링포인트·와이파이·출입정보 추가 (worktree-supplier-info-features)** | 테오: 빌라관리자(공급자) "이용 규칙 · 정보 수정"에 셀링포인트 누락 — 다른 누락도 전수 확인 후 업데이트. **전수 비교 결과** 승인 후 공급자 수정 불가 항목=① features(셀링포인트) ② wifiSsid/wifiPassword ③ accessType/accessInfo ④ bedroomDetails(잠자리 구성) — ①②③을 /my-villas/[id]/info 페이지+PATCH /api/villas/[id]/info에 additive 추가(④는 파생 스칼라 3경로 공유 구조라 별도 태스크로 분리). API: featureRowSchema+refineFeatures 공유 zod·features 전달 시에만 전체 교체(deleteMany→createMany, sales ⓒ 패턴)·hasPoolFeatureTag→hasPool=true 보정·wifi/access 스칼라·AuditLog wifiPassword "(hidden)" 마스킹·features만 전달 시 NO_FIELDS 안 남. UI: 카테고리별 teal 칩 토글(FEATURE_ITEMS)·와이파이 amber 비공개 카드·출입방식 칩 4종+textarea — i18n 신규 키 0(wizard.rules/location·features.* 전량 재사용, ko/vi 자동 패리티) | QA 독립 검증 **PASS(P0/P1/P2 0건)** — 누수 0(salePrice/margin/KRW grep)·타인 빌라 404 유지·zod 우회 차단·features 미전달=기존 태그 보존·i18n 키 전수 실재 확인. tsc0·lint·build 통과. 계약: docs/contracts/T-supplier-info-features-wifi-access.md |
| 2026-07-13 | **Zalo 그룹 다계정 수신 유실 수정 (zaloMsgId 대화별 복합키)** | 테오 보고 "ZALO에 확실히 문제가 있다" — villa go 주문 알림방 새 메시지가 PMS에 안 보임. **진단(라이브 DB+워커 로그 실측)**: `ZaloMessage.zaloMsgId` 전역 @unique 탓에, 같은 그룹에 PMS 연결 계정 3개(테오 SYSTEM_BOT·DK·Villa Go)가 되면서 같은 그룹 메시지가 3개 리스너에 도착해도 첫 저장만 성공 — 나머지 소유자 대화에선 소리 없이 유실(멱등 findUnique 오판 스킵 + 동시 도착 P2002 드롭, 워커 로그 도배). 리스너 연결은 정상(워치독 경보 0이 맞음). **수정**: ① 라이브 DB `ZaloMessage_zaloMsgId_key` 드롭 → `(conversationId, zaloMsgId)` 복합 UNIQUE + zaloMsgId 단독 INDEX(prisma/migrations-manual/2026-07-13-zalo-msgid-per-conversation.sql) ② saveInboundMessage/saveOutboundEcho 멱등을 복합키 스코프 + create P2002만 duplicated 흡수 ③ ext/send persistOutboundOriginal upsert 복합키 ④ 테스트 10케이스 추가(vitest 83). 리액션·webhook·인용 조회는 기존 ownerAdminId 스코프 findFirst라 무변경 | QA 독립 검증 **PASS(결함 0)**. tsc0·build 통과. main 직커밋 8b7c2f4(명시적 add·타 세션 WIP 없음 확인), 웹·워커 8b7c2f4 배포 SUCCESS 실측. 유실분 백필 안 함(계정별 Zalo id 체계 상이로 교차 매핑 불가 — 원본은 실제 Zalo에 존재). 계약: docs/contracts/zalo-msgid-per-conversation.md |
| 2026-07-13 | **티켓 발행·판매 3포지션 전수 감사 + 후속 일괄 수정 7건 (wt/ticket-audit-fixes)** | 테오 지시 "소비자·부가서비스공급자·ADMIN 3포지션 문제 요소 전체 확인". **감사 결과 P0/P1=0**(권한 누수 4종·마진·PII·SSRF·상태기계 전부 클린, 3에이전트 병렬·테스트 500+ 대조), P2 2·P3 5 발견 → 7건 수정: ① `EXCLUDE_FREE_TICKET_WHERE` 공유 상수(lib/service-order.ts) — 무료 티켓을 정산 허브 pending/paid/settleable·vendor-stats에서도 제외(벤더 보드와 정합, status 뷰는 의도적 미적용) ② 미체크인 규칙 variant 신청 400에 셀프 체크인 유도 문구(ticketCheckinRequired 5언어) ③ 무료 티켓 취소 시 벤더 "발주 취소" 오통보 게이트 ④ 티켓 업로드 append `ticketUrls:{equals}` 낙관 가드(동시 업로드 유실→409 재시도, DELETE has 가드와 대칭) ⑤ 로더 TICKET 벤더 연락처 null(방어심층) ⑥ leak-checklist 교훈 축적(배열 append 낙관 가드) ⑦ **취소 주문 QR 소비자 완전 차단**(테오 확정: 로더 ticketUrls 절단+다운로드 404 — DB·admin·vendor는 증빙 보존) | QA 독립 검증 **PASS(결함 0)** — 상수 3곳 바이트 동등·유료 통보 무회귀·정상 업로드 경로 무영향·신규 노출 0. tsc0·vitest **3028**(신규 50)·build 통과. 계약: docs/contracts/T-ticket-audit-followups.md |
| 2026-07-13 | **게스트 화면 금액 ₫ 통일 + 영수증 초과 수납 행 (wt/guest-vnd-display)** | 테오: "소비자 화면 금액은 베트남 동으로 — 다국적을 한번에 커버". ① /g/* 서비스·미니바·청구·소계·합계 = **₫ 원천 단일 표기**(≈원 병기·옵션 페이지 "오늘 환율 기준" 모국통화 환산 제거 — 5언어 통화 기호 일관, convert dead plumbing·불필요 getDailyRates 쿼리 제거). ② **원천 KRW 데이터는 유지**(왜곡 금지): 숙박요금 saleCurrency·보증금/수납 라인 ₩·priceVnd null 폴백. KRW-원천 청구는 총액 "환산 합계 ≈₫" 흡수(환산 불가 시 ₩ 폴백 — 금액 유실 0). ③ **영수증 초과 수납(환불 예정) 행**(테오 실측: 청구 2.8M인데 상계 3.0M·환불 0이 설명 없이 모순돼 보임): rest<−1만₫ → emerald "≈N₫", 미수납 행과 상호 배타·환산 불가 시 생략. excessLabel 5언어 | QA 독립 검증 **PASS(결함 0)** — ₫ 통일 전수 grep·금액 유실 0·초과/미수납 배타·누수 0. tsc0·vitest **3011**·build 통과. 계약: docs/contracts/T-guest-vnd-display.md |
| 2026-07-13 | **★P1 게스트 청구 이중 계상(₫+₩) 수정 (wt/guest-bill-fix)** | 테오 영수증 금액 검증 요청에서 발견: ServiceOrder는 `priceVnd`=판매가 원천·`priceKrw`=주문 시점 환산 표시 스냅샷(전 생성 경로 priceKrwCeil)인데 `computeGuestBill`(ADR-0019, 단일 통화 컬럼 가정 시절)이 **둘을 각각 합산 → 같은 금액 이중 청구**(실사례 2.8M₫를 ≈5.7M₫로 — 보증금 3M 전액 상계·환불 0 오처리, 정답=상계 2.8M+환불 200,000₫). ① computeGuestBill **원천 통화 1회 계상**(priceVnd 있으면 VND만/없으면 KRW) — 게이트·가감산·자동 상계·저장 캐시 자동 정정. ② 영수증 총액=캐시 대신 **라인 라이브 파생**(deriveGuestUsage) — 구 레코드 즉시 정정. ③ FE 표기 원천 우선(₫ 주 + "≈ N원" 참고, ₩소계 0 숨김 — admin·영수증 동일). ④ scripts/backfill-guest-charge.ts(dry-run 기본·--apply·청구 캐시 2컬럼만) | QA 독립 검증 **PASS(결함 0)** — 실사례 재현 테스트·KRW-원천 보존·과소청구 없음·누수 4종 0. tsc0·vitest **3011**·build 통과. 계약: docs/contracts/T-guest-bill-double-count-fix.md. ★해당 예약 200,000₫ 현장 반환 필요(테오 안내됨) |
| 2026-07-13 | **체크아웃 승인 단일화 + 보증금 통화 상계 (wt/checkout-single-approve)** | 테오: ""보증금 전액 환불"이 맞아? 결국 보증금 차감 후 입금 받는 거잖아 — 단어도 틀림". ① **하단 2버튼 폐기 → 단일 "정산 확정·체크아웃 승인"**(환불/차감 구분은 서버 파생 — payload 불변, canApprove=파손완비&&!잔여수납&&!busy). 죽은 키 refundFull/refundDeduct 삭제·guide.submit 갱신. ② **DEPOSIT 상계 라인 통화=보증금 통화로 일반화**(₩200,000 보증금 사례 대응): 서버 3검증(CURRENCY_MISMATCH 신설/OFFSET_EXCEEDS 상한=depositAmount−VND일때만 파손/NOT_HELD), ★depositDeductVnd=파손(VND)+VND상계만(비VND 상계는 라인이 정본 — 통화 혼합 금지), normalizeSettlementLines 반환 depositOffset{vnd,krw,usd}. ③ FE 자동 제안 통화 무관(제안=min(잔여,청구 환산) 통화별 절삭 — 서버 상한 초과 불가 캡)·fx 없고 비₫이면 제안 생략·가감산 요약 3분기(비₫+fx없음=원금 표기)·비₫+파손 한계 안내. ④ **영수증 파생 동반 수정**(BE 플래그): deriveDepositSettlement 보증금 통화 기준(비VND 파손=전액 표시·환불=amount−상계)·방어 필터(라인 통화=보증금 통화만) — ₩상계가 ₫로 합산되던 잠재버그 사전 봉쇄 | QA 독립 검증 **PASS(결함 0)** — FE 제안≤서버 상한 경계·통화 혼합 0·누수 0. tsc0·vitest **3005**·build 통과. 계약: docs/contracts/T-checkout-single-approve-deposit-currency.md |
| 2026-07-13 | **시즌 구분자 준성수기(SHOULDER) 추가 — admin·공급자 전 화면 (PR #284, wt/season-shoulder)** | 테오: 기간별요금에 "준성수기" 구분자 추가(admin/빌라관리자 모두). ① SeasonType enum에 `SHOULDER`(LOW와 HIGH 사이) — 라이브 `ALTER TYPE … ADD VALUE IF NOT EXISTS 'SHOULDER' BEFORE 'HIGH'` 적용·migrations-manual 보존. ② 좁힌 소비처 전수 갱신(29파일): pricing SEASON_PRECEDENCE 재번호(PEAK3>HIGH2>SHOULDER1>LOW0)·extractSeasonRates·zalo-share 라벨·cost-alerts·z.enum 4곳(seasons·rate-periods)·admin 요율 편집기/시즌 달력(뱃지 amber)·공급자 원가 편집기/마법사. ③ 하위호환: villa-schema `SHOULDER` **optional**+`REQUIRED_SEASONS`(LOW/HIGH/PEAK) 분리 — 구 payload 유효, buildRatePeriodRowsFromSeasonCosts는 원가 없는 시즌 기간 스킵. ④ i18n ko "준성수기"/vi "Cận cao điểm" 각 7곳. 뱃지 단계감 emerald<amber<orange<red(마법사 HIGH 아이콘 amber→orange 양보) | QA 독립 검증 **PASS(결함 0·6항목)** — 잔여 좁힌 소비처 0·공급자/공개 경로 재무 노출 신설 0. tsc0·vitest **2985**(신규 SHOULDER 케이스)·build 통과. DB enum 적용 실측 확인(LOW,SHOULDER,HIGH,PEAK). 계약: docs/contracts/T-season-shoulder.md |
| 2026-07-13 | **게스트 정산 영수증 /g/[token]/receipt (worktree-guest-receipt)** | 테오: "정산완료 내용을 소비자가 확인 — /orders인가 체크인 링크인가? 고객은 총 사용 금액을 봐야". TDA 결정=**체크인 링크 포털 하위 /receipt**(정산서=예약 단위 문서, /orders는 부가서비스 부분집합) + 진입점 2곳(/g 메인 카드·/orders 배너, `checkedOut`=CHECKED_OUT일 때만). ① lib/guest-receipt.ts 로더 — **원가·마진·공급자·운영메모(settlementNote·damageNote)·totalSale* select 자체 배제**(사업 원칙 2), 미니바 라인(nameKo·수량·단가·판매가)·부가서비스(판매가)·수납 라인(수단×통화)·settlementFx만. ② 파생 `deriveDepositSettlement`: 상계=ΣDEPOSIT·파손=max(0,deductionVnd−상계)·환불=max(0,보증금−차감) — checkout.ts 저장 의미(deductionVnd=파손+상계)와 정합, 구 데이터=차감총액·settled* 폴백. ③ 페이지=게스트 라이트 톤·모바일 우선·guest-i18n 5언어(ko/en/ru/zh/vi). 상태: 없음=404·만료=안내·체크아웃 전=redirect. 토큰 수명 현행(체크아웃+24h) 유지 | QA 독립 검증 **PASS(누수 0·전 항목)** — build RED는 병행 세션 SHOULDER 클라이언트 레이스(receipt 무관), 커밋 스키마 재생성 후 tsc0·vitest 2987·build 통과. 계약: docs/contracts/T-guest-settlement-receipt.md |
| 2026-07-13 | **체크아웃 화면 UI 후속 — 2열 배치+모바일 최적화 (PR #281, wt/checkout-offset-ui)** | 테오 실측 2건: PC 여백 낭비(청구서·가감산 카드 max-w-xl 절반만 사용)→lg 2열 그리드, 모바일(390px) 수납 라인 금액 잘림·하단 버튼 가로 오버플로→<sm 2줄 재구성([수단][통화]/[금액][✕])·버튼 세로 스택 w-full·터치 44px·iOS 세이프에어리어·파손 섹션 p-5 sm:p-8. 미니바 표는 기존 md표+모바일 카드 이중 구현 확인(수정 불요) | 레이아웃 클래스만(기능·금액 로직 불변). tsc0·lint·build 통과 |
| 2026-07-13 | **Zalo 그룹방 아바타 미표시 수정 (wt/zalo-group-avatar)** | 테오: 앱에서 등록한 그룹방 아바타가 /messages에 안 뜸. 원인=avatarUrl 채움 3경로(maybeRefreshAvatar·maybeRefreshGroupMembers·backfillAvatars)가 전부 개인 프로필 API(getAvatarUrlProfile) — 그룹 id엔 항상 실패해 GROUP 대화 avatarUrl 영영 null. 수정=`fetchGroupAvatarUrl` 신설(getGroupInfo→fullAvt??avt), 3경로 threadType 분기(GROUP=그룹 API·USER=기존 불변). 기존 null 행=백필(avatarUrl null 대상)+수신 재조회(null이면 TTL 무관)로 자동 회복. FE 무변경(렌더는 이미 threadType 무관 공통) | QA 독립 검증 **PASS(P0/P1 0건)** — null 행 회복 트랩·과호출 순증 0·USER 경로 바이트 동등 확인. tsc0·zalo 스위트 130·build 통과. 계약: docs/contracts/zalo-group-avatar.md |
| 2026-07-13 | **체크아웃 보증금 상계(가감산) + 화면 재배열 + 미니바 이중 계상 수정 — ADR-0041 (worktree-checkout-deposit-offset)** | 테오: "보증금 있으면 파손·미니바·부가서비스를 차감하고 잔액만 환불(부족분만 결제) — 청구 전액 수납+보증금 별도 환불은 잘못. UX는 파손 최상단→미니바→부가서비스→가감산→최종 결제". ① **미니바 이중 계상 버그 수정** — 미니바가 guestCharge(청구)와 deductionVnd(보증금 차감, FE가 damageFound=true 합산 전송)에 동시 반영돼 안내대로 하면 두 번 수취. 이제 deductionVnd=파손만·damageFound=실제 파손만·미니바=청구 1회. ② 수납 라인 수단 **`DEPOSIT`(보증금 차감)** 추가(enum additive 라이브 적용, VND 전용) — ADR-0039 라인 원장 재사용, depositDeductVnd=파손+Σ상계(하위호환), 서버 검증 3종 400(OFFSET_EXCEEDS/NOT_HELD/NOT_VND, 보증금 원천은 서버 재조회). ③ FE 섹션 재배열(지시 순서) + **가감산 요약 카드**(보증금−파손−총청구 환산=환불 예정/추가 수납) + DEPOSIT 자동 제안 라인(min(보증금−파손, 청구 환산) 1만₫ 내림·dirty 후 자동 갱신 중단) + 버튼 배타(파손·상계 시 차감 승인만). ④ `narrowVendorSettleMethod` 화이트리스트화(미래 enum 값 안전) | QA 독립 검증 **PASS(결함 0)** — 자동 제안 상한=deposit−damage 정확값이라 fx 근사와 무관하게 서버 400 경계 없음 확인·누수 재검 0. tsc0·vitest **2961**·build 통과. 계약: docs/contracts/T-checkout-deposit-offset.md |
| 2026-07-13 | **P0 fix: 그룹 대화 이미지·파일·공유 오발송 — share 경로 ThreadType 누락 (worktree-share-group-threadtype)** | 실사고: /messages 그룹 대화에서 이미지 첨부 발송 → 그룹이 아닌 **낯선 사용자 1:1 채팅에 배달**(관리자 화면 스크린샷 2장 유출). 원인: `share/route.ts`가 conversation.threadType을 select하지 않고 전 발송 호출(sendChatImageAsAdmin/FileAsAdmin/MessageAsAdmin)에 threadType 미전달 → 기본 ThreadType.User로 그룹 id 발송. ADR-0010 S4에서 runtime 함수 파라미터·텍스트 채팅 라우트는 갱신됐으나 share 라우트 호출부만 누락된 회귀. 수정: select에 threadType 추가 + `sendThreadTypeOf(conv)` 파생(GROUP→Group, 그 외 User — messages/route.ts 동일 패턴) → photo/file/villa(이미지+텍스트 폴백)/proposal/settlement 5개 핸들러 전 호출 전달 | QA 독립 검증 PASS(발송 호출 전수 threadType 확인·수정금지구역 무변경·누수 신설 0). vitest 44(신규 5: 그룹 4+1:1 회귀 1)·tsc 0·build 통과. 계약: docs/contracts/zalo-share-group-threadtype.md |
| 2026-07-13 | **운영자 Zalo 알림 그룹방 발송 — ADR-0040 (worktree-zalo-admin-group-notify)** | 테오: 운영자 계정 2개로 가던 개별 DM 알림을 그룹방 "villa go 주문 알림방" 1건으로 통합. ① `Notification.groupThreadId`(additive, migrations-manual 2026-07-13) — dispatchOne이 NO_ZALO_LINK 판정보다 앞에서 그룹 분기·시스템봇 ThreadType.Group 발송·기존 미러/재시도(BOT_NOT_CONNECTED attempt 미증가) 재사용. ② `lib/operator-notify.ts` 단일 진입점 — 3중 게이트(AppSetting `ZALO_ADMIN_NOTIFY_GROUP_ID` ∧ `GROUP_ROUTED_TYPES` 화이트리스트 8종 ∧ 시스템봇 소유자) 충족 시 1행, 아니면 기존 개별 DM fan-out 폴백. SECURITY_ALERT·ZALO_LISTENER_DOWN은 의도적 제외(개인 행동 요구). 호출부 8곳 전환 + booking-change-request 직발송 최소 수정. ③ 설정 UI/API `/api/settings/zalo-notify-group`(ADMIN·실재 GROUP 대화만 허용=주입 차단·writeAuditLog) + settings 섹션(ko/vi) | QA 독립 검증 **PASS(P0/P1 0건)** — 누수 전수 grep·재시도 groupThreadId 보존 확인. tsc0·vitest **2924**(신규 2스위트)·build 통과. 계약: docs/contracts/zalo-admin-group-notify.md |
| 2026-07-13 | **체크아웃 수납 결제수단 혼합(분할) 지원 — ADR-0039 (worktree-checkout-mixed-settlement)** | 테오: 실수납이 현금/계좌이체 섞여 들어오는데 settlementMethod 단일 값이라 기록 불가. ① 스키마 additive `CheckoutSettlementLine`(수단×통화×amount BigInt 원본통화 정수, FK Cascade) + `GuestSettlementMethod.MIXED`(서버 파생 전용) — 라이브 적용. ② `normalizeSettlementLines` 순수 층=통화별 합계·대표수단(2종↑=MIXED)·(수단,통화) 중복 병합 단일 원천, `settledVnd/Krw/Usd`=Σ라인 캐시 유지(표시·통계 하위호환). ③ route: `settlement.lines[]`(≤12, MIXED 직접지정 zod 자연거부, lines 없으면 method 필수 refine) — 구 shape(method+amounts)·method-only 하위호환 보존. ④ FE 폼: 수단 라디오+3입력 → 수납 라인 rows([수단][통화][금액], 최대 12행), 환산 잔여/초과 표시 유지. 상세: settlementLines 표시 — **select·렌더 showFinance 게이트 안에만**(STAFF 미조회). ⑤ ★enum 확장 함정 재확인: `vendorSettleMethod`(같은 enum 재사용) 소비처 2곳 컴파일 실패 → `narrowVendorSettleMethod`(MIXED→null) 공유 가드로 해소 | QA 독립 검증 1차 FAIL(공유 enum 소비처 회귀 — build 게이트) → BE 재작업 → tsc0·vitest 60(신규 계열)·build 통과. 계약: docs/contracts/T-checkout-mixed-method-settlement.md |
| 2026-07-13 | **게스트 티켓 문의 카카오톡·전화 버튼 (PR #275, worktree-agent)** | 테오: 카카오톡 채널 개설(http://pf.kakao.com/_mVAfX) → 신청 내역 티켓 문의 안내 박스("티켓 관련 문의는 Villa Go로")에 **카카오톡 문의(teal 솔리드·새 탭)·전화 연결(tel: 아웃라인) 버튼 노출**. 연락처=AppSetting CONTACT_KAKAO_URL(값 …/_mVAfX/chat로 갱신 완료)·CONTACT_PHONE 재사용(만료 화면과 동일 원천), 티켓 주문 있을 때만 조회·렌더, 값 없으면(빈 문자열 포함) 해당 버튼만 미노출. guest-i18n ticketContactKakao/Phone 5언어(required 타입=누락 시 tsc 실패). DB만 반영: 카카오 채널 부속물(채널홈 문구·메뉴 6종·자주쓰는 답변 10종·자동응답 5종·배너/아이콘 이미지)은 카카오 관리자센터 측 작업 | QA 독립 검증 **PASS(결함 0)** — 누수 4종·빈값 truthy 가드·GuestOrdersProps optional 무회귀 확인. build·tsc·lint 통과. 계약: docs/contracts/ticket-contact-buttons.md |
| 2026-07-12 | **벤더 발권 현황 명칭·아코디언 — 대량 대응 (worktree-vendor-issuance-ux)** | 테오 2건: ① 티켓 전용 업체에 "예약 현황"은 부정확 → **ticketOnly면 "발권 현황"**(vi Tình trạng phát hành, 일반 벤더 현행·tourAnchor 불변). ② 현황 카드 상시 펼침=스크롤 압박 → **접힘 요약 행 기본**(amber 미완료 점·품목×수량·구분·이용자 요약(`summarizeGuests` — 이름만, PII 최소)·일정·발행 카운터·상태 칩), 펼치면 기존 카드 전부(TicketPanel §3-5 잠금·freeEntry 분기 무변경 — QA 라인 대조 유실 0). 360px 가로 스크롤 구조 차단·aria | QA 독립 검증 **PASS(결함 0)**. tsc0·vitest **2912**(신규 6)·build 통과. 계약: docs/contracts/vendor-issuance-tab-accordion.md |
| 2026-07-12 | **ADMIN 주문 목록 품목 그룹핑 — 구분 분리 주문을 한 구매로 표시 (worktree-admin-orders-grouping)** | 테오: 키스브릿지 1건 구매(무료 1+일반 2)가 두 행으로 보임. 데이터 분리(구분별 가격·발행·처리 상이)는 유지, **표시만 품목+이용일 그룹**: `groupAdminOrders`(lib/service-order.ts 순수 함수 — 키=catalogItemId??type+serviceDate, 총수량Σ·판매가 합 KRW Int/VND bigint·대표 상태 우선순위·attention OR 승격·티켓 카운터 합). 2건+ 그룹=헤더 행(chevron·×Σ·이용일·구분 N건·합계·배지) → 펼치면 라인 들여쓰기(기존 라인 **바이트 불변** — QA diff 대조), 1건=현행 단일 행. 필터=라인 기준(보이는 라인으로 재그룹). 그룹 기본 접힘(attention 있으면 펼침) | QA 독립 검증 **PASS(결함 0)** — 실데이터 SSR 실측·집계 bigint 검증. tsc0·vitest **2902**(신규 9)·build 통과. 계약: docs/contracts/admin-orders-item-grouping.md |
| 2026-07-12 | **발행 완료 후 벤더 티켓 잠금 — ADR-0034 §3-5 (worktree-ticket-complete-lock)** | 테오: 완료된 티켓의 임의 삭제·추가 발행 금지 — 변경은 Villa Go 관리자 경유. ① 서버: 벤더 POST/DELETE가 `vendorCompletedAt != null`이면 409 TICKETS_LOCKED(완료를 만드는 마지막 POST는 스냅샷 기준이라 자기 자신 안 잠금 — QA 경계 검증). PR #261 clearComplete(미달 시 완료 해제)는 도달 불가로 제거, DELETE where에 vendorCompletedAt:null TOCTOU 가드 추가(QA 권고 반영). ② 보드: locked 시 첨부·삭제 버튼 미노출+lockedNote("Villa Go로 연락", ko/vi), 열람 유지. **정정 창=완료 전만**(§3-1 축소), 완료 후 정정=운영자 대리 라우트(상태·완료 불변) | QA 독립 검증 **PASS(결함 0)**. tsc0·vitest **2897**·build 통과. 계약: docs/contracts/ticket-complete-lock.md |
| 2026-07-12 | **ADMIN 주문 목록 아코디언화 — 대량 대응 UX (worktree-admin-orders-accordion)** | 테오: 주문 많으면 통짜 카드 나열=스크롤 과다. ① **접힘 요약 행 기본**(품목·구분·수량·일시·판매가·출처/상태/발주 배지·티켓 카운터 — flex-wrap·가로 스크롤 0), **처리필요 3신호 접힌 채 식별**(REQUESTED amber·미해결 제안 배지·발행 미달 카운터, `orderAttention` 순수 함수). ② 펼침(Set<orderId> — refresh 후 유지)에 기존 기능 전부 이동(기능 유실 0 — QA diff 대조: 공급자 변경+재발주·발주·티켓 첨부·확정가·확정/제공완료(비TICKET)/취소·시간제안). ③ 상태 필터 칩 전체/요청/확정/취소+건수(`orderFilterCounts`, DELIVERED=확정 포함). a11y(aria-expanded/controls·버튼 밖 펼침) | QA 독립 검증 **PASS(결함 0)**. tsc0·vitest 2892 통과(+무관 기존 실패 1건=regional-vendor-guest-order — 별도 후속)·build 통과. 계약: docs/contracts/admin-orders-accordion.md |
| 2026-07-12 | **ADMIN 주문 추가 개편 — 카테고리 셀렉터·티켓 시간 제거·이용자 선택(신장) (worktree-admin-ticket-parity)** | 테오 3건. ① **검증 공유화**: 게스트 라우트의 ticketGuests 검증 5종을 `lib/ticket-order-validation.ts` 순수 함수로 추출(loadConfirmedGuests 콜백 지연 평가) — 게스트·admin 두 경로 공유, 게스트 기존 테스트 **무수정 통과**(동작 불변). ② admin POST: ticketGuests 수용+동일 검증, TICKET serviceTime 미저장, 무료(총액 0)=생성 시 CONFIRMED+VENDOR_ACCEPTED 준용(발주함 미경유·PO 미발송). ③ 폼: 카테고리(ServiceType) 셀렉터→메뉴 필터, TICKET=날짜만, 체크인 명단 있으면 이름 칩+신장 입력+자동 판정(클라·서버 동일 순수 모듈)+variantKey 그룹 분리 POST. 명단 주입={name,birthDate}만(매퍼 재사용) | QA 독립 검증 **PASS(결함 0)** — 리팩터링 동등성(오류 코드·순서·지연 평가) 정밀 대조. tsc0·vitest **2877**(신규 22)·build 통과. 계약: docs/contracts/admin-ticket-order-parity.md |
| 2026-07-12 | **부가서비스 공급자 메뉴 이동 (worktree-vendors-nav)** | 테오: 설정의 공급자 관리 박스 제거, 사이드바 부가서비스 그룹 "부가서비스 정산" 아래로 이동. 사이드바 addon 그룹에 vendors(/settings/vendors, storefront, cap 없음=기존과 동일 isOperator 동선)·설정 Card 6c 박스 제거·죽은 코치마크 스텝(settings-sub)+문구 키 삭제(tour-definitions+NS 동시갱신 규칙)·백링크 제거·nav.vendors ko/vi 추가, 미사용 키 3종 ko/vi 삭제. URL 유지(참조처 무변경) | tsc 0·build 통과·ko/vi 패리티 4047키 일치. 계약: docs/contracts/vendors-nav-relocation.md |
| 2026-07-12 | **업체 담당 지역 커버리지 — 지역 업체 자동 지정 (worktree-vendor-region, ADR-0038)** | 테오: "빌라×업체 1:1 지정은 빌라 많아지면 불편 — 업체 등록 시 지역(다중) 선택하면 자동 지정돼야". ① 스키마 additive `ServiceVendorRegion`(업체×타입×지역, 지역=Villa.complex 단지명 정본 재사용, raw SQL 라이브 적용). ② `lib/regional-vendor.ts` 해석 3단계화: 빌라별 수동지정(VillaServiceVendor) 최우선 > 지역 매칭 활성·승인 업체 **정확히 1곳**이면 자동 지정 > 그 외(0곳·2곳↑·complex null) 카탈로그 기본 폴백 — 주문 3경로 공유·스냅샷 원칙·시그니처 불변. ③ 신규 `PUT /api/vendors/[id]/regions`(isOperator, REGIONAL_VENDOR_TYPES만, replace-set 트랜잭션, AuditLog, 누수 0). ④ 업체 관리 화면 "담당 지역" pill+모달(마사지·이발 타입별 complex 칩 다중 토글, 두 타입 항상 함께 전송=해제 반영). ⑤ 빌라 상세 지정 카드 문구 "(지역 자동/카탈로그 기본)" 갱신, ko·vi 동시 | vitest 24(해석기·라우트 케이스) 통과·tsc 0·build 통과. 계약: docs/contracts/vendor-region-coverage.md. ⚠2곳 이상 매칭은 의도적 비자동(오발주 방지) — 빌라별 수동지정으로 해소 |
| 2026-07-12 | **ADMIN 주문 패널 정리 — 정산 허브 일원화·가로 스크롤 제거 (worktree-admin-panel-cleanup)** | 테오 3건: 좌우 스크롤·정산완료 버튼 중복(패널 vs 허브)·패널 역할 축소. ① **정산 조작=허브 일원화**: 패널의 settleVendor·VendorSettle 삭제(조회성 정산 뱃지만, showCost 게이트) — 건별 정산완료는 허브 PendingList 단일 선택으로 기충족, 공백이던 **건별 해제만 PaidList에 신설**(기존 PATCH markSettled:false 재사용, canViewFinance 이중 게이트). ② 9컬럼 테이블→**카드형 ul 재배치**(overflow-x-auto 0건·min-w 0·flex-wrap — 360px 가로 스크롤 0). ③ 보존: 공급자 변경+자동 재발주·발주·확정/취소·티켓 대리 첨부·확정가·시간제안·이용자 표시 전부(QA diff 전수 대조). 불용 정산 키 제거(ko/vi) | QA 독립 검증 **PASS(결함 0)**. tsc0·vitest **2853**·build 통과. 계약: docs/contracts/admin-order-panel-cleanup.md |
| 2026-07-12 | **티켓 발행=이행 완료 자동 처리 — ADR-0034 §3-3 (worktree-ticket-auto-complete)** | 테오: 티켓은 발행(사진 첨부)이 곧 서비스 완료 — 별도 완료보고 불필요, 단 첨부 실수 정정(삭제·재등록)은 가능해야. ① POST 발행: 발행 수≥quantity && 미완료면 **vendorCompletedAt 자동 세팅** — 수락 전이와 동시(단일 updateMany 원자)든 수락 후 추가 발행이든. 별도 완료 통보 없음(이중 알림 방지)·멱등(최초 완료 시각 보존). ② DELETE 대칭: 미달이 되면 완료 해제(수락은 유지), 여전히 충족이면 유지 — 정정 구간 "완료" 오표시 방지. ③ 자동 완료 후 수동 complete=409 ALREADY_COMPLETED(기존 가드 자연 정합). acceptHint 문구 "수락·완료" 갱신(ko/vi) | QA 독립 검증 **PASS(결함 0)**. tsc0·vitest **2851**(신규 3+갱신)·build 통과. 계약: docs/contracts/ticket-issuance-auto-complete.md |
| 2026-07-12 | **벤더 무료 항목 비노출 + 티켓 소비자 문의 본사 일원화 (worktree-ticket-contact-policy)** | 테오 2건. ① 벤더 /api/vendor/orders base에 `NOT{TICKET && priceVnd 0 && costVnd 0}` — 4탭·cancelled·뱃지·정산 자동 전파(무료=할 일·지급 0 라인 완전 숨김, costVnd>0은 정산 누락 방지 위해 표시). ⚠SQL 3치 논리로 priceVnd null+cost 0 TICKET도 함께 숨겨짐 — QA 실SQL 캡처로 확인·무해(NO_PRICE 가드로 정상 경로 null 불가), 주석·테스트 문구 실동작대로 정정. ② 소비자 신청 내역: **TICKET은 확정 후에도 업체 이름·전화 미노출**(`guestVendorContactVisible` 순수 게이트) — "티켓 문의는 Villa Go로" 안내 5언어 대체(업체=베트남인·소비자 Zalo 부재 현실). 비TICKET(마사지 등 현장 조율형)은 현행 유지 | QA 독립 검증 **PASS**(실 DB SQL 캡처 검증 포함, 저심각 문구 지적 → 당일 정정). tsc0·vitest **2813**·build 통과. 계약: docs/contracts/free-order-hide-and-ticket-contact-policy.md |
| 2026-07-12 | **USD 환율 설정 + 자동 환율 모드 — 유효 환율 단일 해석 (admin-manual-booking 후속3, worktree)** | 테오: "달러 환율 설정 가능해야(1$=26,000₫)·자동환율 무료 API 연결·설정에서 수정 가능". ① 신규 `lib/fx-effective.ts` — **유효 환율 단일 해석**: `FX_MODE`(MANUAL 기본|AUTO) AppSetting. MANUAL=수동키(`FX_VND_PER_KRW`/신규 `FX_VND_PER_USD`), AUTO=getDailyRates(open.er-api 일일 캐시)→캐시→수동 폴백(절대 throw 안 함). 판매가 스냅샷 3경로(booking-quote·admin-booking·proposal)만 치환(MANUAL 기본=현행 바이트 동일·회귀 0), 표시/정산용 소비처는 의도적 미치환. ② `suggestSalePriceUsd`+`roundVndToForeign` 공통 코어(1e4 BigInt·float 0) — 견적 USD 자동 총액(`totalSaleUsd`, rows는 VND 참조 유지=박별 반올림 합 불일치 방지). ③ **FX_AUTO_UPDATE cron→FX_MODE 통합**: runFxAutoUpdate가 FX_MODE=AUTO만 동작(잔존 FX_AUTO_UPDATE=on 무시 — UI 제거 후 못 끄는 회귀 차단)+USD 동시 갱신, `lib/fx-format.ts` 분리로 순환 import 제거. ④ 설정 환율 카드 개편: 모드 토글(수동/자동)+KRW·USD 입력+**"현재 시세 불러오기"**(GET /api/settings/fx-rates, isSystemAdmin)→입력칸 적용→배치 저장. ⑤ 견적 카드 USD 자동 채움(환율 없을 때만 수동 안내+/settings 링크) | QA 독립 검증 **PASS(9/9·수정 0)** — MANUAL 회귀 0 코드 대조·폴백 체인·cron 회귀 경로 차단 테스트 확인·누수 0. tsc0·vitest **2833 전체**·build 통과. **라이브 이관**: 배포 후 AppSetting FX_VND_PER_USD=26000 삽입 필요(+cron 문서 갱신 완료). 계약: docs/contracts/admin-manual-booking.md 후속 확장 3 |
| 2026-07-12 | **무료 티켓 발행 불필요 — 자동 확정+정보성 표시 (ADR-0034 §3-2, worktree-free-ticket)** | 테오: 무료 티켓(1m 미만 등, 판매가 0)은 업체 발행도 소비자 제시도 불필요 — 그냥 입장. ① 게스트 생성: `TICKET && totalPriceVnd 0`(addons 포함 총액 기준)이면 **생성 시 CONFIRMED+VENDOR_ACCEPTED 원자 세팅** — 발주함 미경유·벤더 발주 통보 생략(운영자 A1 유지)·vendorId 정상 스냅샷(예약현황 정보 노출). 혼합 제출은 그룹별 독립 판정(유료=기존 발주 흐름). ② 벤더 GET `freeEntry` boolean 파생 — ★priceVnd는 select만, **값 미노출**(not.toHaveProperty 방어). ③ 보드: 무료 행=발행 패널 대신 "무료 입장 — 티켓 발행 불필요" 안내. ④ 소비자: "티켓 없이 입장 가능(무료)" 안내(5언어)+부분 발행 경고 제외 | QA 독립 검증 **PASS(결함 0·누수 0)**. tsc0·vitest **2802**(신규 2파일)·build 통과. 계약: docs/contracts/free-ticket-no-issuance.md |
| 2026-07-12 | **티켓 발행 완료 시에만 수락 전환 — ADR-0034 §3-1 개정 (worktree-ticket-complete-gate)** | 테오 실측: 2장 주문에 1장만 발행해도 즉시 수락→예약현황 이동(업무 순서 오류). **게이트 변경**: `accept = PENDING && 발행수 ≥ quantity` — 미달 업로드=append만(발주함 잔류·카운터 1/2·통보 없음), 충족 시 VENDOR_ACCEPTED+GUEST 자동 CONFIRMED+통보 1회(나눠 업로드도 마지막에 1회). **유연성 보존**: 발행 ≥1장이면 수동 수락 버튼 노출(확인시트 1장 커버 케이스), 0장=숨김(발행 유도). acceptHint 문구 ko/vi 갱신. 운영자 대리 첨부·DELETE un-accept 없음·소비자 부분 발행 경고(PR #254)와 정합 | QA 독립 검증 **PASS**(저위험 관찰 1건=미달 동시 업로드 낙관적 가드 → IDEAS 백로그 기록). tsc0·vitest **2798**(신규 3)·build 통과. 계약: docs/contracts/ticket-issuance-complete-gate.md |
| 2026-07-12 | **새 예약 폼 견적 카드 — 판매가 자동 계산 (admin-manual-booking 후속2, worktree)** | 테오 피드백 "빌라를 선택해도 가격을 전혀 확인할 수 없고 숙박 일수에 맞춘 금액을 알 수 없다". ① 신규 `lib/booking-quote.ts` buildBookingQuote + `GET /api/bookings/quote`(**canViewFinance 게이트** — 원가·마진 포함이라 isOperator보다 강함) — **제안·예약 저장과 동일 엔진 quoteStayForVilla 재사용**(동일 인자·channel 전달, 드리프트 0 코드 대조 확인), 시즌 구간별 rows 그룹핑(연속 동일 요율 병합)·총판매가·총원가·마진(BigInt 전 경로, float 0)·환율. USD=자동가 없음(제안 관례 동일) → VND/KRW 참조+manual 플래그. 409 RATE_NOT_SET/404 대칭. ② FE 견적 카드: 빌라+유효날짜(+통화·채널) 변경 시 300ms 디바운스 자동 조회 — 총 판매가 크게+{n}박, 시즌별 "라벨·박당가×N박" 브레이크다운, 원가·마진(음수 빨강)·환율, RATE_NOT_SET→판매정보 새탭 링크. **판매가 자동 채움**: 미수정 시 견적 총액 자동 입력+"견적가" 뱃지, 수동 수정 후엔 "견적가 적용" 버튼으로만 재적용, 통화 전환 시 quote.saleCurrency 기준 주입(통화 불일치 경로 0) | QA 독립 검증 **PASS(7/7)** — 결함 1건 확인·수정(rows.label=SeasonType enum raw 노출 → seasons.{LOW,HIGH,PEAK} ko/vi 매핑, 기존 요율 에디터 문구 재사용). STAFF 403·드리프트 0·경합 0. tsc0·vitest(booking-quote 27 등)·build 통과. 잔여 권고(선택)=permissions-routes-leak allowlist에 quote GET 등재. 계약: docs/contracts/admin-manual-booking.md 후속 확장 2 |
| 2026-07-12 | **게스트 신청내역 품목별 그룹핑 + QR 티켓 사전 다운로드 (worktree-guest-orders-grouping)** | 테오: 신청 내역 산만(구분별 주문 분리로 흩어짐)→품목별로 모아달라 + 현장 인터넷 불가 대비 티켓 사전 다운로드. ① 주문 표시명 **type 기준→catalogItemId 기준 품목명**으로 교체(payload catalogItemId 추가), `group-orders.ts` 순수 그룹핑(키=catalogItemId??type·이용일 오름차순·동일일=헤더 1회) — 그룹 카드에 구분·수량·금액·상태·제안 승인/거절·연락처 게이트·셀프취소·라이트박스 전부 보존(정보 무손실 QA 대조). ② **다운로드 프록시** GET /api/g/[token]/service-orders/[id]/ticket-download?u=idx — R2 교차출처라 `<a download>` 미동작 문제를 동일출처 attachment 스트림으로 해결. 보안: guestRateLimit→토큰 410→booking+requestedVia GUEST 스코프 404(**노출 범위=다운로드 범위 일치 확인**)·u=정수 인덱스만(SSRF 차단)·select ticketUrls만. ③ 썸네일 저장+모두 저장(300ms 순차)+오프라인·iOS 안내, guest-i18n 4키 5언어 | QA 독립 검증 **PASS(결함 0)**. tsc0·vitest **2784**(신규 16)·build 통과. 계약: docs/contracts/guest-orders-grouping-ticket-download.md |
| 2026-07-12 | **게스트 티켓 이용자 정보 1회 입력 UX 통합 (worktree-guest-ticket-ux)** | 테오 UX 피드백: 여권 정보가 티켓 카드마다 반복=산만·신장은 1회 입력으로 전체 적용(품목별 인원은 상이 가능 — 키스쇼 2명·심포니 3명). ① **"티켓 이용자 정보" 통합 카드**(TicketGuestInfoCard) — visibleCatalog 첫 TICKET 카드 직전 1회: 이름·생년월일 + 신장 입력(신장 규칙 품목 있을 때만) + 현장 측정 고지 1회. ② **티켓 카드 경량화** — 이름 칩 토글(+자동 판정 구분 pill·단가), 수동 폴백자만 compact 선택기. ③ **신장 상태 리프트** per-card→전역 heightByIdx(판정·소계·제출 3곳 일관, 순수 모듈 시그니처 무변경). **payload·서버 완전 불변**(app/api 무수정) | QA 독립 검증 **PASS**(마이너 1건=불용 ticketGuestAutoBadge 키 → 당일 제거). tsc0·vitest **2768**(신규 3케이스)·build 통과. 계약: docs/contracts/guest-ticket-ux-consolidation.md |
| 2026-07-12 | **티켓 카드 합계 표시 + variant 가격 필수 가드 (worktree-ticket-card-totals)** | 테오 실측 2건. ① 신청 실패 원인=운영자가 에디터에서 variant 가격 비워 저장(케이블카 null)→NO_PRICE 400 — **데이터 즉시 보정**(케이블카 1,200,000/0·콤보와 빈원더스 시니어 bornBeforeYear=1966 누락 추가·빈사파리 무료 0+어린이<140=650k 신설) + **재발 방지**: `variantsHaveRequiredPrices`("0" 허용) 가드를 catalog POST/PATCH 400 VARIANT_PRICE_REQUIRED + 에디터 저장 차단·행 강조. ② 카드 요금 명세: 일반=단가×수량=합계, 티켓=구분별 소계("성인 ×2 = …")+품목 합계 — `ticketGroupSubtotals` 순수 함수, **전 구간 bigint**(부동소수점 0)·서버 재계산과 동형. ③ 설정 오류 문구 구체화(configError 5언어 — VARIANT_REQUIRED/NO_PRICE/UNKNOWN_VARIANT만, 과도 매핑 없음) | QA 독립 검증 **PASS(결함 0)**. tsc0·vitest **2765**(신규 34케이스)·build 통과. 계약: docs/contracts/ticket-card-totals-variant-price-guard.md |
| 2026-07-12 | **벤더 보드 품목(티켓 분류) 필터 (worktree-vendor-item-filter)** | 테오 지시 "티켓업체 화면 검색에 티켓분류 검색 필요". ① /api/vendor/orders `itemId` 파라미터 — withFilters(검색·날짜와 AND) 결합, 4탭+cancelled 배너 적용, **뱃지·정산 합계는 전역 유지**(날짜 필터 원칙 동일). ② 신규 GET /api/vendor/catalog-items — 본인 품목 {id, name(현지화)}만(**select 단계에서 가격류 미로드** — 판매가·원가 누수 원천 봉쇄). ③ 보드에 품목 분류 셀렉트(2종 이상만 노출, 4탭 공통·page 리셋), vendor.itemFilter ko/vi | QA 독립 검증 **PASS(결함 0·누수 0)**. tsc0·vitest **2754**(신규 2파일 11건)·build 통과. 계약: docs/contracts/vendor-board-item-filter.md |
| 2026-07-12 | **티켓 구분(무료/어린이/노인) 자동 판정·구분별 주문 분리 + 벤더 전체명단 폴백 제거 — ADR-0036 개정 (worktree-ticket-variant-person)** | 테오 확정 기준: 빈사파리·빈원더스=무료(<100cm)/어린이(<140cm)/노인(1966년 이전 출생)/성인, 케이블카=노인 없음, 키스쇼=단일가+1m 미만 무료 — **품목별 데이터 주도(하드코딩 0)**. ① variant 규칙 필드 `bornBeforeYear`(여권 생년월일 자동 판정)·`heightMaxCm`(소비자 신장 자가신고)·ageMin/ageMax(보조) — 클라·서버가 `lib/ticket-variant-rules.ts` **동일 순수 모듈 공유**(판정 드리프트 0). ② 게스트 폼: 인원별 자동 배정(변경 불가 배지)+신장 입력+**허위신고 고지 5언어**("현장 측정·초과 시 현장 차액 결제"), variantKey별 **그룹 분리 주문**(그룹=수량=명단). ③ 서버 가드 4종: TICKET_GUESTS_REQUIRED(QA 발견 우회 봉인 — 규칙 variant는 명단 필수)·COUNT_MISMATCH·DUPLICATE·RULE_MISMATCH. ④ 스냅샷 {name,birthDate,heightCm} 3필드 화이트리스트·벤더 표기 "신고 Ncm". ⑤ 벤더 guests=스냅샷만(전체명단 폴백 삭제 — "1장에 3명" 오해 제거), 미지정 문구. ⑥ 카탈로그 에디터 TICKET variant 규칙 입력(ko/vi) | QA 독립 검증 **MEDIUM 1건 발견→봉인→재검증 PASS**. tsc0·vitest **2729**(신규 2파일+갱신)·build 통과. 계약: docs/contracts/ticket-variant-person-assignment.md |
| 2026-07-12 | **새 예약 폼 빌라 검색 필터 (admin-manual-booking 후속, worktree)** | 테오 피드백 "빌라관리에 있는 검색 필터들이 거의 다 있어야 검색 후 예약 가능한 빌라를 예약". ① `lib/villa-search.ts` 신설 — /villas page.tsx 인라인 파싱·where 로직 바이트 동일 추출(parseVillaSearchFilters/buildVillaSearchWhere, 단위 테스트 14) + /villas가 이를 사용하도록 리팩터(동작 불변, tabHref·탭카운트·페이지네이션 무회귀). ② 신규 `GET /api/villas/bookable`(isOperator 첫 줄) — /villas와 동일 파라미터(q·area·minBedrooms·minGuests·pool·breakfast·bedType·beach·tags·ci·co), **sellable 파라미터 무관 ACTIVE+isSellable 항상 강제**(검수 게이트), ci<co면 findFreeVillaIds(requireSellable) 공실 교차, 응답=표시 필드만(금액·supplierId·연락처 0)·상한 100+truncated. ③ /bookings/new 개편: 날짜 우선 흐름→검색 패널(상시 q·지역+접이식 침실≥·인원≥·침대종류·해변≤·수영장·조식·셀링포인트 태그, 400ms 디바운스+AbortController)→결과 카드 선택(선택 요약+변경). guestCount↔minGuests 자동 연동(필터 터치 시 필터 우선), 프리필 1회 해석(그 사이 점유되면 경고), initialVillas 시드로 첫 페인트 공백 방지. 필터 라벨=adminVillas 재사용(NS 추가 0) | QA 독립 검증 **PASS(6/6)** — /villas 리팩터 의미 동일 정독·누수 4종 클리어·half-open 공실 정합·디바운스 경합 없음. QA 수정 1건(P2 유령 선택: 날짜 변경으로 선택 빌라가 결과에서 빠져도 제출되던 경로 → selectionStale 경고 배너). tsc0·vitest(villa-search 14 등)·build 통과. 계약: docs/contracts/admin-manual-booking.md 후속 확장 |
| 2026-07-12 | **게스트 옵션 카테고리 탭 + 마사지·이발 지역 업체 자동 지정 — ADR-0037 (worktree-options-tabs-regional)** | 테오 지시 2건. ① /g 옵션 페이지에 ServiceType 탭(전체+실존 타입만·건수 뱃지, 표시 필터 전용 — 선택·합계·제출은 전체 catalog 기준 유지), guest-i18n serviceTypes 5언어. ② `VillaServiceVendor`(villaId×serviceType→vendorId, additive 라이브 적용) + `lib/regional-vendor.ts`(REGIONAL_VENDOR_TYPES=MASSAGE·BARBER, resolveOrderVendorId=매핑 우선→카탈로그 기본 폴백). 주문 생성 3경로(admin/partner/guest) vendorId 해석 적용 — 게스트는 오버라이드 시 업체 재조회로 **자동 발주 판정·Zalo 대상까지 교체**(발송 대상=저장 vendorId 항상 일치). 빌라 상세 지역 지정 에디터(마사지·이발 2행, 해제 가능)+PUT /api/villas/[id]/service-vendors(isOperator·타입 화이트리스트·APPROVED 검증·AuditLog) | QA 독립 검증 **PASS(결함 0·누수 0)**. tsc0·vitest **2695**(신규 3파일)·build 통과. 계약: docs/contracts/guest-options-category-tabs-regional-vendor.md |
| 2026-07-12 | **관리자 수동 예약 생성 (admin-manual-booking, worktree)** | 테오 지시 "관리자가 예약을 직접 할 방법이 없다". 기존 4경로(제안링크 홀드·확정·공급자직접·연장) 외 **관리자 직접 생성 경로 신설**. ① 신규 `lib/admin-booking.ts` createAdminBooking — 단일 트랜잭션에서 lockVillaInventory→checkAvailability(동시성)·isSellable 검수 게이트 유지(우회 없음)·정원 검증·quoteStayForVilla 원가 스냅샷·환율 스냅샷(제안 경로와 동일 소스), HOLD(만료 필수, 기존 expireHolds cron이 proposalItem 비의존이라 자동 만료 보장) 또는 CONFIRMED(파트너 시 evaluateConfirmCredit 여신 게이트+ensureReceivableForBooking 채권 — confirmHold 동일 규칙) 직접 생성, 공급자 알림=기존 BOOKING_HOLD/CONFIRMED 재사용(판매가·마진·원가 미포함), AuditLog source=ADMIN_MANUAL(금액 미기록). ② 신규 `POST /api/bookings`(requireCapability isOperator 첫 줄, zod, totalSale=숫자문자열→KRW/USD Int·VND BigInt, 에러 400 invalid_input/404 VILLA_NOT_FOUND/409 SOLD_OUT·NOT_SELLABLE·OVER_CAPACITY·RATE_NOT_SET·PARTNER_CREDIT_BLOCKED). ③ FE `/bookings/new` 폼(canViewFinance 게이트, DateField, 채널→통화 기본값, 파트너 셀렉트+agencyName 폴백, HOLD 만료 +24/+48 프리셋, 쿼리 프리필 villaId/checkIn/checkOut) + /bookings "새 예약" 버튼 + **공실보드 AVAILABLE 셀 팝오버 "예약 만들기"**(villaId+checkIn 프리필). i18n adminBookings.create.* ko/vi(화이트리스트 무변경). 스키마 무변경 | QA 독립 검증 **PASS(9/9)** — 누수 4종 차단·여신 드리프트 0·금액 float 0. QA 직접 수정 2건(RATE_NOT_SET/VILLA_NOT_FOUND/GENERIC 에러 매핑 — 미지정 코드가 CREDIT 문구로 오표기되던 버그 동반 수정). tsc0·vitest 211(관련분)·build 통과. 계약: docs/contracts/admin-manual-booking.md |
| 2026-07-12 | **티켓 여권정보(이름·생년월일) 벤더 전달 — ADR-0036 (worktree-ticket-passport)** | 테오 지시: 티켓은 차일드/어덜트/시니어 구분이 있어 업체가 여권 정보를 원함 + 소비자가 체크인된 사람 중 누구 티켓인지 선택해 전달. ① 스키마 additive `ServiceOrder.ticketGuests Json?`(라이브 적용). ② `lib/ticket-guests.ts`=화이트리스트 단일 통로(**이름·생년월일만** — 여권번호·국적·성별·만료일 금지). ③ 소비자 /g 옵션: TICKET 품목에 체크인 명단 체크박스(선택 수=quantity 동기화), 체크인 전엔 기존 수량 입력 폴백. ④ 생성 API: 체크인 확정본 대조(주입 방지 400 2종)+스냅샷 저장, Zalo 문구 미포함(채팅 PII 잔존 방지). ⑤ 벤더 GET guests=스냅샷 우선→체크인 전체 폴백(TICKET 행에만), 벤더 보드·admin 예약상세 표시. guest-i18n 5언어. ⑥ **TICKET 신청 시간 불요**(날짜만) — 폼 시간 필드 숨김+서버 재검증(비TICKET 시간 미제공=400 SERVICE_TIME_REQUIRED), 오전/오후/야간 구분은 추후 카탈로그 variant로 | QA 독립 검증 **PASS(결함 0)** ×2(본검증+델타). tsc0·vitest **2670**·build 통과. 계약: docs/contracts/ticket-passport-info-to-vendor.md |
| 2026-07-11 | **티켓 벤더 전용 보드 + 업체 변경 자동 재발주 (worktree-ticket-vendor-board)** | 테오 지시 2건. ① 업체 변경 시 새 업체 페이지에 발주 미노출 원인=변경이 발주 사이클 리셋(vendorStatus null)인데 재발주가 수동 → **changeVendor 성공 후 dispatch 자동 체이닝**(requestDispatch 헬퍼, Zalo 미연결=changeDispatchedNoZalo 경고·발주실패=changeDispatchFailed 수동 재시도 안내, ko/vi 3키). ② 티켓 업체 전용 보드: **ticketOnly 파생 판정**(활성 카탈로그 ≥1 && 전부 TICKET — `isTicketOnlyVendor`, 하드코딩 없음·업체 추가 자동 대응) → 시간제안 탭 숨김 3탭. TICKET 주문은 벤더 종류 무관 propose 버튼 숨김(발행=수락 겸행이라 대기 중엔 거절 1버튼) + 서버 대칭 가드 `respond propose+TICKET → 400 TICKET_NO_PROPOSAL` | QA 독립 검증 **PASS(결함 0·누수 0)**. tsc0·vitest **2654**(신규 2파일)·build 통과. 계약: docs/contracts/ticket-vendor-board-and-redispatch.md |
| 2026-07-11 | **벤더 발주함 검색에 이용자 이름 추가 (worktree-vendor-search-customer-name)** | 테오 지시 "이용자 명 검색이 되야해"(PR #240 후속). /api/vendor/orders search OR에 `customerName contains` + **폴백 매칭**(`customerName null && booking.guestName contains` — 화면 표시 규칙 `customerName ?? guestName`과 동일해 구주문·관리자 발주도 대표자명으로 검색됨). placeholder "발주·빌라·품목·이용자 검색"(ko/vi). 메인 세션 직접 수정(소규모) + 테스트 1건 추가 | tsc0·vitest **2642**(신규 1)·build 통과 |
| 2026-07-11 | **부가서비스 주문 이용자 이름 — 소비자 입력/대표자 폴백 + 벤더 노출 (worktree-service-order-customer-name)** | 테오 지시 "마사지 신청 시 소비자 이름 입력 또는 예약 대표자 이름을 업체에 보여줘야". ① 스키마 additive `ServiceOrder.customerName String?`(라이브 적용). ② 게스트 신청 폼에 이용자 이름 입력 카드(**대표자 guestName prefill** — 타이핑 최소화, max 80, 5언어), 서버는 빈값이면 대표자 폴백 저장. ③ 벤더 노출: Zalo VENDOR_PO에 "Khách:" 줄(없으면 생략 — 구 payload 하위호환)+벤더 보드 발주함·예약현황 카드(person 아이콘). 전 노출은 `customerName ?? booking.guestName` 폴백이라 구주문·관리자/파트너 발주도 백필 없이 커버. ④ 관리자 패널엔 대표자와 다를 때만 표시. ★PII=이름만(guestPhone 등 미노출 — `not.toHaveProperty` 테스트 방어) | QA 독립 검증 **PASS(결함 0)**. tsc0·vitest **2641**(신규/갱신 17케이스)·build 통과. ⚠사고: 공유 node_modules가 11:52에 통째로 비워져 있던 것 발견(npx가 원격 prisma 7.8.0 폴백해 착시) → npm install 641패키지 복원+v6.19.3 정상화. 계약: docs/contracts/service-order-customer-name.md |
| 2026-07-11 | **벤더 보드 날짜 검색 + 제안 수락→완료보고 동선 (worktree-vendor-board-date-search)** | 테오 실측 "제안 승인 건이 예약현황에 안 온다, 완료보고 불가" — **실측 원인=기능 누락 아님**: 승인 건(CONFIRMED·VENDOR_ACCEPTED)은 예약현황 조건에 포함되나 serviceDate asc 정렬로 시드 과거 465건 뒤 **47페이지(886건/89p)에 묻힘**. 해결: ① /api/vendor/orders `from/to`(serviceDate 양끝 포함, parseUtcDateOnly 관용 검증) — 4탭 목록+cancelled 배너 적용, **뱃지 카운트·정산 전역합계는 날짜 무관 유지**(할 일 총량 의미). ② 보드 탭 하단 날짜 필터 행: DateField(raw date input 금지 규칙)+원탭 칩 오늘/이번 주/전체(VN 기준 resolveQuickRange 재사용, [from,to)→inclusive -1일 변환) — 4탭 공통 상태·탭 전환 유지·변경 시 page=1. ③ 시간제안 APPLIED 카드 "완료보고 하러 가기" → 예약현황 탭+from=to=serviceDate 자동 필터(1페이지 노출 보장). vendor NS ko/vi | QA 독립 검증 **PASS(결함 0)** — off-by-one·UTC 정합·누수·투어 앵커 전건 확인. tsc0·vitest **2633**(신규 7)·build 통과. 계약: docs/contracts/vendor-board-date-search.md |
| 2026-07-11 | **전 부서 프로덕션 오픈 감사 + 문서 정합화 (worktree-open-audit-fixes)** | 프로덕션 오픈 전 11개 부서(PM·TDA·BE·FE·UX-VN·INTEG·QA·FIN·DESIGN·OPS·LOC) 전수 점검. **P0 0건**. 발견 P2 수정: FE 모바일 오버플로·UX-VN vi 텍스트 잘림·BE 승인 가드 2건·LOC 폴백 문구 i18n 키화. 나머지 발견은 오픈 비차단 P2로 TASKS.md "프로덕션 오픈 감사 백로그(2026-07-11)" 섹션에 등재. **문서 드리프트 정리**: 잠자리 개편 라이브 반영 완료 정정(잔여=V21 maxGuests 판단만)·bedroom-sync SQL을 migrations-manual로 정본화·web replica=1 제약 배포 런북 명문화·NOTIFICATIONS A-14 번호 부여·BACKLOG-WAITING 완료분 취소선 정리(테스트 데이터 purge·Zalo 실측·통계 화면). | 문서·SQL 사본만(코드 무변경). 실질 잔여=A1 베트남 사용자 파일럿(T4.4·테오)·러시아어 원어민 감수·OPS 보안 트랙(시크릿 교체·커스텀 도메인+Cloudflare·CSP enforce) |
| 2026-07-10 | **빌라 관리 검색 확장 — 상세·판매정보 필터 + 날짜별 공실 + 예약 날짜 검색 (T-villa-search-expansion, worktree-villa-search)** | 테오 3건+기타 확인. **① /villas**: q에 nameVi 추가·상세 필터 접이식 패널(침실≥·인원≥·수영장·조식·침대종류·해변거리≤·셀링포인트 태그 검색형 멀티셀렉트 AND·"판매가능만" 토글)·**날짜별 공실 검색**(ci/co DateField 상시 노출 — `findFreeVillaIds` 신규 export, findSellableVillaIds와 내부 엔진 공유·ADMIN 전용·점유 기준(살아있는 HOLD=점유), 속성 필터 선반영으로 freeIds 축소, 탭 카운트·total·목록 3자 동일 where·tabHref 클론 리팩터로 신규 파라미터 유실 봉인). **② /bookings**: from/to+dateBasis(투숙중 기본/체크인일/체크아웃일 — half-open windowEnd=to+1일, from 아침 퇴실 미포함·to 입실 포함)·우선순위 `filter>from/to>range>month` URL 상호배타·명시 날짜 시 HOLD 항상표시 해제·q에 guestPhone(혼재 형식 → 숫자 정규화 $queryRaw 파라미터 바인딩, **where 전용·select 금지**)·스코프 배너. lib/booking-date-filter.ts 순수 빌더. 프리셋 상태 핀·스탯·가동률·체크인 시트 무변경. **범위 외 3건 IDEAS 기록**(공급자 my-villas 검색·가격 필터·guestPhoneDigits 인덱스) | QA 독립 평가 **PASS(12/12·결함 0)** — 로컬 prod+실 DB(예약 2,862) 실측: dateBasis 3종 DB 그라운드트루스 일치(115/124/150)·우선순위 조합·탭 정합·누수 4종 전건 차단. tsc0·vitest **2596**·build 통과. 계약: docs/contracts/T-villa-search-expansion.md |
| 2026-07-10 | **벤더 시간제안 — 소비자 승인/거절 + 벤더 가시성 + 거절 통계 (ADR-0035, worktree-proposal-guest-resolution)** | 테오 마사지 실측 결함 4건: 제안하면 벤더 발주함에서 사라짐·소비자 승인/거절 프로세스 없음·거절이 벤더에 안 보임·거절 통계 없음. **해결 주체=소비자**로 전환. ① 신규 `POST /api/g/[token]/service-orders/[id]/proposal` {accept|decline}: accept=제안 일시 적용+outcome=APPLIED+**GUEST 주문 CONFIRMED 원자**(ADR-0033 일관) / decline=outcome=DECLINED+**vendorStatus PENDING_VENDOR 복귀**(발주함 재노출 — 벤더가 원래 시간 재응답, 거절 가시성 구조 보장). 양쪽 where에 status:REQUESTED 쌍(운영자 동시 취소 유령통보 차단 — QA P2 당일 수정). 벤더 인앱(VENDOR_PROPOSAL_DECLINED 신규 문구)+Zalo VENDOR_PROPOSAL_RESULT declinedByGuest 분기(구 payload 하위호환)+운영자 인앱. ② 스키마 additive vendorProposalOutcome String?(APPLIED/DECLINED/DISMISSED — 라이브 적용). 운영자 apply-proposal에 outcome 기록+GUEST apply=CONFIRMED 동반(수동확정 구멍 봉합). 재제안 시 outcome/respondedAt 쌍 리셋. ③ 벤더 보드 4탭(발주함→**시간제안**→예약현황→정산): 미해결 우선·상태 뱃지(응답대기/수락/고객거절/미적용)·발주함 DECLINED 뱃지. ④ 게스트 orders 제안 배너+원래→제안 비교·승인/거절 버튼(5언어). ⑤ 통계: 벤더 stats 제안 블록·관리자 service-orders 칩(제안 진행 중 N·고객 거절 N — CANCELLED 제외 정의 통일). outcome=사실 기록 유지(재수락 해소돼도 거절 통계 계상), UI 경고 뱃지는 미해소(PENDING_VENDOR)만 | QA 독립 검증 **PASS**(P2 decline 원자성·P3 뱃지 잔존·P3 통계 비대칭 — 전건 당일 수정·재검증). tsc0·vitest **2590**(신규 12)·build 통과. ⚠공유 클라이언트가 병렬 세션 generate로 2회 되돌아감(재생성으로 해소). 계약: docs/contracts/proposal-guest-resolution.md |
| 2026-07-10 | **체크아웃 청구서 대표 총액·수납 잔여 자동 가감산 (PR #229 후속, 테오 피드백 2건)** | ① "미니바 바꿔도 ₩ 총 청구액 불변" 혼란 해소 — ₩ 줄은 KRW 옵션 합계였음. 대표 총액(큰 emerald)을 **통합 환산 총액**(₫ + ≈₩/≈$)으로 승격, 기존 ₫/₩ 총액은 통화별 소계로 격하(ADR-0003 통화별 분리 저장 원칙 불변·표시만). ② 수납액 3필드 아래 **잔여 행**(≈₫/≈₩/≈$, 0=수납 완료·음수=초과) + 통화별 **"잔여 채우기" 원터치 칩**(해당 칸을 잔여 전액으로 SET·다른 칸 유지, KRW 100단위·USD 정수 반올림) — 현장 암산 제거. fx=null이면 VND 폴백·₩/$ 칩 숨김. **+절삭 규칙(테오 7/11)**: 잔여·채움 표시 ₫=1만 단위·₩=1,000원 단위·$=정수 내림, 끝전(1만₫ 미만)은 수납 완료 간주(허용치)·0원 칩 숨김. 서버·API·payload 무변경(폼+i18n만). | QA 독립 검증서 결함 1건 발견·수정: fx=null 폴백이 KRW 청구 누락 → 미수납인데 "수납 완료" 오표기(isSettled에 totalKrw===0 가드). 검산 수치 전량 일치·tsc0·vitest 50·build 통과 |
| 2026-07-10 | **티켓형 부가옵션 QR 티켓 발행·열람 (ADR-0034, worktree-ticket-qr-issuance)** | 테오 지시 "티켓(케이블카·사파리·쇼)은 벤더가 수량만큼 QR 발행 → 소비자가 각 티켓 열람". 구분=주문 type=TICKET(벤더 플래그 안 함). ① 스키마 additive: ServiceOrder.ticketUrls String[]+ticketsIssuedAt(migrations-manual SQL 라이브 적용 완료). ② 신규 `POST/DELETE /api/vendor/orders/[id]/tickets`(본인 스코프 404·비TICKET 400·종결/거절 409, **발행=수락 겸행**: PENDING_VENDOR→VENDOR_ACCEPTED+GUEST면 CONFIRMED 원자 전이+운영자 알림 — respond의 통보 블록을 lib/vendor-dispatch.ts sendVendorResponseOperatorNotifications로 추출 공유) + `POST/DELETE /api/service-orders/[id]/tickets`(운영자 대리 첨부 — Zalo로 받은 티켓 관행, 상태 불변). lib/ticket-upload.ts 공용 검증(매직바이트·5MB·30장 상한·전량 사전검증 후 저장=orphan 방지). ③ 벤더 보드 TicketPanel(썸네일·N/quantity 미달 경고·발행/삭제·PENDING엔 "발행 시 수락" 안내), 게스트 orders 티켓 그리드+body포털 라이트박스(5언어), 관리자 패널 AdminTicketCell. 수량 하드 강제 안 함(확인시트·FOC 현실, min1/max30) | QA 독립 검증 **조건부 PASS→해소**: P3(거절 발주 티켓 첨부 허용) 당일 수정(ORDER_REJECTED 409+테스트)·P2(stale client)는 잔존 QA 서버(3210)가 엔진 점유한 것 — kill 후 generate 완전 성공. tsc0·vitest **2539**(신규 11)·build 통과. ⚠공유 node_modules generate 레이스 재확인: 타 세션 PR #229 generate가 내 타입 되돌림 → 메인 schema 선반영 트릭 재사용. 계약: docs/contracts/ticket-qr-issuance.md |
| 2026-07-10 | **빌라 등록 개편 — 잠자리 구성 단일 원천 + 등록 필수정보 4종 (T-bedroom-composition-sync, worktree-wizard-bedroom-sync)** | 테오 "쏘나씨 V21 기본정보 침실 2 vs 잠자리 구성 4방" + "등록 시 잠자리·셀링포인트·위치접근성·와이파이·출입정보 필수". **① 파생 단일화**: VillaBedroom=진실 원천, bedrooms/bathrooms/maxGuests는 서버 파생(`lib/bedding.deriveBedroomScalars` — distinct roomIndex·전용합+`Villa.commonBathrooms`(additive 신규)·capacity 전원입력시 합 50클램프·roomIndex 1..N 재정규화), POST/PUT/PATCH-sales 3경로 공유(빈배열/미전송=스칼라 보존, 전송시 body 스칼라 무시, 상한 20 통일). **② 마법사 7스텝 개편**: 잠자리 구성 독립 스텝 신설("침실 몇 개?"→방 카드 자동생성, 침대 아이콘 칩+개수 스테퍼, 전용욕실 토글, capacity 침대기반 자동추론+수동조정, 공용욕실 스테퍼, 읽기전용 요약 배지 — maxGuests 스테퍼 제거), 위치 스텝에 셀링포인트 칩·구글맵·해변거리 프리셋, 이용규칙 스텝에 와이파이+출입정보(기존 accessType/accessInfo 재사용 — **신규 컬럼 지양(진실 이중화 방지 TDA 정정)**, ACCESS_TYPES 공유상수+SMARTKEY 추가). 레거시(구성 없는 빌라) 재제출은 스칼라 합성 프리필(잔여욕실→공용 승격·maxGuests floor 분배 보존 — QA P2 당일 수정). **③ 관리자**: sales-editor 공용욕실 입력+자동파생 안내(commonBathrooms 매 저장 필수 — 미전송시 과소집계), 체크인 상세·인쇄시트에 출입정보 병기, cleaning-info-editor SMARTKEY 정합(조용한 삭제 경로 봉인). **④ 백필** scripts/backfill-bedroom-scalars.ts(dry-run 기본·bedrooms만·bathrooms는 데이터 있는 빌라 한정·maxGuests 리포트-only) | QA 독립 평가 **PASS**(완료기준 10/10·수정금지구역 침범 0·누수 0 — wifiPassword·accessInfo /p·/g·공급자목록 부재, AuditLog 비밀값 boolean 마스킹)+P2 표적 재검증 PASS. tsc0·vitest **2552**·build 통과. 계약: docs/contracts/T-bedroom-composition-sync.md. **라이브 반영 완료**=commonBathrooms ALTER + 백필 실행 완료(V21 침실 2→4 반영). 잔여=V21 maxGuests 5 vs 파생 8 테오 판단만 |
| 2026-07-10 | **체크아웃 사진 제거 + 미니바 수납 다통화 분할 (T-checkout-photo-removal-and-multicurrency-settlement, worktree)** | 테오 요청 2건. **① 체크아웃 사진 항목 완전 제거**(관리자+공급자 폼·page·zod·lib/checkout 1장 이상 검증 삭제) — 사진은 파손·손실 리포트에서만(파손 시 메모/사진 필수 게이트는 유지). CheckOutRecord.photoUrls 컬럼은 과거 기록 보존(신규=빈 배열). 사진 전용 i18n 키 ko/vi 동시 제거(spaces.* 포함 — 체크아웃 NS 한정 확인 후). **② 미니바 수납 다통화**: 미니바 합계·게스트 청구서에 ≈₩/≈$ 환산 병기+통합 환산 총액(lib/fx-rates 일일 캐시, 표시용 ≈만). 수납액 통화별 분할 입력(₫/₩/$)+실시간 수납환산·잔여/초과 소프트 안내, 수납액 입력 시 결제수단 필수. CheckOutRecord additive 4컬럼(settledVnd/Krw/Usd·settlementFx 스냅샷 — 서버가 getDailyRates 조회, 클라 환율 불신뢰) **라이브 DB 적용 완료**(migrations-manual/2026-07-10). 예약 상세에 게스트 수납 표시(금액은 showFinance 게이트). 공급자 수납 UI는 VND 단일 유지(스코프 밖). | QA 독립 검증 **PASS(결함 0)** — 금액 float 저장 0·공급자 응답 화이트리스트 불변·파손 게이트 3층 유지·zod↔lib 정합·마이그레이션 스키마 일치. vitest checkout 34+누수 170+i18n 82·tsc0·next build 통과. 계약: docs/contracts/T-checkout-photo-removal-and-multicurrency-settlement.md |
| 2026-07-10 | **게스트 부가옵션 벤더 직접 전달 전환 (ADR-0033, worktree-guest-order-direct-vendor)** | 테오 지시 "부가옵션은 벤더에게 직접 전달, admin은 현황만·게스트에게 완료 확인" — 운영자 2중 승인(수동 dispatch→확정) 제거. ① 게스트 주문 생성 시 **자동 발주**: 승인·활성 벤더면 create에 vendorStatus=PENDING_VENDOR+poSentAt 인라인 + 벤더 Zalo(D-01)/인앱(D-03) 즉시 발송(신규 lib/vendor-dispatch.ts 공용 헬퍼 — dispatch·PATCH취소 경로 리팩터링, 동작 무변경). 미승인·미배정 벤더는 현행 수동 폴백. ② **벤더 accept=GUEST 주문 자동 CONFIRMED**(updateMany where에 status:REQUESTED 쌍 — 운영자 동시취소 원자 차단). propose/reject 예외만 운영자 조율. ③ 게스트 셀프 취소를 PENDING_VENDOR까지 확장(수락 전) + 벤더 취소 통보(D-02/D-04). ④ 게스트 화면: ordered=1 성공 배너·상태문구 "담당자 확인 중"·**확정 후 담당자 이름·전화 노출**(직접 연락, 로더 계층 게이트 — 미수락 벤더 신원은 반환값에도 미포함) — 5언어 파리티. NOTIFICATIONS.md 트리거 갱신 | QA 독립 검증 **PASS(P0~P2 결함 0)** — 누수 0(costVnd·bankInfo·판매가)·동시성(취소vs수락 한쪽만 승리)·관리자 패널 무회귀. tsc0·vitest 2525(신규 9)·build 통과. 계약: docs/contracts/guest-order-direct-vendor.md. QA 교훈 leak-checklist 축적 |
| 2026-07-10 | **열린 PR 4건 일괄 정리 (테오 위임 판단)** | **#204 닫음**(모델 라우팅 — 페이로드 14파일이 main과 바이트 동일, b161e9a로 기반영 중복). **#81 닫음**(6/26 모바일 감사 — 대상 화면 대거 재작성으로 충돌·PR #102 및 7/9 다듬기로 대체). **#176 머지**(예약 변경 비용안내 복사+여행사 Zalo 전송 — 클린 머지·심볼 정합 확인·vitest 2492 통과 후 회생). **#82 → PR #226로 대체 이식**(동시성·타임존 12건 — 낡은 패치 직머지 대신 현행 기준 선별 이식: updateMany 가드(발주/응답)·채권 advisory lock(입금 POST/DELETE·confirmHold·파트너지정·수납)·입금 삭제 채권 역계산(reversePaymentFromReceivable 신설)·연체/iCal VN일 기준·signup P2002→409·board 논널 안전화. 이미 반영분은 스킵). | #226: 최신 main 리베이스 후 tsc·build·vitest **2516/2516**(신규 테스트 24건). 계약서 docs/contracts/T-pr82-redo-concurrency.md. 작성(BE)≠검토(메인) 분리 — 돈 계산·락 순서 직접 검토. 원본 #82 superseded 닫음. 열린 PR 잔여 0건 |
| 2026-07-10 | **체크인/아웃 폼 인라인 안내 — 튜토리얼 마지막 갭 (T-tutorial-onboarding-9, worktree-tutorial-onboarding)** | 테오 "인터랙티브 폼에도 튜토리얼 넣어야지" — 폼은 코치마크(오버레이=작업 방해) 대신 **항상 보이는 인라인 배너**가 정답. ① `components/inline-guide.tsx` 신설(WizardGuide 승격 — light=기존 톤 바이트 동일·dark=teal-500/10 관리자용), 마법사 3곳 경로 이관·wizard-guide.tsx 삭제. ② 배너 10개: 관리자 체크인 3(여권 OCR 절차·보증금→환불 계산·서명 필수+wifi 게이트)·체크아웃 3(기준 구도 촬영·미니바 "남은 수량" 역산·환불=체크아웃 확정+청소 자동생성)·공급자 체크인 2·체크아웃 2(vi 명령형 — 미니바는 보증금 비차감·별도 수취). ③ **기존 문구 모순 5건 정정**: adminCheckin unsignedHint·submitCaption("서명 선택/사후 서명 가능"이 ADR-0029 D4 서명 필수 409 게이트와 정면 모순 — 구 정책 잔재), supplier unsignedHint·ocrRun/ocrHint("찍으면 자동으로 읽음"→실제는 버튼 절차), supplierCheckout.submitCaption(청소 자동생성+검수 게이트 보강). 전부 실코드(lib/checkin.ts·checkout.ts) 대조 후 문구화. ④ tests/form-guide-i18n.test.ts(4 NS guide.* ko/vi 패리티 — QA 권고). | tsc0·vitest 2496·build 통과. QA 독립 평가 **PASS(차단 0·누수 0)** — 4폼 12배너 라이브 렌더 실측(다크/라이트·겹침 없음·보증금 스킵 토글 유지)·문구 사실성 전건 코드 대조·타 공급자 체크아웃 404 회귀 확인. 이로써 튜토리얼 체계 완결: 코치마크 31종(목록·상세) + 인라인 안내(마법사 3·폼 10) |
| 2026-07-10 | **Zalo 음성 메시지 표기·번역 완결 (T-zalo-voice-display)** | 테오 "음성메시지가 화면에 표기도 안 된다". 진단: 수신(INBOUND) 파이프라인은 정상(당일 08:58 음성 저장+STT 번역 실측 확인) — 실제 결함 3곳. ① 본인 발신(앱, 특히 전달) 음성 에코가 chat.voice 분류 후 top-level URL 부재로 첨부 0 → self 분기에서 조용히 드롭(08:58 테오 세션 에코 행 부재로 확정). 수정: 미디어 타입(voice/photo/file/sticker/video/location)은 첨부 0이어도 미러 + [voice-nourl] 진단 로그(키 이름만, PII 0) + chat.voice URL 추출에 params JSON 폴백. ② 인박스 미리보기 previewText에 voice/sticker/video/location/contact 케이스 부재 → "—" 표시. 라벨 추가(기존 preview.* 키 재사용, i18n 무변경). ③ 발신 에코 음성 STT 미실행 → saveOutboundEcho 반환 확장(messageId·translateMode) 후 maybeTranscribeVoice 연결(수신과 동일 가드) | QA 독립 검증 PASS(결함 0): vitest 2496 전건·tsc 0·next build 통과. 멱등(zaloMsgId) 경로 무변경으로 프로그램 발신 중복 없음. 잔여: 배포 후 테오 실측(직접 녹음+전달 각 1회)으로 [voice-nourl] 실 payload 키 수집 → params 폴백으로도 URL 못 찾으면 필드 추가 보정 |
| 2026-07-10 | **회원관리 Zalo 수신 연결 패널 실사용 개선 (T-users-zalo-link-ux, PR #221)** | 테오 "알림 발송 건은 사용자관리에서 관리하자". LINK_ZALO/UNLINK_ZALO API·감사로그·중복 409는 기존 완비였으나 후보 드롭다운이 미연결 대화방 507개(그룹 42 포함)를 검색 없이 나열해 실사용 불가(동명 "Villa Go" 3개 구분 불가 — 김태진 연결을 DB 수동으로 했던 원인). 개선: 후보=1:1 대화만·최근 활동순+활동일 표시("대화 없음" 뱃지)·검색 입력(상위 30 렌더+초과 안내)·연결된 사용자에 대화방 이름 표시+"변경" 버튼(기존 API가 이전 연결 자동 해제). i18n ko/vi 동시. API 무변경. | 배포(87e98af) 후 프로덕션 E2E PASS: 검색→연결→이름 표시→해제(DB 원복 확인). 김태진 "Villa Go"·DK "Lee Dokyung" 이름 실측. 리뷰 검출 1건: zaloName 매핑 조회 키 오류(Zalo uid→User.id, 항상 null 되던 결함) 수정 후 머지 |
| 2026-07-10 | **상세 화면 투어 4종 — villaDetail 패턴 적용 (T-tutorial-onboarding-8, worktree-tutorial-onboarding) — 투어 총 31종** | 테오 "다른 상세 화면 해줘". **bookingDetail**(/bookings/[id], 6스텝): 상태 사이클(홀드→확정→체크인→체크아웃)·수납 기록(입금통보 대조)·상태 전환 액션·게스트 셀프체크인 토큰·투숙객 명단(임시거주신고 기준)·부가서비스 발주 — actions는 action-panel 루트 직접 부착(terminal=null이라 래퍼 금지), 나머지 조건부는 블록 안쪽 래퍼(빈 하이라이트 방지). **partnerDetail**(/partners/[id], 5스텝): 여신·aging(8일+ 연체 위험)·신용 조건·취소 미종결 경고 — "?" 라벨 RSC→prop(화이트리스트 무변경). **supplierVillaDetail**(/my-villas/[id], 5스텝 vi): 사진·비품·시즌별 원가("내 원가"만)·직접 판매 링크·규칙/위치. **partnerBookingDetail**(/partner/bookings/[id], 4스텝): 요약(미납 잔액)·부가서비스(조건부 스킵)·승인형 취소/변경 요청·명단 사전제출. 체크인/아웃 폼은 원칙 제외 유지. | tsc0·vitest 2492·build 통과. QA 독립 평가 **PASS(차단 0·누수 0)** — 4투어 라이브 데모 실측(발화·완주·영속·재생·조건부 스킵: EXPIRED=2스텝·파트너 CANCELLED=2스텝). ⚠주석 속 훅 호출 문자열이 admin-i18n-whitelist 스캔 오탐(리워딩 해소 — "use client" 오탐과 동일 클래스). 기록: CHECKED_OUT은 terminal 아님(5스텝 재생 — roster desc 문맥 어긋남은 개선 여지)·QA 실측 중 demo-svc-7006 오클릭(CONFIRMED→DELIVERED)→AuditLog 근거 즉시 원복 |
| 2026-07-10 | **[P0 핫픽스] 삭제·비활성 계정 유령 세션 무효화 (T-ghost-session-invalidate, PR #219)** | 김태진 "5678 빌라 승인 버튼을 눌러도 승인 안 됨" 신고 → 라이브 로그 실측: 06:57~07:00 승인 PATCH 전부 `AuditLog_userId_fkey` P2003 500 롤백. 원인=오늘 하드 삭제된 구 계정(0702635421)의 세션이 기기에 잔존 — auth.ts jwt 콜백 재검사(P0-5②)가 passwordChangedAt만 보고 **사용자 행이 없으면 오히려 유효 통과**(dbMs=null→0). 삭제·비활성 계정 JWT가 7일 유효: 조회 화면 전부 열리고 감사로그 걸린 변경 API만 500. 수정: pwd 재확인 조회에 isActive·deletedAt 포함, `!u \|\| !isActive \|\| deletedAt`이면 두 분기 모두 세션 무효(60초 내 차단). | 배포(8d29d89) 후 프로덕션 E2E PASS: 테스트 계정 로그인→세션 산 채 하드삭제→70초 후 /villas 접근 시 /login 강제 로그아웃·정상 계정 회귀 없음. 빌라 5678은 테오 계정으로 07:00:33 승인 완료(ACTIVE, DB 복구 불필요). 김태진 구 기기 세션은 배포 후 자동 차단→새 계정(01081934171)으로 재로그인하면 됨 |
| 2026-07-10 | **[P0 핫픽스] 임시비밀번호 계정 로그인 무한 루프 (T-login-mustchange-loop, PR #217)** | 김태진(01081934171, OWNER 승격) "로그인 버튼 누르면 로그인 창만 계속" 신고 → 프로덕션 실측으로 원인 확정: 로그인 서버액션 `signIn(redirectTo:"/")`의 **같은 POST 내부 리다이렉트 체인**에 미들웨어 임시비번 게이트(/→/account 307)가 겹치면 내부 재요청에 세션 쿠키가 없어 auth()=null→/logout(쿠키 삭제)→/login 루프. mustChangePassword=true 계정 전원 로그인 불가였음(일반 계정은 미들웨어 307이 안 끼어 잠복). 수정: `redirect:false`+성공 시 클라이언트 전체 리로드(`window.location.href="/"`) — 패스키 로그인과 동일 패턴. | 배포(869ee9e) 후 프로덕션 E2E 실측 PASS: 임시비번 로그인→/account 도달→비밀번호 변경→재로그인→/dashboard·오류문구 회귀 정상. 테스트 계정 생성·검증·삭제 완료. 김태진은 배포 전 플래그 해제로 즉시 차단 해소(감사로그 기록). 교훈: curl 쿠키자로만 쿠키 전파 검증 가능(Playwright MCP는 cookie 헤더 숨김) |
| 2026-07-10 | **투어 내용 개편 — 검색 안내 제거 + 빌라 상세 기능 투어 (T-tutorial-onboarding-7, worktree-tutorial-onboarding)** | 테오 피드백 "누가 검색을 몰라! 상세 페이지 기능을 알려줘야지" 반영. **① villaDetail 투어 신설**(/villas/[id], route=null·명시 tourId): 5스텝으로 테오 7기능 전부 커버 — 헤더(상태배지)→'상세'탭[기간별 요금·성수기 날짜]→'상세'탭[청소 담당자·운영정보 — **의도적 중복 앵커**(같은 탭 두 주제, 하이라이트 고정 채 문구 진행)]→모두펼치기[비품·미니바 비치+회사 표준은 재고 메뉴 «미니바 품목» 탭 안내]→'판매정보'탭[wifi·잠자리 구성]. 탭 콘텐츠는 전환 시 언마운트라 앵커는 항상 보이는 탭 버튼·헤더만. "?"=헤더 액션영역 명시 tourId. **② 새 문구 원칙 명문화+전 투어 적용**: 범용 검색 안내 금지 — adminBookings.filters→**체크인 시트 출력** 교체·adminProposals/adminServiceOrders filters 스텝 삭제(2스텝화)·adminUsers(역할 탭)·adminRevenue(CSV 버튼으로 앵커 이동)·adminAvailability(기간 넘기기) 문구 수정·adminInventory.stock에 회사 표준 미니바 진입점 안내 추가(canSetPrice 조건부 탭이라 문구 안내). 테스트: 스텝 상한 예외 맵(villaDetail≤6, 상세 화면 한정 계약 완화)·중복 앵커 예외 1건. 투어 총 27종. | tsc0·vitest 2472·build 통과. tour NS "검색/tìm" 언급 0건(스크립트 검증). QA 독립 평가 **PASS(결함 0·누수 0)** — villaDetail 5스텝 실측(중복 앵커 픽셀 고정 확인·ko/vi)·bookings 시트 버튼 하이라이트·2스텝 재번호. 관찰(코스메틱): rates 문구 "지금 보이는 '상세' 탭"은 판매정보 탭에서 재생 시 어긋날 수 있으나 하이라이트가 정확해 미미 |
| 2026-07-10 | **비품·시설 수량 입력 + 직접입력(custom) 전 카테고리 확장 (T-amenity-quantity-custom, worktree-amenity-quantity-custom)** | 테오 "비품에 수량 등록 + 직접입력 텍스트박스(수량 필수), vi 입력의 관리자 표시 처리" → **① 수량**: 마법사 사전 항목 타일 선택 시 +/− 스테퍼(1~99, 0=해제) — DB `VillaAmenity.quantity`는 기존 지원이라 UI만. **② 직접입력**: `CUSTOM_ALLOWED_CATEGORIES`에 KITCHEN·BATHROOM·APPLIANCE 확장, 마법사·a13 수정 화면·관리자 편집기 3곳에 "직접 추가"(라벨 vi 최대60자+수량 스테퍼 기본1 강제, 카테고리당 10개 zod cap). **③ vi→ko 번역**: `customLabelKo` additive 컬럼(**라이브 적용·검증 완료**) + `lib/amenity-translate.ts` 저장 커밋 후 best-effort Gemini 번역(dedupe·멱등·실패 격리 — 키 부재/실패에도 저장 성공), 관리자·체크인시트 ko 표면 "번역 (vi원문)" 병기·미번역은 원문+"번역 대기" 배지, 게스트 ko=`customLabelKo ?? customLabel` 폴백. **④ 유실 경로 3건 봉인**: 재제출 edit select customLabel 누락·a13이 custom을 라벨 없이 재전송(400)·관리자 전체교체 PATCH가 custom 미포함 — 전부 수정(관리자 저장 시 custom 보존). MINIBAR 회사표준 drop 불변. | tsc0·**vitest 2474**(신규 amenity-translate 등)·build0·ko/vi 패리티 79=79. QA 독립 평가 **PASS(7/7·결함 0)** — 라이브 실측: vi custom "Ghế tắm nắng"×4 등록→관리자 "선베드 (Ghế tắm nắng)" 병기→타 비품 수정 후 custom 생존, 런타임 400×3(주입·라벨누락·cap). 계약: docs/contracts/T-amenity-quantity-custom.md. 관찰(별도): 데모 Tyy 공급자 포털이 ko로 렌더(locale 상태, 본건 무관) |
| 2026-07-10 | **투어 전수 확장 — 신규 13종, 총 26종 (T-tutorial-onboarding-6, worktree-tutorial-onboarding)** | 테오 "가능한 곳 찾아서 많이" → FE·UX-VN 병렬 회의 전수 판정(채택 13·제외: cost-alerts·activity=열람 전용, messages=앵커 불안정, zalo-connect=화면 자체가 안내, 프로필=단일 폼). **관리자 10**: adminAvailability(필터→범례→타임라인 격자)·adminSettlements(월 집계→요약→수납지급 흐름)·adminReceivables(KPI→에이징→파트너 목록)·adminServiceOrders(합계→정산|중계 탭→필터)·adminRevenue(필터→요약→CSV)·adminStatistics(2)·adminInventory(2, ⚠canSetPrice 조건부 탭바 비앵커)·adminUsers·adminPartners(2)·adminSettings(시즌→홀드시간→하위설정). **포털 3**: earnings(탭→기간→KPI — 미수는 detail 뷰 뒤라 세그먼트 문구가 안내)·myBookings(2, 화면상 첫 카드=pending 우선 판정)·vendorStats. 중앙(정의·i18n·테스트)=메인, 화면 배선=FE·UX-VN 에이전트 병렬(파일 분리). ResponsiveTable류는 순수 래퍼 div(공용 무수정). | tsc0·vitest 2471·build 통과. QA 독립 평가 **PASS(결함 0·누수 0)** — 표본 7화면 실측(avail·settlements·users·settings·earnings·my-bookings 1/1 스킵 발화·vendor/stats vi)+빈 데이터 완료기록 미소모 확인. ⚠직후 테오 피드백: "검색 안내는 불필요 — 상세 페이지 기능(빌라 상세 요금제·비품·미니바·청소담당·wifi·잠자리)을 안내하라" → T-7 개편 착수 |
| 2026-07-10 | **관리자 투어 확장 1차 — 운영 핵심 4화면 (T-tutorial-onboarding-5, worktree-tutorial-onboarding)** | 테오 "admin 페이지 더 보강" → 미래 직원 온보딩 기준 운영 사이클 4종(FE 회의 확정, 인프라 무변경): **adminBookings**(/bookings: 상태 카드→필터→목록 — ResponsiveTable 무수정 래퍼 div 앵커)·**adminVillas**(/villas: 상태 탭→등록 버튼[모바일/데스크톱 이중앵커]→첫 카드[index 0])·**adminProposals**(/proposals: 만들기→필터→상태 탭 — 목록 행은 클라 fetch 비동기라 앵커 금지, 즉시 렌더 요소만)·**adminInspections**(/inspections: 상태 탭→검수 큐→승인/반려 — 모바일 마스터-디테일 양방향 자동 스킵 실측, CoachMark는 key 리마운트 밖 형제 마운트). **/messages는 제외**(실시간 재정렬·대화 선택 후 렌더 UI라 앵커 불안정 — FE 판정). tour NS 4블록 ko/vi. | tsc0·vitest 2458·build 통과. 계약: docs/contracts/T-tutorial-onboarding-5.md. QA 독립 평가 **PASS(결함 0·누수 0)** — 1440px 4화면+390px 스팟(이중앵커·양방향 스킵) 실측, 빈 목록 재번호(1/2)·타 라우트 "?" 미노출·dashboard 회귀 없음. 투어 총 14종. 다음=테오 지시 "가능한 곳 전수 확장"(T-tutorial-onboarding-6) |
| 2026-07-10 | **튜토리얼 보강 — 마법사 인라인 안내·"?" 발견성·실사용 테스트 킷 (T-tutorial-onboarding-4, worktree-tutorial-onboarding)** | 테오 "전산 초보도 되겠나?" → 갭 3건 보강. **① 마법사 인라인 안내**(신규 wizard-guide.tsx, teal·info 고정·화면당 1개·≤2문장): UX-VN 회의로 basic·photos·amenities 3곳만 선정 — basic=침실·욕실 수→사진 슬롯 의존성 예고(가장 비싼 재작업 방지), photos=칸 탭=1장 조작모델+가로 촬영(기존 하단 helper 배너 상단 통합·키 삭제), amenities=탭별 전환(일반="있는 것만+고객 노출"/미니바="수량=비치 개수, 가격 아님"). location·rules·rates는 기존 안내 충분으로 제외. **② replayHint**: TourLabels+buildTourLabels 확장 → 전 투어 9종 마지막 스텝에 "다시 보려면 ? 버튼" 자동 표시(1회성 투어의 재생 경로 발견성). **③ docs/usability-test-checklist.md**: 테오용 관찰 킷 — 개입 금지·60초 규칙·역할별 과제(A공급자5·B청소2·C파트너2·D벤더2)·기록 표·판정 기준·결과→개선 연결 절차. INDEX 등록. | tsc0·vitest 2450·build 통과. 계약: docs/contracts/T-tutorial-onboarding-4.md. QA 독립 평가 **PASS(결함 0·누수 0)** — 마법사 3단계 배너·탭 전환·helper 부재·replayHint 마지막 스텝 한정(공급자+벤더 2투어 실증) 전부 실측. ⚠로컬 3000 타 세션 점유 → NEXTAUTH_URL 3001 오버라이드 레시피로 검증(.env 삭제 완료). 다음=테오가 실제 사용자와 체크리스트 실행 → 결과 붙여넣으면 개선 태스크화 |
| 2026-07-09 | **Zalo 연결(QR) 역할별 커버리지 완결 (T-zalo-connect-role-coverage)** | 테오 지적("역할별 QR 등록 화면이 없는 것도 있다") 후속. ① 전화번호 자동 매칭 role 확대: tryMatchSupplierByPhone→tryMatchUserByPhone, SUPPLIER 단일→SUPPLIER·CLEANER·VENDOR·PARTNER (ADMIN/OWNER 제외 유지 — 운영자 오염 방지, 점유 시 스킵·경합 무시 규칙 보존). CLEANER는 화면만 있고 매칭이 끊겨 있던 버그성 갭 해소. ② /zalo-connect 본문을 components/zalo-connect/(screen·actions·entry-card)로 추출, app/vendor/zalo-connect·app/partner/zalo-connect 래퍼 신설(각 포털 locale 규칙·done→/vendor·/partner, 타 역할 /login 바운스) + vendor/partner profile에 연결 상태 진입 카드. ③ PARTNER 매칭 시 Partner.contactZaloUid **null일 때만** 동기화(수동 세팅 존중). 신규 i18n 키 0(기존 zaloConnect 재사용, profileNotConnected만 역할 중립 문구로). | QA 독립 검증 PASS(권한 격리·IDOR 0·supplier 회귀 바이트 동일·누수 0). zalo-inbound-save 21 + 회귀 193·tsc0·build 통과. 계약: docs/contracts/T-zalo-connect-role-coverage.md |
| 2026-07-09 | **튜토리얼 에픽 후속 — 미실측 투어 3종 실측 + hydration 경고 종결 (worktree-tutorial-onboarding, 코드 무변경)** | ① 데모 상태 재조사(라이브 읽기 전용): **벤더 데모(0791234569) 비번=Test1234! bcrypt 직접 대조 일치 — 2단계 QA "비번 변경돼 로그인 불가"는 오판(정정)**, Tyy 빌라 32·PENDING 태스크 2 존재 → DB 쓰기 없이 실측 가능. ② 로컬 prod+Playwright 390px 실측 3종 전부 PASS: calendar(1/3→3/3 완주·기록·구멍=앵커+6px 정합), cleaningDetail(**기준사진 0장 → baseline 스텝 자동 스킵 1/2부터** — 스킵 설계 실증·완주 기록), vendorBoard(vi 문구 1/3→3/3·완주 기록·"?" 강제 재생 확인). ③ /dashboard React #418 hydration 경고: 390·1440px·재로드 전부 **재현 불가(콘솔 0건)** — 3단계 QA 세션의 일시 현상(당시 localStorage 조작 타이밍 추정)으로 종결, 재발 시 재조사. | 코드·DB 무변경(검증만). **역할별 튜토리얼 에픽 투어 9종 전량 라이브 빌드 실측 완료** — 잔여 0 |
| 2026-07-09 | **역할별 튜토리얼 3단계 — ADMIN (T-tutorial-onboarding-3, worktree-tutorial-onboarding) — 에픽 완료** | adminDashboard 투어(/dashboard): 오늘 현황(KPI)→관리 메뉴→운영 알림벨. **인프라 보강 3건(FE 회의)**: ① `isRectHorizontallyVisible` 순수 함수 — 반응형 이중 앵커(데스크톱 사이드바/모바일 하단네비, KPI그리드/현황박스)에 같은 data-tour를 달면 가시 쪽 자동 선택(anchorEl=querySelectorAll 첫 가시 매치+visibility:hidden 배제) ② measure()마다 앵커 재해석 — 리사이즈/회전 시 (0,0) 유령 구멍 결함 수정(active 목록은 start 고정=known limitation) ③ 키 큰 앵커(사이드바 nav 610px) 말풍선 위·아래 공간 부족 시 중앙 폴백(구멍 유지). "?" 버튼=layout RSC가 다크 스타일 TourHelpButton을 AdminSidebar tourHelp prop으로 주입(데스크톱 푸터+모바일 헤더, 같은 노드 2슬롯). 모바일은 벨 스텝 자동 스킵(2스텝)·데스크톱 3스텝. ADMIN_CLIENT_NAMESPACES 무변경. | tsc0·vitest 2450·build 통과. 계약: docs/contracts/T-tutorial-onboarding-3.md. QA 독립 평가 **PASS**(결함 0·누수 0) — 1440·390px 이중 실측+리사이즈 회귀+타 라우트 "?" 미노출+1·2단계 회귀 없음. 관찰: /dashboard React #418 hydration 경고(기존 이슈 추정, 이번 diff 무관 — 별도 트래킹 권장). **역할별 튜토리얼 에픽(1~3단계) 완료** — 잔여=데모 데이터 복구 후 미실측 투어(calendar·cleaningDetail·vendorBoard) 1회 |
| 2026-07-09 | **역할별 튜토리얼 2단계 — PARTNER+VENDOR (T-tutorial-onboarding-2, worktree-tutorial-onboarding)** | 1단계 인프라(components/tour/) 재사용 — 스텝·앵커·문구만 추가(coach-mark.tsx 동결). 투어 4종: partnerHome(첫 예약카드→알림벨[layout span 래핑]→탭바)·partnerReceivables(미수 teal 요약→청구서 섹션[입금통보 버튼은 행 내부라 섹션 앵커])·partnerProposals(첫 제안카드→열람·예약 링크)·vendorBoard(발주함→일정[완료보고 포함]→정산 — **앵커는 즉시 렌더 탭 3개만**, 비동기 카드 앵커 금지). partnerHome CoachMark는 빈 상태 삼항 밖 마운트(예약 0건 신규 파트너도 벨·탭바 스텝). 파트너 ko 존댓말·벤더 vi 명령형(발주=Zalo 알림 안내 포함, UX-VN·FE 회의 문구). 미승인 게이트: 파트너=showNav, **벤더=layout getVendorForUser approved 게이트(QA 결함 1 → 당일 수정·재검증 승인)**. | tsc0·vitest 2423·build 통과. 계약: docs/contracts/T-tutorial-onboarding-2.md. QA 1차 조건부 반려(미승인 벤더 게이트 "?" 노출)→수정 후 승인. 파트너 3종 라이브 실측 PASS(자동표시·부분스킵·"?"재생·proposals 0건 미표시). ⚠벤더 라이브 실측 불가 — 데모 벤더(0791234569) 비번이 당일 13:24 변경돼 로그인 실패(코드 검증 완료) → 비번 복구 후 실측 1회 권장. 3단계(ADMIN)는 후속 |
| 2026-07-09 | **Zalo 연결 QR·친구추가 링크 관리자 설정화 (T-zalo-connect-qr-admin-setting)** | ① 테오 Zalo 명함 QR 크롭본 public/zalo-qr.png 배포 + env(NEXT_PUBLIC_ZALO_QR_URL=/zalo-qr.png·NEXT_PUBLIC_ZALO_OA_URL=https://zalo.me/0799493138) 설정 — QR에 인코딩된 zaloapp.com/qr/p 링크는 앱 전용이라 버튼은 zalo.me 프로필 링크로 교체. ② 회사 공용 Zalo 대비 관리자 설정화: 설정 화면 "Zalo 연결" 카드(QR 이미지 업로드=/api/uploads 재사용+미리보기, 친구추가 링크 input, 비우면 env 폴백) + AppSetting 키 2종(ZALO_CONNECT_QR_URL/OA_URL, CLEARABLE, https-only·/uploads 접두 검증) + /zalo-connect는 AppSetting 우선→env 폴백. 신규 API 없음(기존 /api/settings ADMIN 가드·writeAuditLog 경로 재사용, 배치 entityId "bank-contact"→"settings-batch" 중립화). adminSettings.zaloConnectCard ko/vi 19키 패리티. | QA 독립 검증 PASS(비ADMIN 403·javascript:/data:/http: 주입 거부·폴백 3단 분기·감사로그). settings-validators 테스트 52(신규 21)·tsc0·build 통과. 계약: docs/contracts/T-zalo-connect-qr-admin-setting.md |
| 2026-07-09 | **역할별 튜토리얼 1단계 — 코치마크 온보딩 (T-tutorial-onboarding, worktree-tutorial-onboarding)** | 공통 코치마크 인프라(components/tour/ — 외부 라이브러리 0, createPortal(body)·z-[70]·visualViewport) + 투어 4종: myVillas(등록FAB→상태배지→탭바)·calendar(빌라선택→날짜토글→범례)·cleaningList(태스크카드→날짜칩, SUPPLIER·CLEANER 공용)·cleaningDetail(기준사진→슬롯촬영→제출, 자체앱바 "?"). 첫 진입 자동 1회(localStorage villa-tour:*)·완주/건너뛰기만 기록(라우트 이탈 미기록)·헤더 "?" 강제재생·**앵커(data-tour) 부재 스텝 자동 스킵**. 문구는 RSC 번역→props(화이트리스트 비의존, cleaning-submit 패턴)·tour NS ko/vi 신설. 유지보수 규칙 명문화: stitch-conversion 규칙#6 + QA evaluation-criteria 체크리스트 + docs/INDEX.md — **투어 걸린 화면 UI 변경 시 tour-definitions.ts·tour 문구 동시 갱신**. 신규 테스트 14(tests/tour-onboarding.test.ts: 패리티·키실존·3스텝상한·앵커실존·스킵·순수모듈) + light-portal-i18n 스캔 dirs에 components/tour 추가. QA 독립 평가 **PASS**(결함 0·누수 0·실측: 자동표시/건너뛰기/재생/부분스킵/CLEANER 격리). | tsc0·vitest 2404·build 통과. 계약: docs/contracts/T-tutorial-onboarding.md. 실측 제약: 라이브 purge 후 데모 빌라·태스크 0건이라 calendar·cleaningDetail 투어는 정적+단위테스트 검증 — 데모 데이터 재보충 후 1회 실측 권장. 2단계(PARTNER+VENDOR)·3단계(ADMIN)는 후속 |
| 2026-07-09 | **빌라 공유 대표 사진 첨부 + 워치독 경보 쿨다운 영속화 (T-villa-share-photo·T-zalo-health-db-cooldown, wt/villa-share-photo)** | **① 사진 첨부(PR #205)**: /messages 빌라 공유(S3)에 대표 사진 1장(sortOrder 1위=목록 썸네일 규칙)+캡션(기존 본문) 발송. 신규 lib/zalo-share-image.ts 로더 — 디스크=파일명 화이트리스트·경로탈출 차단, 원격=STORAGE_PUBLIC_URL 프리픽스만(SSRF 이중가드, 연장형 우회 차단 검증), 매직바이트·10MB 상한. 사진 없음·로드 실패·발송 실패 → 텍스트 폴백(공유 실패 금지). select 추가는 photos.url 하나 — D2 화이트리스트 무변경. **② 쿨다운 영속화(PR #206)**: 테오 보고 'Jini QR 재로그인 경보 반복'(16:45~20:05 5회) — 원인=쿨다운 6h가 인메모리(globalThis Map)라 **배포 재시작마다 리셋**(당일 PR 5건 배포=재시작 5회, 경보 시각이 배포 시각과 일치). 경보 시각을 AppSetting(zalo-health:last-alert:{accountId})에 영속화 — 재시작 후에도 6h 억제+메모리 동기화, fail-open. | 각각 독립 QA PASS(사진)·2402/2405 테스트·tsc0·build0. ★Jini 계정 자체는 진짜 미연결(credential 만료) — **근본 해결=해당 관리자 폰 /zalo-connect QR 재로그인**(코드 수정은 빈도만 해소). |
| 2026-07-09 | **Zalo 알림 정보 보강 + 공유 문구 리치화 (T-zalo-notify-enrichment, wt/zalo-notify-enrichment)** | 테오 NOTIFICATIONS.md 전수 검토 승인분. **① 알림 보강**(lib/zalo.ts 빌더 + 호출부 payload): A-01 공급자명·예약자·예약번호(#뒤6자리) / A-02·03 단지·침실·욕실·정원·사진수 / A-05 판매 채널(파트너명·연락처, agencyName 폴백) / **A-06 베트남어→한국어 전환**(+전→후 원가·영향 제안수, proposalCount) / A-07~10 일정·수량·발주액 요약 / B-01·02 예약번호·예약자명·조식(true만) / B-05 변경 전→후 일정·인원 비교 / C-01 다음 확정 체크인(긴급도) / D-01 옵션 라벨(selectedOptionLabels 가격제거)·시각·벤더 정산액(costVnd 라인총액) / E-01 객실료(채권 VND)·잔금기한·예약번호(hold 커밋후 receivable 조회). 전부 구 payload 하위호환(필드 없으면 줄 생략). **② 빌라 공유 기간 병기**(lib/zalo-share.ts): 시즌 대표행 → 전체 기간 나열 — "기본" 먼저·시작일 순, "성수기 · 라벨 (7.15 ~ 8.31)", half-open endDate는 -1일 포함일 표시. **③ 제안서 공유 리치화**: 빌라 N개·기간·박수 헤더 + 빌라별 침실·수영장·총액(KRW/VND/USD 채널 분기) + 링크·유효기간(만료 안내). D2 불변식 유지(공급자=원가만·고객=판매가만·마진 0, 타입 차단 구조 보존). NOTIFICATIONS.md 전면 갱신 + G절(채팅 공유 문구 4종) 신설. | tsc0·**2374 테스트 그린**(신규 zalo-share 9·zalo 보강 10·partner 2·roster/direct/share-api mock 갱신)·build 통과. C-05 반려 슬롯명은 데이터 부재(사유 텍스트만) → 검수 UI 선행 필요, NOTIFICATIONS.md 검토 포인트에 후속 기록. B 게스트 요청사항은 Booking.note가 운영 내부 메모라 비노출 유지. |
| 2026-07-09 | **전수검사 결함 6건 수정 (T-audit-fixes, wt/audit-fixes)** | 대표 지시 5부서 전수검사(PM·QA누수·BE·모바일실측·OPS — P0/P1 0건) 후속 수정: ①D-1 파트너 승인대기 헤더 로그아웃 fixed→헤더 right 슬롯(360px 언어토글 겹침 해소) ②P2-1 /p done 만료 후 부가서비스 폼 숨김+5언어 안내(API 410 정합) + roster·입금통보는 예약 수명주기 규약 명문화(410 추가 안 함 — D-3 리마인더 보호) ③P2-2 /g 서명 전 wifiPassword 서버 게이트(서명 API 응답으로 즉시표시 유지) ④W-2 통계 매출 seller=OPERATOR 게이트(revenue-ledger 정합, 점유는 무필터) ⑤M-1 i18n requestLocale 존중 — 청소원 문구ko+날짜vi 혼재 근본수정 ⑥M-2 /villas 탭 scrollbar-none. 신규 테스트 4건(tests/audit-fixes-guards.test.ts). QA 독립 평가 PASS(결함 0). | tsc0·vitest 2358·build 통과. 계약: docs/contracts/T-audit-fixes-2026-07-09.md. 잔여=배포 후 360px 실측 1회 |
| 2026-07-09 | **Zalo 리스너 전용 워커 분리 구현 (ADR-0032, 플래그 OFF·미배포)** | 테오 "배포 시 알림 끊김 제거" 지시 → 배포마다 ~16분 인바운드 블랙아웃 근본해결. TDA 설계(ADR-0032)→BE 구현→QA PASS. **구조**: 같은 레포 서비스 2개 — web(세션 미보유)+**zalo-worker**(신규 상시, zca-js 세션 유일 보유·리스너·워치독·발송·QR). 인바운드 실시간=**PG LISTEN/NOTIFY**(Redis 불필요, `lib/realtime-notify.ts`), 발송=웹→워커 내부HTTP 위임(`lib/zalo-worker-client.ts`, ext/send union 재사용·시그니처 무변경), 신규 `worker/index.ts`(node:http `/internal/*`·`/healthz`). **★밴방지 듀얼모드**: `ZALO_SESSION_LOCAL`(기본 true=웹 세션 보유=현행)·`ZALO_WORKER_CONNECT`(기본 false) → **플래그 미설정 시 프로덕션 100% 현행 보존**(no-op), 두 프로세스 동시 접속(code 3000) 코드상 불가. `pg`·`tsx` 설치. BE-7 감사: ext/{nickname,group-members,mark-read}=순수DB(위임 불요). **스키마 무변경.** | QA 4대 불변식 PASS(현행보존·밴방지·누수0[NOTIFY payload=식별신호만]·내부인증 timingSafeEqual). typecheck0·build0·테스트 2366(신규 12). **PR #198**. 활성화=ADR §4 Railway `zalo-worker` 서비스 생성(테오/OPS)+§5 마이그레이션 절대순서(웹 세션 내림 확인 후 워커 접속). |
| 2026-07-09 | **실데이터 전환 — 테스트 시드 purge + 실운영 준비 점검(부서 4종 회의)** | 테오 "프로그램 최종 확인·내일부터 DB 모으기" → **부서 병렬 점검**: OPS(배포·cron 10개·코어env 정상, 블로커0)·QA(4대원칙 위반0·열린 P0/P1 코드결함0)·INTEG(Zalo 파이프라인 준비됨·P0=김태진 QR재로그인+운영자 zaloUserId 연결)·TDA(스키마 안정·FK 드리프트 1건). **실데이터 개시 블로커 0 판정**. 테오 결정: "정리 후 시작"+**데모계정 보존**. 실행: ①`migrations-manual/2026-07-09-fk-reconcile.sql` 적용(Villa_cleanerId_fkey ADD·Payment onDelete 재정렬, 고아0 검증) ②`prisma/purge-test-data.ts --execute`로 테스트 시드 **3,526행 삭제**(booking358·serviceOrder948·villa65·minibar1257 등, 자식→부모 트랜잭션) ③**전체 DB 스냅샷 백업 44,438행/20.78MB → backups/**(pg_dump는 서버PG18↔로컬17 불일치로 불가→Prisma JSON 논리백업 대체). **보존**: Zalo 대화529·메시지32,256·계정3, AppSetting24, 시즌4, 미니바카탈로그5, 서비스카탈로그13, 운영자4, 데모계정6(빌라관리자Tyy·랜드사아시아써니·벤더에이스마사지·파일럿·청소2). 검증: 삭제대상 전모델 0·보존값 불변·프로덕션 health(login200·/p404). | 데모계정은 데이터 비워진 빈 상태(시연 필요시 seed 재실행). purge/export 스크립트 미커밋(prisma/). **잔여 P0(테오)**: 김태진 Zalo QR 재로그인·운영자 zaloUserId 연결. 실데이터 입력 개시 가능. |
| 2026-07-09 | **Zalo 대화창 드래그앤드롭 첨부 (wt/zalo-chat-dnd)** | /messages Composer에 파일·사진 드래그앤드롭 첨부 추가. 기존 붙여넣기(Ctrl+V) 이미지 미리보기를 **첨부 대기열**(이미지+일반 파일, 최대 10개, 순차 전송)로 일반화 — 이미지=resizeImage→photo 경로, 비이미지=type=FILE 경로(AttachMenu와 동일). 창 전역 드래그 감지 오버레이(body 포털)·대화 전환 시 리셋(QA D1)·objectURL 해제(QA D2)·48h 닫힌 대화 드롭 무시. 붙여넣기도 비이미지 파일 지원 확장. i18n adminMessages.dragDrop ko/vi. | tsc0·build0·vitest(share 35·i18n 95) 통과. 드롭 UX는 데스크톱 브라우저 실사용 확인 필요. |
| 2026-07-08 | **DB 채우기 Scenario B 본실행 완료 + 이관 작업들 준비** | ✅**Scenario B 본실행 완료**(프로덕션 DB): 빌라 65개 · 예약 347개 신규(CHECKED_OUT:124 CONFIRMED:178 HOLD:32 NO_SHOW:8 CANCELLED:5) · 부가서비스 943건(TICKET 265·BBQ 87·BREAKFAST 136·GUIDE 50 등) · 거래처 10개 · 청소원 1명(Chị Hoa, 0907654321) + 태스크 6건 · 미니바 64세트 + 판매 126건·529라인(88620000₫). ⚠스크립트 수정 필요: seed-villa-bookings-random.ts 외래키 우회(CheckInRecord/CheckOutRecord/ServiceOrder/Payment/PartnerReceivable/BookingChangeRequest 선삭제) — 멱등성 보장. ✅환경변수 설정: STORAGE_* 5개(R2 저장소) + NIKE_* + ZALO_* Railway에서 로컬 .env 동기화. **다음: S3 Nike ETL 본실행(시간이 오래 걸림, 별도 진행 권장)·QA 통계·권한·누수 전수점검**. | ✅Scenario B 완료 — C(USD·미니바부족)는 선택사항. 본실행 직후 QA 진행 필요(대시보드·통계·권한). S3 ETL은 --limit 100부터 점진적 실행 권장. |
| 2026-07-07 | **DB 채우기 준비 — seed 스크립트 17개 분석·체크리스트 완성 (다음세션 ready)** | Untracked 스크립트 17개 전수 분석 완료. 필수 3개(빌라·예약)·보정 5개·거래처·부가판매 9개로 분류. 실행 시나리오 3가지 정의: **A(필수 8개, 30분)→운영기본**·B(표준, 1시간)·C(완전, 1.5시간). **docs/contracts/T-db-seed-plan.md** 최종 문서 작성: 명령어·백업·체크리스트·다음세션 액션 기록. 메모리(**db-seed-plan-2026-07-07.md**)·MEMORY.md 업데이트. **✅지금부터 DB 채우기 안전**: 스키마 v1.5 완성·Railway 정상·예상 2~3주→8월 1일 여유. | dry-run(로컬)→본실행(테오 확인)→QA검증 |
| 2026-07-07 | **/messages 모바일 키보드 첫 포커스 입력창 가림 수정 (wt/messages-kb-viewport)** | iOS에서 채팅 첫 진입 후 입력창 탭 시 키보드가 입력창을 가림(두 번째 포커스부터만 정상 — 사파리 자동 팬 불발). 원인=컨테이너 `100dvh` 고정(iOS는 키보드에 layout viewport 안 줄임). 수정=visualViewport 추적: 모바일(<lg)에서 키보드 열림(innerHeight−vv.height>150px) 감지 시 컨테이너 인라인 height=vv.height−헤더(3.5rem)+scrollTo(0,0) 팬 상쇄, 닫히면 원복. chat-pane은 vv resize 시 바닥 보던 중이면 바닥 유지. 데스크톱·vv 미지원 무영향. | tsc0·build0. iOS 실기기 검증은 배포 후 사용자 확인 필요(에뮬레이션 불가). |
| 2026-07-06 | **Zalo 리스너 헬스 워치독·경보 (wt/zalo-health-alert)** | "상대 채팅 늦게 도착" 조사 후속(원인: 배포 블랙아웃 + Jini 계정 재로그인 실패 2주 무감지). `lib/zalo-health.ts` 인프로세스 워치독(5분 주기, instrumentation 시작, Railway cron 불필요) — 활성 계정 풀 status 점검, **2회 연속(≈10분) 미연결 시 경보**(계정별 쿨다운 6h). 이중 채널: 인앱(운영자 전원+계정 소유자, href=/zalo-connect — Zalo 불능 시 폴백) + Zalo 큐(`ZALO_LISTENER_DOWN` enum additive·라이브 ALTER 적용). 판정 순수함수(nextHealthState) 분리. IDEAS: 리스너 전용 서비스 분리 등록. | tsc0·2352테스트(신규 9)·build0. 배포 후 Jini 계정 자동 감지→경보 라이브 확인 예정. |
| 2026-07-06 | **파트너 예약목록 서버 페이지네이션 + 상세 체크인·서비스요청 (T-partner-scale · wt/partner-scale)** | ① loadPartnerBookings(partnerId,{q,from,to,skip,take})→{rows,total} — 검색(게스트/빌라명/병기/단지 insensitive)·기간겹침(checkOut≥from·checkIn≤to)까지 서버 where 이관, findMany·count 동일 where 객체(페이지수 왜곡 불가), /partner URL 단일진실(ListSearch URL모드·date input router.replace·PaginationBar URL모드) — 전량로드+클라 slice 제거(PR #165 패턴). ② PartnerBookingDetail에 checkInAt·agreementSignedAt(CheckInRecord 시각만—여권/서류/OCR/서명URL 미select)+serviceOrders(type·status·일정·수량·guestNote만—costVnd·priceKrw/Vnd·selectedOptions·vendor* 미select), 상세 체크인 행+부가서비스 요청 카드(게스트메모 강조). 누수규칙 헤더 명문 갱신(ServiceOrder 비금액 메타=중개자 정당). | 독립 QA PASS — 라이브 실렌더 증거(실존 costVnd 300,000·vendorName 에이스마사지 → 파트너 HTML 0건)·IDOR 404·기간경계/insensitive/롤오버 라이브 검증·select 누수가드 테스트 고정. tsc0·build0·2345 그린(신규 10). ⚠데모 파트너 실비번=Test1234!(메모리 Tast1234! 오기 — QA bcrypt 대조). |
| 2026-07-03 | **파트너 정보 완결 3종 (T-partner-info · wt/partner-info)** | ① 제안서 포털 내 상세 — loadPartnerProposals items(빌라 병기명·기간·박수·제안통화 스냅샷가·booked 뱃지)+saleCurrency, 카드에 아이템 목록+공개 /p 딥링크(가예약은 기존 공개흐름 재사용·새 예약경로 없음). ② 청구서 표시번호 invoiceNo(invoiceDisplayNo leaf) 행 표시+검색 포함. ③ 채권별 입금 이력 — Payment(receivableId in 본인채권+partnerId 이중스코프·관계미정의 컬럼이라 별도쿼리) details 접이식(VN시각·용도·통화별 금액, note/환율 미select). | 독립 QA 조건부 PASS→권고 반영(Payment 스코프·note제외 테스트 2케이스 추가·USD 주석 정정). 누수0(아이템 select 전수·consumer* 부재 자동검증). tsc0·build0·2335 테스트 그린. |
| 2026-07-03 | **파트너 후속 폴리시 5종 (T-partner-polish · wt/partner-polish)** | PR #182·#188 백로그 소진. **1** HOLD 계좌안내 선금 반영 — holdDepositVnd(computeDepositDue 서버계산·비율 비노출·금액만), 선금(강조)+총액 2행+bankDepositHint(VND 계좌는 테오 등록완료로 라이브 활성). **2** 취소 시 대기요청 자동 종결 — autoClosePendingRequestsOnCancel(PENDING→REJECTED·사유 ko/vi 병기), cancelBooking excludePendingRequestId(승인경로 자기요청 제외 — resolve PENDING 가드 충돌 방지), AuditLog pendingRequestsAutoClosed. **3** 취소예약 미종결 채권 경고 배지(관리자 파트너상세 — bookingCancelled 플래그·청구서 조정 유도). **4** 채권 행 연체 N일(overdueDaysFor 순수함수 — stats daysPast와 동일 산식·당일=0). **5** 연장 배지 수 파트너 필터(_count where partnerId — 상세 toLinked와 일관). | 독립 QA PASS(누수0 — depositRatePct 비노출 봉인·경합/순서/i18n 정위치 확인). LOW 2 수용(승인경로 비원자성=기존·직접취소 시 CANCEL요청 REJECTED 라벨 의미론). tsc0·build0·2327 테스트 그린(신규 5). |
| 2026-07-03 | **Zalo 채팅: 이미지 붙여넣기 + 사진 캡션 번역 (wt/zalo-paste-caption)** | 테오 요청 2건. ① 입력창 붙여넣기(Ctrl+V): 클립보드 이미지 → 미리보기 카드(오전송 방지 2단계) → 기존 사진 파이프라인(resizeImage→share 라우트)으로 발송. ② 사진 캡션 번역: `ZaloMessage.captionTranslated`(additive, 라이브 raw ALTER 적용) — OCR 번역(translatedText, on-demand 유지)과 분리해 같은 사진에 둘 다 가능. 수신 자동번역을 photo 캡션까지 확장(maybeTranslateInbound targetField 파라미터)·on-demand /translate 라우트 photo 허용(captionTranslated 저장·멱등)·PhotoCard 캡션 아래 번역 표시+수동 번역 버튼(과거 수신분 채움). i18n pasteImage ko/vi. | tsc0·2309테스트(신규 2)·build0. 독립 QA: D1(대화 전환 시 붙여넣기 미리보기 잔존→타 대화 오전송 위험)·D2(objectURL 잔존) 수정 후 PASS. ⚠공유 node_modules generate 레이스 — 병합으로 해소. |
| 2026-07-03 | **파트너 운영 관리자측 보강 4종 (T-partner-admin-ops · wt/partner-admin-ops)** | 관리자 관점 감사(에이전트2+OWNER 실사용 승인루프 검증) 후속. **①[P0] 취소 시 좀비채권 해소**: cancelBooking이 채권 미정리→취소예약 미수 잔존·연체 오판·여신 차단 오류. writeOffReceivableOnCancel(lib/partner-booking) — 미청구(invoiceId null)+미종결 채권 WRITTEN_OFF(기존 종결상태 재사용·미수/연체/청구후보/여신 전부 제외 확인), 기입금 기록보존(환불/이월 수동), 청구서 묶임은 INVOICED_LEFT 미접촉+AuditLog 기록, 가드 updateMany 경합안전. + 취소 파트너 알림(BOOKING_CANCELLED, 사유 미포함). **②** /bookings 행 대기요청 배지(_count PENDING)·대시보드 파트너요청 배너(오래된순 3건 바로가기)+연체 파트너 배너·활동피드 PARTNER/GUEST_PAYMENT_NOTICE·PARTNER_CHANGE_REQUEST 매핑(amber 강조). **③** modifyBooking 커밋후 파트너 BOOKING_MODIFIED(새 일정+채권 총액, 옛금액 송금사고 방지). **④** 청구서 발행 자동알림 안내캡션(수동 Zalo=PDF 재발송용)·파트너목록 Zalo 연결뱃지·연체 배너. | 독립 QA: MEDIUM 1건(i18n 키 err 오삽입) 수정 후 PASS 조건 충족. LOW 2건 수용기록(CANCEL승인 이중알림=의도·게스트만 변경시에도 변경알림). tsc0·build0·2314 테스트 그린(신규 7). ⚠공유 node_modules에서 타세션 generate 레이스 지속(VENDOR_PROPOSAL_RESULT) — 병합으로 해소. |
| 2026-07-03 | **관리자 청소 검수 연동 — 사진 페어링 슬롯 매칭 (PR #181, wt/admin-cleaning-check)** | 청소원 선택 슬롯 스킵 허용(PR #178)으로 생긴 검수 오배치 결함 수정. **CleaningTask.photoSlots**(String[], additive — 라이브 raw ALTER 적용): photoUrls와 병렬 슬롯 id. 제출 경로(클라·API zod 길이검증·lib 이중방어) 저장 → 관리자 검수 뷰 인덱스 페어링을 **슬롯 id 매칭**으로 전환(`lib/cleaning-photo-pairs.ts` 순수모듈·테스트 8, 레거시 빈배열은 종전 인덱스 폴백)·청소원 읽기전용 라벨도 슬롯 매칭. 부수: 침실/욕실 기준 라벨 '1'→'침실 1', 재배정 select aria-label. 연동 점검: 승인 장수 무관·재배정 API·누수(select 화이트리스트 불변) 확인 | tsc0·**2276 테스트**·build0·독립 QA PASS. **라이브 E2E 검증**: 청소원 API로 발코니 스킵 7장 제출(photoSlots 저장확인) → 관리자 검수 SSR에서 욕실2 기준↔bathroom-2 제출 정확 페어·베란다 기준행 제출없음·기준 없는 침실1/2·욕실1 단독행 라벨 표시 → 승인 gateOpened:true → 청소원 읽기전용 vi 라벨 7장 전부 표시. |
| 2026-07-03 | **소비자 E2E 버그 6건 수정 (T-consumer-bugs, wt/consumer-bugs)** | 2026-07-03 소비자 전수점검 발견분 일괄 해소. **#1 정원초과 가예약**: createHoldFromProposalItem에 guestCount>villa.maxGuests → OVER_CAPACITY 거부(단일 소스), /p hold 라우트 over_capacity 409, 폼 인원 셀렉트 1~정원 상한+초과 인라인 에러(5언어 errOverCapacity). **#2 직판 뷰 분단**: /api/p service-orders 저장 시 booking.channel=DIRECT → requestedVia=GUEST(소비자 본인) — /g 신청내역·정산 미리보기·셀프취소 즉시 포함, /p done 뷰는 DIRECT면 GUEST 포함 조회(loadPartnerAddon includeGuestOrders). 파트너 채널 기존 유지. **#3** 게스트 주문 CANCELLED 라벨 "취소됨"(5언어) — statusOther 폴백 제거. **#4** 관리자 체크인이 GuestCheckinToken.passportPhotoUrls 프리필(서명 채택과 동일 조건, base64 없음→OCR은 재촬영 시) — 재업로드 강제 제거. **#5** 무보증금+미니바 소비 체크아웃 잠김 해소 — canDeductMinibarOnly의 depositVnd!=null 요구 제거(서버 resolveDepositOutcome은 NONE 유지·청구근거 기록으로 이미 지원). **#6** 동의서 조항 가짜 제목(content 24자 절단) 제거 — 번호+전문 1회. | tsc0·2335테스트(신규 2: 정원 거부·경계값)·build0. 스키마 무변경. |
| 2026-07-03 | **관리자 운영 갭 A1~A5 — 소비자 신호 배선 (T-admin-ops-gaps, wt/admin-ops-gaps)** | 소비자 E2E·관리자 운영점검(2026-07-03)에서 확정된 갭 5건 해소. **A1 소비자 신호 알림**: 게스트 입금통보(/api/p/[token]/payment-notice)·게스트/파트너 부가서비스 요청(/api/{g,p}/[token]/service-orders) → 운영자 전원 Zalo 알림(`GUEST_PAYMENT_NOTICE`·`SERVICE_ORDER_REQUESTED` enum 2값, exhaustive switch case, best-effort) + 대시보드 "소비자 확인 필요" 배너(입금통보 대기=HOLD×감사로그 교차·요청 대기·취소 미환불, >0일 때만). **A2 HOLD 가시성**: /bookings 기본(월 겹침) 스코프에서 활성 HOLD 항상 포함(OR, q검색 OR 충돌 방지 AND 래핑) + 하단 KPI "미확정 가예약" 카드를 ?status=hold 링크로. **A3 요청 통합 큐**: /service-orders/requests 신설(전 예약 REQUESTED 횡단, 예약상세 진입, 서버 페이지네이션, 원가 미노출). **A4 환불 추적**: CANCELLED+수납>0 예약상세 환불필요 배너 + 대시보드 카운트. **A5 취소 연쇄**: cancelBooking 트랜잭션에서 미종결(REQUESTED·CONFIRMED) 주문 일괄 CANCELLED + 살아있는 PO는 벤더 Zalo+인앱 취소통보(기존 PATCH 경로와 동일 규칙), 종결 예약의 주문 확정·추가 서버 409 가드 2곳. | tsc0·2311테스트(신규 4)·build0. 라이브 enum ALTER 2값 적용 필요(additive). ⚠공유 node_modules generate 레이스 2회 겪음(재생성으로 해소). |
| 2026-07-03 | **공급자 행동의 관리자 가시성 보강 (T-admin-supplier-visibility, wt/admin-supplier-visibility)** | 관리자 측 공급자 연동 점검(코드감사 3방향+프로덕션 실측 19화면 200)에서 확정된 갭 해소. ① 빌라 승인 무통지: 신규 등록(POST /api/villas 공급자만)·반려 재제출(PUT) → 운영자 전원 Zalo 알림(`VILLA_PENDING_REVIEW`, resubmitted 분기) + 대시보드 승인 대기 배너(>0일 때만, /villas?status=pending). ② 승인(ACTIVE) 후 공급자 사진/비품/규칙 수정 → `VILLA_CONTENT_UPDATED` 알림(빌라×kind PENDING dedup 스팸방지·운영자 자기수정 제외·정렬 PATCH 제외). ③ 매출 거래목록 ROOM 쿼리 seller=OPERATOR 필터 — 공급자 직접판매(미니바 회수 방식·회사 매출 0, 테오 확인)의 0원 행 오염 제거, 미니바 라인 비접촉. enum 2값 라이브 ALTER 적용·zalo exhaustive switch case 추가·i18n ko/vi. | tsc0·lint0·2285테스트(신규 9)·build0. 독립 QA PASS(라이브 E2E: 배너·pending 탭·info PATCH·/revenue·CSV). ⚠운영자 3명 전원 zaloUserId 미연결 — Zalo 알림 수신 0명(no-op), 운영자 Zalo 연결 필요(OPS). |
| 2026-07-03 | **파트너(여행사/랜드사) 업무완결성 5종 (T-partner-workflow-gaps · wt/partner-workflow)** | 파트너 관점 전수감사(코드3+프로덕션 실사용) 후 우선순위 ①→⑤ 구현. **① 파트너 알림**: lib/partner-notify(인앱 InAppNotification+Zalo contactZaloUid 이중채널, country VN→vi 그외 ko, 커밋후 발송·절대 무throw) — 예약확정(confirmHold)·홀드만료(expireHolds)·청구서발행(issue)·연체전이(cron 스냅샷 방식 멱등) 4이벤트 + 파트너 알림센터 벨(/api/partner/notifications GET·read, vendor 미러, 45s폴링). **② 취소·변경·홀드연장 요청(운영자 승인형)**: BookingChangeRequest 테이블(additive raw SQL 라이브적용, kind/status String — enum 금지) + POST /api/partner/bookings/[id]/change-request(본인 partnerId IDOR·상태가드·PENDING 1건 409·CSRF·rate-limit·운영자 Zalo통지) + PATCH /api/booking-change-requests/[id](isOperator, CANCEL승인=cancelBooking 실행, HOLD_EXTEND승인=holdExpiresAt max(now,기존)+1~72h·status:HOLD 가드, 처리결과 파트너 통지) + 파트너 패널·관리자 예약상세 패널. **③** HOLD 만료시각(임박 6h 강조)+미납잔액+입금계좌(getPublicBankInfo 동일소스) 파트너 상세 노출. **④** 채권 행 입금통보 버튼 배선(기존 receivableId API). **⑤** 연장(parentBookingId) 배지·부모/자식 상호링크·묶음합계(취소/만료 자식 제외)·채권 연장배지. invoiceDisplayNo leaf 이동(lib/partner-invoice — pdf tsx 의존 차단). | 독립 QA PASS(누수0·IDOR0·경합가드 확인, MED1 기록·LOW4 백로그). tsc0·lint0·build0·전체 2286테스트 그린(신규 18). i18n ko/vi 패리티 0/0. 라이브 DB 테이블 선적용 완료. |
| 2026-07-03 | **공급자 포털 UX 단절 2건 연결 (T-supplier-ux-links, wt/supplier-ux-links)** | 공급자 관점 전 화면 점검(코드감사 3종+프로덕션 21화면 실측 200·누수0)에서 발견된 흐름 단절 해소. ① 캘린더 예약 바텀시트→검수 진입: 서버가 seller·status로 `inspectAction`(checkin/checkout/null)만 파생 전달(raw seller 비직렬화·누수0), 직접예약 CONFIRMED→체크인·CHECKED_IN→체크아웃 버튼, 운영자 예약 미노출(검수페이지 가드와 조건 동일). ② 청소 상세 빌라명→`/my-villas/[id]` 역링크(SUPPLIER만, CLEANER는 텍스트 유지). ko/vi `calendar.bookingSheet.goCheckin/goCheckout` 추가. | tsc0·lint0·2268테스트 그린·build0. 독립 QA PASS(결함0) — 과거날짜 CONFIRMED 셀 버튼 노출은 늦은 체크인 시나리오로 의도 판정. |
| 2026-07-02 | **예약변경 UX·분할숙박·게스트금액·공실보드 (ADR-0030 후속 · PR #164·#166·#170·#171·#173)** | 세션에서 연속 머지·배포. **#164 보안 red정리**: 병합 main의 red 4건(회귀 아님) 정정 — vendor/notifications/read raw auth()→requireCapability(중앙가드 P1-S8), passkey/login/options 공개 allowlist 등재, users ASSIGNABLE_ROLES→CREATABLE/CHANGEABLE 상수명 추적(OWNER/ADMIN 제외 유지). **#166 변경 UX**: 가용 빌라만 셀렉터(available-villas 엔드포인트·findSellableVillaIds excludeBookingId)·자동 미리보기(디바운스·버튼제거)·저장 확인 모달. **#170 분할숙박 통합**: 별도 BookingExtendPanel 제거→예약변경에서 같은빌라 연장 SOLD_OUT시 인라인 '다른빌라로 이어서'(purpose=extend)·연결예약은 읽기전용목록 + 게스트 `/g/[token]` 숙박요금 표시(직접게스트만·파트너/공급자 비노출 원칙2·guest-i18n 5개언어·운영자 URL복사 카톡수동). **#171 공실보드 전체예약**: getAvailabilityBoard DIRECT제한 제거→모든 빌라의 공급자직접(SUPPLIER)+우리(OPERATOR) 예약 BOOKING셀 표시(관리자전용 원칙1·seller 마커/팝오버·판매가 canViewFinance게이트). **#173 빌라 셀렉터**: 공실보드에 지역 옆 빌라 셀렉터(선택지역의 빌라·villaId 필터·지역변경시 초기화). | 각 PR tsc0·build0·parity0, 최종 전체 2254 테스트 그린. 라이브 배포 SUCCESS(32e4bab). 앞선 에픽 본편은 [[post-checkin-booking-modification-epic]] PR #158·#162. |
| 2026-07-01 | **체크인 후 예약 변경 에픽 Sprint 1(ADR-0030, wt/post-checkin-modify)** | 테오 "전체 개발 진행". 확정 D0 체크인후 인원변경 불허·D1 분할숙박=연결예약(parentBookingId)·D2 감액없음(최초액 하한)·D3 파트너 청구서 추가라인. **T-A 정원검증**: `checkAvailability`/`evaluateAvailability`에 maxGuests·guestCount(OVER_CAPACITY, available 무영향·sellable만 차단·하위호환)·`findSellableVillaIds` 정원필터·modify 게이트(기존엔 정원검증 코드 전무). **T-C 상태별 하한**: `resolveModifiedTotals` 순수함수 — CHECKED_IN=max(기존,재견적)(연장 추가청구·단축 무환불)·CONFIRMED=전체재견적, nights는 실제구간. **T-B 미리보기**: `previewBookingModify`+`POST /api/bookings/[id]/modify/preview` dry-run(쓰기없음·동일코어)·패널 '변경 미리보기' 박스(추가청구/새총액/정원/공실/과수납)·canViewFinance 게이트. **T-D 과수납**: modify가 기수납(VND환산)>새총액이면 overpayment 플래그+AuditLog(하드차단X)·미리보기 경고. | **Sprint1 tsc0·테스트68·lint0**. 커밋 40e86e2·83a533e·7b831f1. PR #158. |
| 2026-07-01 | **↑ 에픽 Sprint 2 완료 (T-E·T-F·T-G) — 다른 세션 완료 후 이어서** | main 병합(패스키·역할·벤더 유입) 후 진행. **T-E 분할숙박(D1)**: `Booking.parentBookingId` self-relation 추가 → **라이브 Railway DB additive ALTER 적용·검증 완료**(prisma db execute, prisma/migrations-manual/2026-07-01-booking-parent-extension.sql). `createLinkedExtensionBooking`(원 빌라 불가 시 대체 빌라 새 예약+parentBookingId 연결, 공실+정원+isSellable, 전체견적, 부모 상속, BOOKING_CONFIRMED 알림 마진비포함)+`POST /api/bookings/[id]/extend`(isOperator·재무게이트)+관리자 상세 BookingExtendPanel(연결예약목록+연장폼, ko/vi). **T-F 파트너 채권(D3)**: 자식도 ensureReceivableForBooking로 자기 채권 생성 → 마감 청구서가 파트너·기간별 묶어 자동 추가라인(부모 무변경·취소재예약 아님·신용 하드게이트 없음·VND전용·멱등). **T-G**: 자식은 독립예약이라 체크인·아웃·청소·정산·게스트토큰·공급자알림 기존 파이프라인 자동, 자식 상세에 원예약 역링크(detail.extendedFrom). | **tsc0·i18n parity0·next build0·에픽 테스트 80**(booking-extend 10 신규 포함). 커밋 T-E·T-F·T-G. ⚠**병합해온 main에 4개 테스트 red(내 것 아님)**: users ASSIGNABLE_ROLES 2개(role-change 세션)·vendor/notifications/read 가드 화이트리스트 2개(vendor 세션) — 그 도메인 세션이 조정 필요. 내 새 라우트(extend·preview)는 가드 스캔 통과. ⚠라이브 DB 컬럼 이미 반영됨(배포 시 재적용 멱등). |
| 2026-06-30 | **공급자 빌라 리스트 → 관리자식 컴팩트 행(wt/supplier-villa-list)** | 테오 요청: 공급자 /my-villas 리스트를 관리자 /villas 처럼 변경. 큰 사진 카드(빌라당 h-56 전체폭, 화면당 1~2개)→**관리자 컴팩트 `<details>` 접기/펴기 행**(좌측 64~80px 썸네일+단지명uppercase+빌라명+메타 아이콘(침실/수영장/조식)+우측 상태배지+chevron, 화면당 5~6개). 펼침에 상세보기·청소보기·재제출 버튼. **테마는 라이트 유지**(공급자 원칙4, 흰 카드+teal). 공급자명 줄 없음(자기 빌라만). select에 complex/hasPool/breakfastAvailable 추가(요율·마진 여전히 미조회=누수0). myVillas i18n 키 2개 추가(detail·cleaningPending, ko/vi). **★잠복버그 수정**: BADGE_CLASS가 `bg-success`/`bg-error` 사용했는데 이 프로젝트 Tailwind엔 success/error 토큰 미정의(primary·secondary만)→투명 렌더. 과거 큰-카드 시절엔 배지가 사진 위라 안 들켰으나 흰 카드선 "판매중" 배지가 안 보임. emerald/rose/amber 표준 팔레트로 교체. | **Playwright 실검증**: 공급자(파일럿 중계인 0900000000) 로그인→/my-villas 16건 컴팩트 표시·"판매중" 초록배지·"중단됨" 회색배지·펼침 "상세 보기" 버튼 확인(390px). typecheck0·lint0(기존 경고만)·build0. ⚠데모 데이터에 notSellable/rejected/pending 빌라 없어 그 배지·청소/재제출 버튼은 라이브 미확인(코드·동일 팔레트라 렌더 보장). ⚠검증 위해 데모 공급자 비번을 문서 기본값 villa-pms-supplier-dev로 재설정함. ⚠Playwright MCP 브라우저는 localhost 미도달→LAN IP(192.168.1.197:3083)로 접속. 후속=독립 QA. |
| 2026-06-26 | **3기능 — 매출관리 거래목록·미니바 소모 자동계산·예약 변경(PR #74)** | 테오 요청 3건, 격리 worktree wt/rev-minibar-bookmod. **F1 매출관리(/revenue)**: 객실료·미니바·부가서비스를 건별 RevenueTxn 통합(lib/revenue-ledger.ts, 통화분리 ADR-0003·BigInt), 필터(기간·유형·채널·빌라·파트너·통화)·검색·정렬·CSV 내보내기(/api/revenue/export), 사이드바 메뉴. 페이지+API+메뉴 3중 canViewFinance 게이트(원칙2). **F2 미니바**: 체크아웃에서 "남은 수량"만 입력→소비량=par−남은수량 서버 재계산·차감(computeConsumptionFromRemaining 순수함수·clamp·음수거부, par 서버 재조회), 기존 CONSUME 원장·게스트청구 무변경. **F3 예약변경(PATCH /api/bookings/[id]/modify)**: 날짜·빌라·인원·투숙객·조식 변경(HOLD·CONFIRMED 전체·CHECKED_IN은 체크아웃일만), 자기예약 제외 가용성 재검사·금액 재계산(환율 스냅샷 유지)·채권 존재 시 빌라/금액 변경 거부·AuditLog·BOOKING_MODIFIED Zalo알림(판매가/마진 비포함)·동시성 updateMany+status. NotificationType.BOOKING_MODIFIED enum 라이브DB additive ALTER 적용. | **독립 QA PASS**: 누수0(F1 STAFF 차단·F3 알림/응답 마진 비노출), F2·F3 PASS. **F1 ×수량 이중계산 버그 QA 적발→수정**(ServiceOrder priceVnd/costVnd는 이미 라인합계, statistics.ts와 일치하게 ×수량 제거). zalo.ts exhaustive switch에 BOOKING_MODIFIED case 추가(빌드 복구). typecheck0·vitest 2008·next build OK. 후속(권장): statistics↔revenue 부가서비스 매출 동일성 회귀테스트, 채권 날짜변경 시 dueDate 재산정(범위밖), Railway 배포·프로덕션 검증. |
| 2026-06-26 | **OPS — 미등록 Railway cron 4개 등록·검증 완료** | 조사로 미등록 cron 4개 확인(notifications·partner-overdue·roster-reminder·periodic-cleaning). Railway CLI는 cronSchedule 설정 불가 → 테오가 대시보드에서 등록(클로드 단계 안내). 패턴: `curlimages/curl` 미니서비스 + `sh -c 'curl ... /api/cron/<name>'` + Cron Schedule + `CRON_SECRET=${{villa-pms.CRON_SECRET}}`. 스케줄: notifications `*/5`·partner-overdue `0 0 * * *`·roster-reminder `0 1 * * *`·periodic-cleaning `0 2 1 * *`(UTC). 함정: 빈 서비스의 Docker 이미지는 §Source **Connect Image**로(Root Directory 칸 아님), 2번째부터 Duplicate가 최단. 런북 docs/ops/cron-registration.md + deployment-pattern 스킬 현행화. | **검증**: 4개 모두 Run now 성공·`cron-notifications`는 5분 자동 실행 3회 연속 성공 확인. 현재 가동 cron 6개. 4개 엔드포인트 무인증 401 확인. **잔여(테오 확인)**: 캔버스의 `cron-expire-holds Copy`(Next in 10h)는 정체불명 잔여 서비스로 추정 — 불필요 시 삭제 권장(expire-holds 중복 호출 가능, 멱등이라 무해하나 정리 필요). [[guest-roster-status]]·[[partner-b2b-receivables-plan]]의 'OPS cron 등록' 잔여 해소. |
| 2026-06-26 | **3기능 — 메시지 실시간(SSE)·비번 자가재설정·빌라 지도 임베드(ADR-0024/0025/0026, QA PASS)** | 테오 결정 3건(비번=Zalo코드·지도=embed-only·노출=공개제안/공급자/운영자) 기획→계약서3+ADR3. **①SSE**: `lib/realtime-bus.ts`(globalThis EventEmitter, 채널=ownerAdminId)+`/api/zalo/stream`(auth·하트비트15s·abort cleanup), 발행=saveInboundMessage(수신)·POST messages(발신) try/catch격리, `messages-client.tsx` 폴링→EventSource(미지원·onerror 3회 시 5초폴링 폴백·가시성 재연결). **payload는 신호{type,conversationId}만**(본문·마진 미포함, 실데이터는 기존 스코프API 재페치)→누수표면 단일화. **②비번재설정**: 신규 `PasswordResetToken`(codeHash bcrypt·attempts·expiresAt, **라이브 Neon raw SQL CREATE TABLE 적용·검증완료**, db push 금지)+`lib/password-reset.ts`+forgot/reset API(열거방지 동일200**+상수시간 더미bcrypt**·rate-limit·attempts≥5폐기·만료·재사용거부)+`/forgot-password`·`/reset-password`(teal 라이트)+middleware public+login 찾기링크+auth.reset i18n(ko/vi). Zalo미연결=관리자문의 안내. **③지도**: `components/villa/map-embed.tsx` `toEmbedUrl`(호스트화이트리스트 google만·https만·사칭/short URL/javascript: 차단=SSRF방어)+3화면(공개/공급자/운영자, 각자 범위빌라만→재고비공개)+i18n. | **독립 QA PASS**: 누수0(SSE 스코프·비번 평문·SSRF·마진/재고 게이트 전부 통과). 결함 D1~D4 처리(D1 generate·D3 주석 수정, **D2 타이밍상수화·D4 verifyResetCode 음성테스트 보강**). **typecheck0·next build 성공·전체 1845 테스트 그린**. 공유메인 작업이라 명시적 pathspec 커밋(타세션 F10/PARTNER WIP·docs/shots 등 비접촉). **미푸시**: 배포 시 ①`prisma generate`(stale 주의 [[stale-deps-look-like-schema-drift]])②라이브DB PasswordResetToken 적용확인(완료)③SSE는 Railway 단일컨테이너 가정(스케일아웃 시 LISTEN/NOTIFY 교체). 후속(별도): 이메일/SMS 재설정채널·지도 다중핀보드(좌표필드). |
| 2026-06-26 | **F10 공급자 직접 판매 채널 Phase A 완료 → PR #48 (다음=Phase B)** | Phase A 전부 구현·**독립 QA PASS·결함0**. T10.0 디자인3종 · T10.1 스키마(라이브DB·124건 백필) · T10.2 직접예약 API+캘린더폼(QA PASS) · T10.2b 다박 스테퍼(1~30박) · T10.3 운영자 타임라인·목록 가시성 · T10.4 정산 seller=OPERATOR 제외 · T10.5 공급자 vi 체크인·아웃+미니바 정산(D6). 누수0(여권 비공개 우회0·마진/재고/costVnd 0). 최신 origin/main 병합(F10+PARTNER+F11 공존). **typecheck0·1806테스트·build성공(76p)**. | **PR #48 MERGEABLE/CLEAN**(github.com/orangetaeo/villa-pms/pull/48) — 테오 머지 대기. **다음 세션 Phase B(T10.6 공급자판매가·T10.7 공개 판매링크)**: 머지 확인 후 **새 워크트리**(wt-new -Name f10-phase-b, 머지된 main 기준) → 킥오프 문서 **docs/contracts/T10-phase-b-handoff.md** 읽고 정식 계약서 선점. 함정: 공유 정션 prisma generate 레이스(빌드 직전 generate+enum확인). |
| 2026-06-26 | **F10 공급자 직접 판매 채널 — ADR-0021 Accepted, Phase A 착수** | 테오 지적: 원래 양방향 모델(공급자가 자기 고객에 직접판매→공실 실시간 공유→우리 선점 재판매)인데 현재 공급자=정보입력만(판매 불가)인 단방향. 신규 직접 판매 채널 기획·확정. **테오 결정 D1~D5**: 둘 다(수동기록→판매링크)·**선착순**(우선권 로직 불필요·기존 availability 트랜잭션 재사용)·**공급자 100%**(정산 `seller=OPERATOR` 필터로 제외)·검수 미승인 빌라 직접판매 허용·**직접예약 게스트도 정식 F4 검수**(공급자가 vi 모바일로 직접 수행, lib/checkin·checkout 재사용). 핵심: `Booking.seller`(OPERATOR\|SUPPLIER)로 공유 가용성에 두 판매 공존, 단어 DIRECT 재사용 금지. | 산출물: **ADR-0021(Accepted)** + SPEC F10 + TASKS Sprint5(T10.0~T10.7, Phase A=T10.0~T10.5) + 계약서 T10-supplier-direct-phase-a.md. **격리 브랜치 wt/f10-supplier-direct에서 커밋**(동시 PARTNER 세션과 공유 메인 HEAD 충돌 회피). 다음=T10.0 Stitch 디자인. **병렬 주의: PARTNER-1도 Booking ALTER 예정 → 스키마 순차 적용**. |
| 2026-06-26 | **PARTNER-3b-2 — 청구서 API + 이중입금 가드(ADR-0022)** | **POST/GET /api/partners/[id]/invoices**(생성=$transaction generateInvoiceForPeriod·NO_RECEIVABLES 422·PERIOD_EXISTS 409·partner 없음 404 / 목록), **PATCH /api/partner-invoices/[id]**(action issue|void, NOT_FOUND 404·INVALID_STATUS 409), **POST /api/partner-invoices/[id]/payments**(수납 $transaction recordInvoicePayment). 전부 canViewFinance 게이트·zod·AuditLog(entity PartnerInvoice)·serializeBigInt. **QA Minor 가드 반영**: bookings/[id]/payments에서 청구서에 묶인(invoiceId≠null) 채권엔 직접 DEPOSIT/BALANCE 입금 차단(RECEIVABLE_INVOICED 409, 이중인식 방지). 다중 write는 전부 $transaction 래핑. 테스트 +17(누수 401/403·에러매핑·직렬화). | typecheck0·전체 **1723**·build. 누수: 청구서 라우트 4종 전부 canViewFinance(STAFF·SUPPLIER 403 실증). 브랜치 wt-partner-3b2. **잔여 3b**: 청구서 UI(/partners/[id] 탭)·PDF(vi)·Zalo 발송. |
| 2026-06-26 | **PARTNER-3b-1 — 마감 청구서 서비스 계층(ADR-0022)** | 테오 결정: **잔금만 청구(선금 차감)** + **마감일까지 미청구 채권 누적**. **lib/partner-invoice.ts**: receivableBalance(총액−선금−잔금입금, 음수0)·computeInvoiceTotal(잔금 합)·invoiceStatusAfterPayment(완납PAID/일부PARTIAL/0ISSUED)·generateInvoiceForPeriod(invoiceId null+billable+dueDate≤periodEnd+잔금>0 누적 묶음, totalVnd=잔금합, dueDate=마감+termDays, @@unique 중복→PERIOD_EXISTS, 0건→NO_RECEIVABLES, 채권 updateMany 연결)·issueInvoice(DRAFT→ISSUED)·recordInvoicePayment(paidVnd 누적·상태 재계산·완납 시 묶인 채권 PAID·잔금0 정산)·voidInvoice(채권 연결해제로 재청구, PAID 불가). InvoiceError(reason 4종). 테스트 16(순수3·생성3·발행2·수납3·무효2 + 경계). | typecheck0·전체 **1706**·build 통과. 누수 0(순수 서비스, route/UI 0 — PARTNER-2 패턴처럼 BE 선행). 브랜치 wt-partner-3b. **잔여 3b**: 청구서 API·UI(/partners/[id] 탭)·PDF(vi)·Zalo 발송. |
| 2026-06-26 | **PARTNER-3a — 미수/여신 대시보드 + 연체 cron(ADR-0022)** | **/receivables**(순수 RSC, canViewFinance): KPI 4종(총 미수·연체 미수·연체 파트너 수·한도초과 파트너 수)+Aging 합산(0-7/8-15/16-30/30+)+파트너별 미수 표(연체 우선·미수액 내림차순 정렬, 연체·한도초과 경보 배지, /partners/[id] 링크). **lib/partner-server**: summarizeReceivables(순수 — 미수>0 필터·정렬·합산·연체/한도초과 카운트)·isOverLimit(등급B/C 미수>한도, A는 false)·getReceivablesOverview. **연체 cron**: lib/partner-booking.markOverdueReceivables(PENDING/PARTIAL+dueDate<오늘UTC→OVERDUE updateMany, 멱등·PAID/WRITTEN_OFF 제외) + /api/cron/partner-overdue(CRON_SECRET Bearer, expire-holds 패턴). 사이드바 "미수/여신" 메뉴(cap=canViewFinance)+adminReceivables i18n ko/vi. 테스트 +7(summarize·overLimit·overdue updateMany). 순수 RSC라 layout 네임스페이스 불필요. | typecheck0·전체 **1658**·build·i18n parity 통과. 누수: /receivables·select 전부 canViewFinance, 순수함수+RSC(클라 컴포넌트 0). **OPS 잔여**: Railway cron `partner-overdue` 1일1회 등록(미등록 시 OVERDUE 전이 안 돼 게이트·대시보드 부정확). **잔여 3b**: PartnerInvoice 마감청구서(주/15/30일)·PDF(vi)·Zalo. 브랜치 wt-partner-3(미PR). |
| 2026-06-26 | **PARTNER-1/2/2b prod 배포 + PARTNER-2c UI 배선(ADR-0022)** | **배포**: PR #32(PARTNER-1+2+2b) main 머지(af9de7e)·Railway 배포 완료. raw SQL은 공개 프록시(thomas.proxy.rlwy.net:36609, DATABASE_PUBLIC_URL — railway run --service Postgres로 비밀번호 비노출 적용)로 prod 반영·검증(테이블3·enum6·Booking.partnerId·Payment 4컬럼). 프로브: /api/partners 401·/partners 307·/dashboard 307(무회귀). **2c**: 예약 상세 우측에 `PartnerAssignCard`(app/(admin)/bookings/[id]) — 여행사/랜드사 채널+canViewFinance만 렌더, 파트너 드롭다운(채널 유형 일치 후보, /api/partners 페치)→PUT /bookings/[id]/partner, 채권 생성 후 변경 잠금(API 409 정합), 미수 상태·잔액 표시, KRW면 vndOnly 안내. booking select에 partner/receivable(재무 전용) 추가. adminPartners.assign i18n ko/vi. | typecheck0·전체 **1640**·build·i18n parity 통과. 브랜치 wt-partner-2c(미PR). **주의**: 체크아웃한 main에서 stale Prisma 클라이언트가 ServiceType FRUIT(타 세션 ADR-0023) 미반영 → npx prisma generate로 해소([[stale-deps-look-like-schema-drift]]). **잔여 2c 폴리시**: 입금 폼 purpose 선택·확정 PARTNER_CREDIT_BLOCKED 토스트. 다음=PARTNER-3. |
| 2026-06-26 | **PARTNER-2b — 여신 게이트 엔진(ADR-0022, 미수 방지 핵심)** | **lib/partner-booking.ts**(순수+tx): evaluateConfirmCredit(예약 확정 신용 게이트, 파트너 미연결=skip)·ensureReceivableForBooking(멱등 채권 생성, 선금30%·기한 등급A=체크인일/B=마감+termDays)·applyPaymentToReceivable(선금/잔금 누적+PAID/PARTIAL/PENDING 재계산)·partnerBalanceBlocksCheckIn(등급A 잔금 미납 차단)·creditPortionVnd. **통합**: ①confirmHold에 신용 게이트(한도초과·연체·BLOCKED/SUSPENDED→HoldRejectedError PARTNER_CREDIT_BLOCKED 409)+확정 시 채권 자동생성 ②payments route Payment.purpose(GUEST 기본/DEPOSIT/BALANCE)+DEPOSIT/BALANCE는 채권 입금 반영·partnerId/receivableId 링크 ③completeCheckIn 등급A 잔금 미납 차단(CheckInRejectedError PARTNER_BALANCE_UNPAID 409) ④PUT /api/bookings/[id]/partner 파트너 지정(canViewFinance·확정후 지정 시 채권 생성·채권 존재 시 변경 409). 파트너 미연결 예약은 전부 무영향(하위호환). 테스트 +20(게이트·채권·입금반영·등급분기). | typecheck0·전체 **1633**·build 통과. 공유 흐름(hold·checkin·payments) 수정에도 기존 테스트 무회귀. **잔여 PARTNER-2c(UI 배선)**: 예약 상세 파트너 지정 드롭다운·입금 폼 purpose 선택·확정 차단 안내. LEDGER 무변경(현금주의·COLLECTION 기존대로). |
| 2026-06-26 | **PARTNER-2 — 파트너 관리 화면+CRUD API(ADR-0022, af00e95, QA PASS)** | 여행사·랜드사 정식 엔티티를 운영자가 등록·관리. **API**: GET/POST /api/partners·GET/PATCH /api/partners/[id] — 전부 `auth()`→`canViewFinance` 첫줄 게이트, zod(creditLimitVnd 숫자만·depositRatePct 0~100), BigInt 변환, AuditLog(신용한도·등급·상태 old/new), serializeBigInt. **lib/partner-server.ts**: 미수/Aging 서버 집계(PARTNER-1 순수헬퍼 결합, 채권 최소 select 화이트리스트). **화면**: /partners 목록(미수·연체 표시·신규 등록 모달)+/partners/[id] 상세(신용정보 수정·Aging 카드·미수 채권·예약 이력), 기존 admin 다크 패턴(ResponsiveTable) 재사용, partner-form 공유. 사이드바 파트너 메뉴(cap=canViewFinance — STAFF 미노출)+layout 네임스페이스+adminPartners i18n ko/vi(74키 parity). API 누수/권한 테스트 15(STAFF·SUPPLIER·비로그인 403/401 실증). | **QA 독립 PASS**(누수 가드 4중 실증: API 게이트·페이지 redirect·사이드바 cap·import 봉쇄 grep / 공급자·게스트·공개에 partner·미수·한도 참조 0). typecheck0·전체 1613·build 통과. **잔여 PARTNER-2b(다음 증분)**: 예약 확정 신용 게이트 통합(canCreateBookingFor+receivable 생성)·체크인 게이트·Payment.purpose 입금기록. QA 개선의견: 한도 변경 OWNER 전용화 검토. PR #32(PARTNER-1+2). |
| 2026-06-26 | **PARTNER-1 — 여행사·랜드사(B2B) 미수·여신 토대 구현(ADR-0022, 브랜치 worktree-partner-1)** | 테오 정책 4건 확정(신규=등급A 선불의무·선금30%·Phase1 ADMIN전담·보증예치금 미도입) 반영. **스키마 additive**: 신규모델 Partner(creditTier·creditLimitVnd·depositRatePct 기본30·paymentTermDays·status)·PartnerReceivable(예약별 객실료 채권, bookingId @unique·depositDue/Paid·balancePaid·dueDate·status)·PartnerInvoice(주/15/30일 마감청구서, @@unique partnerId+기간) + enum 6종(PartnerType·CreditTier·PartnerStatus·ReceivableStatus·PartnerInvoiceStatus·PaymentPurpose) + Booking.partnerId(FK SET NULL, agencyName **dual-read 폴백** 유지)·Payment 확장(purpose@default GUEST·partnerId·receivableId·invoiceId). prisma format·validate·generate 통과. **raw SQL**=prisma/migrations-manual/2026-06-26-partner-receivables-credit.sql(IF NOT EXISTS 멱등·FK DO블록, **미실행**—배포 단계). **lib/partner.ts**(순수 정책 단일소스): computeDepositDue(30%·동단위 올림)·computeDueDate(A=체크인일/B=마감+termDays)·receivableOutstanding·outstandingForPartner(완납·대손 제외)·hasOverdue·canCreateBookingFor(신용게이트: BLOCKED/SUSPENDED/연체/한도초과 차단·등급A는 한도무관 통과)·agingBuckets(0-7/8-15/16-30/30+). lib/partner.test.ts. **LEDGER 무변경**(현금주의 유지·AR은 운영테이블, ADR-0018 정합). | **타 세션 활성**(SPEC·Sprint5 ADR-0021 uncommitted) → worktree 격리(EnterWorktree, origin/main 기준)로 진행. **미배포·합의 대기**: ①QA 누수검토(미수·한도·청구 ADMIN 전용) ②main 병합 ③prod raw SQL 실행 ④generate. UI·API·라우트는 PARTNER-2. 코드 누수 표면 0(lib는 순수함수, route/page 신설 없음). |
| 2026-06-26 | **전체 디버깅 루프 — 빌드 게이트 그린 복구(커밋 c80dad6 푸시)** | origin/main FF 후 4종 게이트(typecheck/lint/test/build) 베이스라인. ①의존성·Prisma stale: PR #29(addon-v2)가 추가한 `qrcode.react`·`@react-pdf/renderer` 미설치 + 스키마 새필드(passportPhotoUrls/serviceTime/nameI18n/descI18n) 클라이언트 미반영 → **`npm install`(postinstall=prisma generate)** 한 방에 해소(schema.prisma는 이미 정상, 드리프트 아님). ②payments-api 4건 실패: LEDGER(ADR-0018) 적재가 payment 라우트에 추가됐는데 테스트 `$transaction` mock에 `ledgerTransaction` 누락 → mock 3메서드(findUnique/create/deleteMany) 보강. ③zalo-inbound 1건(**진짜 코드버그**): `buildCallDetail`이 missed/rejected 통화의 params.duration(벨시간)을 통화시간으로 출력 → 동일커밋 55544c4 테스트미실행 케이스, 미연결통화 duration=0 정규화. | 최종 **테스트 1564/1564·typecheck0·build0**, lint 경고만(비차단). 변경 2파일(15줄). main 직접커밋·푸시(status에 타세션 WIP 0 확인 후 명시경로 add). |
| 2026-06-25 | **정산 2차 P2-3 — 복식부기 LEDGER 구현·PR #19(ADR-0018 Accepted)** | 테오 결정 4건 확정(매출=현금주의/채무=수납 시(COLLECTED) 적립/백필 실거래만/누수 ADMIN전용). TDA 보강: 초안 5계정→**COGS 추가 6계정**(채무 대차 상대, 통화별 균형·마진 장부도출). `LedgerAccount`·`LedgerEntryType` enum + `LedgerTransaction`/`LedgerLine`(additive). `lib/ledger.ts`: 순수 분개 빌더(차변+/대변−, `assertBalanced` 통화별 합0 강제) + 멱등 적재(post*) + `verifyLedger`(통화별 합0 + SUPPLIER_PAYABLE 잔액=미지급 정산 합). 훅: Payment POST→COLLECTION(paymentId 멱등)·삭제→역분개, Settlement COLLECT→COST_ACCRUAL, MARK_PAID→원가적립 보장 후 PAYOUT, ADJUST_FX→FX(replace). `/api/settlements/ledger-verify`(ADMIN)+/settlements 불균형 배너(ko/vi). `scripts/backfill-ledger.ts`(실거래 멱등). | **누수0**: ledger import는 ADMIN 게이트 경로만(공급자·공개·STAFF 미노출). typecheck0·테스트 52통과(ledger 21신규+무회귀)·`next build` 통과. **라이브DB raw SQL 적용·검증 완료**(테이블·enum·FK cascade 실측, 프로덕션 Railway proxy). 배포순서: ①DB적용(완료)②PR #19 머지·배포③`npx tsx scripts/backfill-ledger.ts`. 공유 node_modules Prisma 클라이언트 타세션 클로버 경쟁 → generate+검증 원자실행으로 통과(Railway 클린빌드 무관). 계약 docs/contracts/T-settlement-ledger-p2-3.md. 후속 S5(잔액 대시보드) 별도. |
| 2026-06-25 | **#2b 미니바 회사표준 — 배포·검증·S3 완전 종결(에픽 #2b 클로즈)** | 미니바 배포 상태 점검 중 **3연속 Railway FAILED(9626eda·ebfb957·e4b3964)** 발견 — 격리 worktree(origin/main HEAD 0c3908f)에서 tsc0·`next build`0 통과로 **코드 무결 확정**, FAILED는 다세션 연속 푸시가 진행중 빌드를 슈퍼시드한 것(빌드 게이트 [[deploy-build-gate]]). 직후 머지커밋 **0c3908f Railway SUCCESS**(미니바 커밋 3개 모두 조상 포함 확인). prod DB 검증(공개 프록시 thomas.proxy.rlwy.net, railway run은 internal 도달불가 [[etl-r2-credential-trap]]): `MinibarItem`(stockQty 포함) 존재·표준품목 5종 라이브UI 입력 확인. **nameVi 5건 null→기본값 채움**(Nước suối/Bia/Soju/Coca-Cola/Bánh snack). **S3 실행: `DELETE FROM VillaAmenity WHERE category='MINIBAR'` 17행 삭제(잔존0)**, 백업 `_backup_minibar_amenity_20260625`(17행) 보존으로 복구가능. | 마이그레이션 SQL에 S3 실행기록 추가. 임시 점검 스크립트는 정리(미커밋). DB 직접 변경(앱빌드 무관). 타세션 활성(agreement-editor·statistics·zalo-call·proposals) — 미니바 도메인 파일만 비접촉 확인. [[theo-2026-06-24-epic]] #2b ✅✅ 종결. |
| 2026-06-25 | **정산 페이지 고도화 1차 — 운영자 매출·환차·마진 파생 요약 완료·커밋** | 테오 선택(Phase 2 착수). **스키마 무변경** — 기존 Booking 필드(saleCurrency·totalSaleKrw/Vnd·supplierCostVnd·fxVndPerKrw 스냅샷)에서 운영자 손익 파생. `lib/settlement-finance.ts`(bookingFinance·summarizeFinance: VND/KRW 분기, KRW→VND 스냅샷 환산, 환율 미상은 환산·마진 제외=허위0 금지, 미지원통화 throw) + `lib/pricing.ts` `krwToVndSnapshot`(half-up, suggestSalePriceKrw 역방향). `/settlements`(canViewFinance 게이트)에 **운영자 손익 패널**(수납 통화별·VND환산 합·공급자 지급·마진·환율미상 건수) + 공급자별 마진 컬럼(table/모바일). BigInt 전용, server에서만 계산(클라엔 포맷 문자열만). | **QA 독립 PASS**(결함0) — 누수0 실증: settlement-finance는 ADMIN 페이지만 import, 공급자 /earnings·Zalo 정산공유·GET API에 마진·매출·VND환산 미노출, STAFF redirect. 환산 math·Decimal(14,4) toString·역마진·빈월·환율미상 검증. tsc0·build0·suite 1397/1397(+11). **2차(범위밖)**: 실수납 기록(Payment 모델·부분입금·수납환율), 복식부기 LEDGER(환전 BithumbLedger 이식), Settlement 상태확장(COLLECTED·FX_ADJUSTED)·환차 수동조정, 월 정산서 PDF. 계약: docs/contracts/T-settlement-finance-summary.md |
| 2026-06-25 | **#5 공개 제안 페이지 5개 언어 1차 완료·커밋·푸시(05c3c19)** | `/p` 15개 파일 한국어 하드코딩(~120 distinct) → **`lib/public-i18n.ts` 5개 언어 딕셔너리**(ko/en/ru/zh/vi) 추출. PUBLIC_LABELS(Record<lang,...> + 파라메트릭 fn) + BED/FEATURE_LABELS + WEEKDAYS + formatPublicDateLong/Short + PUBLIC_META 타이틀. **next-intl 글로벌(ko/vi) 무변경** — checkin-sheet-i18n·agreement 딕셔너리 패턴 재사용(5개 언어 코드베이스 관례). 로케일 `?lang= > p-locale 쿠키 > ko`(resolvePublicLang) + 언어 선택기(lang-selector). 내부 네비·redirect·generateMetadata 전부 lang 보존. **빌라명·가격·설명 동적데이터 원문 유지**(1차 정적 UI만). 신규 테스트(5개 언어 parity·해석 우선순위·날짜헬퍼). | **QA 독립 PASS**(누수 0 실증: select·데이터 0변경, 원가·마진·wifi 미노출 / 통화·"—" 폴백 불변 / lang 흐름 보존). tsc0·build0·suite 1385/1386(BANK_VN 별건). 잔여: **ru 기계품질→LOC 감수**(주석 TODO ru-review), 동적 콘텐츠(빌라 설명) 번역 2차. 병렬: 타세션 agreement-editor 동시 진행 — /p 18파일만 explicit-pathspec 커밋(비접촉). 계약: docs/contracts/T-public-i18n-5lang.md |
| 2026-06-25 | **#2b 미니바 회사표준 모델 전환 완료·커밋(b179648+6a917d6 ADR-0016)** | 미니바를 빌라별(`VillaAmenity.MINIBAR.unitPrice`)→**회사표준 1세트 `MinibarItem`**(villaId 없음=공급자 쿼리 구조적 도달 불가, 원칙2 데이터형태 강제). ① 스키마+테이블 prod raw SQL 생성(S0백업 17행·S1 CREATE, db push 금지). ② `/api/admin/minibar` CRUD(쓰기 canSetPrice·읽기 isOperator·AuditLog 전건·BigInt 문자열직렬화) + `/settings/minibar` UI(canSetPrice 게이트). ③ 체크아웃 `MinibarItem(active)` **소모 수량 직접입력**(역산 폐기) 차감=소모×unitPriceVnd BigInt — **차감 BE(lib/checkout·route) 무변경**. ④ 빌라 편집기/마법사/`PATCH·POST /api/villas`에서 MINIBAR drop(레거시행 비-MINIBAR deleteMany로 S3까지 보존). ⑤ 소비처: checkin-sheet 표준 미니바표(5개 언어 라벨)·zalo share amenities `category!=MINIBAR` 필터(공급자 공유 명칭 누수차단). i18n ko/vi(adminMinibar). 단위테스트 신규(권한·AuditLog·BigInt·누수 정적가드) + 기존 minibar-leak 갱신 + create-drop. tsc0·build0·suite 1359/1360(BANK_VN 별건). **QA 독립 PASS**. | **미배포·S3 대기**: ⑴ 푸시·Railway 배포는 테오 확인 후 ⑵ 배포·검증 후 테오가 /settings/minibar에 표준품목 입력 ⑶ 입력완료 후 S3 `DELETE FROM VillaAmenity WHERE category='MINIBAR'` 수동(SQL 주석처리됨). QA 비차단: 공급자 마법사 step-amenities MINIBAR탭 잔존(가격없음·서버drop, 비누수). 병렬: messages json 타세션 mid-edit → [[shared-git-index-private-commit]] 전용인덱스(binary·LF보존)로 내22파일만 surgical 커밋+git reset로 index 동기화. 계약: docs/contracts/T-minibar-company-standard.md |
| 2026-06-25 | **데모 시드 미니바 단가 추가(체크인 시트 정산표 데모)** | `prisma/demo-seed.ts` AMENITIES에 `unitPrice` 지원 추가 — 미니바 water/softDrink(20,000₫)·beer(40,000₫) 단가 부여 + softDrink 항목 추가. 체크인 시트 미니바 정산표(T-checkin-sheet-v4)가 데모 환경에서도 가격 칸 렌더되게. 부수: stale itemKey `refrigerator`→`fridge`(사전 정합, 시트 라벨 정상화). upsert update/create에 unitPrice 반영. | 격리 worktree `wt/demo-minibar`. typecheck0. 시드 데이터 변경(앱 빌드·누수 무관), DB 반영은 `npm run seed:demo` 실행 시. |
| 2026-06-24 | **체크인 시트 v4 — 비품 박스 표 + 미니바 정산 컬럼(T-checkin-sheet-v4) 완료** | 사용자 요청: 비품을 박스(표)로 — 항목·가격·수량·남은수량·합계. amenities select에 `unitPrice`(미니바 고객 청구가 VND, 게스트 노출 OK·판매가/원가 아님) 추가. 비품 섹션을 테두리 표로 교체(카테고리 그룹행+항목행): 가격(₫)·수량(비치)은 인쇄, **남은수량·합계는 빈 칸**(체크아웃 시 손기입). 컬럼 헤더 5개 언어(amenityTable). | 격리 worktree `wt/sheet-minibar`. **QA PASS** — 완료기준 6/6, unitPrice 누수 판정=미니바 청구가(누수 아님), 판매가/원가 select·렌더0, minibar-leak 테스트 6/6 통과, en/zh/ru 표 헤더 정상, typecheck0·build0. |
| 2026-06-24 | **체크인 시트 v3 — 인쇄 문서 전체 다국어 + 비품 인쇄(T-checkin-sheet-v3) 완료** | 사용자 피드백 2건. ① 지금은 동의서만 언어변경 → **인쇄 문서 전체**(섹션 제목·필드 라벨·채널/인원/박수/조식 값·비품·동의서)가 선택 언어로. 신규 `lib/checkin-sheet-i18n.ts`(시트 라벨+비품 49종+카테고리, ko/vi/en/zh/ru 5언어, nights/guestsValue 함수, amenityLabel custom 폴백). 기본=앱 로케일, ?lang으로 en/zh/ru, 날짜이동·언어 보존. 동의서는 단일 선택언어 렌더로 변경. 툴바는 no-print 앱 로케일. ② **비품 섹션 신설**: villa.amenities select(unitPrice 미포함=가격 누수0), 카테고리별 그룹·선택언어 라벨·qty>1 ×n. | 격리 worktree `wt/sheet-i18n`. **QA PASS** — 완료기준 8/8, 누수0(unitPrice/판매가/원가 select·렌더0), 비품 49↔라벨49 정합·5언어 완비, en/zh/ru 전체 전환 실증, 디지털 체크인 회귀0, typecheck0·build0. 비품 ko/vi는 messages 미러(편집기/체크아웃은 messages 유지). |
| 2026-06-24 | **체크인 시트 v2 — 다국어 동의서 + 비품 확장(T-checkin-sheet-v2-i18n-amenities) 완료** | 사용자 요청 3건 반영. ① **다국어 동의서**: 동의서를 messages에서 분리해 전용 단일 소스 `lib/agreement.ts`로(ko/vi/en/zh/ru 5개 언어 docTitle+7조항+수영장 조항, AGREEMENT_VERSION). 체크인 시트에 게스트 언어 선택(`?lang=`, 기본 vi) → **한국어(기록용)+게스트 언어 병기** 인쇄, 날짜 이동 시 lang 보존. 디지털 체크인 동의서도 모듈 기반 리팩토링(ko/vi, 회귀 0). 동의서 길어질 예정 대비 버전 표기. ② **비품 확장**: lib/amenities.ts에 22개 표준 항목 추가(주방9·욕실6·가전6·미니바1) — 3개 편집기 자동 렌더. 스키마 변경 없음(기존 4카테고리). | 격리 worktree `wt/checkin-v2`. **QA PASS** — 완료기준 9/9, 누수0(판매가·원가·마진 select·렌더0), en/zh/ru 한자·키릴 정상, JSON additive-only(ko/vi 각 +23/-0), typecheck0·build0. i18n ko+vi 동시. |
| 2026-06-24 | **하이드레이션/SSR 안전성 감사 — #418 계열 잠복 버그 전수 스윕·수정** | formatDateTime #418 수정에 이어 같은 계열(서버↔클라 렌더 불일치) 잠복 버그를 앱 전반 스윕. **원인 패턴**: 클라이언트 컴포넌트가 SSR+하이드레이션에서 시각을 `hour:"2-digit"`+`hour12:false`(hourCycle 미지정)로 렌더 → Node ICU "24" vs 브라우저 ICU "00" 불일치. **추가 발견·수정 2건(동일 클래스, 비경합)**: ① `cost-alerts-view.tsx`(견적중 원가경보 카드의 detectedAt 표시) ② `bookings/[id]/action-panel.tsx`(여권 전달완료 시각 HH:mm) → 둘 다 `hourCycle:"h23"` 통일. **안전 확인(수정 불요)**: `lib/ical.ts`는 이미 h23 / 카운트다운 3곳(`hold-countdown`·`proposals-list`·`calendar-view`)은 시각을 초기 SSR 렌더에 미노출(useEffect·클라fetch·사용자열기 시트 뒤로 가드) → #418 무관 / RSC 전용 포매터(users·earnings·cleaning·dashboard·date-vn 등)는 서버 텍스트라 하이드레이션 대상 아님 / `toLocaleString`은 전부 명시 로케일(de-DE·ko-KR·en-US)이라 결정적. **보고만(경합 영역 미수정)**: `lib/zalo-chat-message.ts`·`zalo-share.ts`·`_thread-data.ts` 시각 포매터는 서버 단독 산출이라 현재 #418 무위험이나 향후 클라 재포맷 시 주의(타 세션 활성 구역). | typecheck0. 수정 2파일 비경합(cost-alerts·bookings/[id]). 감사 결론: **클라 컴포넌트의 시각 렌더만 #418 위험**, 본 스윕으로 비경합 영역은 0건 잔존. [[messages-inbox-performance]] 무관. |
| 2026-06-24 | **검수 페이지네이션 프로덕션 검증 + 자정 하이드레이션 버그(#418) 수정** | 방금 배포한 /inspections 페이지네이션(d99f635)을 Playwright로 프로덕션 실검증(테오 OWNER 세션, PC 1440). **페이지네이션 PASS**: "1–20 · 총 26건", 페이지 1/2 네비·페이지당 개수 정상, 정렬 우선순위 보존(승인대기4→청소대기6→반려1→승인15), 큐 배지=counts[tab](26) 정확, 자동선택 상세=정렬 첫 행 고정. **검증 중 React #418(하이드레이션 텍스트 불일치) 발견·근본원인 규명·수정**: 원인은 페이지네이션이 아니라 **공유 유틸 `lib/format.ts` `formatDateTime`** — `hour12:false`만 두고 `hourCycle` 미지정이라 자정(00시)을 Node ICU="24", 브라우저 ICU="00"로 갈리게 렌더 → SSR↔클라 불일치. /inspections 페이지1에 00:13 행 2개(QA타인빌라·TEST1) 존재해 발현, 00시 행 없는 thisWeek 필터로는 #418 소멸(근거 확정), 같은 PaginationBar 쓰는 /bookings는 00시 행 부재로 무에러. **수정**: `hourCycle:"h23"`로 양쪽 "00" 결정화(다른 시각 동작 불변). 자정 회귀 단위테스트 2건 신규. | **검증 PASS·버그 수정 푸시(예정)**. 페이지네이션 자체 회귀 0(별도 수정 불요). #418은 사전 존재 버그(내 페이지네이션 무관)이나 검증 중 포착해 동반 수정 — formatDateTime 쓰는 전 화면(예약·검수·대시보드 등)에서 00:00~00:59 타임스탬프 표시 시 하이드레이션 오류 제거. typecheck0·format 테스트 8+신규2 통과. lib/format.ts는 타 세션 WIP 아님(단독 수정). **배포(d007c64에 포함)·프로덕션 재검증 완료**: /inspections 전체목록(00:13 행 포함) 콘솔 에러 0(#418 소멸), 페이지2 "21–26·총26건"+상세 쏘나씨 V12 불변(rows[0] 고정 실증). |
| 2026-06-24 | **관리자 검수(/inspections) 큐 페이지네이션 — 모바일 UX 후속(요청 시)** | 모바일 UX 배치(bb34f8e)에서 마스터-디테일 구조라 제외했던 마지막 리스트. 좌측 검수 큐에 공통 PaginationBar(URL 모드·다크, 기본20·옵션10/20/30/50/100) 적용. **승인 대기 우선 JS 정렬(STATUS_RANK)을 보존**하기 위해 정렬 후 `rows.slice(skip,take)`(상한 200 적재 안에서 분할). **자동선택을 `rows[0]`(정렬된 전체 첫 행)로 고정**(기존 `tasks[0]`=슬라이스 첫 행) → 부모 `key={selected.id}` 리마운트와 무관하게 페이지 이동 시 우측 상세 불변. 큐 개수 배지는 `counts[tab]`(실 전체 건수) 유지. status/task/range/area 쿼리는 바가 보존, 필터 변경 시 page 리셋(tabHref/changeArea/QuickDateFilter 모두 page 미포함). 모바일은 좌측 section 내부 배치라 taskSelected 시 바까지 함께 숨김. | **변경 2파일**(`inspections/page.tsx`·`inspections-view.tsx`)·select 화이트리스트 무변경(누수0). typecheck0. **독립 QA PASS(결함0, 7항목 전수: 누수·정렬보존·자동선택안정·필터보존·cap정합·모바일·타입)**. 신규 i18n 키 불필요(pagination·adminInspections 기존). 병렬 세션 WIP(lib/cleaning·hold·proposal·messages/*·DESIGN) 비접촉 — 내 2파일+계약서+PROGRESS만 pathspec 커밋. 계약: docs/contracts/T-admin-inspections-pagination.md |
| 2026-06-24 | **모바일 UX 배치 — 하단 네비·카드 필터·헤더 접기·리스트 페이지네이션 (프로덕션 테스트 주도)** | 테오 프로덕션 모바일 테스트 피드백을 즉시 반영한 UI 5건. ① **예약 상태 필터 카드화**(ec45f7c): 전체/가예약/확정/체크인/체크아웃/취소만료 탭을 대시보드식 개수 배지 카드 그리드(모바일 grid-3·데스크톱 grid-6)로, 검색 상단 배치. ② **ADMIN 모바일 하단 네비 신설**(ec45f7c): Nike식 중앙 돌출 FAB — 대시보드·예약·[중앙=정산(STAFF는 공실보드)]·메시지(미읽음 배지)·더보기(드로어). `components/admin/mobile-nav-spacer.tsx` + sidebar.tsx 통합(드로어 open 공유). 향후 매출관리 페이지는 정산과 별개로 IDEAS 기록. ③ **/messages 하단 네비 노출 + 채팅 헤더 모바일 접기**(dbd1ed0): 풀스크린 채팅 높이를 헤더+네비(7.5rem) 보정해 입력창 가림 해소, ChatHeaderBar 기본 컴팩트 1줄(번역언어 칩 상시)+탭 펼침(분류·별명편집·원본). ④ **공실보드 빌라명 sticky 열 접기/펴기**(dbd1ed0): 접으면 48px(상태 아이콘만)로 달력 가시영역 확보(모바일). ⑤ **리스트 공통 페이지네이션**(bb34f8e): `lib/pagination.ts`(기본20·옵션10/20/30/50/100)+`components/pagination-bar.tsx`(다크/라이트·URL/controlled 겸용) → 예약·빌라·제안·사용자·공급자 내빌라·청소 적용. | **origin/main 푸시·Railway 배포 완료(ec45f7c·dbd1ed0·bb34f8e)**. 전부 typecheck0. **병렬 세션 고경합**: 같은 폴더서 타 세션이 board-client(터치드래그)·chat-pane·responsive-table·lib/*·messages json 동시 편집 → 내 변경만 격리 커밋(json은 `git cat-file blob HEAD` raw-byte 삽입+`hash-object --no-filters`로 autocrlf 정규화 우회, +N줄만 깔끔 커밋, 메모리 [[shared-git-index-private-commit]] 갱신). i18n nav.more·headerCollapse/expand·pagination·collapseList/expandList ko·vi 추가 + admin/supplier 레이아웃 화이트리스트. **비대상**: 검수(마스터-디테일·계약상 제외)·공실보드/메시지/정산/수익/활동/원가경보(테이블 아님). 인박스 성능·청소검수 페이지네이션은 요청 시 후속. |
| 2026-06-24 | **체크인 시트 프린트(T-admin-checkin-sheet) 완료** | 체크인 담당자 아침 일괄 준비용 `/bookings/checkin-sheet` 신설 — 당일 CONFIRMED 체크인 예약을 빌라별 A4 1페이지(①예약정보 ②보증금 3분기 ③WiFi ④동의서+펜 서명란)로 프린트. `/bookings` 헤더에 "오늘 체크인 출력" 진입버튼. `@media print`로 사이드바·툴바 숨김, 예약당 page-break. 동의서는 기존 `adminCheckin.agreement` 재사용(hasPool 시 수영장 조항 자동). 마진 비공개 준수 — 보증금만 노출, 판매가·원가·마진은 select·렌더 어디에도 없음. | 격리 worktree `wt/checkin-sheet`에서 구현(공유폴더 타세션 미커밋 보호). **QA PASS** — 누수0 실증(테스트 금융값 심어 grep 0건)·완료기준 10/10·typecheck0·`next build`0. i18n ko·vi 동시 추가(additive-only). |
| 2026-06-24 | **ADR-0014 에픽 다각도 디버깅 loop-until-dry 4라운드 완료·수렴(c3d49ce)** | "루프 돌려 다각도 디버깅, 무결함시 완료" 요청. 라운드마다 다른 렌즈 병렬조사→실결함 검증·수정·재검증, 새 결함 안 나올 때까지 반복. **R1**(생성/cost-alerts/표시/견적 4영역): representativeRatesBySeason HIGH/PEAK base 폴백 제거(시즌 오표시·cost-alerts 마진오산 수정, 50553da). **R2**(cost-alerts재조사·UX·동시성·E2E): candidates 미책정(0원) 빌라 후보 제외(63ee167, 실DB `,ㅏㅏㅏㅏ` 0원 ACTIVE 발견). **R3**(적대검증·완결성): R1 폴백제거가 공급자 재제출 prefill 깨뜨린 회귀 발견→prefill 한정 base 폴백 복원(c3d49ce). **R4**(수렴확인): 새 명확버그 0 — **수렴**. **견적/제안/예약/스냅샷 무결성·누수 불변식·마진계산·다기간 박별합산·AuditLog 전부 정상 확인.** | **origin/main 푸시(50553da·63ee167·c3d49ce)**. 각 수정 typecheck0(타세션 messages-client.tsx 미커밋 문법오류는 무관)·테스트 1259통과·Railway 배포(cfb5df00)·프로덕션 스모크 307/401 정상. **미수정·문서화(잠복/정책/i18n, 기능차단 아님)**: base 단일성 DB제약(db push라 비내구)·PATCH status가드(정책)·cost-alerts 다중기간 season한계·fx0·경고사유 i18n(messages 타세션 대규모 미커밋이라 LOC 보류). ADR-0014 디버깅 R1~R3 절 + [[rate-periods-status]] 갱신. | Phase C 후 단일경로 전환 결함 사냥(생성/시드 A·cost-alerts B·표시 C·견적통합 D 병렬). **누수 불변식 전 경로 PASS, 견적/제안/예약/스냅샷 무결성 정상**(D 결함0). **수정**: `representativeRatesBySeason`이 HIGH/PEAK 기간 부재 시 base(LOW)로 폴백 → 비수기 원가를 "성수기" 라벨로 오표시 + cost-alerts 마진 오산. 해당 시즌 실제 기간 있을 때만 반환(없으면 미포함), LOW=base 유지. 단위테스트 4건 신규(기존 0건이라 미검출). **잠복/설계한계 기록(ADR-0014 디버깅 절)**: 마법사 3원가→N기간 손실(다중PEAK 동일원가)·전역시즌 없는 HIGH/PEAK 원가유실·생성경로 겹침 미검증·base 단일성 DB제약 부재·마진0 견적위험·cost-alerts 다중기간 season한계 — 현 데이터 미발현, 후속/TDA 검토. | **origin/main 푸시(2fadafa)**. typecheck0·**공유트리 build0**(타세션 반응형 커밋 완료로 /bookings 정상)·테스트 1259통과(9실패=zalo/seed 타세션 무관). **사고·복구**: worktree 정리 중 Remove-Item -Recurse가 node_modules 정션 따라가 공유 node_modules 61패키지 삭제(@aws-sdk·@hookform) → npm install 복구(프로덕션 영향 0, 로컬만). 교훈 [[worktree-junction-recursive-delete-hazard]]. |
| 2026-06-24 | **Batch B Phase C — 구 요율 테이블 DROP (ADR-0015, 0616127) — ADR-0014 에픽 종결** | 사용자 승인("백업 후 지금 DROP"). **VillaRate·VillaSeasonPeriod 테이블 영구 삭제**(VillaRatePeriod 단일 소스 확정). ① **ADR-0015 작성**(FK안전·코드참조0·스냅샷무영향·비가역/롤백 절차). ② **백업**: DROP 전 두 테이블 전량 JSON 덤프(backups/phase-c-...-2026-06-24.json, VillaRate 30·VillaSeasonPeriod 1행, BigInt "…n"). ③ **schema**: model 2개 + Villa.rates·seasonPeriods 관계 제거 → `prisma db push --accept-data-loss`로 DROP. 유지: SeasonType·전역 SeasonPeriod·VillaRatePeriod. ④ **정리**: 일회성 스크립트(migrate-rate-periods·inspect-rate-state) 삭제, cleanup-test-data _count.rates→ratePeriods. ⑤ **EPERM 복구**: db push 후 generate가 타세션 dev .dll rename 레이스로 EPERM → .dll 삭제(잠금테스트=비잠금 확인)→재generate 복구(prisma-generate-eperm 메모리). | **origin/main(ebbfb16)에 포함**. 검증: typecheck0(구 모델 참조 잔존0)·**격리 worktree build0**·테스트 1255통과(9실패=zalo/seed 타세션 무관). **공유 repo**: 타세션이 내 Phase C 커밋(0616127) 위에 반응형·Zalo 커밋 쌓아 함께 push, 내 커밋 history 보존 확인(merge-base). DB DROP 완료+Phase B 코드(VillaRate 미참조)라 코드↔DB 일관. ADR-0014/0015·[[rate-periods-status]] 갱신. |
| 2026-06-24 | **Batch B Phase B — 구 VillaRate 경로 전면 제거·VillaRatePeriod 단일화(ddf2274)** | 사용자 승인("테스트데이터 자유수정·단계적 전체 제거"). **① 전 빌라 전환**: 전역폴백 9건은 migrate 스크립트 확장(전역 비-LOW HIGH/PEAK 기간 복제, 전역 LOW는 base 배경 담당·겹침 회피)으로 변환, QA 타인빌라(VillaRate 없는 누수픽스처)는 placeholder base 직접 생성 → 11건 전부 전환, `inspect-rate-state.ts`로 게이트①(base미보유 ACTIVE)②(전역폴백 의존) **0 확인**(b88afb6). **② 구 경로 코드 제거**: 공용헬퍼 `buildRatePeriodRowsFromSeasonCosts`(생성/수정/시드 — base LOW+전역 HIGH/PEAK 기간, 시즌 원가 UX 불변·저장만 전환)·`representativeRatesBySeason`(표시/cost-alerts/Zalo공유) 신설. `quoteStayForVilla` dual-read 구분기 제거(단일 경로, base 없으면 MissingBaseRateError). 구 API(villas/[id]/rates·cost·seasons)·구 편집기(rate-editor·cost-seasons-editor) 삭제. pricing 죽은 함수(quoteStay·resolveSeason·VillaRateLike·SeasonPeriodLike) 제거. `MissingBaseRateError extends MissingRateError`(proposal/candidates instanceof catch 호환). **누수 불변식 유지**(원가측 supplierCostVnd·판매가측 salePrice* 분리 select). **유지**: SeasonType·전역 SeasonPeriod(생성 날짜 템플릿)·등록 마법사·VillaRate/VillaSeasonPeriod 테이블(Phase C 대상). | **origin/main 푸시(ddf2274)**. 검증: typecheck0·**격리 worktree build0**(타세션 responsive-table WIP가 공유트리 /bookings build를 깨뜨려 내 커밋만 담은 worktree에서 build 확정)·테스트 1255통과(9실패=zalo/seed 타세션 무관)·**독립 QA PASS(결함0, 누수·쓰기·견적·상속·구경로제거 전수)**. 병렬: 타세션 동시작업(반응형UI·Zalo발송·은행설정) 파일 비접촉, 내 28파일만 literal-pathspec 커밋. 배포검증 진행중. ADR-0014 Phase B 절·[[rate-periods-status]] 갱신. |
| 2026-06-24 | **Batch B — dual-read 소비처 헬퍼 일원화 + 일괄 변환 실행(4c9eccd)** | ADR-0014 일괄 변환 **선행작업** 완료 후 변환 실행까지. **① 비인지 소비처 3곳 일원화**: `lib/pricing.ts` `pickRepresentativeRate(base 기간행 우선→LOW VillaRate 폴백→rates[0]→null)` 신규 — 변환된 빌라에서 stale 가격 차단. 적용처 ⓐ Zalo 공급자 원가뷰 ⓑ Zalo 판매가측 뷰(둘 다 `messages/page.tsx`, 각 findMany에 `ratePeriods{where isBase,take1}` nested select) ⓒ STAFF 원가뷰(`villas/[id]` costOnlyRows, 변환 빌라는 기본요금+기간 원가 표시). **누수책임 분리**: 헬퍼는 행 선택만, 노출 필드는 호출자 select(판매가측=salePrice*만, 원가측=supplierCostVnd만) → 마진 비공개 불변식 유지. 테스트 +4(35통과)·typecheck·next build 그린·독립 QA PASS(누수4종 점검). **② 일괄 변환 실행**: 헬퍼 push→Railway 배포(6b20e400, /·/messages 307 라우트존재 확인)→`scripts/migrate-rate-periods.ts` LIVE 실행. **변환 1건(TEST1: 기본요금1+기간1)**, 스킵 10건(VillaSeasonPeriod 0건=전역폴백, 구 경로 유지·무회귀). dry-run 재실행으로 멱등 확인(0건 변환, TEST1 "이미 변환됨"). | **origin/main 배포(4c9eccd)**. messages/page.tsx는 타세션 toChatMessages 리팩터와 동시편집됐으나 레이스 없이 공존(내 villaCandidate 변경·타세션 message매핑 직교, 내 4파일만 pathspec 스테이징). **다음**: Phase B(전 빌라 전환 확인 SQL 후 구 dual-read 분기·구 편집기/API 제거) — 단 전역폴백 빌라 10건 미전환이라 시기상조 가능. 메모리 [[rate-periods-status]]. |
| 2026-06-24 | **Batch B 디버깅 — 알림 크래시 수정 + dual-read 비인지 소비처 문서화(3bea994)** | **버그 발견·수정**: 공급자 `rate-periods/cost`가 보낸 비호환 알림 payload(`{villaId,proposalId,change}`)를 `lib/cost-alerts.ts`가 `BigInt(p.oldCostVnd)`로 읽어 throw → **관리자 원가경보 페이지·대시보드 배너 크래시**. ① cost-alerts 가드에 season·oldCostVnd 필수 추가(비호환 건너뜀·크래시 차단) ② 공급자 라우트가 원가 실제변경 행(base·매칭기간·삭제기간)마다 **호환 payload**(season·old/newCostVnd·proposalId) 발송 → 정상 소비, 변경0이면 제안조회/발송 단락 ③ 관리자 기간편집기 빈 날짜 사전검증. 테스트 +5(알림 호환 2·cost-alerts 가드 3), rate/pricing 58 통과. **검토 완료(이상無)**: 날짜 round-trip(관리자 UTC iso = 공급자 toDateOnlyString, off-by-one 없음)·겹침 로직·마진 보존·dual-read 분기. **dual-read 비인지 소비처 2건 문서화(ADR-0014)**: Zalo 빌라공유(messages/page.tsx, VillaRate LOW 대표가)·STAFF 원가읽기뷰 — 신규경로 빌라서 stale, Phase A 영향 잠복, 변환 전 공통 헬퍼로 정리 권장(messages는 타세션 활성구역이라 조율 필요). | **origin/main(3bea994)**. 전체 1286 중 9 실패는 전부 zalo/seed(타세션 도메인)·제 변경과 무관(새 실패 0). |
| 2026-06-24 | **Batch B 후속 3건 완료·배포** | ADR-0014 구현의 후속. **①공급자 자가 다기간 원가 입력(589c07c·31b0afb)**: PATCH /api/villas/[id]/rate-periods/cost(SUPPLIER 자기빌라) — 기본요금+기간 원가만, 매칭 행 마진 보존·신규 기간 base마진 상속·판매가 서버재계산, 견적중 ACTIVE 제안 ADMIN 알림, 응답·감사에 판매가/마진 미포함(누수0). 공급자 모바일 vi 편집기(기간 추가/날짜/원가) + 빌라 상세 진입링크 + i18n. dual-read로 저장 시 기간별 경로 전환. 테스트 8. **②일괄 변환 스크립트(a8cd71d)**: scripts/migrate-rate-periods.ts — 멱등·`--dry`. VillaSeasonPeriod 보유+LOW VillaRate 보유 빌라만 변환(전역폴백·LOW없음·겹침은 SKIP·로그), base=LOW VillaRate·기간=각 VillaSeasonPeriod에 그 시즌 가격 매핑, 빌라 단위 트랜잭션. 미전환은 dual-read 구경로 유지(무회귀). tsx 미설치로 로컬 dry-run 불가(운영 시 npx tsx ... --dry). **③구 모델 deprecate 검토**: ADR-0014에 전제조건(전 빌라 base 보유·전역폴백 0·코드 참조 0)·Phase A(현행 유지)/B(구 분기·편집기 제거)/C(테이블 DROP, 비가역·별도 ADR)·검증 SQL 2종·권고(현 시점 Phase A 유지) 추가. | **origin/main 배포(a8cd71d)**. 테스트 누적 79(공급자 원가 8 신규). typecheck 통과. `--no-engine` 클라 정상 generate로 복구(빌드깨짐 위험 해소, memory 반영). 권한분리: 공급자=원가·관리자=판매가 컬럼단위(API select/입력 화이트리스트). 계약: T-batchB-rate-multiperiod.md |
| 2026-06-24 | **Batch B — 요금 다기간화 ADR+구현 완료·배포** | 회의확정(2026-06-23)→**ADR-0014 승인**. 시즌(LOW/HIGH/PEAK) 라벨 유지 + **기간별 가격**. **모델 VillaRatePeriod(df3bd16)**: 기본요금(isBase) 1행(비수기 기준·폴백) + 웃돈 기간 N행(날짜범위+자체 가격). 같은 PEAK라도 기간마다 다른 소비자가. additive·db push. **가격엔진(df3bd16)**: lib/pricing.ts resolveRatePeriod(기간우선·없으면 기본요금)·quoteStayByPeriod + quoteStayForVilla **dual-read**(VillaRatePeriod count>0이면 신규경로, 0이면 기존 VillaRate+SeasonPeriod 폴백 — ADR-0008 패턴, **무회귀·무백필**). 제안·예약 스냅샷 무영향. **API(9e51fc4)**: PATCH /api/villas/[id]/rate-periods — canSetPrice 전용, 전체교체(base 필수·정확히1), 날짜 half-open·**겹침 거부**, BigInt 문자열, AuditLog. **UI(8d855f1·ae04e74)**: 관리자 기간별 요금 편집기(기본요금+기간 N, 마진 자동제안 재사용, native date), page.tsx showFinance 분기서 구 RateEditor→신규 교체, VillaRatePeriod showFinance 전용 로드(누수차단)·없으면 LOW VillaRate 시드. i18n ko/vi. | **origin/main 배포(ae04e74)**. 테스트 누적 71(pricing 35·rate-periods-api 10). typecheck 통과. **후속**: ① 공급자 자가 다기간 원가 입력(현재 ADMIN 직접수집·공급자는 구 cost 편집기) ② 기존 빌라 일괄 변환 스크립트(현재 편집기 저장 시 자연 전환) ③ 전 빌라 전환 후 VillaRate/VillaSeasonPeriod deprecate 검토. 계약: docs/contracts/T-batchB-rate-multiperiod.md, ADR-0014 |
| 2026-06-24 | **Batch A — 관리자 판매정보 보강 3건 완료·배포** | 회의 결정(2026-06-23) 기반 Phase1 보강. **① 수영장 연동(7a73cf8)**: 판매정보 폼에 hasPool 토글 + 셀링포인트 풀 태그(privatePool·kidsPool) 선택 시 서버가 hasPool=true 자동보정(해제 시 자동 OFF 안 함). V11 "수영장 0" 문제 해결. **② 비품 ADMIN CRUD(7a73cf8)**: `PATCH /api/villas/[id]/amenities` 권한을 운영자(isOperator)까지 확장(SUPPLIER 자기빌라만 유지) + 읽기전용 카드→다크 편집기(카테고리탭·수량스테퍼·미니바단가·직접입력행). 미니바 단가=고객청구가(원가아님)라 노출OK. **③ 침실별 전용욕실(68a0602)**: schema VillaBedroom.bathroomCount 추가(additive·db push) + 침실카드 스테퍼 + 저장 시 Villa.bathrooms=roomIndex별 전용욕실 합 자동갱신(빈배열이면 보존). i18n ko/vi(225a438·24b6ff4, private-index 커밋). 테스트 누적 40(hasPool 2·욕실 3·비품 ADMIN 6 신규). typecheck 통과. | **origin/main 배포(24b6ff4)**. 명칭통일: UI/문서 "빌라공급자". **Batch B(요금 다기간화) 미착수** — 회의확정: 시즌유지+기간별요금, 비수기=기본요금1+웃돈기간N, 공급자도 여러기간 입력. ADR 신규 + 마이그레이션 한세션 전담 필요. **병렬세션 주의**: .git/index 공유라 messages는 private-index 커밋으로 처리(memory 기록). schema EPERM은 generate --no-engine·db push --skip-generate로 우회. 계약: docs/contracts/T-admin-batchA-villa-fields.md |
| 2026-06-23 | **운영자 권한 3단계(RBAC) S-RBAC-1~4 완료** | **ADR-0013 채택** — 운영자 ADMIN 단일역할을 OWNER/MANAGER/STAFF로 분리. 돈 경계={OWNER,MANAGER} vs STAFF(완전차단), OWNER↔MANAGER 차이=시스템통제. 입금확인은 STAFF가 하되 **금액 숨김 🟢/🔴 상태만**(§6.1), 카뱅 자동매칭은 **입금 알림 파싱 webhook**(안드로이드 게이트웨이)으로 Phase 2(§6.2). **S-RBAC-1(45ad21c)**: Role enum +OWNER/MANAGER/STAFF(additive·db push), `lib/permissions.ts` 헬퍼 6종. **S-RBAC-2(40444b7)**: app/api/** 45라우트 role===ADMIN ~57곳 헬퍼 치환, 누수테스트163. **S-RBAC-3(7c78a0f)**: middleware 3등급 게이트 + layout isOperator + STAFF 재무 마스킹(서버 select 제외, 원가만=SUPPLIER패턴) + zalo share 누수 차단. **S-RBAC-4(7069af1)**: 계정생성 UI — api/users POST(bcrypt·phone정규화·감사로그·중복409) + PATCH CHANGE_ROLE(본인400·HAS_VILLAS409 가드) + users-manager 6역할 뱃지·생성모달·인라인 역할변경 + roles.OWNER/MANAGER/STAFF i18n(ko/vi surgical staging). 부여가능=MANAGER/STAFF/SUPPLIER/CLEANER. **독립 QA(코드감사) → 누수 2건 발견·수정(7c72185)**: H-1 예약목록(bookings/page.tsx)이 STAFF에 판매가 노출(상세만 막고 목록 누락, RSC→클라 페이로드)→canViewFinance select 분기 / H-2 api/services/[id] PATCH가 isOperator인데 priceKrw 행 전체 반환→canSetPrice 게이트 / M-1 RSC 5페이지 role!==ADMIN 잔존→등급술어 치환(inspections=isOperator·proposals/settlements=canViewFinance·settings/zalo=isSystemAdmin). 회귀테스트 누적 321. ADMIN 동작 불변. | **기능 사용가능 상태 완성** — 테오(ADMIN=OWNER취급)가 /users에서 직원·매니저 계정 생성·역할부여, STAFF는 돈 차단. **이월 1건**: `app/(admin)/messages/page.tsx`는 타 세션(zalo @멘션) 활성구역이라 role===ADMIN 유지(fail-closed·누수0) — 추후 isOperator+share후보 마스킹과 함께. **배포 완료(2026-06-23)**: origin/main push → Railway 빌드·배포 성공(villa-pms ● Online), QA 픽스 포함 라이브. **S-RBAC-final 실행(8708ef9)**: ① OWNER-ready 코드 배포(cost route 알림수신자 in[OWNER,ADMIN], seed→OWNER) ② **프로덕션 데이터 마이그레이션 완료** — `UPDATE User SET role=OWNER WHERE role=ADMIN`(4계정: 테오시드·테오실계정·김태진·QA Admin, 사용자 승인 "4개 전부 OWNER"). 결과 ADMIN 0·OWNER 4·CLEANER 3·SUPPLIER 6. 캐시 JWT 무중단(ADMIN·OWNER 둘 다 헬퍼 포함). **ADMIN enum 값은 deprecated로 유지**(Postgres enum DROP 미지원·Prisma $Enums 정합, 미사용이라 기능차0). QA교훈 leak-checklist 4종 기록(fc1df74). **잔여 후속**: messages/page.tsx 이월(타 세션 zalo 구역), QA Playwright STAFF 실검증(권장). 계약: docs/contracts/S-RBAC-1~4.md |
| 2026-06-23 | **Nike↔villa Zalo 통합 라이브 + S4 그룹 완결(villa-local)** | **S1+S2 실배포 → 세션 통합 가동, S4 그룹 채팅 실작동.** 발단: 테오가 "Nike Zalo에 대화 없음" 신고 → 원인은 villa·Nike가 **같은 테오 계정**을 양쪽에서 물려 villa 배포 때마다 code 3000으로 Nike 세션이 튕김(그룹코드 무관, 구조적). **해결=S1+S2 활성화**: 양쪽 Railway env 설정(villa `ZALO_EXT_SHARED_SECRET`·`ZALO_WEBHOOK_HMAC_SECRET`·`NIKE_WEBHOOK_URL` / Nike `ZALO_THEO_USER_ID`·`VILLA_EXT_BASE_URL`+공유 secret)+재배포 → **villa `__system__` 단독 세션·충돌 종료, Nike가 villa 정본에서 읽기 복구**(ext/threads 200·대화 433 실측, Nike 채팅 복구 확인). 코드는 양쪽 다 이미 배포돼 env로 dormant였음(위험 푸시 0). **S4 그룹**: 스키마 D(prod db push)+수신/발송 A7+표시 D4 배포 → **실 그룹 메시지 수신·villa·Nike 표시 실작동 확인**(테스트 그룹 BBBBB, GROUP 대화 저장). **멤버이름 보강**(b725399): getGroupInfo.currentMems 비는 그룹은 getGroupMembersInfo(memberIds)로 프로필 폴백. **ETL 그룹분**(70111fd): 과거 그룹 이관 코드완료(Nike 멤버 로스터 부재→발신자 best-effort, 아바타는 런타임 보강). | **✅ ①② 완료(2026-06-23 후속)**: ① 멤버이름 v2(매 그룹메시지 발신자 senderUid+dName을 groupMembers에 점진 누적 `mergeGroupMember`, 커밋 aca9d1d) → BBBBB 실명(Binny·Taeo·Cho·Min) 검증. ② 그룹 ETL 본실행 완료(`NIKE_DATABASE_URL` 로컬 .env): 그룹 30개·메시지 12,496 삽입·멱등 skip 11,844(06-21 user분)·첨부 3,325·실패 0 → **ext/threads 462대화·그룹 30개 검증**(JUSTSHOES·CTV ISORA MARKET48명 등 실그룹). ✅ **③ Nike B6도 완료**: villa ext/messages가 senderName 해석해 내려주고(b34b31f, 실측 Taeo·Binny), Nike `villa-ext-client.ts` 어댑터가 isGroup·memberCount·senderName 통과(Nike master **2433cad3**, worktree-to-master — 타 세션 fix/mb-cancel-invariant 무접촉). Nike UI는 이미 렌더 지원 → 배포 시 그룹 버블 발신자명·그룹표식 표시. **남은 일**: ④ NIKE_WEBHOOK_URL 실도메인(테오 수정함). **S4 그룹 에픽 전부 완료**(villa-local·멤버이름·과거 ETL·Nike B6). **병렬 세션**: schema/ko/vi는 `git update-index` surgical 스테이징. 커밋 9d30f30·b725399·70111fd·aca9d1d. 계약: docs/contracts/zalo-integration-s4.md |
| 2026-06-23 | T4.6 런칭 재점검 + T4.4 실사용 테스트 키트 | **런칭 최종 점검(읽기·문서 only) + 베트남 공급자 실사용 테스트 키트 작성.** ① **프로덕션 스모크 재실측**(villa-pms-production): 공개 200·보호 7종(공실보드 `/availability` 포함) 전부 307→login·미인증 쓰기 403/401/401(villas·calendar-blocks/bulk·zalo/messages)·무효토큰 404·manifest 200 — 신규 라우트 게이트 정상. 06-16 이후 배포된 신규기능(공실보드·판매정보·강제판매·Zalo그룹·멀티관리자채팅·첨부)은 **오픈 차단요인 추가 0** 확인. docs/LAUNCH.md §6.6 재점검 섹션 추가. ② **T4.4 키트**(docs/T4.4-pilot-test-kit.md 신규): 합격기준 A~D(혼자 빌라등록+캘린더토글+막힘0+재현가능), 사전준비 체크, **vi 과제카드**(설명없이 3과제), 단계별 관찰 체크리스트(로그인·5단계 마법사·캘린더, 🟢🟡🔴 판정), 사후 설문 5문항(vi/ko), 결과판정·후속(IDEAS 우선기록 규칙). INDEX 등록. **오픈 차단요인은 여전히 전부 테오님 액션**(Zalo 라이브·시드 실행·T4.4 진행·테스트데이터 삭제). | 코드 변경 0(문서만). 병렬 세션 WIP(lib/cleaning·hold·proposal·zalo-runtime·prisma/schema·ko/vi.json·DESIGN.md·루트 LAUNCH.md·partB png) **전부 비접촉** — docs/LAUNCH.md·docs/T4.4-pilot-test-kit.md·docs/INDEX.md·TASKS.md·PROGRESS.md만 pathspec 스테이징. 계약: docs/contracts/T4.6-launch-readiness.md |
| 2026-06-23 | T-admin-availability-board (운영자 빌라별 공실 보드) | **기획→디자인→구현→독립QA→배포까지 완결, 프로덕션 라이브 검증.** 공급자가 예약을 공유하지 않는 운영 현실 반영 — 운영자가 공급자에게 확인한 공실을 날짜별로 잠그는 채널매니저식 보드 신설. ① **TDA**: Villa.availabilityCheckedAt(마지막 확인일) additive·prod db push. ② **BE**: `getAvailabilityBoard`(빌라×날짜 집계, Booking 미조회=재고/마진 누수0) + `POST /api/villas/[id]/availability-checked`(+AuditLog) + 일괄 `POST /api/calendar-blocks/bulk`(범위 잠금/해제, 공실만 잠금·MANUAL만 해제, ICAL/예약/과거 skip, $transaction+advisory lock+AuditLog, SUPPLIER 스코프 단일라우트 동일 복제) + `minDate` 클램프(과거 컬럼 미생성). calendar-blocks 단일 POST/DELETE에 ADMIN 허용 additive(SUPPLIER 자기빌라 스코프 보존). ③ **FE**: `/availability`(가로 스크롤 타임라인 3개월·sticky 빌라명·셀3상태 공실/MANUAL/ICAL·마지막확인일 뱃지✓/⚠·검색/지역/확인필요만 필터) + **가로 드래그 범위 선택→팝오버(잠금N/해제N, 마우스·펜 전용, 단일클릭 회귀0)** + 과거숨김(오늘부터·현재기간 prev 비활성). 디자인 design/stitch/b11-availability-board. ④ **사이드바 10번째 메뉴 「공실 보드」**(빌라 다음) — ADR-0003·DESIGN.md 9→10 정합. ⑤ **VI 번역 누락 수정**(스크린샷 깨짐): availabilityBoard가 ko.json에만 있어 VI 전환 시 키 원문 노출 → vi.json 전체 추가(LOC). **독립 QA 2회 PASS**(권한누수4종·SUPPLIER스코프·일괄로직·과거숨김). 프로덕션 라이브 확인: bulk API 401(배포)·VI 정상렌더(깨진키0)·과거숨김(6/22부터)·드래그 사용자 직접확인 OK. | **병렬 세션 고경합 대응**: ko.json/vi.json/DESIGN.md/PROGRESS가 타 세션(Zalo그룹·라이트박스·편의시설·토글버그·worktree) 동시편집 → 내 변경만 surgical 스테이징(ko=HEAD blob 직접삽입+`git update-index`, vi/DESIGN=hunk 격리 `git apply --unidiff-zero`, 코드는 pathspec). prisma/schema(타세션 v1.3b)·lib/cleaning·hold·proposal·LAUNCH·partB 전부 비접촉. 커밋 b3295d6(선점)·db44ca0(보드)·377c808(메뉴)·e8aff26(DESIGN)·db625ed(드래그+과거+VI). 교훈 메모리: [[admin-screens-need-vi-json]]. 계약: docs/contracts/T-admin-availability-board.md |
| 2026-06-23 | 토글 버그 + ADMIN 빌라 직접등록 + worktree 격리 도구 | **3건 완료·푸시·라이브 검증.** ① **토글 안 바뀜 버그 수정**: 수영장/조식(step-basic)·체크아웃 파손여부(checkout-form) 토글이 숨겨진 체크박스 `peer-checked` CSS에 의존 → 환경따라 노브 미동작. **React 상태(checked) 직접 제어**로 교체(`translate-x-*` 조건부 클래스), 옛 peer-checked 0건. ADMIN 라이브로 노브 동작 확인. ② **ADMIN 빌라 직접등록(T-admin-villa-register)**: `POST /api/villas` ADMIN 허용+`supplierId` 필수·검증(존재·role=SUPPLIER, SUPPLIER는 세션 강제 회귀 0), `/my-villas/new` ADMIN 진입+공급자 select(필수), villaCreateSchema supplierId optional, i18n ko/vi wizard.basic.supplier* 키, **vitest 7건**. 테오 결정=등록 시 공급자 목록 선택. 스키마 무변경(TDA a16 판매정보 폼과 별개). 커밋 a37c3b8 + 키 4dcad19. ADMIN 라이브: `/my-villas/new` 200+공급자 드롭다운 노출. ③ **병렬 세션 worktree 격리 도구**(177f89b): `scripts/wt-new.ps1`(격리 worktree·자기 인덱스·dev포트·node_modules 정션·.env)·`wt-finish.ps1`(공유폴더 미접촉 origin/main FF 갱신+정션 안전정리)·`docs/parallel-worktree.md`(INDEX 등록 6e7cb76). 생성→정리 1사이클 실증. | **병렬 세션 난전 메모**: 작업 내내 타 세션들이 공유 인덱스에 `git add`/`commit` → 내 staged 파일이 타 커밋에 휩쓸리는 사고 반복. i18n 키는 `git update-index`로 HEAD 기준 내 3키만 surgical 스테이징(타 세션 adminVillas 검색·area필터·lightbox·rangePopover 키 working tree 보존), 코드는 pathspec 커밋으로 분리. **이 사고 자체가 worktree 도입 동기** → [[parallel-session-worktree-isolation]]. dev서버는 stale .next/동시 빌드로 500 반복(타 세션 npm install 시 node_modules/.bin 소실 포함) — worktree로 구조 해결. 메모리: parallel-session-worktree-isolation 기록 | **그룹방이 villa /messages에 안 뜨던 문제 해소**(리스너가 `ThreadType.User`만 처리하고 그룹 드롭 — 의도적 단순화였음). 커밋 9d30f30(배포됨). ① **스키마 D(additive, prod db push)**: `enum ZaloThreadType{USER,GROUP}` + `ZaloConversation.threadType`(@default USER)·`groupMembers Json?` + `ZaloMessage.senderUid String?`. 기존 1:1 행 USER 백필. ② **수신 A7**(zalo-runtime handleInboundEvent): `if(type!==User)return` 제거→USER/GROUP 둘 다 처리. GROUP은 threadId=그룹id(zaloUserId 슬롯)·senderUid=uidFrom·threadType=GROUP 저장, `maybeRefreshGroupMembers`(api.getGroupInfo→currentMems 스냅샷, fire-and-forget·실패 swallow). isSelf echo도 그룹 지원. **그룹 전화매칭 스킵 가드**(`!isGroup` — 다중 발신자 User.zaloUserId 오염 방지). 자동번역/STT/OCR 그룹 적용. ③ **발송**: 발송 함수 7종에 `threadType:ThreadType=User` additive 파라미터, messages·ext/send 라우트가 conv.threadType=GROUP이면 ThreadType.Group. ④ **표시 D4**(messages/page·inbox·chat-pane): 인박스 그룹 아이콘·멤버수 칩, 버블 발신자명·아바타(senderUid→groupMembers, 미해석 시 원문/이니셜 폴백 R14). ⑤ **ext DTO**(B6용): threads에 threadType·groupMembers(sanitize: zaloId/name/avatarUrl만), messages에 senderUid. **QA 독립검증 PASS(누수 0·1:1 회귀 0·차단결함 0)**. tsc 0·zalo vitest 145·next build 통과. | **잔여(배포 의존)**: Nike B6(테오 그룹 읽기)·그룹 ETL(C1)은 S1/S2/S3 배포 후. 그룹 실수신/발송 Playwright 실측은 봇 세션 활성 시. 참고: GROUP을 CUSTOMER 등으로 수동 재분류 시 기존 공유메뉴 판매가 노출 가능(기본 UNKNOWN이라 자동발화X — Phase2 "그룹 공유후보 비활성" 검토). **병렬 세션 보호**: schema/ko/vi는 `git update-index`로 내 그룹 변경만 surgical 스테이징(타 세션 availabilityCheckedAt·lightbox·supplier 키 working tree 보존). 계약: docs/contracts/zalo-integration-s4.md |
| 2026-06-22 | 빌라 판매정보 확장(ADR-0011)·강제판매(ADR-0012) | **데이터 수집 기획→설계→디자인→구현→배포 완결.** 판매용 정보 4그룹(②잠자리 `VillaBedroom`·③위치·④이용규칙·⑤셀링포인트 `VillaFeature`) 추가: schema additive(Villa 스칼라14+모델2+enum2, prod `db push` 무손실)·`lib/features`·`lib/bedding`·ADR-0011 / **ADMIN 판매정보 입력폼**(`/villas/[id]` 탭 `sales-editor` + `PATCH /api/villas/[id]/sales` 단일트랜잭션·wifi 마스킹) / **공개페이지 /p 표시**(`villa-sales-section`, **wifi `select` 제외 누수가드**) / **공급자 vi 표시**(`my-villas/[id]`, 디자인 `a16-sales-info-vi`). 단계별 독립 QA PASS(누수0)·전부 배포. **강제 판매가능(ADR-0012)**: ADMIN 검수게이트 오버라이드 `lib/villa-gate.ts` 별도경로(`lib/cleaning` 무수정)+`POST /force-sellable`+확인모달, QA 하드게이트 6/6. | 사진수집은 구글드라이브(bizcomhome "빌라데이터수집") 병행. **잔여**: 공급자 청소화면 라이트박스·신규 vi 라벨 LOC 감수·제안 생성 성공모달 배경닫기(테오 결정 대기). 선별 스테이징(타세션 zalo/availability/cleaning WIP 비접촉) |
| 2026-06-22 | 이미지 라이트박스·UI 정리 | 사진 클릭 확대 라이트박스 전 화면 적용(관리자빌라 `photo-gallery`·공개제안 `photo-carousel`·검수+체크아웃 공용 `components/image-lightbox`·공급자빌라 `photo-lightbox`·채팅 `messages/photo-lightbox` — **이미지뷰어 5곳**) + 원본직접로드(`unoptimized`)·인접 프리로드로 모바일 로딩 속도개선. **배경 클릭/터치 닫기 일관화**: 라이트박스5 + 확인모달2(강제판매·빌라반려 — 제출중 보호·내부 `stopPropagation`). 요율표 입력칸 여백 추가 + 가로 스크롤 제거. | 제안 생성 성공모달만 배경닫기 의도적 제외(링크 복사 유도, 테오 결정 대기). typecheck clean·전부 배포. 로컬 `build`는 타세션 prisma 엔진 락으로 차단됐으나 Railway 클린빌드(타입체크 게이트 통과) |
| 2026-06-21 | S5 forward·alias·음성STT 동등 구현+QA (Nike↔villa Zalo) | **S5 코드 게이트 통과(QA 독립검증 FAIL 0, 7 PASS·2 배포후)**. ① **villa(허브)** forward: `sendChatForwardAsAdmin`(zalo-runtime, api.forwardMessage 래핑·텍스트 전용·BotSendResult 정규화) + ext send `kind:"FORWARD"`(message min1→빈값 400). alias 양방향: 신규 `app/api/zalo/ext/nickname/route.ts`(시크릿 게이트·ownerAdminId 테오 서버고정·404·trim max40·AuditLog, conversations route 수정0). 음성STT: `transcribeVoice`(gemini.ts, audio inline_data·키게이트·30s·로그0) + `maybeTranscribeVoice`(zalo-inbound, OFF스킵·voiceUrl 15s GET→STT→ko번역→translatedText UPDATE→재push 멱등·try/catch swallow) + handleInboundEvent INBOUND voice 트리거(fire-and-forget). **BLOCKER 해소**: chat-pane voice 카드에 translatedText STT 자막 렌더(ko/vi voiceTranscript 키)—기존엔 SimpleTypeCard가 무시. ② **Nike(별도레포·미커밋)** `villa-ext-client` villaExtForward/villaExtSetNickname+FORWARD분기 / zalo.ts changeAlias·removeAlias(null) 테오→ext nickname 위임·getVoiceTranslations 테오 no-op / route.ts forward 케이스 테오 분기 / chat page 원본 text 전달. voice STT 표시는 adaptVillaMessage translatedText로 자동. ③ **QA**: forward·STT FE painted·STT graceful·alias 양방향·누수0·회귀 전부 PASS. villa typecheck0·build·vitest 807/808(seed-data 타세션 무관). 스키마 diff 0·신규deps0·신규env0(GEMINI_API_KEY·S1시크릿 기존) | **배포 미실시 — 테오 액션 대기**(S1 활성 전제, 스키마 무변경). 순서: S1활성 확인→villa A6→Nike B5. 배포후 실측: forward 실송·alias 양방향 표시·voice STT 자막. 첨부 forward·STT 원문(vi) 병기는 범위밖(IDEAS). 커밋: 내 경로만 선별(타세션 cleaning/hold/proposal 제외) |
| 2026-06-21 | S3 과거 대화·첨부 ETL 이관 구현+QA (Nike↔villa Zalo) | **S3 코드 게이트 통과(QA 독립검증 8/8 PASS, FAIL 0)**. 신규 `scripts/etl-nike-zalo.ts` 일회성 ETL(`npx tsx ... [--dry-run] [--limit] [--attachments-only]`). ① **C1 대화·메시지**: Nike `ZaloThread`+`ZaloMessage`(테오 accountId·`threadType="user"`만, 그룹 skip→S4) → villa `ZaloConversation`+`ZaloMessage`. 대화 upsert(ownerAdminId_zaloUserId), 메시지 zaloMsgId findUnique skip(멱등), id 신규 cuid, direction/source/msgType(image→photo)/createdAt 보존/reactions 별테이블→Json집계/status=SENT. **quote 2-pass**(globalMsgId→zaloMsgId 사전→인용메시지 자신 zaloMsgId로 quotedMsgId UPDATE·미해석 null+스냅샷). 대화메타 재계산(lastMessageAt/lastInboundAt/unreadCount=0). ② **C2 첨부 영구보존**: **H1 원본 우선** originalUrl GET(20s)→실패시 thumbData 폴백→없으면 skip, R2 고정키 `nike-attach/{zaloMsgId}/{index}`+HeadObject skip(멱등), villa 절대URL, original/thumb-fallback/failed 분리집계, 동시성4+재시도1패스. **H2** ocrTranslatedText→메시지 translatedText 보강(빈 경우만). ③ **보안**: Nike read-only($queryRawUnsafe SELECT만, write 0), credentials 미select, ownerAdminId=getSystemBotOwnerId(null중단), 마진 비참조, NIKE_DATABASE_URL 미출력, raw SQL 전부 $1 파라미터(인젝션0)·컬럼명 Nike스키마 1:1 대조. dry-run 전 write경로 차단·env없을때 안전종료. 신규 deps 0(@prisma/client datasourceUrl로 Nike 읽기). villa 스키마 무변경. typecheck 0 | **미실행 — 테오 실DB 작업 대기**. 실행 전: villa env `NIKE_DATABASE_URL`(읽기전용 role 권장)·`NIKE_THEO_USER_ID`(기본 cmmdavtkx00001dqytx9v1ss2)·`STORAGE_*`(R2) 설정 + **`npm i -D tsx`**(미설치 확인됨). 순서: S1활성 확인→`--dry-run` 검증→본실행→멱등 재실행(증가0)→첨부 표본(원본해상도·절대URL 200). 경미후속(IDEAS): R2 설정 storage.ts와 중복(향후 동기화 리스크). 커밋: scripts+.env.example만 선별(타세션 cleaning/hold/proposal 제외) |
| 2026-06-21 | S2 채팅 읽기 정본+실시간 수신(webhook→SSE) 구현+QA (Nike↔villa Zalo) | **S2 코드 게이트 통과(QA 독립검증 8/8 PASS, FAIL 0)**. ① **villa(허브)** 신규 `lib/zalo-ext-auth.ts`(S1·S2 공통 시크릿게이트 timingSafeEqual+테오 owner 서버결정) / `app/api/zalo/ext/threads/route.ts`(A2 대화목록, page.tsx 인박스 select L122-143 동일) / `app/api/zalo/ext/messages/route.ts`(A3 `?conversationId&before&limit`, where{id,ownerAdminId:테오}→404, 스레드 select L205-219) / `lib/zalo-webhook.ts`(A4 pushInboundToNike — HMAC-SHA256 `${ts}.${rawBody}` 서명·AbortController 4s·attachmentUrls 절대URL 보정·env미설정 no-op·실패 swallow). 변경: send route 공통헬퍼 추출(동작불변)·`lib/zalo-runtime.ts` handleInboundEvent 두 저장지점(INBOUND L712·OUTBOUND echo L669) `saved===true` 직후 push 1줄씩(L651-654 순수비텍스트 조기return 제외). villa 스키마 무변경. ② **Nike(C:\Projects\Nike, 미커밋)** `villa-ext-client.ts` 읽기클라(villaExtGetThreads/Messages)+어댑터(adaptVillaThread/Message 방어적) / B3 `zalo/route.ts` 테오 read4(recentchat/conversation/messages/poll) 위임을 **503게이트(L313) 위(L178-256)로** 끌어올림(poll 503사망 회피) / B4 신규 `ext-inbound/route.ts`(raw body HMAC검증·±5분·timingSafeEqual·zaloMsgId LRU dedup→기존 emitZaloMessage 호출, SSE파이프 무변경). ③ **QA**: 시크릿·HMAC4케이스·누수0(공유후보 L277-420 미복제)·스코프격리404·push트리거정확·poll게이트·멱등·무변경보호(use-zalo-sse/events/sse-emitter unchanged) 전부 PASS. villa typecheck0·build·vitest 802/803(seed-data 1실패=타세션 무관) | **배포 미실시 — 테오 액션 대기**(S1 활성 전제). 배포순서: S1활성확인→villa A2·A3·A4(env `NIKE_WEBHOOK_URL`·`ZALO_WEBHOOK_HMAC_SECRET`)→Nike B4(동일 HMAC secret)→Nike B3. 배포후 실측 4: 실시간 webhook→SSE·폴백poll·읽기정본일치·세션단일성. Nike 전체build는 타세션 멀티지점(branchId 69에러) WIP로 막힘(내 3파일 tsc 0, 무관). 커밋: 내 경로만 선별(타세션 schema/auth/branch 제외) |
| 2026-06-21 | S1 세션 단일화+발송 위임 구현+QA (Nike↔villa Zalo) | **S1 코드 게이트 통과(QA 독립검증 FAIL 0)**. ① **villa(허브)** 신규 `app/api/zalo/ext/send/route.ts`(POST) — 서버-서버 발송 위임. `x-zalo-ext-secret` `crypto.timingSafeEqual` 게이트(미설정/없음/불일치→401), ownerAdminId는 요청 미수신·서버에서 `getSystemBotOwnerId()`→env `ZALO_SYSTEM_OWNER_ID`로만 결정(미해석 503), 기존 sendChat{Message,Image,Reply}AsAdmin·addReactionAsAdmin 재사용(신규 발송로직 0), 응답 DTO 최소필드(ok/kind/messageId)·credential 0. 본문 zod discriminatedUnion(TEXT/IMAGE base64/REPLY/REACTION). villa 스키마 무변경. ② **Nike(C:\Projects\Nike, 별도레포·미커밋)** 신규 `src/lib/villa-ext-client.ts`(isTheoUser=`ZALO_THEO_USER_ID` 분기·미설정시 false→기존동작/villaExtSend 20s·실패 throw/reactionIconToVillaKey) + B1 zalo-pool.ts 로그인 가드 3경로(connectAllUsers continue·connectUser return false·startQRLoginForUser throw) + qr/route.ts managed_by_villa + B2 zalo.ts 3발송함수 테오 분기 위임(additive 112ins/0del). ③ **QA 판정**: 시크릿게이트·ownerAdminId주입차단·credential비노출·타관리자격리(ADR-0007)·Nike분기안전성 전부 PASS, 양레포 typecheck clean. 완료기준 9종 중 5~8 PASS, 1~4(실송수신·code3000부재)는 **배포후 항목**(테오 협조) | **배포 미실시 — 테오 액션 대기**. 배포 절대순서: villa 배포 → Nike B1(env `ZALO_THEO_USER_ID`=테오 Nike User.id 설정 후) 배포·테오 미로그인 확인 → villa `__system__` 단독 connected 확인 → Nike B2(env `VILLA_EXT_BASE_URL`+`ZALO_EXT_SHARED_SECRET`) 전환. villa env: `ZALO_EXT_SHARED_SECRET`. Nike 커밋·푸시는 테오 검토 후. vitest는 환경 Node24 비호환으로 미실행(S1 무관, Node20에서 재실행 필요). 타 세션 WIP(lib/cleaning·hold·proposal·LAUNCH.md·partB png) 비접촉, 선별 스테이징 |
| 2026-06-18 | PM — Nike↔villa Zalo 통합 에픽 등록 + S1 착수 준비 | ADR-0010(채택·A안·풀스펙 확정)을 작업 큐에 등록(코딩 0, 문서만). ① **TASKS.md**: "신규 에픽 — Nike↔villa Zalo 통합" 섹션 추가, S1~S5를 우선순위·의존도와 함께 등록(S1=현재 스프린트 [~]). ② **S1 계약서** docs/contracts/zalo-integration-s1.md 작성(착수 선점 단독 커밋 예정): 범위(villa A1 ext/send·A5 인증격리 / Nike B1 로그인제거·B2 발송위임), 완료기준 9종(텍스트·이미지 실발송·Nike 미로그인·code 3000 무발생·시크릿 401·credential 미반환·ownerAdminId 고정·타관리자 격리·호출부 무변경), QA 검증 6단계, **수정 금지 구역**(villa: lib/cleaning·hold·proposal·INDEX.md·LAUNCH.md 등 타 세션 WIP / Nike: 별도 레포·세션 경계), 보안체크(credential 비노출·ownerAdminId 하드코딩·채팅 전용 DTO), 배포 순서 절대규칙(Nike B1 先→villa 허브 단독 확인→B2). ③ **PROGRESS 핸드오프** 갱신(아래 §Nike↔villa Zalo 통합 에픽). 확정 의사결정 기록: A안/SPOF 수용/ETL/그룹 포함/첨부 재업로드/기능 동등 | **사용자 확정사항(테오 2026-06-18)**: ① A안(villa 허브 단독, Nike는 ext API) ② SPOF 수용(villa 다운 시 양쪽 정지 트레이드오프) ③ 과거 대화 ETL 이관 ④ 그룹채팅 포함 ⑤ 첨부 바이너리 R2 재업로드 영구보존 ⑥ forward/alias/음성STT 기능 동등 구현. 코딩 0(문서만). 타 세션 WIP(lib/cleaning·hold·proposal·INDEX.md·LAUNCH.md·partB png) 비접촉 — 신규 파일(계약서)+TASKS·PROGRESS 자기행만 선별 스테이징 |
| 2026-06-16 | T-admin-quick-date-filter | ADMIN 8개 목록 화면에 날짜 빠른 필터 바(QuickDateFilter) 추가 완료(회의→기획→디자인→구현→독립QA). ① **공유 인프라**(커밋 184f8ff): lib/date-vn에 resolveQuickRange/vnDayStartUtc/quickRangeWhere(VN UTC+7 고정·주=월요일 시작·반개구간 [from,to)·date kind=@db.Date 자정 비교/timestamp kind=−7h 오프셋) + components/admin/quick-date-filter.tsx(pill 버튼 바 `[✓전체][오늘][어제][이번주][지난주][이번달][지난달][다음달]`, ?range= 동기화, presets prop로 노출 제어) + design/stitch/quick-date-filter 디자인 레퍼런스 + i18n quickDateFilter ko/vi 9키 + 단위테스트 22건. ② **8개 페이지 연결**: 예약(checkIn,date,8종·range 우선 월대체)·정산(yearMonth 변환,월3종)·제안(createdAt,client배열필터)·검수(createdAt,RSC)·활동(createdAt,후필터)·비용경보(detectedAt,RSC)·메시지(lastMessageAt,RSC·ownerAdminId스코프)·사용자(createdAt,RSC). ③ layout.tsx ADMIN_CLIENT_NAMESPACES에 quickDateFilter 등록(client 라벨 MISSING_MESSAGE 방지). 검증: typecheck 0·next build 컴파일+타입검증 통과(manifest ENOENT는 타 세션 dev 서버 .next 점유 환경충돌)·date-vn 22건+intl 5건 green. **QA 독립평가 PASS**(블로커 0, 누수 4종 이상무 — select 화이트리스트 무변경·where만 추가). 계약: docs/contracts/T-admin-quick-date-filter.md | **범위 정정(2026-06-16 사용자 지시)**: 메시지·사용자 2곳은 날짜 버튼 불필요 → 제거(기능 이전 상태로 완전 복원, typecheck 0). 최종 적용 6곳(예약·정산·제안·검수·활동·비용경보). **후속(경미)**: activity는 loadActivityFeed(최근50건 캡) 후필터라 오래된 범위 일부 누락 가능 — 공유 로더(lib/dashboard) where 주입은 별도 태스크로. 라이브 Playwright는 타 세션 dev 서버 점유로 보류(QA 정적 통과). 타 세션 WIP(lib/cleaning·hold·proposal·prisma/schema·zalo) 비접촉, 선별 스테이징 |
| 2026-06-16 | TDA — 채팅 상대 분류 5종 확장 (ADR-0009 개정2) | 스키마·설계·db push까지 TDA 범위 완료. ① **ADR-0009-chat-attachments.md 개정2(2026-06-16) 분류 5종 확장** 섹션 추가(R2-1~R2-6: D1 분류·D2 누수 그룹·통화 매핑·번역 기본값·메뉴 가시성·additive 판정) + 상단 개정2 라인·결과 1줄 요약. ② **schema.prisma**: ZaloCounterpartyType에 TRAVEL_AGENCY(VND)·LAND_AGENCY(VND) 추가(additive enum), 주석에 누수 그룹·통화 명시, 헤더 v1.2a. ③ **npx prisma db push** 프로덕션(Railway) DB 적용 성공 — 백필 불필요(기존 행 SUPPLIER/CUSTOMER/UNKNOWN 유지). ④ prisma generate 신규 enum 반영 확인(Windows EPERM은 엔진 DLL rename 락일 뿐 — 동일 바이너리·코드/배포 무영향, 스테일 .tmp 36개 정리). ⑤ **typecheck 0 + next build 통과**(전 라우트 컴파일, /messages·share route 정상, 빌드 차단 0) | 핫 세션 무충돌(PROGRESS.md만 갱신). **후속 인계**: ▸ BE — share/route.ts 게이트(=== CUSTOMER 단일비교 → 판매가측 그룹 판정)·handleVilla 고객경로 saleCurrency=KRW 하드코딩 제거(분류값으로 통화 결정), lib/zalo-counterparty.ts 헬퍼 권고(isSellSideType·isCostSideType·currencyForType), lib/zalo-share.ts 빌더 무변경. ▸ FE — counterparty-control.tsx 여행사·랜드사 옵션·chat-pane 첨부 메뉴 가시성 그룹 함수 기반. ▸ LOC — 분류 라벨 i18n(여행사/랜드사 ko·vi). BE/FE/LOC 후속 태스크 생성은 PM 판단 |
| 2026-06-16 | 보안 (Phase 1) T-sec-csp-report | CSP **Report-Only** 롤아웃 + Permissions-Policy 완료(T4.9 후속 ② CSP — 안전 롤아웃). next.config.ts: Permissions-Policy(camera/mic/geolocation=() — 미사용 grep 0건 확인) + Content-Security-Policy-Report-Only(실제 소스 반영: Google Fonts·R2·picsum·googleusercontent·data, script/style unsafe-inline은 Next 인라인용·nonce화 후속) + report-uri. 신규 app/api/csp-report(위반 수신 204·로그 JSON.stringify 이스케이프로 인젝션 차단·2000 슬라이스·IP 120/10분). typecheck 0, QA 독립 평가 **통과(5/5, 결함 0)** — Report-Only 무중단·미사용기능 정확·소스 누락 0 검증. 커밋 f92fdb0. **후속: 배포 후 헤더 curl 스모크 → 리포트 관찰 → enforce 전환(nonce script-src)**. 경미 백로그: 429 Retry-After·바디 크기 한도. 계약: docs/contracts/T-sec-csp-report.md | next.config.ts clean 확인 후 추가만+즉시 커밋 |
| 2026-06-16 | 보안 (Phase 1) T-sec-public-hardening | 공개 표면 하드닝 완료. ① 전역 HTTP 보안 헤더 5종(next.config.ts headers — HSTS 2y·nosniff·X-Frame-Options SAMEORIGIN·Referrer-Policy strict-origin-when-cross-origin[제안 token referrer 누수 차단]·DNS-Prefetch off) ② 공개 HOLD 엔드포인트 rate limit(토큰 15/10분 스푸핑불가·IP 30/10분, 초과 429, lib/rate-limit 재사용). typecheck 0, QA 독립 평가 **통과(5/5, 결함 0)** — 헤더 부작용·과잉차단·회귀 무해 확인. 커밋 ce55cb2·4a6e501. **비차단 후속: 배포 후 헤더 curl 스모크 1건(OPS)**. CSP는 후속(CDN/인라인 검증). 계약: docs/contracts/T-sec-public-hardening.md | next.config.ts Zalo 커밋 후 clean 확인, 추가만+즉시 커밋 |
| 2026-06-16 | 보안 (Phase 1) T-sec-auth-ratelimit | 로그인·가입 무차별대입/스팸 방어 완료(테오 지시 — Phase 2 보류, Phase 1 보안 우선). lib/rate-limit.ts(인메모리 슬라이딩 윈도우·now 주입·clientIp) + auth.ts authorize(전화번호 5회/10분·IP 20회/10분, 초과 시 bcrypt 생략 잠금, 성공 시 phone 키만 리셋) + signup/actions.ts(IP 10회/시간). vitest 9개, typecheck 0. QA 독립 평가 **조건부→통과**(코어 9/9·차단결함0, 1차방어 전화번호 한도 스푸핑불가). 조건 반영: IP 성공리셋 제거·계약 정정·XFF 한계 명시. 커밋 c759777. **후속 보안 백로그: XFF rightmost 보정(프로덕션 토폴로지 실측 후)**. 계약: docs/contracts/T-sec-auth-ratelimit.md | 권한누수(T4.1)와 다른 차원. 핫 세션(next.config·schema·messages·zalo) 무접촉 |
| 2026-06-16 | T7.1 부가서비스 BE (Phase 2 착수) | 부가서비스(ServiceOrder) 판매 백엔드 완료 — lib/service-order.ts(상태 전이표 REQUESTED→CONFIRMED→DELIVERED·*→CANCELLED 종결 가드, 입력 검증, 마진 KRW=판매가−원가VND→KRW suggestSalePriceKrw 재사용) + GET/POST /api/bookings/[id]/services + PATCH /api/services/[id]. **마진 비공개: 전 라우트 ADMIN 강제**(원가·판매가·마진 노출 차단), AuditLog CREATE/UPDATE diff. 스키마 변경 0(모델 완비)·deps 0·공유파일 무접촉. vitest 17개, QA 독립 평가 **통과**(7/7, 누수 0건, 실측 401, Minor 2건 즉시 반영 — GET 마진 항목별 격리·costVnd 정수 검증). 커밋 c4d8ad7. **UI는 Stitch 디자인 선행 후속(T7.2)** — 디자인 없이 BE 우선(T2.1 패턴). 계약: docs/contracts/T7.1-service-order-be.md | Phase 2 첫 착수. 핫 세션(cleaning/hold/proposal·zalo-runtime·ADR-0006) 무충돌 |
| 2026-06-16 | 프로덕션 스모크 (T4.7 PWA·보호라우트) | 배포 후 스모크 잔여 종결(읽기 전용·파일 비접촉). villa-pms-production.up.railway.app 실측 8종 전부 PASS: **T4.7 PWA** /manifest.webmanifest 200 application/manifest+json(본문 검증 — standalone·lang vi·theme #0D9488·bg #F8FAFC·portrait·icons SVG any+maskable, 계약 정합)·/icon.svg 200 image/svg+xml·/apple-icon 200 image/png 2173B(next/og PNG 프로덕션 렌더 정상). **인증 게이트**: /dashboard·/my-villas 미인증 307→/login·/login 200. **공개 토큰**: /p/invalid 404(무효 토큰 처리). T4.7 잔여 중 HTTP 스모크 해소 — 모바일 설치 프롬프트 실기기 실측만 잔여 | tsc/test 무관(인프라 검증). 타 세션 WIP(service-order.ts·ADR-0006·cleaning/hold/proposal) 비접촉 |
| 2026-06-16 | QA-eval (T3.6·T6.3-rates) | 보류됐던 독립 QA 게이트 2건 종결(작성자≠평가자 — 본 세션은 양쪽 미작성). **T3.6 여권 Zalo 전달 PASS**: 완료기준 6종 전수 충족 — lib/tamtru.ts 화이트리스트 payload(villaName·guestName·checkIn·passportPhotoUrls)·tamtru.test 8건(401/403/404/NO_CHECKIN·NO_PASSPORT 400/성공+큐잉+AuditLog/재전달/미연결 경고/payload 오염차단)·lib/zalo.ts TAMTRU_PASSPORT 빌더 마진·연락처 무노출·FE action-panel 전달 전후 분기(여권 존재 조건·전달완료 HH:MM·재전달·미연결 토스트). PROGRESS가 요청한 "토큰만료·타임아웃 실패케이스"는 적정 계층(lib/zalo.test isRetryableFailure)에 이미 존재 — TOKEN_NOT_SET 영구 재시도·일반 타임아웃 3회 미만 재시도. **T6.3 rates-api PASS**: villa-rates-api.test 13건이 라우트 실거동과 정합(supplierCostVnd update 제외 불변식 route:105↔test:140, 응답 {updated,skipped} 본문 누수 0 route:145↔test:175, AuditLog BigInt 문자열 diff). 코드 결함 0 — 제품/테스트 무변경 | 비차단 관찰 2건(결함 아님): ① rates 라우트가 salePriceVnd<원가(음수 마진) 미차단 — ADMIN 신뢰·Phase1 수동 오버라이드 허용 ② AuditLog가 $transaction 밖 기록(타 라우트와 동일 best-effort). 기준선 green: tsc 0·vitest 493/493. 타 세션 WIP(lib/cleaning·hold·proposal T4.1 타임아웃, LAUNCH.md) 비접촉 |
| 2026-06-16 | T6.3-rates-api-test | 운영자 마진·판매가 PUT 엔드포인트(`/api/villas/[id]/rates`) 단위 테스트 12개 추가(커밋 6d4f93e) — 사업 핵심 원칙 2(마진 비공개) 누수 가드. 가격 수식(pricing.test.ts)만 있고 API 계약은 무테스트였던 공백 해소: 401/403(SUPPLIER·CLEANER)+권한차단 시 DB 미접근, 400(INVALID_BODY·음수 KRW·비숫자 마진·중복 시즌·빈배열·max3 초과), 404, 성공 영속(margin·salePrice 저장 + **supplierCostVnd update data 제외 불변식** — 공급자 입력 영역 수정 차단), skipped(레코드 없는 시즌 미생성), AuditLog VillaRate UPDATE old/new diff, **응답 누수 0**(BigInt·원가·마진 본문 미포함). 제품 코드 무변경(결함 미발견). 부수 정리: `.playwright-mcp/`·`qa-tmp-*.txt` .gitignore 추가(병렬 세션 git add 사고 방지) | 신규 12/12 통과·전체 vitest 회귀 0·내 파일 typecheck 0. QA 독립 평가(작성자≠평가자) 잔여. 계약: docs/contracts/T6.3-rates-api-test.md |
| 2026-06-16 | T6.7-admin-responsive | 관리자 반응형 마무리 완료(Ralph Loop 자율 검증, 코드 변경 0): (admin) 전수 정적 감사 결과 반응형 인프라 이미 완비·결함 0 — 드로어(layout `main lg:pl-64 pt-14`+sidebar `lg:hidden` 햄버거·오버레이, 경로 변경 시 자동 닫힘)·목록 카드(b5·b12·b13 ResponsiveTable, b9 grid-cols-1 md:2, settlements `md:block`/`md:hidden` 수동 카드, inspections flex)·가로 넘침(특수 표 5곳 전부 의도된 overflow-x-auto/2열 미니바). 완료기준 7종 충족: typecheck exit 0·lint 에러 0·vitest **443/443**(zalo.test 24/24 회복). 타 세션 미커밋 WIP 존재 상태에서도 전부 green. `<promise>T6.7-DONE</promise>` 판정 | 이전 반복 미충족(완료기준 5·7)은 T1.2b의 VILLA_REJECTED 다운스트림(zalo buildNotificationText case 누락)이 원인 → **T3.6이 case 보강(lib/zalo.ts:220)**하여 해소. 라이브 360×800 Playwright는 QA 후속. 계약: docs/contracts/T6.7-admin-responsive.md |
| 2026-06-16 | T1.2b-villa-reject | 빌라 반려(REJECT) 플로우 완료(백엔드+재제출, 커밋 4367911 main 통합): 스키마 VillaStatus.REJECTED+Villa.rejectionReason+NotificationType.VILLA_REJECTED(db push). PATCH /api/villas/[id] REJECT(updateMany 상태 가드 count!==1→409, rejectionReason 필수, VILLA_REJECTED Notification enqueue, AuditLog), PUT 재제출(SUPPLIER·supplierId+REJECTED 가드, villaCreateSchema 재사용 전체 교체, status→PENDING_REVIEW·rejectionReason→null, 마진/판매가 리셋 불변식), 마법사 edit 모드(villaToWizardState 사진 슬롯 역매핑·초과 드롭, villaId면 PUT). vitest 19개(슬롯 매핑·401/403/404/409·사유 필수·AuditLog·Notification) | zalo buildNotificationText VILLA_REJECTED case는 T3.6이 보강(다운스트림 회복, zalo.test 3건 회복). **공급자 가시화(목록/상세 REJECTED 배지+재제출 진입 버튼)는 T1.10에 인계**(my-villas/* 동시 작업). 계약: docs/contracts/T1.2b-villa-reject.md |
| 2026-06-16 | T3.6-tamtru-passport | 여권 Zalo 전달(임시거주신고) 완료: lib/tamtru.ts(sendTamTruPassport — $transaction: TAMTRU_PASSPORT 큐잉 + CheckInRecord.tamTruSentAt 갱신 + AuditLog, 전제 검증 NO_CHECKIN/NO_PASSPORT, 재전달 허용) + POST /api/bookings/[id]/tamtru(ADMIN 401/403, 404/400) + FE b3 버튼(action-panel CHECKED_IN: "공급자에게 전달"/재전달, tamTruSentAt 시 "전달 완료 HH:MM"(VN), Zalo 미연결 경고 토스트) + i18n adminBookings.detail.actions.tamtru(ko/vi 구조 동일). **Zalo 이미지 발송 기술판단**: sendZaloText는 텍스트 전용 → Phase 1은 텍스트 알림 + passportPhotoUrls를 payload·ZaloMessage.attachmentUrls 미러에 증빙 기록(extractAttachmentUrls, msgType image), 실 이미지 발송(OA upload API)은 잔여(dispatchOne 주석). payload 화이트리스트 4필드만(마진·연락처 차단). 부수: 공유파일 회귀로 발견된 VILLA_REJECTED 빌더 누락(HEAD에서 zalo.test 3건 fail·tsc exhaustive error) 수정. tsc 0·vitest 428 통과·build OK | QA에 실패케이스(토큰만료·타임아웃) 테스트 요청 필요. 계약: docs/contracts/T3.6-tamtru-passport.md |
| 2026-06-16 | T1.7-bank-contact | 입금 계좌·연락처 설정 UI 완료 (b8 Card 3 변환, 3라운드 디자인): /settings Card 4 BankContactForm(은행명·계좌번호·예금주·전화·카카오URL) + /api/settings 배치 PUT(entries — 5키 화이트리스트·키별 검증·빈값 clear, 단일 키 경로 무변경) + validators.ts 순수 분리 + vitest 28개. 공개 제안 완료/만료 페이지가 소비하던 AppSetting 5키의 ADMIN 입력 공백(T2.2 잔여) 해소. AuditLog diff 기록. QA 독립 평가 **통과**(8/8, 누수 0건·javascript 스킴 쓰기차단·배치 원자성·DB round-trip 라이브 실증). 커밋 85a68c9 | 병렬 세션(T1.2b·T1.10·Zalo) 무충돌 — messages는 bank 하nk만 선별 스테이징. 계약: docs/contracts/T1.7-bank-contact.md |
| 2026-06-11 | - | 사업계획서 V1.0, CLAUDE.md/SPEC.md/TASKS.md/schema.prisma 스캐폴딩 완료 | 개발 착수 전 |
| 2026-06-11 | - | 에이전트 체계 완성: 기존 8개 + 신규 3개(DESIGN·OPS·LOC) 섭외, 스킬 3종 추가, CLAUDE.md/INDEX.md/TASKS.md 반영 | DESIGN은 Stitch MCP 직접 사용 |
| 2026-06-11 | - | 오픈 스펙 확정(ADR-0002): SPEC v1.1(F0 가입·F6 최소 정산·엣지 케이스), 스키마 AuditLog·AppSetting 추가, LAUNCH.md 신설, 태스크 6건 추가 | 오픈 이슈 4건 해소 |
| 2026-06-11 | - | SPEC v1.2: F7 운영자 대시보드·예약 관리 추가 (관리자 IA·대시보드·예약 목록·청소 검수 목록), DESIGN.md B5~B8 프롬프트 추가, 태스크 반영 | 관리자 페이지 기획 완결 |
| 2026-06-11 | T1.0/T2.0/T3.0/T4.0 | Stitch 1차 디자인 16장 전체 생성 완료 (A0~A4, B1~B8, C1) → design/stitch/ 저장, 디자인 시스템 2종 | 테오 검토 대기, 수정은 edit_screens |
| 2026-06-11 | 디자인 검수 | 기획 대비 검수: 누락 8장 추가(마법사 2·4단계, 내 빌라, 내 수익, 빌라 목록·상세, 예약 상세, 제안 목록) → 총 24장. 불일치 수정: C1 탭바 제거, B2 수수료 블록 제거, 사이드바 8메뉴 통일, A5 팁 카드 제거 | SPEC F1 마법사 4단계 확정, DESIGN.md 프롬프트 8종 추가 |
| 2026-06-11 | 디자인 전체 회의 | PM·UX-VN·FE·QA 4자 검토 → **조건부 통과**. 권한 누수 0건(QA). 누락 화면 4건 확정: 로그인, C1 상태 변형(만료·마감·가예약 완료), 청소 태스크 목록. 가독성 결함: A그룹 중요 6건(a6 영어 잔존, a5 원가 문구·Tết 0₫, a1 베란다 누락, a3 범례 대비, 마법사 용어 불일치), B그룹 중요 5건(b3·b6·b8 사이드바 비표준 재발, b4 VND 구분자 혼재, b3·b6·c1 영어 잔존), 환율 표기 USD 기준(b1) | 수정은 DESIGN 에이전트 edit_screens로 진행 예정, 상세는 회의록 참조 |

| 2026-06-11 | T5.1~T5.3 | 회의 후속 완료: 신규 4장 생성(a0-login, a8-cleaning-tasks, c2-proposal-expired, c3-booking-request) + 기존 12장 결함 수정 + LOC 2차 수정 + b8-users→b13-users 재번호. LOC 용어 사전 확정(i18n-pattern.md). QA 재검수 **통과**(누수 0건, 회귀 9건 ✓, 용어 0건 — 반려 2건 수정 완료) → 총 28장, 디자인 단계 종결 | 잔여: T5.4 Stitch 중복 화면 수동 삭제(테오), T5.5 변환 시 처리 목록 |

| 2026-06-11 | T0.1/T0.2/T0.6 | Next.js 15 프로젝트 초기화 + Railway 배포 완료. URL: villa-pms-production.up.railway.app. PostgreSQL 연결 + prisma db push 완료. GitHub: orangetaeo/villa-pms | Tailwind v4→v3 다운그레이드, nixpacks.toml로 npm install 강제 적용 |

| 2026-06-11 | 2차 전체 회의 (테오 7건) | ① 글자 세로 낙하 전수 박멸 — 근본 원인: 한글 글리프 없는 폰트(Public Sans 등)+word-break → Noto Sans KR 폴백+keep-all/nowrap 16장 적용 ② ADR-0003 확정: 채널별 통화(여행사·랜드사 VND, 산정 기준 VND), 비품 VillaAmenity 모델+마법사 5단계, Zalo 채팅 b14(번역 통합, 48h 창), 관리자 반응형(1024px 햄버거·768px 카드) ③ 신규 3장(b14·b1-mobile·a9-amenities)+수정 9장+VND 변형 2장 → **총 33장** ④ QA 최종 **통과(반려 0건)** — 누수 0건·낙하 회귀 0건·스펙 반영 100% ⑤ SPEC v1.3, schema v1.2(validate 통과), a6·a7 재캡처 완료 | 주의: schema v1.2는 T0.2 db push 이후 변경 — **다음 세션에서 prisma db push 재실행 필요**. 잔여: T5.4(Stitch 웹 중복·고아 화면 수동 삭제), T5.5(변환 시 처리 목록), playwright-core devDependency 추가됨(OPS 인지) |

| 2026-06-11 | T0.3/T0.5 | 인증·i18n 완료: NextAuth v5 Credentials(전화번호+비밀번호), 자가 가입 /signup(SUPPLIER, AuditLog 기록), 로그인 /login — **화면은 Stitch a0-login·a0-signup 그대로 변환**(라이트 teal, Be Vietnam Pro, Material Symbols, 56px 터치 타겟). Role 미들웨어(역할별 허용 경로 맵 + locale 쿠키 자동: admin→ko, supplier·auth→vi), 라우트 그룹 (admin)/(supplier) 가드 레이아웃, lib/prisma.ts·lib/audit-log.ts(writeAuditLog), 루트 / role별 분기. Noto Sans KR 폴백+keep-all 전역 적용. 빌드·typecheck 통과, 미인증 보호 경로 6종 /login 리다이렉트 확인 | **교훈: UI는 반드시 design/stitch/ export 변환 — 초기에 임의 디자인으로 만들었다가 전면 재작업.** 메모리에 규칙 영구 저장. 잔여: 실 DB 연결 로그인 E2E(로컬 .env DATABASE_URL placeholder), dashboard·my-villas는 플레이스홀더(각 변환 태스크에서 구현) |

| 2026-06-11 | T0.4 | 이미지 저장소 완료 (ADR-0004): lib/storage.ts 백엔드 자동 선택 — STORAGE_* 5종 설정 시 Cloudflare R2(S3 SDK), 미설정 시 디스크. 인터림 Railway volume(villa-pms-volume, /data) + UPLOAD_DIR=/data/uploads — 재배포 시 파일 소멸 문제 해결. app/uploads/[name] 서빙 라우트(경로 탈출 차단·immutable 캐시), lib/image-resize.ts 클라 리사이즈(긴 변 1600px·EXIF 회전·HEIC 폴백). T1.1 세션과 lib/storage.ts 공동 작업(QA MIME 화이트리스트 반영). ADMIN 계정(테오) 시드 + 프로덕션 로그인 검증 | 잔여: 테오 Cloudflare R2 버킷+API 토큰 → STORAGE_* 입력 시 R2 전환(코드 무변경). sharp 서버 압축 미채택(nixpacks 네이티브 리스크) — 클라 리사이즈로 대체 |

| 2026-06-11 | T1.3 | 가용성 판정 완료 (병행 세션 — T1.1과 충돌 0건): lib/availability.ts 단일 소스 — 순수 판정층(overlapsHalfOpen·assertValidStayRange·evaluateAvailability: available/sellable 분리+사유 코드) + DB 래퍼층(checkAvailability·findSellableVillaIds, PrismaClient/$transaction 클라이언트 주입 — T2.3 HOLD 트랜잭션 재검증 재사용 설계). vitest 도입(`npm test`, vitest.config.ts), 단위 테스트 18개(half-open 경계·back-to-back 허용·비점유 4종 제외·검수 게이트 분리·복합 사유). typecheck·lint 통과, QA 독립 평가 **통과**(반려 0건) | QA 권고 2건을 TASKS.md에 부착: ① T2.3 — API 날짜 입력 UTC 자정 정규화 ② T2.1 — findSellableVillaIds 전체 조회는 ADMIN route 한정(누수 검사 항목). 계약: docs/contracts/T1.3-availability.md |

| 2026-06-11 | T1.1 | SUPPLIER 빌라 등록 마법사 5단계 완료: `app/(supplier)/my-villas/new` — Stitch a2(기본정보 1/5)→a2b(위치 2/5, 건너뛰기)→a1(사진 3/5, 침실·욕실 수 동적 슬롯+즉시 업로드+클라 리사이즈)→a9(비품 4/5, 4탭+미니바 스테퍼, lib/amenities.ts 품목 사전 25종)→a5(원가 5/5, 시즌 3카드+숫자 키패드, 미입력 시 제출 disabled). ICU `Bước {n}/{total}` N/5 재번호, VND 점 구분, 헤더 "Đăng ký villa" 통일. POST /api/villas($transaction: Villa PENDING_REVIEW+Photo+Amenity+Rate, supplierId 세션 강제, writeAuditLog), lib/villa-schema.ts 공유 zod. globals.css nowrap 전역 규칙(T5.5), my-villas에 "+ Thêm villa" FAB. QA 독립 검증 **통과**(권한 누수 0건, 완료 기준 7/7) — 1차 조건부 통과의 M1(업로드 SVG stored XSS)은 MIME 화이트리스트 강제로 수정 후 재검증 통과, 교훈 leak-checklist 등재. tsc·lint·build 통과 | 합의 편차: react-hook-form 미사용(제어 컴포넌트+zod — package.json 동결 시점 결정). VillaRate 마진·판매가는 0 초기화 — T1.2 운영자 화면에서 설정. 계약: docs/contracts/T1.1-villa-wizard.md. 비차단 권고: app/uploads/[name] 응답에 X-Content-Type-Options: nosniff 1줄(BE, 다음 스프린트) |
| T1.7(BE) | 가격 계산 lib/pricing.ts (박별 합산·통화 분기·마진·환산·듀얼 컬럼 검증) + vitest 22개 — T2.1·T2.3·T1.2 요율 제안의 기반 |
| T2.3/T2.4 | HOLD 수명주기 lib/hold.ts (생성 트랜잭션·만료 cron·확정/취소) + cron/expire-holds + bookings confirm·cancel API — QA 2차 통과 |
| T2.1(BE) | 제안 생성 lib/proposal.ts + proposals·candidates API — T2.2 /p/[token]의 기반, QA 2차 통과 |
| T2.2 | 공개 제안 페이지 /p/[token] (c1·c2·c3 변환) + 공개 HOLD API — F3 가예약 체인 완성, QA 3차 통과 |
| T3.4(BE) | 청소 검수 게이트 lib/cleaning.ts + cleaning-tasks API + 정기 방역 cron — isSellable 단일 setter, QA 통과 |
| T3.3/T6.5 | 체크아웃 lib/checkout.ts + b4 화면 + 미니바 읽기 전용 — 게이트 체인(체크아웃→청소→검수→재판매) 완성, QA 통과 |
| T3.2 | 동의서·터치 서명 (b3 §3) + 사후 서명 경로 + T3.3 NONE 핫픽스 — F4 체크인 5단계 전부 완성 |
| T2.6 | 대시보드 완성 (b1/b1-mobile — 스탯·피드·충돌 배너·모바일) — F7 관리자 IA 전 화면 구현 완료 |

| 2026-06-11 | T1.7(BE)/T6.3(일부) | 가격 계산 완료 (병행 세션 — T1.2·T1.4·T1.6과 충돌 0건): lib/pricing.ts 단일 소스 — 순수층(resolveSeason [start,end) half-open·LOW 폴백·겹침 PEAK>HIGH 우선, quoteStay 박별 합산+KRW number/VND bigint 통화 분기+MissingRateError, computeSalePriceVnd 마진 PERCENT 내림/FIXED_VND, suggestSalePriceKrw 1e4 스케일 BigInt half-up 환산, assertSaleAmountColumns 듀얼 컬럼 검증) + DB 래퍼(quoteStayForVilla tx 주입, getFxVndPerKrw). vitest 22개(총 40), tsconfig target ES2020 1줄(BigInt 리터럴 — 계약서 선언, 전체 typecheck 회귀 0건). QA 1차 반려(D1 USD 무검증 통과·D2 원가 노출 가이드) → 수정 후 2차 **통과** | 교훈 2건 money-pattern 등재(통화 화이트리스트 게이트·BigInt 환율 반올림), 원가 누수 점검 항목 leak-checklist 등재. **StayQuote는 원가 포함 — ADMIN 외 응답 직렬화 금지.** 잔여: /settings/seasons·환율 입력 UI(FE). 계약: docs/contracts/T1.7-pricing.md |

| 2026-06-11 | T1.2 | ADMIN 빌라 목록·승인·요율 완료 (b9·b10 변환): 공통 사이드바 components/admin/sidebar.tsx(9메뉴·햄버거 드로어) + ResponsiveTable 컴포넌트, /villas 목록(상태 필터 탭+승인 대기 카운트), /villas/[id] 상세(사진·비품 읽기 전용·요율 편집·승인/중단/재개). API: PATCH /api/villas/[id] 상태 전이표(409 가드)·PUT rates 시즌 upsert(supplierCostVnd 수정 차단)·GET 목록(SUPPLIER는 supplierId 스코프+select 화이트리스트로 마진 쿼리 차단). lib/format.ts(BigInt 문자열 천단위)·lib/serialize.ts. QA 독립 평가 **통과**(완료 기준 7/7, 누수 4종 ✓ — 제2 공급자 시드 교차 실증, 교훈 3건 leak-checklist 등재). nosniff 헤더 권고 반영 | 반려(REJECT)는 T1.2b로 보류(VillaStatus 반려 상태 없음 — 공급자 수정 화면과 함께 설계). 권고: rate-editor 16자리+ 입력 시 오류 안내 부재(경미). QA 시드: ADMIN 0900000002·SUPPLIER2 0900000003 — 오픈 전 삭제 대상 |

| 2026-06-11 | T1.4 | SUPPLIER 월 달력 완료 (a3 변환): `app/(supplier)/calendar` — 빌라 칩 행(supplierId 스코프), 월 이동, 셀 4상태 색+패턴(Trống 초록 외곽선/Đã đặt 파랑 실선/Giữ chỗ 파선/Đã khóa 빗금), 범례, 바텀시트 Khóa ngày·Hủy khóa(MANUAL만, ICAL은 안내·과거 읽기 전용). 서버 컴포넌트가 셀 상태로 가공해 전달 — **고객명·금액·예약 id 비전달 구조**(마진·재고 비공개). POST/DELETE /api/calendar-blocks(소유권 404, 겹침 409, UTC 자정 정규화, AuditLog). 하단 탭바 components/supplier/tab-bar.tsx(4탭, 마법사에서 숨김) + /cleaning·/earnings placeholder. QA 1차 조건부 통과 → D1(advisory lock — lib/availability.ts `lockVillaInventory()` 공용 헬퍼, T2.3 HOLD와 락 키 공유)·D2(401/403 분리)·D3(과거 블록 삭제 거부) 수정 → 재검증 **통과**(누수 0건) | 합의 편차: 기간 차단 대신 단일 날짜 토글(a3 확정 디자인 우선) — 기간 UI는 IDEAS. 교훈: READ COMMITTED 재조회 race → availability-pattern.md 등재. 계약: docs/contracts/T1.4-supplier-calendar.md. 잔여: 배포 후 Playwright UI 검증 |

| 2026-06-11 | T1.6 | iCal 수신 동기화 완료 (병행 세션 — T1.2·T1.7과 충돌 0건, 신규 파일만): lib/ical.ts 단일 소스 — 순수층(parseIcs: RFC 5545 언폴딩·VALUE=DATE·TZID 벽시계 직취·Z→Asia/Ho_Chi_Minh 변환·DTEND 시간 성분 올림·CANCELLED 제외, diffIcalEvents UID 멱등 분류, findEventBookingConflicts) + DB 래퍼(syncVillaIcal: **모든 URL 성공 시에만 합집합 기준 소멸 삭제 — 1개 실패 시 삭제 전면 스킵**(재고 보수), MANUAL 불변, 충돌이어도 블록 생성+보고, AuditLog 커밋 후 기록; runIcalSync 빌라 단위 실패 격리; findUnresolvedIcalConflicts — T2.6 배너용 ADMIN 전용). GET/POST /api/cron/ical-sync(CRON_SECRET Bearer 선검증, 미설정 500, force-dynamic). vitest 31개, typecheck·lint 통과, QA 계약 합의(조건 5건 반영)+독립 평가 **통과** | 공유 파일: vitest.config.ts `@` alias 1건(선언·회귀 0건). 잔여: **Railway 30분 cron 실등록(테오/OPS)**. TDA 백로그: NotificationType ICAL_CONFLICT. 교훈 .claude/skills/integ/ical-pattern.md 신설+ops cron 패턴 등재. 계약: docs/contracts/T1.6-ical-sync.md |

| 2026-06-11 | T2.3/T2.4 | HOLD 수명주기 완료 (병행 세션 — 충돌 0건): lib/hold.ts 단일 소스 — `createHoldFromProposalItem` 단일 $transaction(lockVillaInventory 공용 빌라 잠금 → 제안 검증 → checkAvailability 재검증[더블부킹 최종 방어선] → 스냅샷 생성: 판매가=ProposalItem 복사·원가=quoteStayForVilla HOLD 시점 합산·fx=Proposal 복사), Proposal ACTIVE→USED **원자 가드**(QA D-1: 빌라 락이 못 지키는 제안 레벨 race를 updateMany status 가드로 차단), `expireHolds`(cron, status 가드)·`confirmHold`(만료 경과 거부)·`cancelBooking`(사유 필수). 라우트: cron/expire-holds(CRON_SECRET)·bookings/[id]/confirm·cancel(ADMIN 전용, serializeBigInt). 공급자 Notification PENDING 큐 적재(payload에 판매가·마진·원가 없음 — 실발송 T3.5). lib/audit-log.ts tx 주입(additive)·lib/availability.ts lockVillaInventory 공용 헬퍼 추가. vitest 19개(전체 85), QA 1차 반려(D-1 제안 race·D-2 상태 가드) → 수정 후 2차 **통과** | QA 편차 판정: HOLD→CANCELLED 허용 **수용**(SPEC v1.4 반영 권고), holdHours 영속 방식은 T2.1로 이관(TASKS 메모). T2.2 완료 기준 2건 이관(SOLD_OUT detail 비노출·MissingRateError 처리). 교훈 availability-pattern 등재(락 단위 밖 불변식은 원자 가드). 잔여: Railway cron 등록(OPS). 계약: docs/contracts/T2.3-T2.4-hold.md |

| 2026-06-11 | T1.7(FE) | 운영 설정 화면 완료 (b8-settings 변환): /settings — 시즌 달력 CRUD(LOW/HIGH/PEAK 뱃지, 겹침 시 경고 — PEAK>HIGH 우선 규칙 안내), 홀드 시간 스테퍼(1~168h), 환율 카드(1 KRW = x VND, 마지막 수정 시각). API: /api/seasons GET/POST/PUT/DELETE(UTC 자정 정규화·half-open·겹침 overlaps 응답) + /api/settings GET/PUT(화이트리스트 2키 — 임의 키 주입·노출 차단), 전부 ADMIN 전용+AuditLog. 환율 저장 → T1.2 요율 KRW 자동 제안 연동 확인. QA 독립 평가 **통과**(완료 기준 7/7, 6메서드 403/401 전수, 경계값 400 전수) | 비고: QA 테스트로 FX_VND_PER_KRW=18.6·HOLD=48 설정됨(무해 — 의도값 아니면 /settings에서 수정). rate-editor의 KRW 제안 float 중복 구현은 기록만(저장값 아님). 교훈 2건 leak-checklist 등재 |

| 2026-06-11 | cron 등록 (OPS) | T1.6·T2.4 잔여 해소 — Railway cron 2종 실등록·가동 확인: `cron-ical-sync`(*/30)·`cron-expire-holds`(*/5), 각각 `curlimages/curl` 미니 서비스 + CRON_SECRET 참조 변수(`${{villa-pms.CRON_SECRET}}`) + Bearer 호출. **후속 장애·해결**: 등록 직후 스케줄 실행이 양쪽 모두 연속 401 — 원인은 Start Command의 `$CRON_SECRET`가 **스케줄 트리거에서만 셸 확장이 안 됨**(Deploy 직후·Run now는 확장됨 — 경로별 실행 방식 차이). `sh -c '...'` 래핑으로 해결, 스케줄 자동 실행 성공(12:40 expiredCount 로그)까지 확인. 테오 대시보드 작업 + CLI 변수 설정·진단 협업 | 교훈 정정: ① Start Command `$VAR`는 `sh -c` 필수 ② 검증은 4단계(무인증 401 → 수동 200 → Run now → **스케줄 트리거 성공**) — Run now와 스케줄은 실행 경로가 다름. ops/deployment-pattern.md 등재. Zalo 재시도 cron(10분)은 T3.5에서 동일 패턴 |

| 2026-06-11 | T2.1(BE) | 제안 생성 BE 완료 (병행 세션 — 충돌 0건): lib/proposal.ts — defaultCurrencyForChannel(DIRECT→KRW·여행사/랜드사→VND), effectiveProposalStatus(시각 기준 서버 판정 — c2 단일 렌더 소스), uniformNightlyPrice(균일가만 perNight, 시즌 경계 null — 평균 가공 금지), generateProposalToken(crypto 192bit — cuid 추측 가능성 차단), createProposal(트랜잭션: 항목별 sellable 재검증→박별 합산 스냅샷→듀얼 컬럼 검증, 부분 생성 금지, fx 스냅샷, AuditLog). 라우트: POST·GET /api/proposals, GET /api/proposals/candidates(전체 재고 ADMIN 강제, 요율 미설정 빌라 warnings 분리). vitest 14개(전체 101). QA 1차 반려 → 2차 **통과** | **QA D-1(중대)**: Prisma.Decimal(fxVndPerKrw)이 serializeBigInt에서 {s,e,d}로 파손 — FX 미설정이면 null이라 안 보이는 "공허 통과". serialize.ts isDecimal 분기로 단일 수정(bookings/confirm 잠재 결함 동시 치유) + 교훈 leak-checklist 등재. 401/403 분리 5핸들러. 스코프 확정: holdHours Phase 1 전역값만·항목 1~3(QA 수용). 잔여: B2·b12 FE 변환. 계약: docs/contracts/T2.1-proposal-be.md |

| 2026-06-11 | T1.8 | ADMIN 사용자 관리 완료 (b13-users 변환): /users — 검색·역할 탭, 역할/Zalo 연결/활성 뱃지, 활성 토글(본인 행 disabled+API 400 이중 방어), Zalo 수동 매칭(미연결 ZaloConversation 선택 연결·해제, 웹훅 전 빈 상태 안내). API: GET(select 화이트리스트 — passwordHash 차단)·PATCH(discriminated union 4액션, LINK_ZALO 중복 409, $transaction으로 ZaloConversation userId 동기화), AuditLog 전 변경 기록. QA 독립 평가 **통과**(34항목 — 비활성화→로그인 실거부→재활성화 복구 실증, 누수 0건) | CLEANER 계정 0명이라 해당 뱃지는 코드 경로 확인만 — CLEANER 생성 태스크에서 재확인. 교훈 2건 leak-checklist 등재. ResponsiveTable rowClassName prop 추가(비파괴) |

| 2026-06-11 | T1.5 | ADMIN 타임라인 매트릭스 완료 (b1 변환, 병행 세션 — T1.8·T2.1·T2.2와 충돌 0건): lib/timeline.ts 단일 소스 — 순수층(buildDayAxis UTC 자정 축·M/D 라벨 getUTC*, computeVillaRow: half-open·우선순위 CHECKED_IN>CONFIRMED>HOLD>BLOCKED·isSellable=false 공실=NOT_SELLABLE·윈도우 클리핑, 오늘=Asia/Ho_Chi_Minh 기준 — QA 합의 조건) + DB층 loadTimeline(ACTIVE 전체 — ADMIN 전용 소비 주석, select 최소화). 셀 상태 6종 enum 고정(T2.6 재사용 호환). components/admin/timeline-matrix.tsx(b1 충실: sticky 빌라명 열·범례 4종·빗금 HOLD — striped/sticky는 arbitrary value로 globals.css 무수정), /dashboard placeholder 교체. 셀에 상태만 — 고객명·금액·예약 id 비전달. vitest 14개(전체 115), QA 조건부 통과 → 배포 후 프로덕션 HTTP 3종 증적(비로그인 307·SUPPLIER 세션 307 차단·ADMIN 200+타임라인 마크업) 제출 → **최종 통과** | messages는 T1.8 미커밋 키 혼입 방지 위해 git update-index 부분 스테이징(adminDashboard만 — QA가 b48110a diff 전수 확인, 배포 원자성 유지 편차 승인). 교훈 후보: 공유 JSON 부분 스테이징 패턴. T2.6 잔여: todayIndex 계산식 교체. 계약: docs/contracts/T1.5-timeline.md |

| 2026-06-11 | T2.2 | 공개 제안 페이지 완료 (병행 세션 — 충돌 0건): /p/[token] Stitch c1·c1-vnd 변환(통화 분기 — KRW "1,350,000원"/VND "25,500,000₫" 쉼표, 만료 배지, 사진 캐러셀, 날짜 요약/카드별 분기), c2 변환(만료 EXPIRED·REVOKED/마감 USED·notice — **서버 판정 단일 렌더**, 카카오·전화 버튼 AppSetting), c3 변환(가예약 입력 RHF+zod+인원 추가, 완료: 입금 안내 카드+계좌 복사+실시간 카운트다운, bg-mesh 재현) + 공개 HOLD API(교차 토큰 404, 거부 사유 expired/closed 2종 축약 — T2.3 QA 이관 이행, MissingRateError 500 금지). 미들웨어 공개 통과 확인. tests/public-format 8개(전체 123). QA 1차 반려(M1~M7 디자인 충실도·L1~L4)→수정→2차 반려(MESH_BG radial 위치)→3차 **통과**, 누수 0건 | AppSetting 신규 키 5종(은행 3·연락처 2) — T1.7 /settings 입력 UI 잔여. 교훈 2건 leak-checklist 등재(미신고 편차 반려 원칙·globals.css 동결 시 컴포넌트 내 해결). rate limit은 Phase 1 수용(QA) — IDEAS 후보. 계약: docs/contracts/T2.2-public-proposal.md |

| 2026-06-11 | T3.4(BE) | 청소 검수 게이트 완료 (병행 세션 — 충돌 0건): lib/cleaning.ts 단일 소스 — 상태기계(PENDING→제출→승인|반려→재제출, 직행 금지), createCheckoutCleaningTask(tx 주입 — T3.3 체크아웃 트랜잭션에서 게이트 닫기와 원자 묶임), **게이트 규칙 canOpenSellableGate: 같은 빌라 미결 CHECKOUT 0건일 때만 isSellable=true — PERIODIC 승인 우회 차단**, 반려는 게이트 닫힌 채 유지, 정기 방역 월 1회 멱등(monthKeyVn VN 시차 기준). API: GET 목록(ADMIN 전체/SUPPLIER 자기 빌라/CLEANER 배정분 스코프 강제), submit(3자 권한)·approve·reject(ADMIN), cron/periodic-cleaning(CRON_SECRET). vitest 11개(전체 154). QA **통과** — 동적 DB 검증 17건(throwaway 시드 실측): PERIODIC 우회 불가·연속 체크아웃 마지막 승인에만 개방·동시 제출 가드·멱등 실증, isSellable=true setter는 전 코드베이스에서 승인 함수 1곳뿐 | QA 비차단 3건 TASKS 백로그(승인 tx 직렬화·404/403 순서·cron 중복 방어), **TDA 안건 T3.4b 신설: 신규 빌라 게이트 초기 개방 절차**(닭과 달걀 — 첫 판매 경로 부재). 결함 #1(status 필터 프로토타입 체인 500)은 즉시 수정(Object.hasOwn). 계약: docs/contracts/T3.4-cleaning-gate.md |

| 2026-06-11 | T2.1(FE) | 제안 화면 완료 — **F3 판매 루프의 마지막 조각**: /proposals/new(b2 변환 — 3패널, 채널→통화 자동 전환(여행사·랜드사 VND 쉼표+결제 캡션/직접 KRW)+환율 참고 칩(FX 미설정 안내), candidates 후보 카드(photoUrl additive·warnings 배너)·최대 3개 선택·마진 요약(VND 정확/KRW 환율 참고)·24h/48h 토글·생성 모달 /p/{token} 복사) + /proposals(b12 변환 — effectiveStatus 탭·만료 카운트다운·링크 복사·회수, ResponsiveTable 재사용) + PATCH /api/proposals/[id] revoke 신규(**updateMany 상태 가드** — 공개 가예약 ACTIVE→USED 원자 전이와의 race에서 USED 덮어쓰기 차단, 404/409 사후 구분, AuditLog tx 기록). QA 1차 **반려**(D-1 revoke race High)→수정→2차 반려(D-4 i18n 키 경로)→수정→**전체 통과**(누수 0건, 비로그인 실측 401/307). 금액 합산 전부 BigInt | b2 인원 입력 제외(스키마 무필드)·b12 검색/필터/푸터 지표 미구현 — 계약 편차 기재. createdAt는 Asia/Ho_Chi_Minh 표기(dotDateVn). messages는 부분 스테이징(adminProposals만 — T1.5 패턴, T2.5 병행 키 혼입 방지). 계약: docs/contracts/T2.1-proposal-fe.md |

| 2026-06-11 | T2.5 | 예약 목록·상세 완료 (b5·b11 변환, 병행 세션 — T2.1-FE·T2.2·T3.4와 충돌 0건): /bookings(탭 6종 건수·월/빌라/채널/검색 필터·체크인 임박순·PAGE_SIZE 20·HOLD 카운트다운 뱃지·EXPIRED 흐림·ResponsiveTable 카드 전환·스탯 4종 — 가동률 computeOccupancyRate: NO_SHOW 포함/HOLD·CANCELLED·EXPIRED 제외, 월 경계 클리핑) + T2.6 링크 계약 `?filter=today-checkin|today-checkout`(VN 오늘). /bookings/[id](4단계 타임라인 스트립·종결 상태 배너·가격 스냅샷(원가는 ADMIN 가드 내)·결제 기록·AuditLog 활동 로그·내부 메모). 신규 API는 PATCH note 1개뿐(zod strip — 전이 우회 불가 테스트, 401/403/404), 액션은 기존 confirm/cancel만 호출. lib/booking-stats.ts(카운트다운 구조체+가동률 — 계약 편차: format.ts 대신 신규 파일, QA 수용). vitest 20개(전체 154), QA 조건부 통과 → 프로덕션 HTTP 증적(비로그인·SUPPLIER 307, ADMIN 200+마크업, 프리셋 200) + 커밋 8cee8f3 혼입 0건 확인 → **최종 통과** | messages adminBookings 블록만 update-index 부분 스테이징(2회째 — 패턴 정착). QA 회귀 권고: 실예약 데이터 생성 시 상세·확정/취소 실연 1회. 계약: docs/contracts/T2.5-bookings.md |

| 2026-06-11 | T3.5 | Zalo OA 발송 모듈 완료: lib/zalo.ts — enqueueNotification(비즈니스 로직 유일 진입점, $transaction 주입 가능) + dispatchPendingNotifications(배치 20, 재시도 3회 — NO_ZALO_LINK 영구 제외·ZALO_TOKEN_NOT_SET는 attempt 미증가로 토큰 입력 시 자동 회복, attempt는 payload._attempt — 스키마 무변경) + 발송 성공 시 ZaloMessage(SYSTEM·OUTBOUND) 미러(b14 채팅 정합, 미러 실패는 SENT 비롤백). vi 템플릿 9종(LOC 용어 사전 준수, 마진·판매가 화이트리스트 차단 — 오염 주입 테스트). /api/cron/notifications(CRON_SECRET Bearer). vitest 22개(전체 176 회귀 0), QA 독립 평가 **통과**(5/5, 동적 검증: PENDING→FAILED 전이·영구 제외·401/500) | 잔여: 실발송 검증(토큰 대기)·Railway cron 등록(OPS). 비차단 권고 2건: 소진 FAILED 큐 기아(PERMANENT_ 접두 권고)·cron 동시 실행 중복(단일 cron 전제 수용). QA 교훈: vitest는 대문자 드라이브(C:\) 경로에서 실행 — 소문자 cwd면 러너 이중 로딩 전멸 |

| 2026-06-11 | T3.4(FE) | 청소 검수 화면 완료 (b6 변환): `/inspections` 2패널 — 좌 목록(승인 대기 우선 정렬·상태 탭 5종 groupBy 카운트·?task= 선택 유지), 우 상세(기준 사진(isBaseline) vs 청소 후 쌍 그리드 — 라벨 #E5E7EB, 개수 불일치 단독 표시, REJECTED 사유 박스), 하단 액션(승인 초록/반려 빨강 — 사유 필수 1000자, PHOTOS_SUBMITTED만 활성, **gateOpened=true 시 "판매 가능 전환" 배너** — 검수 게이트 가시화). RSC prisma 직접 조회(신규 API 0) — booking·고객명·금액·assignee **비직렬화**(select 화이트리스트). QA 1차 조건부(D-1: 태스크 전환 시 반려 사유 잔존 → key 리마운트) → 2차 반려(D-2: 승인 후 선택 첫 행 튐 → 배너 소멸) → 액션 성공 시 `router.replace`로 선택 URL 고정 + selectedId fallback 완화 → 3차 **전체 통과**(누수 0건) | 편차: submittedAt 컬럼 부재 → "생성:"·"승인:" 정직 표기(TDA 검토 후보), 반려 모드 중 승인 버튼 숨김(오클릭 방지). QA→BE 전달: submit API photoUrls 형식 refine 권고(Low). messages는 adminInspections만 부분 스테이징(3회째). 계약: docs/contracts/T3.4-inspections-fe.md |

| 2026-06-11 | T3.3/T6.5 | 체크아웃 완료 (선점 프로토콜 첫 적용 — 규칙 #8): lib/checkout.ts `completeCheckout` 단일 트랜잭션 — CHECKED_IN 상태 가드(updateMany, 중복 차단) → CheckOutRecord(파손 아님 시 파손 필드 정규화) → 보증금 상태기계(resolveDepositOutcome: 파손⇒차감액>0 필수⇒PARTIAL_DEDUCTED / 없음⇒차감 금지⇒REFUNDED) → **createCheckoutCleaningTask(tx) 원자 호출(게이트 닫기, SPEC F4-4)** → AuditLog(BigInt 문자열화). API: POST bookings/[id]/checkout(ADMIN, 차감액 숫자 문자열→BigInt). FE: b4 변환 — 기준사진 비교 업로드(공간순, 0장 폴백 슬롯), 미니바 읽기 전용(T6.5), 파손 리포트(토글·증빙·차감액 red), 하단 고정 바(BigInt 환불 예정액·전액/차감 버튼 배타 활성화), action-panel 체크아웃 버튼 활성화. adminCheckout i18n ko/vi 44키. vitest 20개(mocked prisma — 가드 경합 시 태스크 미생성·tx 동일성 증명, 전체 234). QA 1차 반려(D1 DB층 테스트 부재 Major + D2~D5) → 전건 수정 → 2차 **통과** | 결정: tsconfig exclude에 reference/ 추가 — T0.7 수집 외부 발췌 코드는 컴파일 비대상(typecheck 전역 차단 해소, QA 정당성 승인). messages 커밋에 T4.5 세션 earnings 키 동반(미사용 무해 — 코드 없이 키만 선행). 계약: docs/contracts/T3.3-checkout.md |

| 2026-06-11 | T3.1 | 체크인 완료 (b3 변환, 병행 세션 — T2.6·T3.4FE·T3.3·T4.5와 충돌 0건, 계약서 선점 패턴): lib/checkin.ts(CONFIRMED→CHECKED_IN status 가드 updateMany·CheckInRecord(bookingId unique 2차 방어)·보증금 HELD/NONE(Int 상한)·AuditLog tx 주입 — 여권 데이터 미기록·장수만), lib/gemini.ts(REST fetch OCR — 의존성 0, gemini-2.0-flash, 미설정 GeminiNotConfiguredError→503 폴백, 이미지·결과 로그 금지), lib/ai-utils.ts [SHARED-MODULE] 5단계 JSON 추출기. **여권 비공개 파이프라인(QA 조건 A)**: 업로드 ADMIN 전용→R2 무관 디스크 고정(미설정 시 public/ 밖 private/ — QA M1 수정), 서빙 /api/passports/[name] ADMIN 가드+private,no-store+nosniff, checkin zod가 /api/passports/ 경로만 수용(외부·공개 URL 400 테스트). b3 폼: 여권 그리드+장별 OCR(ADMIN 확인·수정 확정본만 저장 — 자동 저장 금지)+보증금 통화 토글. 동의서·서명(T3.2)·공급자 전달(T3.6) 미렌더(조건 B — 가짜 완료 표시 금지). 테스트 27개(전체 234), QA 조건부 통과→M1·M2 수정→프로덕션 증적(무인증 401·/uploads/passports 정적 404·SUPPLIER 403·ADMIN OCR 503 폴백) → **최종 통과** | action-panel·messages 부분 스테이징(T3.3 미커밋 체크아웃 분기 혼입 0 — QA diff 확인). 교훈 leak-checklist 등재(비공개 파일은 public/ 정적 범위 밖 — 가드 라우트만으론 불충분). **운영 주의(QA 조건 C): T3.2 서명 머지 전 실투숙 체크인은 종이 동의서 병행**. 잔여: 테오/OPS GEMINI_API_KEY 입력(OCR 활성), T3.6 여권 Zalo 전달·T3.2 서명이 이 화면에 섹션 추가 예정. 계약: docs/contracts/T3.1-checkin.md |

| 2026-06-11 | T3.4b | 신규 빌라 게이트 초기 개방 완료 (TDA 결정 ADR-0006 v2, 병행 세션 — T3.2·T3.8·T4.5와 충돌 0건): **런치 블로커 해소** — isSellable 기본 false + 개방 경로가 청소 승인뿐인데 신규 빌라는 체크아웃 불가(닭과 달걀). 결정 ① 최초 APPROVE tx 안에서 초기 검수 CleaningTask(PERIODIC) 자동 생성(빌라 태스크 0건일 때만 — 멱등, 공급자 CLEANING_REQUEST 알림+AuditLog) ② approveCleaningTask 게이트 분기 확장: CHECKOUT 또는 **빌라의 첫 APPROVED**(자기 제외 count) — 기존 ACTIVE(시드) 빌라도 첫 월간 검수 승인으로 개방, 미결 CHECKOUT 0건 조건 공통 적용(우회 차단), 게이트 setter는 approveCleaningTask 단일 유지. 수동 개방 토글 기각(원칙 3 — 사진 검수 없는 우회 금지). QA 1차 **반려**(Critical: PERIODIC 승인이 CHECKOUT 전용 게이트 분기 미진입 — 목적 미달성·ADR 허위 서술·E2E 테스트 부재) → v2 재작업(분기 확장+ADR 정정+게이트 E2E 4종) → **최종 통과** + 배포 후 무인증 PATCH 401 회귀 ✓ | 교훈: "게이트 메커니즘"을 오독한 계약이 자기검증을 통과 — 목적(isSellable=true)을 직접 단언하는 E2E 테스트를 완료 기준에 처음부터 넣을 것. 운영 흐름: 승인→공급자 알림→사진 제출(a4/a8, T3.8)→/inspections 승인→판매 가능. 계약: docs/contracts/T3.4b-initial-gate.md |

| 2026-06-11 | T4.5 | F6 최소 정산 완료 (b7·a7 변환): lib/settlement.ts — 월 집계(체크아웃 월 기준 CHECKED_OUT·NO_SHOW, 공급자별 BigInt 합산, 멱등 — CONFIRMED/PAID 불변 skip·타 정산 귀속 예약 skip 보고) + 전이표(DRAFT→CONFIRMED→PAID, updateMany 경합 가드) + PAID 시 SETTLEMENT_READY enqueue(T3.5 같은 트랜잭션). /settlements(b7 — 집계 실행·확정/지급·매출 KRW/VND 분리), /earnings(a7 vi — 자기 원가만·점 구분 ₫·정산 상태). vitest 신규 12(전체 258 회귀 0), QA 독립 평가 결함 3건 수정 후 **통과**: ① $transaction N+1 타임아웃 → 일괄 쿼리+30s ② **루트 layout이 messages 전체를 직렬화해 admin 마진 라벨이 공급자·공개 페이지 HTML에 노출(D-2)** → 구역별 네임스페이스 화이트리스트 provider(lib/intl-messages.ts)로 구조 개선 ③ 미들웨어 /earnings·/cleaning(SUPPLIER) 경로 누락 → locale·접근 제어 수정 | D-2는 전 화면 공통 구조 개선 — 이후 클라이언트 useTranslations 네임스페이스 추가 시 구역 layout 화이트리스트 등록 필수. 교훈 4건 leak-checklist 등재 |

| 2026-06-11 | T3.8 | 공급자·청소자 청소 화면 완료 (a8·a4 변환, 신규 등재 — F4 사용자 측): /cleaning 목록(role 스코프 SUPPLIER=자기 빌라/CLEANER=배정분, 배지 4종, 미결 우선 정렬, Trả phòng DD/MM, REJECTED 사유 미리보기) + /cleaning/[id] 사진 제출(빌라 등록 동일 슬롯 buildPhotoSlots 재사용, 클라 리사이즈 업로드, 전 슬롯 완료 시에만 Gửi để duyệt, REJECTED 재제출·SUBMITTED/APPROVED 읽기 전용) + /api/uploads CLEANER 허용 1곳(MIME 화이트리스트 등 보안 회귀 0). **layout.tsx 비접촉 우회**: 클라 라벨 전부 RSC getTranslations→props (병행 T4.5의 layout 수정과 충돌 0). 게이트 루프 사용자 측 완성 — 제출분이 /inspections 승인 대기 큐 최상단 노출 확인. QA **조건부 통과**(누수 0건, booking은 checkOut만 직렬화, 404 비노출 3분기 동일) | 조건: 편차 기재 완료(a8 menu·알림 아이콘 등), 배포 후 실측 1회 잔여. BE 백로그 2건(QA): submit max(30)<슬롯 최대 45, submit 403/404 통일. 교훈: 동적 슬롯↔고정 상한 API 정합 검사(leak-checklist 등재). 계약: docs/contracts/T3.8-supplier-cleaning.md |

| 2026-06-11 | T3.2(+T3.3 핫픽스) | 동의서·터치 서명 완료 (선점 프로토콜): b3 §3 변환 — 동의서 7조항(수영장 조항 hasPool 자동 포함, 동적 번호 재부여 — QA 승인 편차), canvas 서명 패드(PNG→비공개 증빙 파이프라인 sig- 접두 — 여권 90일 삭제 정책과 분리·분쟁 대비 보관), 체크인 폼 §3 통합(서명 **선택** — 비게이트 유지, T3.1 조건 C 연속) + 사후 서명 경로(상세 action-panel 미서명 배지 → /checkin 사후 서명 모드 → POST /api/bookings/[id]/agreement, signatureUrl null 가드로 동시 서명 차단). **T3.3 핫픽스**: resolveDepositOutcome에 depositStatus 인자 — NONE(미수취) 체크아웃이 REFUNDED로 둔갑하던 결함 차단(NONE 유지, 파손 시 deductionVnd=청구 근거). T3.8 QA 백로그 처리(submit max 30→50). 서명 개인정보 3중 방어(클라 대상·zod·lib — 공개 /uploads 거부 테스트). vitest 19개(전체 262), QA 1차 반려(D1 진입점 부재·D2 sig- 미적용 — 테스트 허위 정합 지적) → 수정 → 2차 **통과** | 계약 결정 4건 QA 승인: 비게이트+사후 서명·소급 처리·비공개 파이프라인 재사용·NONE 핫픽스. messages 키는 T4.5 커밋(c245635)에 선행 동반됨. 계약: docs/contracts/T3.2-agreement-signature.md |

| 2026-06-12 | T2.6 | 대시보드 완성 (선점 프로토콜): lib/dashboard.ts 단일 소스 — VN 기준 오늘 집계(**/bookings 프리셋과 where 절 동일 — 카드 건수=링크 목록 보장, QA D-2**), AuditLog→피드 매핑(15종 + generic 폴백 — 미지 엔티티에 깨지지 않음 테스트 증명, 여권·금액 미노출), vnDateLabel(Asia/Ho_Chi_Minh 표시 규약). b1 변환: iCal 충돌 배너(findUnresolvedIcalConflicts — 0건 미렌더, 해결하기→예약 상세), 스탯 4종(체크인/체크아웃 예정·HOLD+최근 만료 배지·청소 대기 — 각 목록 화면 링크), 좌9/우3 활동 피드(타임라인 dot·상대시간), 푸터(시스템·최근 동기화·FX 칩 — USD 표기 폐기 이행). b1-mobile: <lg 타임라인→오늘 중심 리스트(체크인/체크아웃/청소 카드)+콤팩트 2×2 스탯. vitest 9개(전체 271). QA 1차 반려(D-1 링크 대소문자·D-2 집계/프리셋 불일치·D-3 UTC 날짜) → 수정 → 2차 **통과** | 신고 편차 7건 QA 승인(썸네일·전체 로그 버튼·USD 폐기·배너 조건부·시각 미표시·충돌 피드 generic·모바일 그리드). 비차단 관찰 2건(HOLD 전역 집계 vs 월 필터 — 실무 창 극소, vi fxChip 구분자 — LOC 감수 후보). F7 관리자 화면 전부 완성. 계약: docs/contracts/T2.6-dashboard.md |

| 2026-06-16 | T4.1 | 권한 누수 4종 전수 스윕 완료 (오픈 게이트): 전 표면(API 36·페이지 26·layout 4·파일서빙 2·cron 4) 정적 분석 + 동적 실행 검증. **High 결함 0건, 사업 핵심 원칙(재고·마진 비공개) 위반 0건 → 오픈 가능 판정.** ① SUPPLIER↔SUPPLIER2 교차 스코프(빌라·캘린더·청소태스크 404/403, ADMIN 대조 전건 표시로 공허통과 방지) ② 공급자 화면·API에 margin·salePrice·fx·고객연락처 0건, 공개페이지 원가·타재고 0건, ADMIN candidates엔 원가·판매가 양성 실증 ③ 공개토큰 crypto 32자 랜덤(cuid 아님)·USED/REVOKED/EXPIRED 만료렌더(가격·빌라명 잔존 0)·HOLD 동시성 201+409·half-open 경계·경로탈출 5종 차단 ④ 무인증 API 36 전부 401/403·페이지 13 전부 307·cron 8/8 401·CLEANER 역할 격리 | 제품 코드 결함 0(수정 없음). 오탐 1건(next-error 인라인 CSS margin:0 — grep 표준식 교훈화). 환경 메모: 병렬 세션이 QA 테스트 계정(0900000002·0900000003) 비번 변경 — **테오 ADMIN(0799493138)은 정상 확인**. T1.2b WIP(/my-villas/[id] rejectionReason) 머지 후 dev 재기동 시 빌라상세 500 해소. 교훈 4건 leak-checklist 등재 |

| 2026-06-16 | T6.6+T3.7 | Zalo 화면 완료 (디자인 잔여 2장 — 선점 프로토콜): 운영자 /messages(b14 2-pane — 인박스 unread·48h 경과 뱃지·검색, 대화창 vi원문+ko번역 병기[숨기기 토글]·OUTBOUND·SYSTEM 미러 버블·날짜 구분자, 48h 응답창 비활성+amber 경고/이내 ko입력+vi미리보기) + 공급자 /zalo-connect(a0 — Bước 2/2·친구추가 딥링크·QR·스킵, 연결됨/미연결 분기). lib/zalo-chat.ts isReplyWindowOpen 순수함수, lib/gemini.ts translateText additive, POST /api/zalo/messages(48h 409·토큰미설정 FAILED 200·AuditLog·마진 미포함)·translate(503 폴백)·conversations 읽음 PATCH 멱등. vitest 63개(전체 ~318), QA 독립 평가 **통과**(완료기준 9/9+1 N/A, 누수 0건 — adminMessages admin layout 화이트리스트 한정·select 금액 0). messages 부분 스테이징(adminMessages·zaloConnect만 — T1.10 myVillas/villaDetail 비혼입). | 라이브 경계: webhook 실수신·zca-js 실발송은 테오님 Zalo QR 활성화 의존(T3.5 잔여 동일 성격). 계약: docs/contracts/T6.6-T3.7-zalo-screens.md. **디자인 33장 전부 구현 완료** |

| 2026-06-16 | T4.6 | 오픈 전 전체 점검 1차 완료 (점유 계약): LAUNCH.md 체크리스트 11항목을 코드·테스트·프로덕션으로 실측 — F1 빌라사이클·F2 캘린더/iCal·F3 제안/홀드·F4 검수게이트·F6 정산·권한누수(T4.1)·배포스모크 **PASS**, F0/F5(Zalo)·파일럿시드·T4.4는 활성화/실행 의존 **PARTIAL/BLOCKED**. 프로덕션 스모크 증적(/api/health 200·login/signup 200·보호라우트 307·미인증 쓰기 403/405). F5 알림 템플릿 10종 확인, cron 4종 라우트 존재. 코드 수정 0(읽기 점검) — 결함은 담당 세션 이관. | **결론: 코드 측면 오픈 준비 완료, 잔여는 전부 테오님 액션(Zalo 활성화·시드 실행·실사용자 테스트)**. 신규 Zalo 라우트는 배포 완료 후 재스모크 필요. 상세 docs/LAUNCH.md §6. |

| 2026-06-16 | T3.6 | 여권 Zalo 전달(임시거주신고) 완료 — F5 알림 8종 연결 완결: lib/tamtru.ts(sendTamTruPassport — CheckInRecord·passportPhotoUrls 전제검증, $transaction으로 TAMTRU_PASSPORT enqueue+tamTruSentAt+AuditLog, 재전달 멱등) + POST /api/bookings/[id]/tamtru(ADMIN 401/403) + 예약상세 action-panel '공급자에게 전달' 버튼(b3 변환, 전달완료 HH:MM 분기·미연결 경고). payload 화이트리스트 4필드(villaName·guestName·checkIn·passportPhotoUrls) — 고객연락처·마진·판매가 미포함, AuditLog는 passportPhotoCount(장수)만(여권URL·OCR 미기록). Zalo 이미지 발송은 CS API 텍스트 전용이라 Phase 1=텍스트 알림+ZaloMessage.attachmentUrls 증빙(msgType=image), 실 이미지 발송(OA upload API)은 토큰 후 잔여. extractAttachmentUrls 추가+dispatch 미러 첨부 기록. **HEAD의 VILLA_REJECTED 빌더 누락 버그 수정**(zalo.test 3건 실패→회복). vitest 32(전체 428 회귀 0), tsc 0 | QA 독립평가는 API 레이트리밋으로 후속 진행 — 자체검증(권한 첫줄·payload 화이트리스트·AuditLog 미기록·테스트·tsc) 통과 후 사용자 지시로 커밋·배포. 잔여: 실 이미지 발송(Zalo OA 토큰), 여권 90일 삭제 cron(데이터정책) |
| 2026-06-16 | T1.10 | 공급자 내 빌라 홈(a6)·상세(a10) + REJECTED 가시화 완료(이전 세션 미커밋분 랜딩+보완): /my-villas(supplierId 스코프 카드 목록·대표사진·상태배지·빈상태·FAB) + /my-villas/[id] 신규(기본정보·사진그리드 spaceLabel·비품 카테고리 요약·원가 시즌3 VND점구분). **누수 차단**: VillaRate select=supplierCostVnd만 — salePriceVnd·salePriceKrw·marginValue·marginType select 부재(구조적 보장), 홈은 rates 미조회. 소유검증 getVilla(타인·부재 동일 404 notFound), 비SUPPLIER redirect. **T1.2b 머지 후 REJECTED 보완**(T1.2b 인계): 배지 5종(rejected 추가)·홈 카드 "Sửa và gửi lại"→/[id]/edit·상세 반려사유 카드(rejectionReason select 추가)+재제출 버튼. i18n myVillas.status.rejected·editResubmit·villaDetail.rejectionTitle ko/vi + tests/my-villas-i18n-keys.test.ts 5건. tsc 0, QA 독립 평가 **통과**(조건부 tsc 1건 즉시 해소 — 중첩객체 캐스트 제거). | 신규 파일·공유파일(messages 부분 스테이징)만 경로 커밋, 다른 세션(lib/cleaning·hold·proposal) 무접촉. 계약: docs/contracts/T1.10-supplier-villa-home.md |
| 2026-06-16 | T6.4 | 비품 종결 — 공급자 빌라 비품 수정 신설(잔여 마지막): PATCH /api/villas/[id]/amenities(SUPPLIER supplierId 스코프, 타인 404 존재비노출, $transaction deleteMany+createMany+writeAuditLog 원자, **status·isSellable 불변**=승인게이트 무영향, VillaRate 미참조 누수0, 응답 {id,amenityCount}) + /my-villas/[id]/amenities(소유검증 prefill + a9 UI 재사용 클라 에디터: 카테고리4탭·체크타일·미니바 스테퍼·인라인 저장/취소) + 상세 비품카드 "수정" 진입. itemKey isValidAmenity 사전검증·수량1~99·빈배열 허용. i18n amenities.save/editTitle 등·villaDetail.editAmenities ko/vi(amenities는 SUPPLIER_CLIENT_NAMESPACES 화이트리스트). tests/villa-amenities-api.test.ts 8건. tsc 0, 전체 518. QA 독립 평가 **통과(7/7 결함0, 누수·권한 grep+동적 실증)**. ADMIN b10 비품 조회는 T1.2 기완료 확인 → T6.4 전체 종결. | 신규 라우트 파일로 격리(T6.3 rates-api-test·[id]/route.ts 무접촉). 계약: docs/contracts/T6.4-supplier-amenities-edit.md |
| 2026-06-16 | T0.9 | 스키마 드리프트 검증 종결(TDA 위임, 읽기 전용): prod Neon DB의 information_schema·pg_enum를 $queryRawUnsafe로 실측 — Role·VillaStatus(REJECTED)·Villa.rejectionReason·VillaAmenity+AmenityCategory·Booking 통화4컬럼·ProposalItem 듀얼통화4·ZaloConversation/ZaloMessage·NotificationType(VILLA_REJECTED·TAMTRU_PASSPORT)·AppSetting/AuditLog·VillaRate 5컬럼 **10항목 전부 존재** → **DB ⊇ 커밋 스키마(HEAD), push 불필요**. 임시 검증 스크립트 작성→실행→삭제(git 미커밋). prisma db push/migrate/schema 수정 0. | zca-js 미커밋 추가분(ZaloAccount, ADR-0006)은 검증 대상 제외 — 그 세션 push 전담(병렬규칙6 스키마 1세션 원칙). 계약: docs/contracts/T0.9-schema-drift-verify.md |
| 2026-06-16 | T4.2 | 파일럿 시드 스크립트 완료(멱등): prisma/seed.ts — ADMIN(테오)·SUPPLIER(파일럿), AppSetting 7키 전체(SETTING_KEYS 화이트리스트 import), 2026 시즌달력(설·여름·연말), 빌라 4채(쏘나씨 V11/V12/V25+썬셋 사나토 A3)×시즌3 요율+사진. 고정 id/unique 키 upsert로 재실행 안전. salePriceVnd=원가×(100+마진)/100, salePriceKrw=÷FX 천원 라운딩. 키 상수 하드코딩 0(lib/hold·pricing·settings/validators import). package.json 동결 준수(npx tsx 직접실행). tests/seed-data.test.ts 37건(마진 양수·계절 단조성·금액 타입·키 충돌). tsc 0, 전체 428. | 사진·계좌·연락처·가격 placeholder(테오 실값 교체). **프로덕션 DB 적재는 테오 확인 후 `npx tsx prisma/seed.ts` 수동**(멱등 재실행 안전). 계약: docs/contracts/T4.2-seed-data.md |
| 2026-06-16 | T4.3 | 공급자 온보딩 가이드(vi 1장) 완료: design/stitch/a-guide Stitch 생성→/guide 변환(라이트 teal #0D9488, Be Vietnam Pro, 390px). 4단계 카드(빌라등록 add_home_work·캘린더 calendar_month·청소사진 cleaning_services·Zalo알림 notifications_active)→각 기능 이동 + 인라인 CTA(시작·Zalo연결). 디자인 fixed 하단버튼→인라인 변환(TabBar 고정과 이중바 방지). TabBar 5번째 탭(help, /guide) 추가. guide 네임스페이스 13키+tabs.guide ko/vi 동기, tests/guide-i18n-keys.test.ts 15건. 정적 안내(prisma/금액 미참조 — 누수 자명 통과). QA 독립 평가 **통과(6/6 + 추가 점검 전항)**. tsc 0, 전체 443. | 진입은 TabBar만(홈헤더/가입직후 링크는 T1.10·T3.7 hot 정리 후 후속). 계약: docs/contracts/T4.3-supplier-onboarding-guide.md |

| 2026-06-16 | T3.5b S0~S2 | Zalo zca-js QR 로그인 MVP 완료 (ADR-0006): OA 폐기→개인계정 QR 로그인 전환 1차. ZaloAccount 모델 추가(additive db push, credential AES-256-GCM 암호문·select 격리), lib/zalo-credentials.ts(Nike 이식)·lib/zalo-runtime.ts(봇 단일 인스턴스 — Nike 멀티풀 축약, globalThis 슬롯·connectPromise mutex·code3000 중복로그인 감지), /api/zalo/qr·status(ADMIN 전용), /settings/zalo 화면(QR 생성→폴링→연결됨), instrumentation.ts(재시작 자동 재로그인 NEXT_RUNTIME 가드). railway numReplicas=1(동시로그인 밴 방지), zca-js@2.1.2, serverExternalPackages. QA 독립 평가 **통과** — **credential 누수 0건**(응답·HTML·AuditLog·로그), vitest 527 회귀 0, 실 QR 생성(16KB PNG) 확인 | **테오님 직접: /settings/zalo에서 휴대폰 Zalo QR 스캔→연결**. 잔여: S2 자동재로그인 프로덕션 1회 실측(OPS), S3 수신 리스너→/messages 실데이터, S4 발송 OA→zca-js 전환(dispatchOne 1줄), S5 이미지·번역. zalo-credentials/runtime 단위테스트 보강 권고. ADR-0006 정본 |

| 2026-06-16 | T3.5b S3+S4 | Zalo 수신 리스너+발송 zca-js 전환 완료 — 알림 실발송·채팅 실수신 배선 완결: S3 lib/zalo-inbound.ts(message 리스너→ZaloConversation/ZaloMessage 저장, ThreadType.User만·그룹 제외, isSelf/ownId 에코 dedup+zaloMsgId 멱등, 전화번호 자동매칭 T3.7 — userId null+SUPPLIER phone 일치 시 User.zaloUserId 연결). S4 dispatchOne 전송 sendZaloText(OA)→sendBotMessage(getBotApi().sendMessage, ThreadType.User), ERROR_BOT_NOT_CONNECTED(attempt 미증가 자동회복), 48h 가드 제거(개인계정 D5.5 — /messages 입력창 항상 활성). enqueueNotification/dispatchPendingNotifications/buildNotificationText 시그니처 무변경(호출부 무수정). QA 독립 평가 **통과** — credential·세션·마진·고객정보 누수 0, 시그니처 변경 0, vitest 549 회귀 0, 전화번호 매칭·멱등 라이브 DB 실증 | 잔여: S5 여권이미지 실발송·vi↔ko 번역, S6 끊김 Web Push, instrumentation register() 프로덕션 1회 실측(WebSocket 상주), 실송수신(테오 봇↔테스트). 교훈 4건 leak-checklist 권고 |

## 현재 상태 (2026-06-11 기준)

### 완료된 태스크
| 태스크 | 내용 |
|---|---|
| T0.1 | Next.js 15 + TypeScript + Prisma + next-intl + NextAuth v5 초기화 |
| T0.2 | schema.prisma → Railway PostgreSQL prisma db push 완료 |
| T0.6 | Railway 배포 완료 (nixpacks, Node 20, Tailwind v3) |
| T1.0~T4.0 | Stitch 디자인 28장 생성 + QA 통과 (design/stitch/ 저장) |
| T5.1~T5.3 | 디자인 결함 수정 + LOC 용어 사전 확정 |
| T0.3 | 인증 완료 — NextAuth v5, /signup·/login (Stitch a0 변환), Role 미들웨어, (admin)/(supplier) 라우트 그룹 |
| T0.5 | i18n 완료 — locale 쿠키 기반 ko/vi, auth 네임스페이스 키, Noto Sans KR 폴백 전역 |
| T1.3 | 가용성 판정 lib/availability.ts + vitest 단위 테스트 18개 — T1.4·T1.5·T2.1·T2.3의 기반 |
| T0.4 | 이미지 저장소 — R2 백엔드+디스크 폴백 자동 선택, Railway volume 인터림, 클라 리사이즈 (ADR-0004) |
| T1.1 | SUPPLIER 빌라 등록 마법사 5단계 (a2·a2b·a1·a9·a5 변환) + POST /api/villas + 비품 품목 사전 — QA 통과 |
| T1.2 | ADMIN 빌라 목록·승인·요율 (b9·b10 변환) + 공통 사이드바·ResponsiveTable — QA 통과 (반려는 T1.2b 보류) |
| T1.7(BE) | 가격 계산 lib/pricing.ts (박별 합산·통화 분기·마진·환산) + vitest 22개 — /settings UI는 잔여 |
| T1.4 | SUPPLIER 월 달력 (a3 변환) + /api/calendar-blocks + 공급자 하단 탭바 — QA 통과 (D1~D3 수정 후 재검증) |

### 진행 중 / 대기 중
| 태스크 | 상태 | 담당 |
|---|---|---|
| T5.4 | Stitch 중복 화면 3건 수동 삭제 대기 (테오) | 테오 직접 |
| 디자인 추가 작업 | **완료** — 2차 전체 회의(테오 7건) 처리: 낙하 수정+신규 3장+수정 9장, 총 33장, QA 통과 (위 이력 참조) | DESIGN/QA |
| schema v1.2 push | **완료** — 2026-06-11 T1.1 세션에서 `prisma db push` 확인 결과 already in sync (Railway CLI `railway link` 후 DATABASE_PUBLIC_URL로 실행 — 로컬 .env는 placeholder 유지) | TDA/BE |
| R2 전환 | 대기 — 테오가 Cloudflare R2 버킷·API 토큰 발급 → Railway STORAGE_* 5종 입력 (코드 무변경) | 테오 직접 |

### 다음 세션 시작 시 할 일 (2026-06-11 T1.4 세션 종료 핸드오프)
**Sprint 1 잔여: T1.5(타임라인)·T1.6(iCal)·T1.7 FE(/settings)·T1.8(사용자). T1.6·T2.3/T2.4는 병행 세션 진행 중이었음(lib/ical.*, lib/hold.*, app/api/bookings·cron 미커밋) — 착수 전 git status로 충돌 확인 필수**

1. **T1.5** — ADMIN 타임라인 매트릭스 뷰 (b1 변환) (FE) — components/admin/sidebar.tsx·ResponsiveTable(T1.2 산출) 재사용
2. **T1.7(FE)** — /settings/seasons 시즌 달력 + 홀드 시간 + 환율(FX_VND_PER_KRW) 입력 UI (b8 변환) (FE) — lib/pricing.ts getFxVndPerKrw 연동
3. **T1.8** — ADMIN 사용자 목록 (b13 변환) (FE)
4. 프로덕션 Playwright 검증: T1.1 마법사 + T1.4 캘린더 E2E (SUPPLIER 테스트 계정 0900000001)
5. 재고 경합 쓰기 규칙: 같은 빌라 재고를 만지는 모든 트랜잭션은 첫 줄에 `lockVillaInventory(tx, villaId)` (availability-pattern.md 교훈 — T2.3 HOLD·T1.6 iCal upsert 필수)

**변환 공통 규칙 (docs/DESIGN.md 필독)**: Noto Sans KR 폴백 + word-break keep-all/nowrap을 globals.css 전역 강제, 사이드바 9메뉴 공통 컴포넌트, ADMIN VND 쉼표·공급자 VND 점, 마법사 단계는 ICU `Bước {n}/{total}` (N/5), T5.5 변환 시 처리 목록(TASKS.md) 확인

### 인프라 정보 (다음 세션 참조용)
| 항목 | 값 |
|---|---|
| 배포 URL | https://villa-pms-production.up.railway.app |
| GitHub | https://github.com/orangetaeo/villa-pms |
| DB | Railway PostgreSQL (내장) — `${{Postgres.DATABASE_URL}}` |
| Railway 프로젝트 | outstanding-vibrancy / production |
| 기술 스택 | Next.js 15, Prisma 6, NextAuth v5 beta, next-intl 3, Tailwind v3 |

### 알려진 기술 결정사항
- Tailwind CSS v3 사용 (v4는 Railway nixpacks 네이티브 바이너리 충돌로 제외)
- nixpacks.toml로 `npm install` 강제 (`npm ci` lockfile 불일치 우회)
- `prisma db push` 방식 사용 (migration 파일 없음 — 코드 안정화 후 migrate dev로 전환 예정)
- **헬스체크는 `/api/health` 전용** (railway.toml) — 루트 `/`는 role 리다이렉트(307)라 헬스체크 불가. 인증 등 리다이렉트 추가 시 헬스체크 경로 주의
- **NextAuth v5 Railway 필수 변수**: `NEXTAUTH_URL`=실제 서비스 도메인(villa-pms-production…, 프로젝트 도메인 아님!) + `AUTH_TRUST_HOST=true` (프록시 뒤 필수). 잘못되면 모든 리다이렉트가 엉뚱한 호스트로 가고 세션 판정 오작동
- 로컬 .env의 DATABASE_URL은 Railway Postgres `DATABASE_PUBLIC_URL`(thomas.proxy.rlwy.net) 사용 — `railway variables --service Postgres --json`으로 조회
- QA 테스트 계정: SUPPLIER `0900000001` (프로덕션 DB, 오픈 전 삭제 — LAUNCH.md 체크리스트에 추가 필요)

### 프로덕션 E2E 검증 (2026-06-11, 인증 배포 후)
/login·/signup 렌더링 200(Stitch 화면), 미인증 보호 경로 → /login 리다이렉트, 테스트 계정 실로그인 → 세션 쿠키 발급 → / → /my-villas role 분기, SUPPLIER의 /dashboard 접근 차단, 오비밀번호 로그인 거부 — 전부 통과

---

## ADR-0007 멀티 관리자 개인 Zalo 채팅 — S0~S3 구현 완료 (2026-06-16)

### 완료 단계
- **S0 (스키마 + 백필)**: 프로덕션 DB에 안전 적용(데이터 손실 0). `scripts/zalo-multiadmin-backfill.mjs`(멱등)로 컬럼 추가 + 값 백필 → `prisma db push --accept-data-loss`로 제약 전환 2단계.
  - `ZaloAccountKind` enum(SYSTEM_BOT|ADMIN_PERSONAL), `ZaloAccount.kind`(default SYSTEM_BOT)·`userId` NOT NULL·`@@unique([userId,kind])`
  - `ZaloConversation.ownerAdminId`(FK, NOT NULL)·`@@unique([ownerAdminId,zaloUserId])`. 기존 `zaloUserId @unique`·`userId @unique` 제거 완료(인덱스 확인)
  - 백필: 기존 ZaloAccount→SYSTEM_BOT(테오), 기존 대화 2건 ownerAdminId=테오. 시스템봇 소유자 테오 userId=`cmq9dzydp0000uk94gx0ip100`
- **S1 (멀티풀 부활 + 라우팅 분리)**: `lib/zalo-runtime.ts` 단일 슬롯→`Map<string,instance>`(키: adminUserId, 시스템봇=`"__system__"`). `getSystemBotApi()`/`getApiForAdmin()`/`connectAllActive()`/`startQRLoginForAdmin(userId,kind)`/`getStatusForAdmin()`. **시스템 발송 `sendBotMessage` 시그니처·동작 무변경**(내부 `getSystemBotApi()` 분기). 통합 모드: 테오 SYSTEM_BOT 1인스턴스를 시스템 발송+개인 채팅 공유(이중 로그인 0).
- **S2 (개인 QR)**: `/api/zalo/qr`·`status`·`/settings/zalo` 본인 계정 스코프(kind 자동 결정 — 시스템봇 소유자/최초연결=SYSTEM_BOT, 그 외 ADMIN_PERSONAL). 통합 모드 안내 라벨 추가.
- **S3 (개인 스코프 + 수신 귀속)**: `saveInboundMessage(ownerAdminId, isSystemBot)` 복합키 upsert + 전화번호 매칭 시스템봇 수신만(D4). `/messages` `where ownerAdminId=session`(인박스+선택대화 findFirst 소유 스코프). 발신 `sendChatMessageAsAdmin(session.user.id)` + 소유 검증. mark-read도 ownerAdminId 스코프.

### 검증
- tsc 0 errors, vitest 560 passed(신규 `tests/zalo-multipool.test.ts` 포함), `next build` 성공, lint 신규 파일 0 경고
- 누수 검증 PASS: QA Admin(다른 관리자) 대화 조회 0건, 테오 2건(라이브 DB)
- 시스템 발송 무변경: `enqueueNotification`/`dispatchPendingNotifications`/`buildNotificationText`/cron 시그니처 그대로, dispatchOne만 미러 대화 복합키(시스템봇 소유자 귀속)로 보정

### 잔여 (S4 + 디자인)
- S4(시스템 미러 귀속 정리 일부 완료 — dispatchOne 미러는 테오 소유 복합키. 계정별 끊김 경보 Web Push는 미구현)
- 디자인 인계 대기: ADR-0007 9절 #1(개인 연결 카드 신규)·#3(`/messages` 미연결 빈 상태 신규)는 라벨/텍스트 수준만 적용. **새 레이아웃 필요 시 DESIGN 선행** — 현재는 기존 b14/zalo-connect 컴포넌트 재사용
- 백필 스크립트 `scripts/zalo-multiadmin-backfill.mjs` 보존(감사용, 멱등)

## Phase 1 빌라 관리자 페이지 업그레이드 — 구현 완료 (2026-06-16)

기획→스키마(ADR-0008)→Stitch 7화면→QA 디자인검토→구현→QA 최종검증 파이프라인 완주. QA 최종: **조건부 Go** (코드 레벨 GO, 치명 누수 0 / 실측 Playwright는 Railway 배포 반영 후 Part B 4건 재실행 필요).

### 구현 항목
- **스키마(ADR-0008, db push)**: `VillaSeasonPeriod`(빌라별 시즌 날짜), `VillaAmenity.unitPrice`(미니바 고객청구가 VND), `NotificationType.RATE_CHANGED_DURING_PROPOSAL`. 백필 없음 → 기존 견적 회귀 0(전역 폴백).
- **BE(8129816)**: 사진 add/delete/reorder API(기준사진+진행중예약 시 409 보호, AuditLog), 공급자 원가 PATCH/DELETE(판매가·마진 서버측 재계산·응답엔 원가만, ACTIVE 제안 포함 시 운영자 알림), 빌라별 시즌 CRUD(겹침 409), 비품 PATCH 확장(unitPrice·customLabel·note), `lib/amenities.ts` 수건 대/중/소+custom, `lib/pricing.ts` 폴백 2단계(count 보유판정↔교차분 로드 분리, 회귀 테스트 4/4).
- **공급자 5화면(e4b2aeb)**: 사진 확대 라이트박스, 사진 관리, 비품 수정 개편(전 품목 수량+미니바 직접입력/가격+수건 3종), 캘린더 예약 상세 바텀시트(확정/가예약 분기, 원가·인원·박수만), 원가 관리+빌라별 시즌.
- **운영자 2화면(f42ae64)**: 견적중 원가변경 경보(대시보드 배너+제안 영향+판매가 재확인), 체크아웃 미니바 차감 자동계산(소모×unitPrice BigInt 실시간).
- **i18n**: 신규 ko/vi 키, LOC vi 감수 5건 교정(기준사진 Ảnh gốc 등).
- **디자인(439e3bb)**: Stitch export a11~a15, b15~b16.

### 잔여
- **Railway 배포 후 QA Part B 4건 실측 재검증**(비품 3종 렌더·캘린더 2상태·미니바 실시간·캘린더 응답 누수). 푸시는 완료, 빌드 지연.
- 시즌 겹침 동시성(경/정보) → IDEAS.md 등재.

---

## Nike↔villa Zalo 통합 에픽 — ADR-0010 채택 (2026-06-18, S1 착수 준비)

### 확정 의사결정 (테오, 2026-06-18)
- **A안 채택**: villa-pms를 Zalo 허브로 단독 보유(세션·정본 DB), Nike는 villa ext API로 송수신. "테오 세션은 villa만 WebSocket 로그인, Nike는 테오 계정 절대 미로그인" 한 문장으로 code 3000 원천 차단. A→C(별도 zalo-gw) 무중단 승격 경로 보존.
- **SPOF 수용**: villa(허브) 다운 시 Nike 테오 송수신도 정지 — 가용성 트레이드오프 테오 명시 수용. 완화책 끊김 경보 우선.
- **테오 계정 한정**: villa `__system__` 인스턴스(테오 SYSTEM_BOT) 1개만 대상. 다른 ADMIN_PERSONAL 계정 0 영향(ADR-0007 ownerAdminId 격리 유지).
- **과거 대화 ETL 이관**: 통합 이전 Nike 테오 대화·메시지를 villa 정본으로 일회성 이관(S3).
- **그룹 채팅 포함**: villa 스키마 additive 확장(ZaloThreadType enum·threadType·groupMembers Json·senderUid, S4 마이그레이션).
- **첨부 영구 보존**: Nike DB 첨부 바이너리를 villa R2로 재업로드(originalUrl 만료 위험 회피).
- **기능 동등**: forward·alias(=기존 nickname 매핑)·음성STT(=lib/gemini.ts 재사용) villa 동등 구현.

### 참조
- ADR: docs/decisions/ADR-0010-nike-villa-zalo-session-chat-share.md (A안·풀스펙·Nike 실사·모델 대조표·WBS·배포순서·ETL)
- S1 계약: docs/contracts/zalo-integration-s1.md
- 관련: ADR-0005(zca-js), ADR-0006(런타임 단일봇·instrumentation), ADR-0007(멀티풀·`__system__`·ownerAdminId 격리)

### 스프린트 (의존도순)
| 스프린트 | 작업 | 의존 | 상태 |
|---|---|---|---|
| **S1 세션 단일화+발송 위임** | villa A1·A5 + Nike B1·B2 | 없음(전제) | **현재 — 계약서 작성됨, QA 합의 후 착수** |
| S2 채팅 읽기 정본+SSE | villa A2·A3·A4 + Nike B3·B4 | S1 | 대기 |
| S3 ETL 텍스트+첨부 | 그룹 C(C1·C2, 1:1 한정) | S1 | 대기 |
| S4 그룹 채팅 | 그룹 D(스키마 마이그레이션+UI) + A7 + B6 + C1 그룹분 | S2 | 대기 |
| S5 forward/alias/STT | villa A6 + Nike B5 | S1(S4와 병렬 가능) | 대기 |

### 다음 단계 (S1 착수 — 핸드오프)
1. **계약서 QA 합의** — docs/contracts/zalo-integration-s1.md를 QA가 검토·합의(완료기준 9종·검증 6단계). **합의 전 코딩 금지**.
2. **착수 선점** — 계약서 단독 커밋·푸시(`chore: zalo-integration-s1 착수 선점`)로 점유 선언.
3. **villa 구현(BE/INTEG)**: A5(시크릿 게이트·ownerAdminId 하드코딩·credential 미반환 DTO) → A1(`POST /api/zalo/ext/send`, 기존 sendChat* 함수 재사용). villa 스키마 무변경.
4. **Nike 구현(Nike INTEG 세션)**: B1(테오 로그인 3경로 제거) → B2(테오 발송 villa HTTP 위임). **별도 레포·세션** — villa 세션은 ext API 계약만 제공.
5. **배포(절대 순서)**: villa A1·A5 배포 → **Nike B1 먼저** 배포·확인(테오 미로그인) → villa 허브 단독 확인 → **그 다음 Nike B2** 전환.
6. **QA 검증**: 시크릿 401·credential 누수 0·ownerAdminId 주입 차단·세션 단일성·실송수신(테오 협조)·회귀 0.

### 테오 직접 액션 (S1 배포 시)
- **villa Railway env 추가**: `ZALO_EXT_SHARED_SECRET`(강한 랜덤), 테오 villa userId 상수 확인(현 `__system__` 소유자 `cmq9dzydp0000uk94gx0ip100`).
- **Nike Railway env 추가**: `VILLA_EXT_BASE_URL`(=https://villa-pms-production.up.railway.app), `ZALO_EXT_SHARED_SECRET`(villa와 **동일 값**).
- **배포 순서 협조**: B1 배포 확인 → 허브 단독 확인 → B2 전환(순서 위반 시 code 3000·밴 위험).
- **세션·실송수신**: villa `__system__` QR 로그인 활성 상태(/settings/zalo, ADR-0006)에서 Nike UI로 테오 텍스트·이미지 발송 실측.

### 수정 금지 구역 (병렬 세션 — 2026-06-18 git status 기준)
- villa: `lib/cleaning.ts`·`lib/hold.ts`·`lib/proposal.ts`·`docs/INDEX.md`·`LAUNCH.md`·`partB-evidence-*.png` (타 세션 WIP — 비접촉)
- Nike: 별도 레포(C:\Projects\Nike) — B1·B2는 Nike INTEG 세션 담당, villa 세션 직접 수정 금지
